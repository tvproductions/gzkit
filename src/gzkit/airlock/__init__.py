"""Airlock pure data layer (ADR-0.33.0, OBPI-0.33.0-01).

Ships the frozen seam/preflight/drift Pydantic models and the closed
``SeamKind``/``Provenance``/``Authority``/``Decision``/``Verdict`` StrEnums that
airlock-IN (OBPI-02) and airlock-OUT (OBPI-03) construct against. No behavior,
no compute, no CLI — shapes only.
"""
