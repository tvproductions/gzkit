{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vendor_manifest",
  "type": "object",
  "required": ["content_type_routes"],
  "additionalProperties": false,
  "properties": {
    "content_type_routes": {
      "type": "object",
      "description": "Maps each content type class name to the vendor mirrors that receive its rendered output.",
      "additionalProperties": {
        "type": "array",
        "items": {"type": "string"},
        "minItems": 1
      }
    },
    "content_type_delivery_caps": {
      "type": "object",
      "description": "Observed hard delivery limit, in bytes, that a vendor imposes on a rendered content type. A missing entry means no known limit — never a fabricated one. Codex truncates root AGENTS.md past project_doc_max_bytes (openai/codex#7138).",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "content_type_temperatures": {
      "type": "object",
      "description": "Per-vendor temperature for each content type. Each key is a content type class name; each value maps vendor name to temperature tier.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": {
          "type": "string",
          "enum": ["lite", "medium", "heavy"]
        }
      }
    },
    "surface_content_types": {
      "type": "object",
      "description": "Maps each rendered control surface to the content type that owns it \u2014 the inverse direction of content_type_routes, and the authority that makes \"is this consumer on-route FOR THIS SURFACE\" answerable. Without it a route test can only ask whether a consumer is routed for SOMETHING, which grades an off-content-type rendition (GHI #840).",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      }
    }
  }
}
