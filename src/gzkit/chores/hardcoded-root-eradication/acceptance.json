{
  "criteria": [
    {
      "type": "exitCodeEquals",
      "command": "uv run gz lint",
      "expected": 0,
      "description": "Lint passes including parents-pattern lint rule (AST; covers subscripted and bare .parents access across all of src/gzkit/, superseding the former per-directory greps)"
    },
    {
      "type": "exitCodeEquals",
      "command": "uv run gz check-config-paths",
      "expected": 0,
      "description": "Config-path audit passes including source path literal scan"
    },
    {
      "type": "exitCodeEquals",
      "command": "uv run -m unittest -q",
      "expected": 0
    }
  ]
}
