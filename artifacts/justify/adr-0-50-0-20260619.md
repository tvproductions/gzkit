---
anchor_id: draft-adr-0-50-0
anchor_kind: draft
generated_at: 2026-06-19T14:21:52.997657+00:00
scaffold_version: 1.0
---

# Walkthrough: draft-adr-0-50-0

## 1. What I see (the problem)

**Prompt:** *What did I observe that motivates this change? What hurts if nothing happens?*

**Evidence:**

- ADR-0.50.0

The `gz validate --evaluation-justify-binding` gate (ADR-0.0.26) is fail-closed
against artifact-id `ADR-0.50.0`: its latest `adr-evaluation` ledger event
(ts `2026-05-18T11:46:30Z`) scored the **Problem Clarity** dimension at **1.0**,
below the 3.0 threshold, with no `gz justify` record to answer for it. This
walkthrough is the forced reasoned response (GHI #628 sweep). Until it exists,
the gate stays red for any `gz check` run touching this artifact-id.

The substantive question: *is the 1.0 Problem Clarity score a real defect or an
artifact of evaluation timing?* Unlike its sibling ADR-0.49.0, the answer here
is **mixed, leaning toward a real kernel**. The score is partly stale (frozen
against an identity that was demoted back to pool), but Problem Clarity is the
one dimension the evaluator held at 1.0 across *three* same-day re-evaluations
while other dimensions climbed — which I read as a genuine, persistent
weakness in how the artifact frames its problem, not a timing accident. I
document both findings honestly below.

## 2. Per-instance severity

**Prompt:** *How bad is each occurrence? One incident, a pattern, or a class of failure?*

**Evidence:**

- `.gzkit/ledger.jsonl` — three `adr-evaluation` events for `ADR-0.50.0`: 11:44:44Z (Problem Clarity 1.0, weighted 2.45, **NO_GO**, 1 action item), 11:45:25Z (Problem Clarity 1.0, weighted 3.05, GO), 11:46:30Z (Problem Clarity 1.0, weighted 3.05, GO)
- `.gzkit/ledger.jsonl` — `artifact_edited` events on the ADR body at 11:44:58Z, 11:45:08Z, 11:45:21Z (between evaluations)

The telling pattern: between the first and later evaluations the operator edited
the ADR body three times, and the evaluator's scores *moved* — Decision
Justification 1.0 -> 3.0, Architectural Alignment 1.0 -> 3.0, Scope Discipline
3.0 -> 4.0 — lifting the overall verdict from NO_GO to GO. But **Problem Clarity
stayed pinned at 1.0 through all three runs.** That is the opposite of a stale
score: it is a dimension that resisted improvement even as the author actively
revised. The most defensible reading is that the edits addressed the
justification/alignment critiques but never reframed the *problem statement*,
which the evaluator continued to judge weak. Severity to the project today is
still low (the artifact is dormant pool backlog), but the critique itself is
substantive, not procedural.

## 3. Why this scope

**Prompt:** *Why is the change boundary drawn here and not wider or narrower?*

**Evidence:**

- Task constraint: touch ONLY files under `artifacts/justify/`
- `docs/design/adr/pool/ADR-pool.gz-architecture-review-skill.md` (status: Pool, Superseded)

Scope is one file: this justification artifact. Even though I am finding a real
Problem Clarity kernel, the correct boundary is still to *document* it, not to
rewrite the pool ADR's § Intent to chase the score — that would game the gate
and is forbidden by ADR-0.0.26's intent. The artifact is dormant pool backlog
(`status: Pool`); the right time to repair its § Intent is at re-promotion, when
a `gz-design` dialogue reframes the problem and a fresh evaluation is run. This
walkthrough records the finding so that future promotion work inherits the
diagnosis rather than rediscovering it. Editing the ADR body now is out of scope
both by task constraint and by routing doctrine.

## 4. What it proposes

**Prompt:** *In one paragraph, what is the change?*

**Evidence:**

- `docs/design/adr/pool/ADR-pool.gz-architecture-review-skill.md` § Intent, § Decision

This artifact proposes no code change; it records the disposition of the 1.0
Problem Clarity score. The disposition is: **accept the gate clear via documented
reasoning, while recording an honest finding that the Problem Clarity critique
has a real kernel to repair at re-promotion.** Concretely, the pool ADR's §
Intent frames its motivation as a *capability gap plus external inspiration* —
"gzkit has multiple code-quality review skills ... none operate at the
module-boundary / architectural seam tier ... inspired by Matt Pocock's
`improve-codebase-architecture` skill." That is a clear statement of what is
*absent*, but it is thin on the *observed pain*: no incident, no ledger signal,
no GHI count, no concrete review that failed for lack of seam-tier analysis. A
strong Problem Clarity section would anchor the gap in evidenced harm. The
proposal is to note this as the corrective target, not to fabricate evidence now.

## 5. Routing decision

**Prompt:** *Direct fix, OBPI ceremony, or new ADR? Cite the threshold that routed it.*

**Evidence:**

- AGENTS.md § Defect-fix routing — direct-fix thresholds (≤2 files, single surface, not new feature work)
- Operator doctrine: "GHIs are AUTHORIZED for direct repair, always"

The gate-clear itself routes as **direct fix** under GHI #628: one non-source
artifact, no contract surface, defect repair not feature work — every direct-fix
criterion holds. The *separate* finding (weak Problem Clarity § Intent) is NOT
repaired here; it is a latent authoring gap on a dormant pool artifact. Its
correct route is **deferred to re-promotion via `gz-design`** — when/if this pool
ADR is promoted, the design dialogue must reframe § Intent around evidenced pain
before re-evaluation. Per operator doctrine "discovering that more is needed to
fulfill the intent of a feature is a correction, not an enhancement," but here
the artifact is unpromoted backlog, so there is no shipped surface failing its
intent yet — the correction attaches to the promotion, not to a fresh ADR now.

## 6. Why this design is right-sized

**Prompt:** *Why isn't this bigger or smaller? What does this shape defend against?*

**Evidence:**

- `gz justify validate` 8-section schema
- ADR-0.0.26 (gate forces a REASONED response, not a rubber stamp)

The 8-section walkthrough is right-sized because it forces the honest mixed
verdict that a one-liner would hide. A smaller "stale, dismissed" disposition
would have papered over a real, persistent Problem Clarity weakness — exactly the
rubber-stamp failure ADR-0.0.26 exists to prevent. A larger response (rewriting
the ADR's § Intent now) would over-reach: it would game the score and mutate a
dormant artifact outside its promotion ceremony. The walkthrough shape defends
against two failures at once — false-dismissal of a real critique, and
score-gaming of a real artifact — by making me cite the ledger pattern (three
re-evals, Problem Clarity stuck) and the on-disk § Intent text.

## 7. What convinces me (evidence)

**Prompt:** *Which rules, ledger events, and commits ground this decision?*

**Evidence:**

- cross-platform (.gzkit/rules/cross-platform.md)
- governance-core (.gzkit/rules/governance-core.md)
- models (.gzkit/rules/models.md)
- pythonic (.gzkit/rules/pythonic.md)

Two converging lines of evidence ground the mixed verdict:

1. **Staleness (procedural half).** `artifact_renamed` pool_demotion
   2026-05-23T23:25:11Z (GHI #520, operator g0) returned
   `ADR-0.50.0-gz-architecture-review-skill` to
   `ADR-pool.gz-architecture-review-skill`. The latest evaluation (2026-05-18) is
   frozen against a now-retired feature identity; the artifact today is
   `docs/design/adr/pool/ADR-pool.gz-architecture-review-skill.md`, `status: Pool`.
   Per `governance-core`, the ledger is Layer-2 and on-disk canon is Layer-1 —
   both now agree this is dormant backlog, not active feature work.
2. **Real kernel (substantive half).** Across three `adr-evaluation` events with
   body edits between them, Problem Clarity held at 1.0 while Decision
   Justification, Architectural Alignment, and Scope Discipline all rose. A score
   that resists active revision is evidence of a genuine framing weakness, not
   timing noise. Reading the surviving § Intent confirms it: the problem is stated
   as an *absence* ("none operate at the module-boundary tier") and an external
   *inspiration* (Matt Pocock's skill), without evidenced harm — no incident, GHI
   count, or failed-review citation.

Verdict: **stale in identity, real in critique.** The gate may clear (the score
attaches to a demoted, dormant artifact), but the Problem Clarity finding is
genuine and is recorded here as the corrective target for any future promotion.

## 8. Residual uncertainty

**Prompt:** *What am I not sure about? What would change my mind?*

**Evidence:**

- `.gzkit/ledger.jsonl` (three un-noted-rationale evaluations for ADR-0.50.0)

I cannot see the evaluator's rubric notes — the ledger records the 1.0 dimension
score but not *why* Problem Clarity specifically failed three times, so my "weak
because framed as absence + inspiration, not evidenced pain" reading is inferred
from the surviving § Intent text plus the resist-revision pattern, not from a
recorded rationale. What would change my mind: if a fresh `gz adr evaluate` of
the current pool body returned Problem Clarity ≥ 3.0, the kernel would be
disproven and the verdict would collapse to pure staleness; conversely, a re-run
still at 1.0 would confirm the corrective target. I do not re-run the evaluator
here (task scope is `artifacts/justify/` only, and re-evaluating a dormant pool
artifact is discretionary authoring outside this defect-repair sweep). The honest
disposition is the mixed verdict above, with the re-evaluation path named for
re-promotion.
