---
anchor_id: draft-adr-0-0-56
anchor_kind: draft
generated_at: 2026-06-19T14:21:09.725094+00:00
scaffold_version: 1.0
---

# Walkthrough: draft-adr-0-0-56

## 1. What I see (the problem)

**Prompt:** *What did I observe that motivates this change? What hurts if nothing happens?*

**Evidence:**

- ADR-0.0.56

This walkthrough exists because the `gz validate --evaluation-justify-binding` gate (ADR-0.0.26) fires on ADR-0.0.56: its latest evaluation scored the **Architectural Alignment** dimension at **1.0**, below the 3.0 floor, with no justification record on file. The gate forces a reasoned response rather than a rubber stamp, so the question I must answer honestly is: is the 1.0 a real architectural defect in the closeout-defect-accounting design, or is it an artifact of how the score was produced?

What I see when I read the evidence is unambiguous: the 1.0 is the **CLI deterministic pre-screen** score, and the authoritative **manual** scorecard for this same dimension is **4** — a four-point overturn explicitly recorded as a heuristic false negative. `EVALUATION_SCORECARD.md:31` reads: *"Architectural Alignment | 10% | 1 | 4 | CLI 1 → manual 4 — heuristic false negative."* The verdict section (`EVALUATION_SCORECARD.md:15-17`) names the exact mechanism: *"The CLI heuristic looks for `src/gzkit/...` path references in the ADR body and flagged 'no source file path references.' Foundation-invariant ADRs carry source paths in their OBPI briefs (e.g. `closeout.py:359`, `obpi_stages.py:224`), not the ADR narrative."* The scorecard header (`:5`) states the governing precedence rule: *"The manual scorecard supersedes the CLI pre-screen."*

What hurts if nothing happens: the gate stays red and blocks closeout readiness for a dimension whose authoritative score is already a passing 4. The hurt is procedural (a stuck gate), not architectural (a real design flaw) — and this walkthrough is the record that distinguishes the two so the gate can clear on grounded reasoning rather than a deferral.

## 2. Per-instance severity

**Prompt:** *How bad is each occurrence? One incident, a pattern, or a class of failure?*

**Evidence:**

- ADR-0.0.56-closeout-defect-accounting-invariant.md (ADR body)
- EVALUATION_SCORECARD.md (manual override record)

This is a single-instance scoring artifact, not a pattern of architectural weakness and not a class of failure in the design. Severity assessment by category:

- **As an architectural defect: none.** The dimension's authoritative manual score is 4/4. There is no occurrence of architectural misalignment to grade.
- **As a scoring-mechanism artifact: low and well-understood.** The CLI heuristic's root cause is named precisely (`EVALUATION_SCORECARD.md:17`): it greps the ADR *narrative* for `src/gzkit/...` paths and scores 1 when it finds none. This is a known false-negative class for foundation-tier ADRs, which by convention keep source-path detail in their OBPI briefs (the ADR body states the invariant and the port; the briefs carry the adapter paths). The same heuristic class misfired on this ADR's OBPI-02 independence (CLI 1 → manual 3, `EVALUATION_SCORECARD.md:18`) and on Problem Clarity (CLI 3 → manual 4, `:24`), confirming the cause is the pre-screen regex family, not the ADR.

Stale-vs-real verdict for this section: the score is **not stale-from-later-work** (ADR-0.0.56 is still Draft, 0/6 OBPIs done — `gz adr status` confirms `pre_closeout`, `0/6`, `BLOCKED`). It is instead a **heuristic false-negative produced at evaluation time** (2026-05-22) and already overturned in the same scorecard. The 1.0 never reflected the dimension; the authoritative 4 did.

## 3. Why this scope

**Prompt:** *Why is the change boundary drawn here and not wider or narrower?*

**Evidence:**

- artifacts/justify/adr-0-0-56-20260619.md (this artifact)
- governance-core (.gzkit/rules/governance-core.md)

The boundary of this work is exactly one justification artifact for one ADR's one failing dimension — nothing wider, nothing narrower. The task constraint is explicit: touch only files under `artifacts/justify/`. That is the correct boundary because:

- **Narrower would be insufficient.** The gate requires a justification *record* keyed to ADR-0.0.56; a one-line note does not clear `validate_evaluation_justify_binding`, which expects the full 8-section walkthrough to validate.
- **Wider would be wrong.** Re-running the evaluation, editing the scorecard to "promote" the manual score into the CLI slot, or editing the ADR body to sprinkle `src/gzkit/` paths into the narrative to satisfy the regex would all be defect-suppression (gaming the heuristic), not honest justification. The doctrine is the opposite: surface the artifact honestly and explain it. The scorecard's manual override already did the architectural-judgment work; this artifact records and grounds it.

The change boundary is therefore drawn at "author one grounded justification artifact that documents why the 1.0 is a pre-screen false-negative refuted by the authoritative manual 4," which is exactly the `gz justify` artifact's purpose.

## 4. What it proposes

**Prompt:** *In one paragraph, what is the change?*

**Evidence:**

- ADR-0.0.56-closeout-defect-accounting-invariant.md
- EVALUATION_SCORECARD.md

The change is the authoring of this grounded `gz justify` walkthrough, which records the reasoned position that ADR-0.0.56's Architectural Alignment dimension is sound and the 1.0 is a CLI pre-screen heuristic false-negative already overturned to 4 by the authoritative manual review. The substantive architectural claim the walkthrough stands behind is the one the manual reviewer (`quality-reviewer`) verified: ADR-0.0.56 is architecturally aligned because it (a) **reuses existing surfaces** rather than inventing parallel ones — `gz closeout` already records a closeout event and `gz check --json` already emits structured output, so the mechanism adds only a snapshot payload and a reconcile predicate (ADR § Decision, Consequences/Positive #2); (b) **composes with ADR-0.0.36** (universal OBPI attestation) without overlap — the reconcile is machine-mechanical and precedes attestation, deliberately *not* loaded onto the Gate-5 payload (ADR § Alternatives #2); (c) **applies port-vs-adapter framing** explicitly — the invariant + `gz validate --closeout-defect-accounting` scope is the port, and the three per-surface integrations (`gz closeout` embed, `gz obpi complete` extension, the ghi-close PreToolUse hook) are adapters behind it (ADR § "Port-vs-adapter framing"); and (d) **differentiates from the neighbouring pool ADR** `ADR-pool.contract-surface-mechanical-defenses` rather than duplicating it (`EVALUATION_SCORECARD.md:31`). These are the hallmarks the Architectural Alignment dimension grades, and they are present.

## 5. Routing decision

**Prompt:** *Direct fix, OBPI ceremony, or new ADR? Cite the threshold that routed it.*

**Evidence:**

- AGENTS.md (Defect-fix routing thresholds)
- artifacts/justify/adr-0-0-56-20260619.md

Routing: **direct authoring of a justification artifact** — neither an OBPI ceremony nor a new ADR. This is the route prescribed by the gate itself (ADR-0.0.26): a sub-3.0 evaluation dimension with no justification record is cleared by authoring a `gz justify` record, full stop. Against AGENTS.md § Defect-fix routing, every direct-route threshold holds: the diff is a single new file under `artifacts/justify/` (≤2 files, well under the source-line ceiling); the scope is a single named surface (one ADR's justify artifact); the trigger is a defect surfaced in flight (the red gate during the GHI #628 sweep); and no CLI/schema/runtime contract changes. OBPI ceremony would be category error — there is no code or contract to implement, only a reasoned record to author. A new ADR would be a gross over-application — the architecture is already attested sound at manual 4. The artifact route is the minimum surface that clears the gate honestly.

## 6. Why this design is right-sized

**Prompt:** *Why isn't this bigger or smaller? What does this shape defend against?*

**Evidence:**

- EVALUATION_SCORECARD.md
- governance-core (.gzkit/rules/governance-core.md)

The artifact is right-sized at exactly the 8 grounded sections the validator requires — bigger would be padding, smaller would fail validation. What this shape defends against is the precise failure the gate exists to prevent: a *rubber stamp*. A degenerate justification ("CLI was wrong, ignore it") would technically fill the sections but would not be grounded; it would re-introduce the vibing surface the gate closes. So the shape is deliberately evidence-anchored — every claim cites a concrete artifact (the scorecard line, the ADR section, the `gz adr status` state) so the reasoning can be audited, not trusted. It defends equally against the *opposite* over-correction: treating the 1.0 as a real architectural defect and spinning up remediation work that the manual 4 shows is unwarranted. The right size is "document the truth with citations and stop," which is what this is.

## 7. What convinces me (evidence)

**Prompt:** *Which rules, ledger events, and commits ground this decision?*

**Evidence:**

- cross-platform (.gzkit/rules/cross-platform.md)
- governance-core (.gzkit/rules/governance-core.md)
- models (.gzkit/rules/models.md)
- pythonic (.gzkit/rules/pythonic.md)

The decision that the 1.0 is a pre-screen false-negative, not an architectural defect, is grounded in:

- **`EVALUATION_SCORECARD.md:31`** — the dimension-8 row records `CLI 1 | manual 4` with the verbatim note "heuristic false negative."
- **`EVALUATION_SCORECARD.md:15-17`** — the verdict section names the exact heuristic ("looks for `src/gzkit/...` path references in the ADR body") and the structural reason it misfires on foundation ADRs (paths live in OBPI briefs: `closeout.py:359`, `obpi_stages.py:224`).
- **`EVALUATION_SCORECARD.md:5`** — the precedence rule: "The manual scorecard supersedes the CLI pre-screen."
- **`EVALUATION_SCORECARD.md:32`** — weighted total 3.90/4.0, verdict **GO**, confirming the dimension passes on authoritative scoring.
- **ADR body § "Port-vs-adapter framing" and § "Why foundation tier?"** — the architectural reasoning the `quality-reviewer` verified: port = invariant + validate scope; adapters = the three per-surface integrations; composes with ADR-0.0.36; differentiated from `ADR-pool.contract-surface-mechanical-defenses`.
- **`gz adr status ADR-0.0.56`** — lifecycle `Pending`, `0/6` OBPIs, `pre_closeout`: the work has not landed, so the 1.0 is a *evaluation-time heuristic* artifact, not a stale-relative-to-later-work artifact.
- **`.claude/rules/governance-core.md`** — the Layer-1/Layer-2/Layer-3 source-of-truth doctrine that justifies treating the manual scorecard (authored judgment) as authoritative over the CLI pre-screen (a derived heuristic).

The cross-platform / models / pythonic rules are scaffold-default citations; they are not load-bearing for this justification and I note them only for completeness.

## 8. Residual uncertainty

**Prompt:** *What am I not sure about? What would change my mind?*

**Evidence:**

- EVALUATION_SCORECARD.md
- ADR-0.0.56-closeout-defect-accounting-invariant.md

Honest residuals:

- **The gate reads the CLI score, not the manual override.** My account treats the manual 4 as authoritative (per the scorecard's own precedence rule and Layer-1/2 doctrine), but the `evaluation-justify-binding` gate clearly keys off the *recorded* sub-3.0 value (the CLI 1). This is itself a possible upstream defect — the gate may not be reading the manual-override score — but it is out of scope here (touch only `artifacts/justify/`) and is exactly the kind of thing the justify artifact is meant to absorb: the gate fires, the human-grounded record explains it. If the gate's data source is later corrected to honor manual overrides, this artifact still stands as the contemporaneous reasoning.
- **The architecture is judged, not yet built.** The manual 4 is a *design-time* architectural-alignment score; ADR-0.0.56 is Draft with 0/6 OBPIs implemented. The reuse-of-existing-surfaces claims (e.g. that `gz check --json` is stable enough to fingerprint) are design assertions the ADR itself flags as the load-bearing risk (Consequences/Negative #1, #3). If implementation reveals the snapshot/reconcile surfaces do not compose as cleanly as the ADR assumes, the Architectural Alignment judgment could legitimately drop — but that would be a *new* finding from implementation evidence, not a vindication of the original CLI 1.
- **What would change my mind:** concrete evidence that the ADR's architecture actually conflicts with an existing surface (e.g. the closeout event schema cannot carry the snapshot payload, or the reconcile genuinely duplicates `ADR-pool.contract-surface-mechanical-defenses`). I found no such evidence; the manual reviewer found none; the design reuses rather than duplicates. Absent that, the 1.0 stands as a heuristic false-negative.
