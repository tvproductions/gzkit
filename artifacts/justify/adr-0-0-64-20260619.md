---
anchor_id: draft-adr-0-0-64
anchor_kind: draft
generated_at: 2026-06-19T14:21:09.864128+00:00
scaffold_version: 1.0
---

# Walkthrough: draft-adr-0-0-64

## 1. What I see (the problem)

**Prompt:** *What did I observe that motivates this change? What hurts if nothing happens?*

**Evidence:**

- ADR-0.0.64

This walkthrough exists because the `gz validate --evaluation-justify-binding` gate (ADR-0.0.26) fires on ADR-0.0.64: its latest evaluation scored the **Problem Clarity** dimension at **1.0**, below the 3.0 floor, with no justification record on file. The gate forces a reasoned response, so the question is whether the 1.0 reflects a genuinely unclear problem statement or an artifact of how the score was produced.

What I see is the same shape as the other ADRs in this GHI #628 sweep: the 1.0 is the **CLI deterministic pre-screen** score, and the authoritative **manual** score for Problem Clarity is **3** (a passing GO). `EVALUATION_SCORECARD.md:14` records it verbatim: *"Problem Clarity | 15% | 1 | 3 | CLI false-negative: regex looks for literal 'before'/'after' tokens; ADR carries semantic before/after framing (`d70793c4` baseline → validator signatures) in Intent. Quantification present (`7,897` events, 8 worklog types, GHI cluster). Dense paragraph form costs the 4."* The CLI reconciliation table (`EVALUATION_SCORECARD.md:80`) classifies it as *"↑ False-negative: literal-token regex misses semantic before/after framing."*

What hurts if nothing happens: the gate stays red and blocks QC readiness for an ADR whose work is already fully landed (5/5 OBPIs `attested_completed` per `gz adr status`), over a problem-clarity score that the authoritative manual review already moved to a passing 3. The hurt is a stuck gate, not an unclear problem — and this artifact records the reasoning that distinguishes the two.

## 2. Per-instance severity

**Prompt:** *How bad is each occurrence? One incident, a pattern, or a class of failure?*

**Evidence:**

- ADR-0.0.64-task-envelope-and-planning-decomposition.md (ADR body)
- EVALUATION_SCORECARD.md (manual override record)

This is a single-instance scoring artifact whose severity is low, assessed by category:

- **As a problem-clarity defect: minimal.** The authoritative manual score is 3/4 — a passing grade. The only real content behind the gap from a 4 is *form*, not substance: the scorecard's exact words are "Dense paragraph form costs the 4" (`:14`). The advisory revision asks (`EVALUATION_SCORECARD.md:73`) confirm this is cosmetic polish, not a clarity failure: *"Add 2-sentence before/after lede atop Intent. Closes D1 problem-clarity dense-paragraph cost"* — explicitly listed as **non-blocking, optional polish before promotion**.
- **As a scoring-mechanism artifact: low and well-understood.** The CLI's root cause is named: a literal-token regex for the words "before"/"after" that misses the ADR's *semantic* before/after framing (the `d70793c4` granularity-locked baseline → the additive validator-signature target state). The same regex family misfired on Decision Justification (CLI 3 → manual 4, `:15`), confirming the cause is the pre-screen, not the ADR.

Stale-vs-real verdict for this section: the score is **both** a heuristic false-negative **and** moot-from-later-work. ADR-0.0.64 is fully implemented — `gz adr status` shows all 5 OBPIs `attested_completed`, closeout `READY` — so even the cosmetic "dense paragraph" concern is now behind the project; the problem the ADR named (TASK tier carries no per-labor-unit attribution) was real, was clearly stated, and has been mechanically closed (GHI #553).

## 3. Why this scope

**Prompt:** *Why is the change boundary drawn here and not wider or narrower?*

**Evidence:**

- artifacts/justify/adr-0-0-64-20260619.md (this artifact)
- governance-core (.gzkit/rules/governance-core.md)

The boundary is exactly one justification artifact for ADR-0.0.64's one failing dimension, per the task constraint (touch only `artifacts/justify/`). Narrower (a one-liner) fails the validator, which requires the full grounded 8-section walkthrough. Wider would be wrong in two specific ways: (a) editing the ADR body to inject literal "before"/"after" tokens to satisfy the regex would be gaming the heuristic, not clarifying the problem — and the ADR is already attested-completed, so its body is a settled contract; (b) re-running the evaluation or promoting the manual score into the CLI slot would be defect-suppression. The honest move is to author the record that explains the score, which is precisely the `gz justify` artifact's scope. The boundary is drawn at "one grounded artifact" because that is the minimum that clears the gate without touching settled canon.

## 4. What it proposes

**Prompt:** *In one paragraph, what is the change?*

**Evidence:**

- ADR-0.0.64-task-envelope-and-planning-decomposition.md
- EVALUATION_SCORECARD.md

The change is this grounded `gz justify` walkthrough recording the reasoned position that ADR-0.0.64's Problem Clarity is sound (authoritative manual 3, GO) and the 1.0 is a CLI pre-screen literal-token false-negative, further mooted by the ADR being fully implemented. The substantive problem-clarity claim the walkthrough stands behind is the one the `spec-reviewer` verified: ADR-0.0.64 states its problem with concrete, quantified, dated evidence. It names the doctrine-runtime gap precisely — ADR-0.22.0 declared the TASK-driven workflow binding, but the `Task:|Ceremony:|Eval-feedback-source:` commit-trailer OR-rule made TASK attribution silently optional, and commit `d70793c4` (2026-05-26) auto-emitted `task_started`/`task_completed` at `seq=01` per REQ, satisfying the surface check while landing granularity-locked (worklog events carry no `task_id`). It quantifies impact (8 worklog event types affected; 7,897 prior ledger entries that a required-field approach would break — ADR § Alternatives #3). It situates the failure in a named class ("presence != envelope," anti-vibing operative claim 4) and a dated GHI cluster (#553, same T1→T2 family as #537/#538/#551). The before-state (`d70793c4` coarse bucket) and the target-state (additive `task_id` + four-channel coherence validator) are both explicit in the Intent and Decision — they simply are not spelled with the literal tokens the CLI regex hunts for.

## 5. Routing decision

**Prompt:** *Direct fix, OBPI ceremony, or new ADR? Cite the threshold that routed it.*

**Evidence:**

- AGENTS.md (Defect-fix routing thresholds)
- artifacts/justify/adr-0-0-64-20260619.md

Routing: **direct authoring of a justification artifact** — the route the gate (ADR-0.0.26) prescribes for a sub-3.0 dimension with no justification record. Against AGENTS.md § Defect-fix routing, every direct-route threshold holds: one new file under `artifacts/justify/` (≤2 files); single named surface (one ADR's justify artifact); trigger is a red gate surfaced in flight during the GHI #628 sweep; no CLI/schema/runtime-contract change. OBPI ceremony is category error — there is nothing to implement; the ADR's own OBPIs are already 5/5 attested-completed. A new ADR is a gross over-application — the problem was clearly stated, the work is done, and the only gap-from-4 is cosmetic form. The artifact route is the minimum surface that clears the gate honestly.

## 6. Why this design is right-sized

**Prompt:** *Why isn't this bigger or smaller? What does this shape defend against?*

**Evidence:**

- EVALUATION_SCORECARD.md
- governance-core (.gzkit/rules/governance-core.md)

The artifact is right-sized at the 8 grounded sections the validator requires. Bigger is padding; smaller fails validation. The shape defends against the rubber-stamp the gate exists to prevent: rather than asserting "the CLI was wrong," every claim cites a concrete artifact (the scorecard line, the ADR Intent/Decision, the `gz adr status` completion state) so an auditor can check the reasoning. It defends equally against the opposite over-correction — treating a cosmetic "dense paragraph" deduction as a real clarity failure and re-opening settled, attested-completed work. The right size is "ground the truth in citations and stop"; that is what this is. Notably, the *honest* finding here is not "the score is meaningless" — it is "the 4 was lost to prose density, the manual 3 is a fair passing grade, and the work shipped." That nuance is what a right-sized justification preserves.

## 7. What convinces me (evidence)

**Prompt:** *Which rules, ledger events, and commits ground this decision?*

**Evidence:**

- cross-platform (.gzkit/rules/cross-platform.md)
- governance-core (.gzkit/rules/governance-core.md)
- models (.gzkit/rules/models.md)
- pythonic (.gzkit/rules/pythonic.md)

The decision that the 1.0 is a pre-screen false-negative (not an unclear-problem defect) is grounded in:

- **`EVALUATION_SCORECARD.md:14`** — the Problem Clarity row: `CLI 1 | manual 3`, with the verbatim note naming the literal-token regex, the semantic `d70793c4`-baseline→validator-signatures framing, the quantification (`7,897` events, 8 worklog types, GHI cluster), and "Dense paragraph form costs the 4."
- **`EVALUATION_SCORECARD.md:80`** — the reconciliation table classifying dimension 1 as "↑ False-negative: literal-token regex misses semantic before/after framing."
- **`EVALUATION_SCORECARD.md:73`** — the advisory revision ask ("Add 2-sentence before/after lede atop Intent") listed under **non-blocking, optional polish**, confirming the gap is cosmetic.
- **`EVALUATION_SCORECARD.md:23-24`** — weighted total 3.50/4.0, threshold 3.0 GO, verdict **GO**.
- **ADR body § Intent / § Decision item (1)** — the concrete problem statement: the commit-trailer OR-rule, `d70793c4`'s `seq=01` granularity-lock, the 8 worklog types, the 7,897-entry backwards-compat constraint (§ Alternatives #3), the named "presence != envelope" failure class and GHI #553 cluster.
- **`gz adr status ADR-0.0.64`** — all 5 OBPIs `attested_completed`, closeout `READY`: the named problem is not only clearly stated but mechanically closed, so the score is moot-from-later-work as well as a heuristic artifact.
- **`.claude/rules/governance-core.md`** — the source-of-truth doctrine that justifies treating the manual scorecard (authored judgment) as authoritative over the CLI pre-screen (a derived heuristic).

The cross-platform / models / pythonic rules are scaffold-default citations, not load-bearing for this justification; noted for completeness.

## 8. Residual uncertainty

**Prompt:** *What am I not sure about? What would change my mind?*

**Evidence:**

- EVALUATION_SCORECARD.md
- ADR-0.0.64-task-envelope-and-planning-decomposition.md

Honest residuals:

- **The gate reads the CLI score, not the manual override.** As with the sibling ADRs in this sweep, the `evaluation-justify-binding` gate keys off the recorded sub-3.0 value (CLI 1) rather than the authoritative manual 3. This may be an upstream defect in the gate's data source, but it is out of scope here (touch only `artifacts/justify/`) and is precisely what the justify artifact absorbs.
- **The manual 3 is honest about a real (if cosmetic) gap.** Unlike a pure 4-point heuristic flip, this dimension's authoritative score is 3, not 4 — the reviewer deliberately withheld the top mark for "dense paragraph form." I am not claiming the ADR's Intent is flawless prose; I am claiming it is *clear* (problem named, quantified, dated, classed) and merely dense. If a reader genuinely could not extract the before/after states from the Intent, that would be a real clarity defect — but the manual reviewer, reading the full prose, extracted them and scored a passing 3, and I find the same on reading the body.
- **What would change my mind:** evidence that the problem statement is actually ambiguous or unsupported — e.g. if the `d70793c4` baseline claim or the 7,897-entry figure were unverifiable, or if a reader could not tell what state the ADR moves the system *from* and *to*. I found the opposite: both states are concretely cited and the work that realized the target state is 5/5 attested-completed. Absent contrary evidence, the 1.0 stands as a literal-token heuristic false-negative on an otherwise clearly-stated, now-closed problem.
