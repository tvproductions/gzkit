---
anchor_id: draft-adr-0-0-26
anchor_kind: draft
generated_at: 2026-06-19T14:21:45.014508+00:00
scaffold_version: 1.0
---

# Walkthrough: draft-adr-0-0-26

## 1. What I see (the problem)

**Prompt:** *What did I observe that motivates this change? What hurts if nothing happens?*

**Evidence:**

- ADR-0.0.26

The `gz validate --evaluation-justify-binding` gate (authored by this very ADR, OBPI-0.0.26-02) fires for ADR-0.0.26 itself: its latest evaluation (`EVALUATION_SCORECARD.md`, dated 2026-05-04) scored dimension 8 — **Architectural Alignment** — at **1/4** (weighted 0.10), and no `gz justify` artifact exists to discharge the low score. The recorded findings are exactly two: "No source file path references in ADR" and "No exemplar/precedent language". This walkthrough is the reasoned response the gate demands.

What hurts if nothing happens: the gate stays red for ADR-0.0.26, which is otherwise fully landed — `gz adr status ADR-0.0.26-evaluation-feedback-loop-doctrine` reports lifecycle **Validated**, closeout **READY**, QC **READY**, and 5/5 OBPIs `attested_completed`. A red gate on a Validated, human-attested ADR (Attestation Block: Validated by g0 2026-05-03, "accept audit … validated end-to-end") is a false-positive blocker, not a real architectural defect. The honest task is to determine whether the 1.0 is a real gap or a scoring artifact, and to say so with evidence.

## 2. Per-instance severity

**Prompt:** *How bad is each occurrence? One incident, a pattern, or a class of failure?*

**Evidence:**

- `src/gzkit/adr_eval_scoring.py` `_score_architectural_alignment` (lines 351-371)

The 1.0 is a **deterministic keyword-heuristic miss**, not a substantive architectural-alignment defect. `_score_architectural_alignment` runs three mechanical checks and maps the pass-count to a 1-4 score:

1. `re.search(r"`src/[^`]+`", content)` — requires a backtick-wrapped `` `src/...` `` path span.
2. `_has_keywords(content, ["existing pattern", "exemplar", "precedent", "follows"])`.
3. `_has_keywords(content, ["anti-pattern", "do not", "avoid", "prohibited"])`.

For ADR-0.0.26 the recorded findings show checks 1 and 2 failed and check 3 passed (only two findings emitted, and check 3's vocabulary is satisfied by the "rejected" alternatives and "Mitigation" prose) — so 1/3 checks passed, which `_passes_to_score` floors to 1.

Per-instance breakdown:
- **Check 1 (src path):** A *true-but-immaterial* observation. ADR-0.0.26's implementation surface genuinely is **not** primarily `src/`-rooted — the doctrine binds the ledger (`.gzkit/ledger.jsonl`), a thresholds data file (`data/eval_feedback_thresholds.json`), a chore (`chores/eval-feedback-cluster/`), tests (`tests/governance/test_evaluation_feedback_loop.py`), a feature file (`features/evaluation_feedback_loop.feature`), and rule docs (`.claude/rules/tests.md`). These concrete referents are all present in the Evidence and Decision sections; none happens to be a backtick-wrapped `src/` path because the doctrine's surface legitimately lives elsewhere. The regex measures one specific lexical form, not the presence of architectural grounding.
- **Check 2 (precedent language):** A *false negative*. The ADR is densely precedent-grounded — it cites the "Opus 4.7 § 6.1.3 'Claude reviews its own alignment section' pattern", "the same epistemology", GPT-5.5 § 9.2 (Apollo) eval-awareness data, and the Anthropic Prompt Engineering 101 talk (Hannah Moran + Christian Ryan) treating CoT as an inspectable scratch pad. It simply phrases the alignment with synonyms ("same epistemology", "same pattern") rather than the four literal tokens the matcher hunts for.

Severity: **cosmetic / artifactual**. There is no architectural class-of-failure here; the dimension's narrow lexicon under-counts genuinely present content.

## 3. Why this scope

**Prompt:** *Why is the change boundary drawn here and not wider or narrower?*

**Evidence:**

- Task brief: GHI #628 sweep, touch only `artifacts/justify/`

The boundary is a single justification artifact for ADR-0.0.26. It is drawn **narrower** than "fix the scorer" deliberately: the gate (ADR-0.0.26 itself) is designed so that a low score is discharged by a *reasoned response*, not by editing the score or the rubric to make the number go away. Widening scope to retune `_score_architectural_alignment`'s lexicon would be a separate, contract-bearing change to the deterministic evaluator (`src/gzkit/adr_eval_scoring.py`) — a CLI/scoring-surface change that belongs under its own GHI/OBPI, not under a defect-clearing sweep that is constrained to `artifacts/justify/`. It is not drawn **wider** to re-evaluate the ADR because the ADR's substance is already validated end-to-end and re-running the deterministic scorer would reproduce the same lexicon miss.

## 4. What it proposes

**Prompt:** *In one paragraph, what is the change?*

**Evidence:**

- `artifacts/justify/adr-0-0-26-20260619.md`

This artifact proposes nothing be changed in the ADR, the scorer, or the ledger. It records the reasoned disposition that the Architectural Alignment 1.0 for ADR-0.0.26 is a **deterministic keyword-heuristic artifact** (a false-negative on precedent language and a true-but-immaterial observation that the doctrine's surface is not `src/`-rooted), and that the correct disposition is **accept-as-is with rationale**. Authoring this grounded walkthrough is exactly the action the `evaluation-justify-binding` gate exists to compel; the artifact's presence at the slug-matched path clears the gate for ADR-0.0.26.

## 5. Routing decision

**Prompt:** *Direct fix, OBPI ceremony, or new ADR? Cite the threshold that routed it.*

**Evidence:**

- AGENTS.md § Defect-fix routing; operator doctrine "GHIs are AUTHORIZED for direct repair, always"

**Direct fix**, tracked under the GHI #628 sweep. The routing facts: diff is one new file under `artifacts/justify/` (≤10 source lines of *code* — zero, in fact); single named surface (the justify artifact set); trigger is a defect surfaced in flight (a red gate), not new feature work; no new CLI/schema/runtime contract is added (the artifact is data the existing `gz justify validate` and `validate_evaluation_justify_binding` already consume). All direct-fix criteria hold and none of the "OBPI ceremony required when ANY hold" criteria fire. Per operator doctrine, a GHI-tracked defect repair routes to direct fix regardless of the ceremony criteria — "the GHI is the work order and the receipt."

## 6. Why this design is right-sized

**Prompt:** *Why isn't this bigger or smaller? What does this shape defend against?*

**Evidence:**

- ADR-0.0.26 § Decision item 2; § Consequences (Goodhart risk)

The right-sized response to an artifactual low score is the smallest action that the gate accepts as a genuine reasoned response: one grounded 8-section walkthrough. Smaller (e.g. a one-line "stale, ignore") would be the rubber-stamp the gate explicitly defends against — ADR-0.0.26's own Decision item 2 makes `gz-justify` "required before the parent artifact's lifecycle can advance", precisely so the low score forces reasoning, not dismissal. Bigger (retuning the scorer lexicon) would risk **Goodhart drift** — the named Negative consequence of this very ADR: changing the scoring surface to make this ADR's own number pass is the loop's signal directly shaping the loop's input. Keeping the scorer untouched and discharging via reasoning preserves the observational-not-prescriptive posture the ADR commits to. This shape defends against both rubber-stamping (too small) and self-serving rubric-gaming (too big).

## 7. What convinces me (evidence)

**Prompt:** *Which rules, ledger events, and commits ground this decision?*

**Evidence:**

- cross-platform (.gzkit/rules/cross-platform.md)
- governance-core (.gzkit/rules/governance-core.md)
- models (.gzkit/rules/models.md)
- pythonic (.gzkit/rules/pythonic.md)

Concrete, checkable grounding for the "artifact, not gap" disposition:

1. **The scorer source.** `src/gzkit/adr_eval_scoring.py:351-371` shows the three keyword checks verbatim; the failing checks for this ADR are the `` `src/` `` regex and the four-token precedent list. The mechanism is lexical, not semantic — confirmed by reading the code, not inferred.
2. **The ADR's actual precedent content.** ADR-0.0.26 lines 36-50 cite Opus 4.7 § 6.1.3, GPT-5.5 § 9.2 (Apollo, 22.1% eval-awareness), and the Anthropic Prompt Engineering 101 talk; lines 159-166 (Q&A Transcript) ground the doctrine in those cards. This is precedent/exemplar reasoning the keyword matcher's four literal tokens did not catch.
3. **The ADR's concrete referents.** Decision/Evidence sections name `.gzkit/ledger.jsonl`, `data/eval_feedback_thresholds.json`, `chores/eval-feedback-cluster/`, `tests/governance/test_evaluation_feedback_loop.py`, `features/evaluation_feedback_loop.feature`, `.claude/rules/tests.md` — concrete file-path grounding that simply isn't `src/`-rooted because the doctrine's surface legitimately lives in ledger/data/chore/test space.
4. **Independent validation of substance.** `gz adr status` reports Validated / READY / READY, 5/5 OBPIs `attested_completed`; the Attestation Block records a human-attested "validated end-to-end" on 2026-05-03 with a live `gz adr evaluate` re-run scoring **3.70/4.0 weighted (GO)**. The weighted total already clears the 3.0 GO threshold — the single 1.0 dimension is the only sub-threshold cell, and its findings are lexical.

## 8. Residual uncertainty

**Prompt:** *What am I not sure about? What would change my mind?*

**Evidence:**

- `src/gzkit/adr_eval_scoring.py` `_score_architectural_alignment`

The disposition (accept-as-is) rests on the claim that the scorer's lexicon is too narrow, not that the ADR has no alignment gap. The residual uncertainty is **check 1**: ADR-0.0.26 genuinely contains no backtick-wrapped `` `src/...` `` path. One could argue a foundation ADR codifying a ledger/validator/chore loop *should* name at least one `src/gzkit/...` runtime module inline (e.g. the validator module that implements `validate_evaluation_justify_binding`). I judge this immaterial because the implementation landed and is human-attested, and the ADR's surface is deliberately ledger/data/chore-rooted — but a reasonable reviewer could call it a thin spot.

What would change my mind: if the four-token precedent check were broadened to recognize "same epistemology / same pattern / cross-model card citation" and the score *still* came back 1, that would indicate a real alignment gap rather than a lexicon miss. The clean follow-up, if the operator wants the number itself corrected rather than discharged, is a separate GHI against `_score_architectural_alignment` to widen its lexicon (and optionally accept non-`src/` concrete referents) — out of scope for this `artifacts/justify/`-only sweep, but the named, trackable next action.
