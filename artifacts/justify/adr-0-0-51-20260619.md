---
anchor_id: draft-adr-0-0-51
anchor_kind: draft
generated_at: 2026-06-19T14:21:45.014508+00:00
scaffold_version: 1.0
---

# Walkthrough: draft-adr-0-0-51

## 1. What I see (the problem)

**Prompt:** *What did I observe that motivates this change? What hurts if nothing happens?*

**Evidence:**

- ADR-0.0.51

The `gz validate --evaluation-justify-binding` gate fires for ADR-0.0.51: its latest evaluation (`EVALUATION_SCORECARD.md`, dated 2026-05-18) scored dimension 8 — **Architectural Alignment** — at **1/4** (weighted 0.10), with no `gz justify` artifact on disk to discharge it. The recorded findings are exactly two: "No exemplar/precedent language" and "No anti-pattern guidance". This walkthrough is the reasoned response the gate compels.

Context differs from ADR-0.0.26: `gz adr status ADR-0.0.51-milestone-maintenance-pipeline-with-goal` reports lifecycle **Pending**, closeout **BLOCKED**, 0/5 OBPIs done (all five `pending`, "ledger proof of completion is missing"). So ADR-0.0.51 is an **unimplemented design** still in the planning lane — the milestone-maintenance pipeline (the fourth artifact-lifecycle pipeline) has not been built. The question for this dimension is narrower than implementation completeness: does the **ADR document** carry genuine architectural-alignment reasoning, and is the 1.0 a real gap in the design's grounding or a scoring artifact?

## 2. Per-instance severity

**Prompt:** *How bad is each occurrence? One incident, a pattern, or a class of failure?*

**Evidence:**

- `src/gzkit/adr_eval_scoring.py` `_score_architectural_alignment` (lines 351-371)

The 1.0 is the same **deterministic keyword-heuristic miss** seen on ADR-0.0.26 — and the recurrence across two distinct foundation ADRs is itself signal that the dimension's lexicon is narrow, not that two ADRs independently lack alignment grounding. The three mechanical checks: (1) backtick-wrapped `` `src/...` `` regex; (2) tokens `["existing pattern","exemplar","precedent","follows"]`; (3) tokens `["anti-pattern","do not","avoid","prohibited"]`. For ADR-0.0.51 the recorded findings show check 1 **passed** (the ADR names `` `src/gzkit/milestone_maintenance_runtime.py` `` in backticks at line 49) and checks 2 and 3 **failed** — 1/3 passed, floored to 1.

Per-instance breakdown:
- **Check 2 (precedent language):** A *false negative*. ADR-0.0.51 is heavily precedent-grounded: an explicit **port-vs-adapter** framing (lines 20-21); a five-row **pipeline cohort table** (lines 33-39) positioning this as pipeline #4 alongside the design/implementation/validation/chore cohort and naming ADR-0.0.50 as the canonical precedent it parallels; the "service interval" doctrine (operator's vehicle-maintenance analogy); Matt Pocock's `improve-codebase-architecture` as the named exemplar this skill adapts; and the ADR-0.0.31 distribution invariant it commits to satisfy. It expresses precedent via "same posture as ADR-0.0.50 § Stage 5", "Pocock-inspired", and "canonical adapter" — none of which are the four literal tokens the matcher seeks.
- **Check 3 (anti-pattern guidance):** A *false negative*. The entire **Alternatives Considered** section (lines 174-180) is anti-pattern guidance — five rejected designs, each with a stated reason (bundle-into-validation, add-redteam-terminal, calendar-cadence, flat-manifest, build-`/goal`-natively). The Negative consequences section (lines 126-130) names concrete failure modes (inter-ADR latency, version-floor dependency, manifest aging, ADR-0.0.50 coupling). The ADR also frames the `/goal` evaluator as "anti-vibing structurally". The content the check wants is present; it is phrased as "rejected … because" rather than "do not / avoid / prohibited".

Severity: **cosmetic / artifactual** for the dimension score. The design's architectural grounding is strong — arguably the strongest dimension of this ADR, given the cohort table and port/adapter framing.

## 3. Why this scope

**Prompt:** *Why is the change boundary drawn here and not wider or narrower?*

**Evidence:**

- Task brief: GHI #628 sweep, touch only `artifacts/justify/`

The boundary is a single justification artifact for ADR-0.0.51. It is **not** widened to implement the pipeline (the ADR is Pending with 0/5 OBPIs — building the milestone-maintenance runtime is the planned ADR work, gated through the OBPI pipeline, not a defect sweep). It is **not** widened to retune the scorer lexicon (a contract-bearing change to `src/gzkit/adr_eval_scoring.py` belonging under its own GHI). The sweep's job is to discharge the false-positive gate with a grounded reasoned response, constrained to `artifacts/justify/`.

## 4. What it proposes

**Prompt:** *In one paragraph, what is the change?*

**Evidence:**

- `artifacts/justify/adr-0-0-51-20260619.md`

This artifact proposes no change to the ADR, the scorer, or the ledger. It records the reasoned disposition that ADR-0.0.51's Architectural Alignment 1.0 is a **deterministic keyword-heuristic artifact** — false negatives on both the precedent-language check (the port/adapter framing and the pipeline cohort table grounding this as pipeline #4 against ADR-0.0.50 and the Pocock exemplar) and the anti-pattern check (the five-entry Alternatives Considered section). The disposition is **accept-as-is with rationale**; the artifact's presence at the slug-matched path clears the `evaluation-justify-binding` gate for ADR-0.0.51.

## 5. Routing decision

**Prompt:** *Direct fix, OBPI ceremony, or new ADR? Cite the threshold that routed it.*

**Evidence:**

- AGENTS.md § Defect-fix routing; operator doctrine "GHIs are AUTHORIZED for direct repair, always"

**Direct fix**, tracked under the GHI #628 sweep. Routing facts: one new file under `artifacts/justify/`, zero source-code lines, single named surface (the justify artifact set), trigger is an in-flight red gate (defect), no new CLI/schema/runtime contract added. All direct-fix criteria hold; no OBPI-ceremony criterion fires. The clearing of the gate must not be confused with completing ADR-0.0.51's planned work — that remains a separate, unstarted OBPI pipeline. Per operator doctrine, the GHI-tracked defect repair routes to direct fix regardless of the ceremony criteria.

## 6. Why this design is right-sized

**Prompt:** *Why isn't this bigger or smaller? What does this shape defend against?*

**Evidence:**

- ADR-0.0.51 § Decision Stage 2 (Iron Law); § Consequences (anti-vibing framing)

One grounded 8-section walkthrough is the smallest action the gate accepts as a genuine reasoned response. Smaller (a terse "stale") is the rubber-stamp the gate exists to prevent. Bigger has two wrong forms: (a) implementing the pipeline now — that is the planned ADR work, not a gate-clearing action, and forcing it here would conflate "discharge a scoring artifact" with "build the feature"; (b) retuning the scorer to make the number pass — a Goodhart move that lets the scored ADR reshape the scoring surface. Discharging via reasoning while leaving both the scorer and the (still-pending) implementation untouched defends against rubber-stamping, premature implementation, and rubric-gaming simultaneously.

## 7. What convinces me (evidence)

**Prompt:** *Which rules, ledger events, and commits ground this decision?*

**Evidence:**

- cross-platform (.gzkit/rules/cross-platform.md)
- governance-core (.gzkit/rules/governance-core.md)
- models (.gzkit/rules/models.md)
- pythonic (.gzkit/rules/pythonic.md)
- f311fea chore: update .gzkit (2 files), docs/design/adr (45 files), docs/governance/hexagonal-architecture.md, tests (3 files) (gz git-sync)

Concrete, checkable grounding for the "artifact, not gap" disposition:

1. **The scorer source.** `src/gzkit/adr_eval_scoring.py:351-371` shows the three keyword checks; for ADR-0.0.51 check 1 (`` `src/` `` regex) passes on line 49's `` `src/gzkit/milestone_maintenance_runtime.py` ``, and checks 2/3 fail on lexicon, not substance. Read from source, not inferred.
2. **The ADR's precedent content.** Lines 20-21 (port-vs-adapter), lines 31-39 (the four-pipeline cohort table naming ADR-0.0.50 as the validated precedent and this as pipeline #4), line 95/165 (Pocock `improve-codebase-architecture` exemplar), line 103 (ADR-0.0.31 distribution invariant) — dense precedent grounding the four-token matcher missed.
3. **The ADR's anti-pattern content.** Lines 174-180 — Alternatives Considered with five rejected designs and reasons; lines 126-130 — five named Negative consequences. This is exactly the "what not to do and why" the check wants, phrased as rejections.
4. **Lifecycle truth.** `gz adr status` reports Pending / BLOCKED / 0-of-5; the weighted total was 3.25/4.0 (GO). The single sub-threshold dimension is the 1.0 Architectural Alignment cell, whose two findings are both lexical false-negatives. Note the *other* sub-4 cells (Problem Clarity 3, Decision Justification 3, OBPI Decomposition 3) are above the 3.0 gate floor and do not trigger the binding; only the 1.0 does.

## 8. Residual uncertainty

**Prompt:** *What am I not sure about? What would change my mind?*

**Evidence:**

- `src/gzkit/adr_eval_scoring.py` `_score_architectural_alignment`

Two residuals. First, unlike ADR-0.0.26 this ADR is **unimplemented** (0/5 OBPIs). The Architectural Alignment dimension scores the *document's* reasoning, not delivery — so the disposition (the document's alignment grounding is strong; the score is a lexicon artifact) holds independently of implementation state. But a reviewer should not read this discharged gate as any signal that the pipeline is built; it is not. Second, the recurrence of the identical false-negative shape across ADR-0.0.26 and ADR-0.0.51 strengthens the case that `_score_architectural_alignment`'s lexicon is too narrow (it rewards four specific precedent tokens and four specific anti-pattern tokens, missing "port/adapter", "cohort", "same posture as ADR-N", and "rejected … because").

What would change my mind: if a broadened lexicon still scored ADR-0.0.51 at 1, the gap would be real. The named, trackable follow-up (out of scope for this `artifacts/justify/`-only sweep) is a single GHI against `_score_architectural_alignment` to widen its precedent/anti-pattern vocabulary and to credit non-`src/` concrete referents — a fix to the *class* of false negative, not the two instances.
