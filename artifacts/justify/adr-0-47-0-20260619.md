---
anchor_id: draft-adr-0-47-0
anchor_kind: draft
generated_at: 2026-06-19T14:22:32.411691+00:00
scaffold_version: 1.0
---

# Walkthrough: draft-adr-0-47-0

## 1. What I see (the problem)

**Prompt:** *What did I observe that motivates this change? What hurts if nothing happens?*

**Evidence:**

- ADR-0.47.0

The `gz validate --evaluation-justify-binding` gate (ADR-0.0.26) fires for
artifact-id `ADR-0.47.0` because its only `adr-evaluation` ledger event
(`.gzkit/ledger.jsonl`, ts `2026-05-10T12:40:24.516560+00:00`) records
TWO dimensions below the `3.0` threshold: `OBPI Decomposition: 1.0` and
`Evidence Requirements: 1.0` (weighted_total 2.7). No qualifying
`gz-justify` artifact existed, so the binding fired fail-closed. The
decisive timing fact: that evaluation ran at **12:40:24** on 2026-05-10,
while the OBPI brief files and the ADR's own Decomposition/Evidence
sections were authored **afterward** — the `artifact_edited` ledger events
for `OBPI-0.47.0-04`, `-05`, `-01`, `-03` are stamped 13:03–13:27 the same
day. The evaluator scored decomposition and evidence that did not yet
exist on disk. This walkthrough is the REASONED disposition: are the two
1.0s still true of the shipped artifact?

## 2. Per-instance severity

**Prompt:** *How bad is each occurrence? One incident, a pattern, or a class of failure?*

**Evidence:**

- ADR (pool) `## OBPI Decomposition — Work Breakdown Structure` (lines 214–222): 5 OBPIs
- ADR (pool) `## Evidence` (lines 237–246): tests, docs, chore, skill, receipts, pool cross-ref

**Both dimensions: STALE, and now doubly moot.** The 1.0s were artifacts of
a pre-authoring evaluation snapshot, not substantive defects of the
shipped artifact.

- **OBPI Decomposition (1.0 → stale):** the shipped ADR now carries a full
  `## Decomposition Scorecard` (lines 170–200: Dimension Total 9, Final
  Target 5), a 5-item `## Checklist` (lines 208–212), and a Level-1 WBS
  table (lines 216–222) enumerating OBPI-0.47.0-01…-05 with lanes — a
  clean 1:1 decomposition. None of this existed at 12:40:24.
- **Evidence Requirements (1.0 → stale):** the shipped ADR now carries a
  populated `## Evidence` block (lines 237–246) naming concrete test
  files, manpage/runbook docs, the chore `CHORE.md` + `proofs/`, the
  skill SKILL.md + mirrors, `arb-step-` receipts, and the pool-ADR
  cross-reference. OBPI-0.47.0-01 actually shipped with attested receipts
  (ledger `obpi_receipt_emitted`, 2026-05-10T14:10:23, resolved receipt
  IDs `arb-step-unittest-…`, `arb-ruff-…`, `arb-step-typecheck-…`,
  `arb-step-coverage-…`, `arb-step-mkdocs-…`).

Second-order: the artifact was **demoted to pool** (`ADR-pool.owasp-top10-2025-scan`,
ledger `artifact_renamed`, 2026-05-23T23:25:10, GHI #520). A pool ADR is
backlog awaiting promotion — per AGENTS.md § Kinds, pool carries no active
semver/OBPI obligations — so the decomposition/evidence dimensions no
longer gate it at all. The score is both stale (pre-authoring) and moot
(post-demotion).

## 3. Why this scope

**Prompt:** *Why is the change boundary drawn here and not wider or narrower?*

**Evidence:**

- ledger `artifact_renamed` ADR-0.47.0 → ADR-pool.owasp-top10-2025-scan (2026-05-23, GHI #520)

Scope is exactly: author one grounded justify record for artifact-id
`ADR-0.47.0`, covering both sub-threshold dimensions, touching only
`artifacts/justify/`. Narrower fails the gate (the binding needs a
slug-matching artifact). Wider is unwarranted and would be wrong-headed:
the artifact has been demoted to pool under GHI #520's "Prequel Day-0
demotion" (25-deep Pending feature queue collapsed back to pool to unblock
recovery promotions). Re-promoting it, re-decomposing it, or
re-evaluating it is out of bounds for a defect-repair sweep and would
violate Architectural Boundary 1 ("Do not promote post-1.0 pool ADRs into
active work") absent an operator-ratified promotion decision.

## 4. What it proposes

**Prompt:** *In one paragraph, what is the change?*

**Evidence:**

- ADR (pool) frontmatter `status: Pool` (line 3)

This record proposes no change to the ADR. It asserts that both failing
dimensions (OBPI Decomposition 1.0, Evidence Requirements 1.0) are
stale-evaluation artifacts produced before the OBPIs and Evidence section
were authored, and that the shipped artifact already satisfies both
criteria (5-OBPI WBS + decomposition scorecard; populated Evidence block
with attested OBPI-01 receipts). It further notes the artifact is now a
pool/backlog ADR (`status: Pool`), where these dimensions do not gate. The
disposition is "no corrective action on the artifact; clear the gate with
a grounded justification."

## 5. Routing decision

**Prompt:** *Direct fix, OBPI ceremony, or new ADR? Cite the threshold that routed it.*

**Evidence:**

- GHI #628 (justify sweep, defect repair)
- AGENTS.md § Defect-fix routing thresholds

Routed as a **direct authoring task under GHI #628**, touching only
`artifacts/justify/`. No code/schema/CLI surface change; no brief boundary
crossed. AGENTS.md § Defect-fix routing reserves OBPI ceremony for
contract-surface or cross-brief changes — neither applies. Spinning up an
ADR/OBPI to discharge a gate-clearing record would contradict operator
doctrine ("Never spin up an ADR or OBPI merely to discharge a GHI") and
Architectural Boundary 1 (no promoting pool ADRs into active work).

## 6. Why this design is right-sized

**Prompt:** *Why isn't this bigger or smaller? What does this shape defend against?*

**Evidence:**

- ADR (pool) `## Decomposition Scorecard` (lines 170–200) and `## Checklist` (lines 208–212)

Not smaller: the gate is fail-closed; only a slug-matching, fully-filled
justify artifact clears `gz check` for `ADR-0.47.0`. Not bigger:
re-authoring the decomposition or evidence would be fabricated work — both
sections already exist and are well-formed (Decomposition Scorecard total
9 → 5 OBPIs; Checklist 1:1 with the WBS table). Re-promoting the pool ADR
to re-run an evaluation would be a scope violation. This shape defends
against two failures at once: (a) silencing a real gap by editing the ADR
to chase a stale number, and (b) laundering a backlog artifact back into
active work under cover of a quality gate. The honest move is to record
why the 1.0s do not reflect the shipped, now-pooled artifact.

## 7. What convinces me (evidence)

**Prompt:** *Which rules, ledger events, and commits ground this decision?*

**Evidence:**

- cross-platform (.gzkit/rules/cross-platform.md)
- governance-core (.gzkit/rules/governance-core.md)
- models (.gzkit/rules/models.md)
- pythonic (.gzkit/rules/pythonic.md)

1. **Eval predates authoring**: `adr-evaluation` ts 2026-05-10T12:40:24
   vs. OBPI `artifact_edited` events 13:03–13:27 the same day — the
   evaluator scored decomposition/evidence that did not yet exist.
2. **Decomposition now well-formed**: ADR `## Decomposition Scorecard`
   (lines 170–200), `## Checklist` (5 items, lines 208–212), WBS table
   (lines 216–222) → 5 OBPIs, 1:1.
3. **Evidence now populated**: ADR `## Evidence` (lines 237–246) names
   tests/docs/chore/skill/receipts; OBPI-0.47.0-01 shipped attested
   (ledger `obpi_receipt_emitted` 2026-05-10T14:10:23 with five resolved
   `arb-step-`/`arb-ruff-` receipt IDs; anchor commit `b8ad39a`).
4. **Artifact is now pool/backlog**: ledger `artifact_renamed`
   2026-05-23T23:25:10 (GHI #520, `pool_demotion`); current file
   `docs/design/adr/pool/ADR-pool.owasp-top10-2025-scan.md` carries
   `status: Pool`. Per `.gzkit/rules/governance-core.md`, the ledger is
   Layer-2 truth — and it records both an attested OBPI-01 and a
   subsequent pool demotion, neither consistent with a genuine 1.0.

## 8. Residual uncertainty

**Prompt:** *What am I not sure about? What would change my mind?*

**Evidence:**

- ledger: only OBPI-0.47.0-01 reached `obpi_receipt_emitted`; -02…-05 did not before demotion

The honest residual: only OBPI-0.47.0-01 reached an attested completion
receipt before the 2026-05-23 pool demotion; OBPI-0.47.0-02 through -05
were launched (pipeline events) but not completed. So the *Evidence
Requirements* dimension was thin at the moment of the 12:40 score in the
narrow sense that most OBPI-level evidence had not yet been produced — but
the ADR-level Evidence *section* (the dimension the rubric scores) was
authored shortly after and is complete, and the demotion to pool moots any
further completion obligation. What would change my mind: if the artifact
were re-promoted to an active feature ADR and a fresh `gz adr evaluate`
still scored either dimension <3.0 after reading the now-authored
Decomposition and Evidence sections, that would indicate a genuine
residual gap rather than a pre-authoring snapshot. Absent re-promotion
(which Architectural Boundary 1 forbids here), the correct disposition is
the stale-and-moot verdict above.
