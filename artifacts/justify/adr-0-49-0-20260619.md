---
anchor_id: draft-adr-0-49-0
anchor_kind: draft
generated_at: 2026-06-19T14:21:52.859890+00:00
scaffold_version: 1.0
---

# Walkthrough: draft-adr-0-49-0

## 1. What I see (the problem)

**Prompt:** *What did I observe that motivates this change? What hurts if nothing happens?*

**Evidence:**

- ADR-0.49.0

The `gz validate --evaluation-justify-binding` gate (ADR-0.0.26) is fail-closed
against artifact-id `ADR-0.49.0` because its most-recent `adr-evaluation`
ledger event (ts `2026-05-18T11:43:50Z`) scored the **Decision Justification**
dimension at **1.0** — below the 3.0 threshold — and no `gz justify` record
exists to answer for it. This walkthrough is the reasoned response the gate
forces. Without it, the gate stays red and `gz check` cannot run clean for any
sweep that touches this artifact-id (GHI #628).

The substantive question the gate asks is: *is the 1.0 Decision Justification
score a real defect in the artifact, or an artifact of evaluation timing?* My
verdict, grounded below, is that it is a **stale-evaluation artifact**. The
score was recorded during a transient ~5-day window in which the pool stub was
briefly promoted to a feature ADR; the artifact has since been demoted back to
pool and the score has never been re-run against the body that actually exists
on disk today.

## 2. Per-instance severity

**Prompt:** *How bad is each occurrence? One incident, a pattern, or a class of failure?*

**Evidence:**

- `.gzkit/ledger.jsonl` — `adr-evaluation` event for `ADR-0.49.0` (2026-05-18T11:43:50Z): `Decision Justification: 1.0`, `weighted_total: 3.05`, verdict GO
- `.gzkit/ledger.jsonl` — `artifact_renamed` (pool_demotion) for `ADR-0.49.0-obpi-pipeline-redteam-retrofit` -> `ADR-pool.obpi-pipeline-redteam-retrofit` (2026-05-23T23:25:11Z, GHI #520)

This is a single instance, not a pattern, and its real-world severity to the
project is low. The evaluation that triggered the gate is a one-shot record
from 2026-05-18; only one `adr-evaluation` event exists for `ADR-0.49.0` and it
was never superseded by a re-evaluation (unlike sibling `ADR-0.50.0`, which was
re-run three times the same day). Critically, the evaluator returned an overall
**GO** verdict with a `weighted_total` of 3.05 and **zero action items** — the
low Decision Justification sub-score did not block the artifact even at
evaluation time.

The "failure" is purely procedural: the gate keys on the bare artifact-id
`ADR-0.49.0`, which no longer designates a live feature ADR. Five days after the
evaluation, the 25-deep Pending feature queue was collapsed back to pool under
GHI #520 ("get-out-of-jail-plan-2026-05-23") to unblock recovery promotions.
The artifact now lives as `docs/design/adr/pool/ADR-pool.obpi-pipeline-redteam-retrofit.md`
with `status: Pool`. So the gate is enforcing a score against an identity that
has been retired. Severity: a stuck gate, not a content defect.

## 3. Why this scope

**Prompt:** *Why is the change boundary drawn here and not wider or narrower?*

**Evidence:**

- Task constraint: touch ONLY files under `artifacts/justify/`
- `docs/design/adr/pool/ADR-pool.obpi-pipeline-redteam-retrofit.md` (status: Pool, Superseded)

The boundary is exactly one file: this `gz justify` artifact. The right scope
for clearing a stale-evaluation gate is to *document the reasoning*, not to
mutate the artifact, re-run the evaluator, or alter the ledger. The pool ADR
body is not broken and must not be edited to "improve a score" — that would be
score-gaming, the precise failure ADR-0.0.26 exists to prevent. Re-running the
evaluator would manufacture a fresh ledger event that does not reflect any real
authoring work and would itself be a vibe-action. Demoting/withdrawing further
is out of scope and unwarranted: the pool ADR is legitimate backlog. The
narrowest honest action that clears the gate is a grounded justification record,
which is what this is.

## 4. What it proposes

**Prompt:** *In one paragraph, what is the change?*

**Evidence:**

- `docs/design/adr/pool/ADR-pool.obpi-pipeline-redteam-retrofit.md` § Decision, § Alternatives Considered

This artifact proposes nothing to the codebase; it records the reasoned
disposition of the 1.0 Decision Justification score. The disposition is:
**accept the score as stale and document why the underlying artifact's decision
justification is, in fact, adequate.** Concretely — the pool ADR's § Decision
gives a fully specified retrofit (new redteam-verify terminal stage after Stage
5; Codex-inline-primary with opposite-Claude-model fallback dispatch; Iron Law
update; receipt path `.gzkit/receipts/redteam-obpi-pipeline-<OBPI-ID>-<iso>.json`;
`gz validate --redteam-verification-receipts` extension; `--from=redteam` resume
point), and its § Alternatives Considered rejects three named alternatives
(leave-as-is, full rewrite, separate post-hoc skill) each with a load-bearing
reason. A body with this structure does not warrant a 1.0 "decision is asserted
without justification" score against today's content.

## 5. Routing decision

**Prompt:** *Direct fix, OBPI ceremony, or new ADR? Cite the threshold that routed it.*

**Evidence:**

- AGENTS.md § Defect-fix routing — direct-fix thresholds (≤10 source lines OR ≤2 files; single surface)
- Operator doctrine: "GHIs are AUTHORIZED for direct repair, always" (GHI #628 sweep)

Routed as **direct fix** under GHI #628. This is defect repair (a stuck gate),
not planned feature work; the change is one non-source artifact file; it crosses
no brief boundary, adds no CLI/schema/runtime contract. Every direct-fix
criterion holds and no OBPI-ceremony trigger fires. The operator doctrine that
"a GHI-tracked defect repair routes to direct fix regardless of the OBPI-ceremony
criteria" governs here. No new ADR — authoring an ADR to discharge a gate clear
would be exactly the over-ceremony the routing thresholds forbid.

## 6. Why this design is right-sized

**Prompt:** *Why isn't this bigger or smaller? What does this shape defend against?*

**Evidence:**

- `gz justify validate` 8-section schema
- ADR-0.0.26 (evaluation-justify-binding — the gate forces a REASONED response, not a rubber stamp)

The 8-section walkthrough is the deliberate shape: it cannot be smaller (the gate
requires every `## ` section filled — a one-line "stale, dismissed" would re-open
the rubber-stamp hole ADR-0.0.26 closes) and should not be larger (it documents a
disposition, not a redesign). The shape defends against two opposite failures:
(1) gaming the gate by editing the ADR body to chase a number, and (2) waving the
gate away without inspecting whether the low score is real. By forcing me to read
the actual on-disk body and cite its § Decision / § Alternatives, the format makes
the stale-vs-real call evidence-bound rather than assertion-bound.

## 7. What convinces me (evidence)

**Prompt:** *Which rules, ledger events, and commits ground this decision?*

**Evidence:**

- cross-platform (.gzkit/rules/cross-platform.md)
- governance-core (.gzkit/rules/governance-core.md)
- models (.gzkit/rules/models.md)
- pythonic (.gzkit/rules/pythonic.md)

The decisive evidence is the ledger lineage for this artifact-id, read against
the on-disk body:

1. **The score is from a transient promotion window.** `adr-evaluation` for
   `ADR-0.49.0` fired 2026-05-18T11:43:50Z, the same minute the pool stub was
   promoted (`artifact_renamed` pool_promotion 2026-05-18T11:43:39Z). It scored
   Decision Justification 1.0 but returned **GO / weighted_total 3.05 / 0 action
   items**.
2. **The artifact was demoted back to pool five days later.** `artifact_renamed`
   pool_demotion 2026-05-23T23:25:11Z (GHI #520, operator g0) returned
   it to `ADR-pool.obpi-pipeline-redteam-retrofit`. No re-evaluation was ever run
   against the pool form — the 1.0 is frozen against a retired identity.
3. **Today's body justifies its decision.** `docs/design/adr/pool/ADR-pool.obpi-pipeline-redteam-retrofit.md`
   carries a specified § Decision (six concrete mechanisms) and a § Alternatives
   Considered with three rejected options, each with a load-bearing reason
   (universal redteam doctrine per ADR-0.0.50; battle-tested existing skill;
   optional-redteam degrades to no-redteam). Per `governance-core` the ledger is
   Layer-2 truth and the on-disk canon is Layer-1 — both agree the current
   artifact is well-justified backlog, not an unjustified decision.

Verdict: **stale**. The 1.0 Decision Justification score reflects a
2026-05-18 promotion-window snapshot that no longer corresponds to the artifact's
identity or content; the pool ADR's decision is adequately justified.

## 8. Residual uncertainty

**Prompt:** *What am I not sure about? What would change my mind?*

**Evidence:**

- `.gzkit/ledger.jsonl` (single un-superseded evaluation for ADR-0.49.0)

What I cannot fully reconstruct is the evaluator's 2026-05-18 reasoning for the
1.0 — the ledger records the dimension scores but not the rubric notes, so I
infer staleness from lineage and current-body inspection rather than from a
recorded rationale. What would change my mind: if a fresh `gz adr evaluate` run
against the *current pool body* still returned Decision Justification < 3.0, the
gap would be real and would route as a corrective authoring pass on the § Decision
section. I am not re-running the evaluator here because (a) the task scope is
limited to `artifacts/justify/`, and (b) a re-run on a pool artifact would be a
discretionary authoring action outside this defect-repair sweep. The honest
disposition is: stale on the evidence available, with the re-evaluation path noted
should the artifact ever be re-promoted.
