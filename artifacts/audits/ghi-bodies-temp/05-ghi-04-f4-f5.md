## Class of failure

**F4:** 4.7's Adaptability is a **strength** per System Card §6.2.2.2 — *"reliably diagnoses root causes rather than patching surface symptoms."* gzkit governance text couples "root-cause fix" with "full OBPI ceremony-mandate" (Iron Law, rule 6c). 4.7's natural thoroughness gets channeled into ceremony — the GHI #195 / withdraw commit `d2ed160b` failure mode. The fix is decoupling, NOT degrading 4.7's Adaptability.

**F5:** Where two surfaces state incompatible rules, 4.7's literal read either violates one or deadlocks. Four active contradictions identified.

## Evidence

### F4 — over-ceremony (no skill references defect-fix-routing.md)

| Location | Pattern | Severity |
|---|---|---|
| `.gzkit/skills/gz-obpi-pipeline/SKILL.md:29-75` | Iron Law mandates 5-stage ceremony; no scope gate | **P0** |
| `.gzkit/skills/gz-skill-router/SKILL.md:60` | "Execute the full implementation pipeline → gz-obpi-pipeline" — routes ALL OBPI work to pipeline, no defect branch | **P1** |
| `.gzkit/rules/governance-core.md:18-25` | "Required workflow order" 6 steps, no scope | **P1** |

### F5 — cross-surface contradictions

| A | B | Conflict | Severity |
|---|---|---|---|
| `gh-cli.md:11` "Use `gh` only when explicitly requested" | `governance-core.md:16` + `behavioral-invariants.md:31` "Every defect must be tracked" | Deadlock: file (violate gh-cli) or skip (violate governance-core) | **P0** |
| `behavioral-invariants.md:61` 6c "Prefer the more thorough fix over the narrower fix" | `defect-fix-routing.md` "default-to-ceremony is the failure mode this rule exists to close" | Direct semantic conflict; both landed April 2026 | **P0** |
| `arb.md:36-50` ARB "when debugging" (optional) | `attestation-enrichment.md:37-88` ARB fail-closed mandatory for heavy-lane | Scope mismatch | **P0** |
| `git-sync/SKILL.md:46` "`--lint --test` is redundant" | Same skill `:80` "`--apply` without `--lint --test` = red flag" | Same skill, 34 lines apart | **P1** |

## Fix plan

Two coordinated commits under this GHI:

1. `fix(skills): add defect-fix-routing scope gate to pipeline and router (GHI #<N>)` — F4 decoupling:
   - `gz-obpi-pipeline § When to Use`: *"If the change is ≤10 source lines + ≤2 files + in-flight defect, invoke `.gzkit/rules/defect-fix-routing.md` instead of this pipeline."*
   - `gz-skill-router` decision tree: add defect-fix branch with same threshold.
   - `governance-core.md § Required workflow order`: scope to OBPI implementation; cross-reference `defect-fix-routing.md` for non-OBPI paths.

2. `fix(governance): resolve cross-surface contradictions flagged in 4.7 audit (GHI #<N>)` — F5:
   - `gh-cli.md:11`: explicit carve-out — *"Use `gh` for (a) defect tracking per governance-core.md, (b) closeout per adr-audit.md, (c) release ceremony, or (d) explicit user request."*
   - `behavioral-invariants.md:61` rule 6c: *"Choose fix scope per defect-fix-routing.md thresholds, not intuition."* (Removes "prefer the more thorough fix" framing.)
   - `arb.md:36-50`: reframe as decision matrix keyed on attestation context; fail-closed for heavy-lane, optional for lite. Links to the GHI-00 command-example fix (separate sub-GHI under umbrella #224).
   - `git-sync/SKILL.md`: remove L80 red flag (pre-commit hook covers the gate per L46-47).

**Implementer note:** Iron Law stays intact. F4 fix is adding scope-scaling, not weakening thoroughness expectations. 4.7 Adaptability is a strength to preserve, not a behavior to degrade.

## Routing

Multi-file (7+ files); ~35-40 lines total. Two commits. Direct-fix per `defect-fix-routing.md`.

## Tracked under

Umbrella GHI #224 (4.7 regression — governance surface hardening).
