gz adr fidelity: ADR-0.33.0-airlock-membrane
  Assertions: 4

  PASS  Airlock-IN's ping consumes the HULL sonar (`gz ontology reach`), which
is present (green now — anchors the reach dependency)
        command:  uv run gz ontology reach ADR-0.32.0-gzkit-ontology
        expected: 0  observed: 0

  PASS  Airlock-IN computes the seam-map and reaches a go/no-go at the pipeline
door
        command:  uv run gz airlock in --target OBPI-0.33.0-01 --phase build
--dry-run
        expected: 0  observed: 0

  PASS  The section-5 enforcement floor verifies the airlock-in-unaccounted-seam
live NC (registered by OBPI-02) among its passing claim set
        command:  uv run gz validate --qc-binding
        expected: 0  observed: 0

  PASS  Airlock-OUT emits the drift-diff and logs to L2 at the pipeline exit
        command:  uv run gz airlock out --target OBPI-0.33.0-01 --dry-run
        expected: 0  observed: 0

  Summary: 4 pass, 0 fail
