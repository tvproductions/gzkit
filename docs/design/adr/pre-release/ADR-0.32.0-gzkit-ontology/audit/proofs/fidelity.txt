gz adr fidelity: ADR-0.32.0
  Assertions: 4

  PASS  `gz ontology sense` images the current shape and exits clean on a
healthy tree
        command:  uv run gz ontology sense
        expected: 0  observed: 0

  PASS  `gz ontology trace` walks a real node's vertical + lateral lineage
        command:  uv run gz ontology trace ADR-0.31.0-obpi-state-machine
        expected: 0  observed: 0

  PASS  The Harness-Purity fence refuses a product object placed in
`ownership:harness`
        command:  uv run gz validate --ontology-purity
        expected: 0  observed: 0

  PASS  `sense --json` emits the machine-readable shape plus the
rebuild-fidelity self-report
        command:  uv run gz ontology sense --json
        expected: 0  observed: 0

  Summary: 4 pass, 0 fail
