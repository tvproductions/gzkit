---
id: ADR-pool.bounded-advisory-impact
status: Pool
parent: PRD-GZKIT-1.0.0
lane: heavy
enabler: null
---

# ADR-pool.bounded-advisory-impact: Bounded advisory change-impact report

## Status

Pool

## Intent

Preserve the three-pillars assessment's unimplemented advisory-impact opportunity
under [GHI #1053](https://github.com/tvproductions/gzkit/issues/1053). Import-only
retrieval missed real CLI registry and test consumers in the dated
[measurement](../../../evals/three-pillars-impact-2026-09-19/report.md).
The goal is to expose evidenced consumers and explicit coverage gaps before
local changes are judged complete, without conferring acceptance authority.

## Decision

Operator g0, verbatim: **"pool, not feature."** Recorded 2026-09-19.

Book the [reviewed design](../../../evals/three-pillars-impact-design-2026-09-19.md)
as a pool proposal. Its Concrete product contract and semantic acceptance matrix
are retained as the detailed candidate specification. This ADR is the registered
destination for GHI #1053; it does not ship the capability or claim the proposed
validation has run. No release, feature scheduling or OBPI initiation follows
from pool registration.

Candidate scope comprises a current-byte capture; source import consumers;
complete known handler-registry witness chains; direct test imports and
recognized subprocess entry-point consumers; full text/JSON populations; and
explicit unsupported relations, failed parses and unstable captures. Declared
REQ/document links stay separately attributed. These are scope components, not
an OBPI checklist. Pool entries have no implementation briefs.

Broad proof/review currency remains unchanged under the separate
[#1029 ruling](../../../evals/three-pillars-proof-currency-design-2026-09-19.md).
The report must not certify safety, narrow proof currency or install an airlock
gate. A short display is not evidence of a bounded review population.

## Alternatives Considered

- Import-only report: insufficient for the measured dispatch and test omissions;
  useful as one section of the candidate.
- General semantic dependency graph: unsupported completeness and scope claims;
  excluded from this proposal.
- Narrow acceptance currency using the advisory result: insufficient authority;
  the operator separately selected retention of broad currency.
- Active feature now: not selected; the operator explicitly chose pool.

## Consequences

The design and its evidence have a durable registered home. No capability is
implemented by this record; discovery limits and unmeasured human review cost
remain. Reassess current code and the stored design before any promotion.

## Promotion constraints

ADR-0.37.0 Alternative 2 requires a measured reviewable bound for its file-coupling
successor. Preserve that condition; do not claim the count comparison satisfied
all of it. The design proposes a human-effort protocol, but the upstream ADR
does not prescribe a timing threshold. Later authoring must demonstrate or
explicitly resolve that proposal without inventing measured benefit. Normal
campaign order and operator initiation apply. This pool does not join the
runtime track or reorder other work by existing.

## Persona

main-session: craftsperson, governance-aware, whole-file reasoning, direct.
Name witnessed relationships and uncertainty; never substitute a populated
record for observed behavior.

## Q&A Transcript

The agent requested a feature ADR. Operator g0: "pool, not feature." The agent redundantly asked about downstream boundaries. Operator g0: "you are creating a pool ADR, what about that is confusing to you?" The agent proceeded with the explicit pool instruction. Earlier, g0 selected "A" for retaining broad proof currency under #1029.

The companion interview JSON distinguishes operator quotations from agent-authored
design synthesis; no additional operator answers or implementation approval are implied.

## Forcing Functions

### Pre-Mortem

An apparently short report is mistaken for proof that omitted consumers cannot exist; unsupported relationship types and stale capture remain hidden. Preserve per-type evidence and gaps; never confer completion authority.

### What Would Have to Be True

Typed sections must recover measured omissions while remaining reviewable in real cases. Import-only would be preferable only if those missing relationships ceased to matter or were reliably supplied elsewhere; the existing measurement contradicts that condition for its sampled cases.

### Constraint Archaeology

ADR-0.37.0 Alternative 2 requires a measured reviewable bound. Human timing is a proposed evaluation method, not an immutable upstream requirement. Existing campaign sequence and independent acceptance authority remain in force.

### Assumption Surfacing

Assumes witnessed consumers reduce omissions enough to justify review effort. Counts are proxies, not human benefit. If typed output only adds noise, revise or decline promotion rather than hide a tail.

### The 2am Operator Question

Need the exact selected paths, scanned roster, producer identity, source locations, failures and unsupported relationships so an empty or misleading result can be diagnosed.

### Reversibility

Pooling commits no runtime contract. A later advisory query can be removed without rewriting proof identity or completion history; retaining this boundary makes reversal local.

### Scope Minimization

Minimum useful proposal includes source imports, complete known-registry chains and test consumers together, with capture and gap accounting. Import-only repeats the measured false-empty case.

### Downstream Decisions Forced

Pool status carries no implementation commitment. Any later promotion and implementation follow normal campaign and feature/OBPI governance, retaining the documented reviewability condition. Proof currency remains broad under the separately recorded #1029 ruling.

## Evidence

- [Implementation plan](../../../evals/three-pillars-implementation-plan-2026-09-19.md)
- [Measurement and retained full populations](../../../evals/three-pillars-impact-2026-09-19/report.md), GHI #1052
- [Reviewed concrete design and proposed validation protocol](../../../evals/three-pillars-impact-design-2026-09-19.md)
- [Interview record](bounded-advisory-impact-interview.json)

These are analysis, design and routing evidence, not implementation evidence.

## Notes

Pool ADRs are backlog items: no semver or kind frontmatter, no implementation
attestation, no OBPIs. The scaffold's heavy lane reflects the proposed CLI
exposure; no runtime contract is changed by this document. Promotion, if later
chosen, must re-evaluate the design against then-current code.
