gz adr fidelity: ADR-0.0.72-meta-governance-coherence
  Assertions: 2

  PASS  Re-scoped (2026-07-13): the coherence thesis is realized as localized
writer-model checks, not a global validator. The insights writer surface
(OBPI-03's `InsightRecord` adapter target, closing C4/GHI #575) is
shape-coherent against its own authoring model.
        command:  uv run gz validate --insights-shape
        expected: 0  observed: 0

  PASS  The Fidelity Assertions block is parseable by the fidelity gate.
        command:  uv run gz adr fidelity ADR-0.0.72-meta-governance-coherence
--check
        expected: 0  observed: 0

  Summary: 2 pass, 0 fail
