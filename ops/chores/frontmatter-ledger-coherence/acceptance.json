{
  "criteria": [
    {
      "type": "exitCodeEquals",
      "command": "uv run gz frontmatter reconcile --dry-run",
      "expected": 0,
      "description": "Ledger-wins reconciliation dry-run exits 0 when no drift would be rewritten. If non-zero, run the non-dry-run variant (uv run gz frontmatter reconcile) to resolve drift, then re-verify."
    }
  ]
}
