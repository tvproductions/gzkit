{
    "criteria": [
        {
            "type": "exitCodeEquals",
            "command": "uv run gz lint",
            "expected": 0,
            "description": "Project lint baseline holds (uses gzkit's configured exclusions)"
        },
        {
            "type": "exitCodeEquals",
            "command": "uv run gz typecheck",
            "expected": 0,
            "description": "Project typecheck baseline holds (uses gzkit's configured exclusions)"
        }
    ]
}
