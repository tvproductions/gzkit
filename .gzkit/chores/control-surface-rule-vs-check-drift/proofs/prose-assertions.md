# Prose Assertions — Enforcement Claims Extracted from Rule Canon

**Generated:** 2026-08-01 (audit-only re-run; supersedes the 2026-05-10 artifact)
**Sources:** the 25 authored rules under `.gzkit/rules/` plus root `AGENTS.md`.
Vendor mirrors (`.claude/rules/`, `.agents/`, `.github/`) are derivatives and were
not audited. `.gzkit/rules/AGENTS.md` is a generated aggregate
(`<!-- Generated by gzkit — do not edit manually -->`, line 1) of
`agent-failure-modes.md` + `skill-surface-sync.md`; its claims are scored on the
source rules, not separately.

**Extraction filter.** This pass captures only prose that **claims mechanical
enforcement** — a named command, flag, test, hook, constant, or function asserted
to make the rule bind. Doctrine that states an obligation without naming an
enforcer is recorded in § Z as *unclaimed* (no parity row: a rule that never
claimed a check cannot have drifted from one). Verbatim quotes are marked
**[V]**; everything else is a faithful paraphrase with a line cite.

Claim IDs (`C-nn`) are referenced by `parity-diff.md`.

---

## 1. `adr-audit.md` (rule-version 0.2.0, `paths: docs/design/adr/**`)

| ID | Claim | Cite |
|---|---|---|
| C-01 | **[V]** "Bare commands emit no receipt; step 4 then fail-closes at exit 3 on Heavy lane and `foundation` kind (`_zero_receipt_result(fail_closed=True)`). Locked by `CANONICAL_STEP_COMMANDS`" | `adr-audit.md:32` |
| C-02 | **[V]** "a payload with zero receipt citations fail-closes at exit 3 before the attestation is recorded" | `:49` |
| C-03 | REQ-kind diagnosis branches are backed by `gz validate --req-kind-discipline`; a `@covers` test is BEHAVIOR's only proof channel | `:60-62` |
| C-04 | Five covers-backfill exemption shapes; the inline marker's reason text is **[V]** "mechanically required so the exemption can't be a one-token escape hatch" | `:68-79` |

## 2. `agent-failure-modes.md` (0.4.0)

| ID | Claim | Cite |
|---|---|---|
| C-05 | **[V]** "**Loading posture:** Advisory vocabulary, not a mechanical gate. The defenses (AGENTS.md § Never #1, ARB receipts, hook fail-closed, `gz validate --commit-trailers`, T1/T2/T3) are the shared backstops." | `:27` |

*Self-disclaiming. The only enforcement claim is by reference to backstops it does not own.*

## 3. `agents-md-map-doctrine.md` (0.3.0)

| ID | Claim | Cite |
|---|---|---|
| C-06 | `gz validate --agents-md-map-conformance` is the shape gate (ADR-0.0.54 / OBPI-0.0.54-03) | `:14`, `:34`, `:38` |
| C-07 | **[V]** "The **live enforced budgets are the values in `data/instructions_files_budget.json`** — the single source of truth read by `gz validate --instructions-files-budget`. This doctrine never duplicates those numbers into prose" | `:30` |
| C-08 | **[V]** "the GHI #712 delivered-surface witness is now in the default `gz check` scope" | `:14` |
| C-09 | The conformance gate asserts paragraph shape, prohibited subsection titles, `See`-link resolution, and file size **in AGENTS.md** | `:34-38` |

## 4. `brief-heading-conventions.md` (0.1.0)

| ID | Claim | Cite |
|---|---|---|
| C-10 | **[V]** frontmatter `description:` — "Brief evidence sections must use H3 (not H2) — enforced by gz validate --brief-headings (GHI #238)" | `:5` |
| C-11 | **[V]** "Exits 3 on drift. Recovery: rewrite the heading as H3 and re-run completion. Do not 'accept both levels' at the hook — the hook is the contract; the brief is the defect." | `:48-50` |
| C-12 | Three canonical evidence sections are consumed by `gz obpi complete` / `extract_closing_argument` | `:21-25` |

## 5. `changelog-release-notes.md` (1.1.0)

| ID | Claim | Cite |
|---|---|---|
| C-13 | **[V]** "**Structure (hermetic, offline):** `gz validate --changelog` fails closed on changelog shape — SemVer versions, ISO `YYYY-MM-DD` dates, category set, and one `GHI #N` citation per entry." | `:48` |
| C-14 | **[V]** "**Coverage (release-time, networked):** `gz-patch-release` cross-checks that every closed-since-tag user-visible GHI appears before publish." | `:49` |
| C-15 | **[V]** "no mechanical release-notes validator — the curated narrative is not machine-checkable" | `:50` |
| C-16 | **[V]** "runs standalone and at release-time (operator-chosen: not in the default `gz check`)" | `:53-54` |

## 6. `chores.md` (0.3.0)

| ID | Claim | Cite |
|---|---|---|
| C-17 | **[V]** "Stray `CHORE.md` or `acceptance.json` outside the two canonical roots is a defect — `gz validate --chores-layout` enforces it (REQ-0.0.21-08-04)." | `:55` (§ Core Principles table) |
| C-18 | **[V]** "`uv run gz validate --chores-layout      # Fail closed (exit 3) on stray CHORE.md or acceptance.json`" | `:101` |
| C-19 | **[V]** "Enforced going forward by `gz validate --deprecated-verb-prescription`." | `:16-17` |
| C-20 | `doctor` never touches `proofs/` (REQ-0.0.21-09-05) and never modifies project-local-only slugs (REQ-0.0.21-09-06) | `:103-104` |

## 7. `cli.md` (0.3.0)

| ID | Claim | Cite |
|---|---|---|
| C-21 | **[V]** "**Mechanical check:** `uv run gz cli audit` (see § Core Principles — Consistency)." | `:33` |
| C-22 | **[V]** "run `uv run gz cli audit`; it must exit 0 with the new verb covered across manpage, command doc, and index, **and the new flag's usage line agreeing with its parser** (required-ness and value-taking; GHI #693) … the audit is the mechanical check, not operator taste." | `:43` |
| C-23 | Exit-code map, binding on gzkit CLI surfaces: 0 success / 1 user-config error / 2 system-IO error / **3 policy breach**. **[V]** "Use `sys.exit(code)`. Document codes 2/3 in help text." | `:49-58` |
| C-24 | **[V]** "`gz cli audit` does **not** adjudicate this … Its green is evidence about docs, never about lane assignment." | `:101` |

## 8. `complexity-doctrine.md` (0.3.1)

| ID | Claim | Cite |
|---|---|---|
| C-25 | Seven selection criteria are **binding — all must hold** | `:31-42` |
| C-26 | Seven corpus disqualifiers are **binding — any disqualifies** | `:44-52` |
| C-27 | **[V]** "The citation is a three-field tuple … codified by the Pydantic `Citation` model at `src/gzkit/complexity/citation.py` (frozen, `extra='forbid'`) and mirrored by the JSON Schema at `src/gzkit/schemas/complexity_citation.json` (`additionalProperties: false`). The tuple is the only valid citation shape" | `:85-92` |
| C-28 | **[V]** "Every cited boundary MUST appear as **both** a percentile-of-corpus **AND** the absolute-number-at-that-percentile … citing either form alone is a contract violation." | `:111-113` |
| C-29 | **[V]** "The link-integrity validator (`gz validate --complexity-doctrine-links`, OBPI-0.0.27-07) flags but does NOT auto-rewrite." | `:120-121` |

## 9. `complexity-thresholds.md` (0.4.0)

| ID | Claim | Cite |
|---|---|---|
| C-30 | **[V]** "corrected `> See [...]` pointer paths … so `gz validate --pointer-anchors` resolves them correctly (ADR-0.0.33 Invariant 3)" | `:16` |
| C-31 | **[V]** "Every metric MUST carry a `block` band; a metric without a `block` band is prose, not a threshold." | `:46-48` |
| C-32 | **[V]** "the loader fails closed when a canonical metric is missing a block-band row" | `:80-81` |
| C-33 | **[V]** "Exactly three values are accepted by the schema … A fourth value (e.g. `info`, `fatal`, `debug`) is forbidden" | `:52`, `:62-64` |
| C-34 | **[V]** "**Silent edits are forbidden** by the validator (OBPI-0.0.28-03 -- `gz validate --complexity-thresholds`)." | `:97` |

## 10. `cross-platform.md` (0.5.0)

| ID | Claim | Cite |
|---|---|---|
| C-35 | **[V]** "Enforced by `tests/governance/test_path_separator_portability.py` (GHI #383)." | `:31` |
| C-36 | **[V]** "Do NOT prefix `uv run gz` with `PYTHONUTF8=1`. The runtime guard covers only `uv run gz ...` — fresh `python -c` or helper scripts need explicit `sys.stdout.reconfigure(...)`. Enforced by `gz validate --utf8-prefix` (GHI #275)." | `:35` |
| C-37 | **[V]** "Enforced by `tests/governance/test_subprocess_errors_replace.py` via `audit_subprocess_errors` (GHI #582)." | `:39` |

## 11. `gate5-runbook-code-covenant.md` (0.2.0)

| ID | Claim | Cite |
|---|---|---|
| C-38 | ARB-wrapped canonical invocations are the receipt source; **[V]** "on Heavy lane and `foundation` kind, missing receipt IDs are fail-closed (`gz adr emit-receipt` exits 3 before attestation is recorded). Locked by `CANONICAL_STEP_COMMANDS` in `src/gzkit/arb/validator.py`" | `:43` |
| C-39 | `gz validate --documents --surfaces` is a covenant proof command | `:47` |

## 12. `gh-cli.md` (0.3.0)

| ID | Claim | Cite |
|---|---|---|
| C-40 | **[V]** "The prohibition is on the **caller**, not the string: `/ghi-author` itself invokes `gh issue create` at `SKILL.md:199` as its own final step, so the sanctioned and forbidden invocations are byte-identical commands. **Nothing mechanical can tell them apart — the discipline is yours to keep.**" | `:30` |
| C-41 | `gz issue file` **[V]** "auto-stamps provenance, validates surface references, and hard-rejects bodies referencing no gzkit surface" | `:55` |

*The most honest disclaimer in the corpus. C-40 is the model this chore's findings should be measured against.*

## 13. `governance-core.md` (0.7.0, `paths: "**/*"` — loaded on every edit)

| ID | Claim | Cite |
|---|---|---|
| C-42 | **[V]** "Human attestation is **universal** for every OBPI completion, regardless of kind, lane, or sensitivity (ADR-0.0.36; `_requires_human_obpi_attestation` returns `True` unconditionally). There is no lane in which Gate 5 is optional." | § Non-negotiable rules |
| C-43 | **[V]** "Every `gz <verb>` string appearing in an operator-facing doc must resolve to a registered parser verb. Scope: `docs/**/*.md`, `docs/**/*.feature`, `features/**/*.feature`, `.gzkit/skills/**/SKILL.md`, and runbooks under `docs/user/runbook.md` + `docs/governance/governance_runbook.md`." | `:73` |
| C-44 | **[V]** "**Multi-word subcommands count (`gz adr status`, `gz obpi complete`), not just top-level verbs.**" | `:75` |
| C-45 | **[V]** "Enforced by `gz validate --cli-alignment`. **Exit 3 on any unresolvable reference.**" | `:79` |
| C-46 | **[V]** "Enforced by the same `--cli-alignment` flag (`audit_manpage_alignment`) … Terminal OBPI briefs are exempt … (scoped via `is_terminal_brief_status`, the predicate `--brief-command-shape` and the `--sensitivity` floor also use)." | `:81` |
| C-47 | **[V]** "Drift between the committed index and on-disk canon is fail-closed by **`uv run gz validate --adr-status-fresh`**, which is part of the default `uv run gz check` pipeline." | `:93-96` |
| C-48 | **[V]** "`gz validate --deprecated-verb-prescription` fails closed on any governed surface that prescribes a deprecated verb." | `:25` |
| C-49 | **[V]** "`--attestor` and `--reason` are required and fail closed when empty (exit 1, no ledger write). `--cause` is a closed enum" | § Withdraw vs Repudiate |

## 14. `guardrail-feedback-prose.md` (0.1.0)

| ID | Claim | Cite |
|---|---|---|
| C-50 | **[V]** "**Every fail-closed hook and validator emits agent-actionable three-part recovery prose** … Scope: hooks (exit 2 + stderr), `gz validate` scopes (exit 3 diagnostics), ceremony gates, and pre-commit/pre-push guards. Advisory (non-blocking) surfaces SHOULD follow the same bar but are not fail-closed by this rule." | `:20`, `:35-37` |
| C-51 | **[V]** "asserted against this bar by `tests/hooks/test_stop_turn_feedback.py`" | `:42` |

## 15. `hexagonal-architecture.md` (0.2.0)

| ID | Claim | Cite |
|---|---|---|
| C-52 | § Verify names `gz lint` and `gz typecheck` only. **No import-boundary / port-adapter validator is claimed.** | § Verify |

## 16. `model-selection.md` (0.3.0)

| ID | Claim | Cite |
|---|---|---|
| C-53 | **[V]** "**Skill SKILL.md files carry explicit `model:` frontmatter.** No inference; declaration is the contract." / "Every skill SKILL.md MUST declare the model tier" | `:20`, `:38` |
| C-54 | **[V]** "**Valid values:** `haiku`, `sonnet`, `opus`. No inference; no runtime detection; declared value is the contract." | `:50` |

*No enforcer is named for either. Recorded as unclaimed-but-binding (§ Z).*

## 17. `models.md` (0.1.0)

| ID | Claim | Cite |
|---|---|---|
| C-55 | § Verify — all models extend `BaseModel`; **all models have `model_config` with at least `extra="forbid"`**; immutable snapshots use `frozen=True` | § Verify |
| C-56 | `@dataclass` is the named anti-pattern for governance models | § Do Not |

*Neither names `gz validate --pydantic-models`, which is the check that exists.*

## 18. `mx-mode.md` (1.0.1 marker / **1.0.0 blockquote**)

| ID | Claim | Cite |
|---|---|---|
| C-57 | **[V]** "Honor the marker: when `.gzkit/mx.json` exists, most guards drop to advisory" | `:23` |
| C-58 | **[V]** "`gate5_invariants` remain **fail-closed**. Gate 5 is never advisory." | `:28` |
| C-59 | **[V]** "The MX awareness hook fires every turn to remind the agent of the open state." (`.claude/hooks/mx-awareness.py`) | `:29`, `:56` |
| C-60 | The file's own version marker (`<!-- rule-version: 1.0.1 -->`, `:12`) disagrees with its blockquote (`> **Rule version:** \`1.0.0\``, `:16`) — the exact `marker-blockquote-drift` predicate of `--rule-version-markers` | `:12` vs `:16` |

## 19. `pythonic.md` (0.2.0)

| ID | Claim | Cite |
|---|---|---|
| C-61 | **[V]** § Size Limits self-audit table header — "What is actually enforced today:" with an explicit `Enforced by` column | `:45-52` |
| C-62 | Classes <= 300 lines — **[V]** `gz validate --class-size` named as the **live gate** | `:49` |
| C-63 | Functions <= 50 lines and modules <= 600 lines — the table's own `Enforced by` column names **no mechanism** | `:47-52` |
| C-64 | **[V]** "*(cyclomatic)* | `.pre-commit-config.yaml` `uvx xenon --max-absolute C` (CC 11–20) | **live gate** — a *third* ceiling, matching neither authority: the table's `radon_cc` blocks at 11.0, so a CC-15 function passes xenon while `block` per the table" | `:52` |
| C-65 | **[V]** "`tests/governance/test_type_ignore_syntax.py` fail-closes on any" bracketed `# type: ignore[...]` | `:97` |

*C-61–C-64 are a rule that audits its own promotion status in-band. Scored as self-disclosed.*

## 20. `security-sensitivity.md` (0.5.1)

| ID | Claim | Cite |
|---|---|---|
| C-66 | **[V]** "**Auto-detect floor.** Briefs whose allowed-paths overlap a registered security surface MUST declare `sensitivity: security` (exit 3 on violation)." | `:27` |
| C-67 | **[V]** "Both an *omitted* declaration (`sensitivity-floor-violation`) and a *wrong* declaration (`sensitivity-escape-attempt`) over an overlap fail closed at exit 3 (GHI #625)." | `:28` |
| C-68 | **[V]** "**The floor demotes to advisory inside the MX hangar** … `sensitivity` is **not** a member of that set (`src/gzkit/mx/invariants.py`), so `checkpoint.resolve('sensitivity', …)` returns ADVISORY and the exit-3 errors above are dropped from the exit code. The 'escape is fail-closed' language in clause 2 does **not** hold in the hangar." | `:29` |
| C-69 | **[V]** "`gz validate --sensitivity` reads OBPI brief frontmatter only … a briefless commit to `data/security_surfaces.json` is never inspected, so the self-bootstrapping floor is **unenforced on the path operator canon mandates** … the declaration on a direct-fix registry edit is a **discipline obligation, not a mechanical one**." | `:23` |
| C-70 | **[V]** "`gz obpi complete` on a `sensitivity: security` brief fires an extended Gate 5 walkthrough … Scanner-unavailable is fail-closed (no degradation)." | `:37` |

## 21. `skill-surface-sync.md` (0.10.0)

| ID | Claim | Cite |
|---|---|---|
| C-71 | **[V]** "**Skills** carry the marker **nested under `metadata:`** as `metadata.skill-version` … Presence is enforced (`SKA-METADATA-SKILL-VERSION-MISSING`, blocking) alongside the pre-existing format check." | `:22` |
| C-72 | **[V]** "(hard cutover invariant, enforced by `tests/commands/test_skills.py::TestSkillCommands::test_init_scaffolds_adr_create_and_removes_adr_manager`)" | `:110` |
| C-73 | Canonical surface class-classifier table — five `_classify_*_file` helpers each with a named test | `:184-188` |
| C-74 | `gz validate --distribution` consults those classifiers, so a `package_only` / `runtime_state` verdict is not distribution drift | `:152`, `:190` |
| C-75 | Rule files carry a body-level `<!-- rule-version: X.Y.Z -->` marker (non-negotiable rule #2) | § Version discipline |

## 22. `task-discovery.md` (0.5.0)

| ID | Claim | Cite |
|---|---|---|
| C-76 | **[V]** "any commit touching `src/**` or `tests/**` MUST additionally carry a `Task:` trailer" | `:24` |
| C-77 | **[V]** "`gz validate --commit-trailers` fails closed on src/ and tests/ scope regardless of decoration (`has_task_trailer()` in `src/gzkit/tasks.py`; GHI #552). `@advances` registration has no commit-time consequence." | `:26` |
| C-78 | **[V]** "Layer-drift across the channels (different TASK IDs for the same logical unit) is itself a fail-close signature" and **[V]** "The OBPI-04 validator will fail Heavy lane closeouts on layer-drift; **Lite lane warns.**" | `:26`, § Layer-drift fail-close |
| C-79 | **[V]** "The eight validator-enforced worklog event types in `src/gzkit/events.py` (`artifact_edited`, `gate_checked`, `evidence_emitted`, `policy_breach`, `validator_run`, `tool_invoked`, `agent_message`, `lint_run` — the set `gz validate --task-envelope-coherence` signature (a) checks)" | `:98` |
| C-80 | **[V]** "Default-bucket-only OBPIs (every REQ has only `seq=01`) without a `req_atomic: list[str]` exemption in brief frontmatter are themselves a fail-close signature (OBPI-04 validator scope)." | § Subdivision sub-invariant |
| C-81 | **[V]** decoration-time fail-close — "Invalid TASK ID format → `ValueError` at import"; "Unknown parent REQ → `ValueError` at import" | § Convention: Python `@advances` |
| C-82 | **[V]** "`.gzkit/hooks/prepare-commit-msg-task-trailers` appends a `Task:` line per in-progress TASK on `src/**`/`tests/**` commits; an authored trailer of ANY form suppresses it. **Witness status unruled — GHI #731.**" | § Convention: Commit trailer |

## 23. `tests.md` (0.13.0)

| ID | Claim | Cite |
|---|---|---|
| C-83 | **[V]** "**Smoke/BVT <=60s** — binds the `@smoke`-marked subset run by `uv run gz smoke`, NOT the full unit tier. **Enforced by that verb (exit 3 on breach or on an empty tier)** and run as the `Smoke tier` step of `gz check` (GHI #724)." | `:18` |
| C-84 | `gz validate --test-tiers` forbids a third tier under `tests/` | `:44` |
| C-85 | **[V]** "**src/tests commits MUST carry a `Task:` trailer.** Enforced by `gz validate --commit-trailers`. `Ceremony:` and `Eval-feedback-source:` no longer substitute for `Task:` on src/tests scope" | `:87` |
| C-86 | **[V]** "Enforced by `gz validate --behave-req-tags` (fires on `Completed`/`Validated` briefs only, GHI #276/#323). Waivers in `data/behave_coverage_waivers.json`." | `:118` |
| C-87 | **[V]** "A BEHAVIOR REQ with neither a scenario tag nor an `@covers` test still fails closed; a missing `[kind]` tag defaults to BEHAVIOR." | `:120` |
| C-88 | **[V]** STRUCTURAL-FENCE proof channel "pins … to the explicit OBPI-combination anchor token `(OBPI-NN[, OBPI-MM, …])`, making the parent-ADR `## Boundary Invariants` binding mechanical rather than heading-presence-only … enforced by `resolve_fence_proof`." | `:12` |
| C-89 | **[V]** SUPPORT ledger arm "is now path-specific. A bare event of the cited type no longer proves; an event must *cite the path*" — 81 pre-cutover REQs in `data/support_proof_grandfather.json` | `:12` |
| C-90 | **[V]** "`gz test` runs `unittest` over `tests/` then `behave` over `features/`. Both gates must pass for `gz check`." | `:108` |

## 24. `token-block-discipline.md` (0.3.0)

| ID | Claim | Cite |
|---|---|---|
| C-91 | **[V]** "**Validator Rule (OBPI-0.0.41-04):** Entries lacking any of these four fields MUST fail the `gz validate --lock-handoff-coupling` validator (exit 3, fail-closed)." | `:51` |
| C-92 | **[V]** "The structured channel is canonical, named per-field: items 1, 2, and 4 are **frontmatter keys**; item 3 is the **`## Decisions Made` body section**. This is the single channel `gz validate --lock-handoff-coupling` reads … free-body prose for items 1/2/4 does NOT satisfy the validator." | `:41` |
| C-93 | **[V]** "Unregistered categories are rejected (fail-closed) at CLI parsing time." | `:35` |
| C-94 | **[V]** "At 12 hours (50% TTL), the SessionStart hook logs a WARNING to console and ledger"; "At 24 hours (100% TTL), the next SessionStart hook invokes `lock_manager.reap_expired_locks()` automatically" | `:79-80` |
| C-95 | **[V]** "The release call fails-closed if the handoff does not exist." | `:90` |
| C-96 | **[V]** "The `gz validate --lock-handoff-coupling` validator replays the ledger, checks every `obpi_lock_released` event, and fails-closes if any event lacks a valid `handoff_path` reference or the referenced handoff violates the minimum-information rule." | `:107` |
| C-97 | **[V]** Sub-Invariant 6 — completion "writes a full (non-abandoned) completion handoff … The handoff is written BEFORE the delete (fail-safe …)" | `:111` |

## 25. `tool-skill-runbook-alignment.md` (0.2.0)

| ID | Claim | Cite |
|---|---|---|
| C-98 | **[V]** Invariant 1 — "Every CLI verb registered in `src/gzkit/cli/` must be invoked by at least one skill under `.gzkit/skills/` — either via the skill's frontmatter `gz_command:` field or in the skill's body instructions." | `:21` |
| C-99 | **[V]** Invariant 2 — "Every skill's declared `gz_command:` … must resolve to a CLI verb that the runbook prescribes for the same operator moment." | `:25` |
| C-100 | **[V]** Invariant 3 — "the CLI verb in the skill's `gz_command` must produce that form as its default human-readable output." | `:29` |
| C-101 | **[V]** Preamble — "These three invariants are **the mechanical test** for layer alignment" | `:15` |

## 26. Root `AGENTS.md`

| ID | Claim | Cite |
|---|---|---|
| C-102 | **[V]** "Per-file char budget for AGENTS.md / CLAUDE.md / `.claude/rules/*.md` — `gz validate --instructions-files-budget`; budgets in `data/instructions_files_budget.json`." | § Mechanical scopes |
| C-103 | **[V]** "The editor/IDE authoring-guide protocol envelope is defined by `src/gzkit/schemas/authoring_guide_protocol.json` — **schema-validated at runtime** (ADR-0.0.30)." | § Mechanical scopes |
| C-104 | **[V]** "`Field(min_length=1)` on `AdvisorDiagnosis.proof` — `gz validate --advisor-proof-binding` (OBPI-0.0.29-08)." | § Mechanical scopes |
| C-105 | **[V]** "Complexity calibration is grounded in an empirically-measured exemplar corpus (**seven selection criteria**) — `gz validate --complexity-doctrine-links` (OBPI-0.0.27-07)." | § Mechanical scopes |
| C-106 | **[V]** "Heavy/foundation lane requires explicit human attestation before completion — `gz closeout` pipeline." | § Mechanical scopes |
| C-107 | **[V]** "`.gzkit/rules/*.md` with `paths: '**'` or missing `paths:` **may not live under any vendor-surface rules directory** (ADR-0.0.20) — `gz validate --unscoped-rules`." | § Mechanical scopes |
| C-108 | **[V]** "`gz validate --invariant-coherence` — composition drift fail-close: re-renders the registry and byte-compares against committed AGENTS.md (ADR-0.0.37); **in the `gz check` default scope**." | § Mechanical scopes |
| C-109 | **[V]** "OBPI brief reconciles against current project shape **before Stage 2 and before completion** — `gz validate --brief-reconcile` (ADR-0.0.37)." | § Mechanical scopes |
| C-110 | **[V]** "`abandon categories are closed` — lock release is coupled to a handoff/register entry (ADR-0.0.41)." | § Mechanical scopes |
| C-111 | **[V]** REQ-kind discipline — "each kind has exactly one proof channel … — `gz validate --req-kind-discipline` (ADR-0.0.59)." | § Mechanical scopes |
| C-112 | **[V]** "`gz validate --taxonomy` enforces: `foundation` ⇒ `0.0.x`, `feature` ⇒ non-`0.0.x`, **`pool` ⇒ no `kind`/`semver` frontmatter**" | § Kinds |
| C-113 | **[V]** "`kind:` frontmatter on every non-pool ADR; validated against schema enum `{foundation, feature}` (`src/gzkit/schemas/adr.json`)" | § Kinds |
| C-114 | **[V]** "Enforced by `_requires_human_obpi_attestation`." | § OBPI Acceptance Protocol |
| C-115 | **[V]** "`--accept-uncovered` is refused on every lane, because BEHAVIOR's only proof channel is a `@covers` test (GHI #537)." | § OBPI Acceptance Protocol |
| C-116 | **[V]** Attestation table of five canonical invocations (`gz arb ruff`, `gz arb typecheck`, `gz arb step --name unittest`, `gz arb coverage`, `gz arb step --name mkdocs`) — "**Locked by `CANONICAL_STEP_COMMANDS`; `gz arb validate` flags drift.**" | § Attestation |
| C-117 | **[V]** "**Lane behavior:** **Lite lane:** missing receipt IDs produce a warning. **Heavy lane:** missing receipt IDs are fail-closed." | § Attestation |
| C-118 | **[V]** "Enforced by `forbid-pytest` pre-commit hook and `.gzkit/rules/tests.md`." | § STDLIB-FIRST |
| C-119 | **[V]** Gate Covenant table — Gate 1 `gz validate --documents`; Gate 2 `gz test`; **Gate 3 Docs updated → `gz lint`**; Gate 4 BDD verified → **Manual check**; Gate 5 `gz attest` | § Gate Covenant |
| C-120 | **[V]** "`gz validate --taxonomy` enforces … `gz adr promote --kind {foundation,feature}` writes `kind:` into promoted ADR frontmatter" | § Kinds |
| C-121 | **[V]** "the trailer is validated by `gz validate --commit-trailers` and traces the rule change back to the evaluation feedback loop source artifacts (ADR-0.0.26)" | § Behavior Rules — Always #12 |
| C-122 | **[V]** "self-tested via `gz validate --advisory-scorecard`" (of `docs/governance/advisory-rules-audit.md`) | § Governance doctrine surfaces |

---

## Z. Binding prose that names **no** enforcer (no parity row)

Recorded so the corpus count is honest — these are not drift, they are undeclared
promotion candidates. A rule that never claimed a check cannot have drifted from one.

| Rule | Unclaimed binding surface |
|---|---|
| `agents-md-map-doctrine.md` | Section-ownership + decrease-only ratchet deferred to ADR-0.35.0 § Decision 3 |
| `complexity-doctrine.md` | C-25 seven selection criteria, C-26 seven disqualifiers, C-28 percentile+absolute pairing, distillation cadence triggers |
| `gh-cli.md` | Prohibited-without-approval list (settings mutations, secret management, force pushes, unauthorized merges) |
| `hexagonal-architecture.md` | Port/adapter import boundaries (C-52) |
| `model-selection.md` | C-53, C-54 — `model:` frontmatter presence and value enum |
| `models.md` | C-55, C-56 — no flag named; `--pydantic-models` exists and the rule does not point at it |
| `mx-mode.md` | "Do not exit the hangar while any detectable defect remains unfixed" |
| `tool-skill-runbook-alignment.md` | C-99, C-100 — Invariants 2 and 3 |
| root `AGENTS.md` | PRIME DIRECTIVE 1-7; DO IT RIGHT 1-11; Anti-vibing operative claims 1-4; Operator-economy claims 1-6; Behavior Rules Always #1-#18 except #12/#13; Never #1-#10; § Defect-fix routing thresholds; **operator-PII prohibition** (highest-consequence unclaimed rule in the corpus — recovery from a leak is a filter-repo rewrite + force-push, and `operator-pii` *is* a `GATE5_INVARIANTS` member (`src/gzkit/mx/invariants.py:23-31`) with no `gz validate` scope behind it) |

**Counts.** 122 enforcement claims extracted across 25 authored rules + root `AGENTS.md`.
Two rules (`agent-failure-modes.md`, `gh-cli.md`) make no unqualified mechanical claim at
all; two more (`pythonic.md`, `security-sensitivity.md`) audit their own promotion status
in-band and name their own gaps.
