# Conformance sweep — every scope in its own process

Generated by `check_reachability.py --sweep` on 2026-09-12, working tree at
HEAD `464dd4ff8fa2ee12d98afe99a867280e1b82b431`.

```

1 failing scope(s) run on no commit path, so `gz check` stays green while they fail — the headline finding is the gap, not the failure.
swept 98 scope(s); 1 non-zero
  exit 3  --evaluation-justify-binding  [UNGATED (tier B)]
EXIT=3
```

The other 97 scopes returned zero. The instrument emits only failing rows;
it does not retain individual stdout for passing scopes. The matrix is in
`reachability-matrix.md`. This is a validator sweep, not a full quality-suite pass.

Direct rerun of `uv run gz validate --evaluation-justify-binding` returned exit 3:
three error rows name ADR-0.33.0-airlock-membrane, ADR-0.35.0, and
ADR-0.35.0-canon-entry-corpus-landing. Each reports a below-threshold Feature
Checklist score without the expected justification artifact. The two corpus
identifiers require identity/producer inspection before treating them as two
independent ADR failures. These are sampling signals for the later intent trace.

Disposition: investigate the requirement, evaluation records and artifact resolver
under the owning ADRs. No justification, waiver or ADR completion was fabricated
to clear this audit. The broad claim in the instrument's stderr about `gz check`
is a static reachability inference; this run did not execute the full `gz check`.
