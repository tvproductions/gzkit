# Frontier Model Card Currency

Detect drift between the frontier system cards vendors have published and the
cards gzkit's doctrine is actually sourced to, then route the refresh.

Scan + route only — it never rewrites doctrine. Card evaluation is judgment work
that lands as a GHI-tracked direct doc fix.

## Quick Start

```bash
uv run gz chores advise frontier-model-card-currency
```

## Lane

**lite**
