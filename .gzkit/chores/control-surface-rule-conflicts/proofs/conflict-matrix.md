# Conflict Matrix — Rule-Pair Pass A

> Chore: `control-surface-rule-conflicts` (Lite lane, audit-only)
> Run: **2026-08-01**. Supersedes the 2026-07-16 matrix.
> Schema: `ADR-pool.control-surface-rule-pair-conflict-audit` § Audit-row schema (GHI #448)
> Surface: 26 canonical rules under `.gzkit/rules/*.md` + root `AGENTS.md` + `CLAUDE.md` = 28 files, 378 unordered pairs.
> Vendor mirrors (`.claude/rules/`, `.github/instructions/`) are derivatives and were not audited.

**Binding on this and every future run (carried from the 2026-07-16 run's own finding):**
the *Mechanical winner* cell MUST cite a `file:line` that the author opened, never a
rule's claim *about* its enforcement. Every `file:line` below was re-opened this run;
line numbers that drifted since 2026-07-16 have been re-read, not copied.

Row ids (`R01`…) are stable within this run only. Prior-run row numbers are given in the
Rule A cell as `was: row N` so the retirement trail is legible.

| Rule A | Rule B | Worked example | Evidence | Mechanical winner | Suggested resolution | Severity |
|--------|--------|----------------|----------|-------------------|----------------------|----------|
| **R01** `.gzkit/rules/chores.md:33` § Two-Surface Layout — *"Canonical (package) &#124; `src/gzkit/chores/<slug>/` &#124; Authoritative templates"* and `:124` § Authoring a New Chore — *"Canonical chore packages live in `src/gzkit/chores/<slug>/` (shipped in the wheel). Project-specific overrides go in `.gzkit/chores/<slug>/`."* (was: row 12) | `.gzkit/rules/skill-surface-sync.md:42` § Surface layout — the `.gzkit/chores/<slug>/` cell sits under the header **Canonical (edit here)**, with `src/gzkit/chores/<slug>/` in the **Pkg copy (wheel-shipping)** column — and `:166` § Canonical surface class-classifier — *"**canonical** &#124; Operator-authored content present at `.gzkit/<surface>/` — the source of truth &#124; Required &#124; `.gzkit/ → src/gzkit/`"* | An agent authors or amends a `CHORE.md` and must pick a surface. The two rules name opposite canonical sides for the same directory, and **both prescribed repair commands are live and each destroys the other's edit**: edit `.gzkit/chores/…` per Rule B, then run `gz chores doctor` (which `chores.md` § Health and Layout prescribes) and the file is overwritten from package bytes; edit `src/gzkit/chores/…` per Rule A, then run `gz agent sync control-surfaces` (which skill-surface-sync #3 prescribes, and which `gz check` runs) and the file is overwritten from `.gzkit` bytes. Neither command warns. | GHI #448. SHAs: `322f07473` (last `chores.md` change — a `gz gates`→`gz check` repoint, not a direction fix) and `a58d01126` (last `skill-surface-sync.md` change — a `metadata.skill-version` fix, not a direction fix). Neither Pass-A remediation commit (`3a4aacf32`, `1ddb407d7`) touched this pair; it is the only 2026-07-16 blocking row left entirely unaddressed. | **`skill-surface-sync.md`, in steady state.** `src/gzkit/sync_surfaces.py:771-781` copies `.gzkit/chores → src/gzkit/chores` unconditionally on every sync (docstring at `:707`: *"Reads ONLY from `.gzkit/<surface>/` — never from `src/gzkit/`"*). `src/gzkit/commands/chores.py:501` (`_repair_damaged_doctor_slug`, def at `:482`, source `importlib.resources.files("gzkit.chores")` at `:489`) writes the **opposite** direction but only under the `_DOCTOR_STATUS_DAMAGED` guard at `:562`. `gz validate --chores-layout` and `--distribution` both exit 0 today, so neither adjudicates. | Reconcile in one rule: delete `chores.md` § Two-Surface Layout's and § Authoring's surface tables; replace with a pointer to `skill-surface-sync.md` § Surface layout. Amend `src/gzkit/chores/README.md` in the same patch — `chores.md:139` delegates the *"full agent contract"* to it, and it is the louder wrong signal. Then fix `_repair_damaged_doctor_slug`'s direction or rename `doctor` to name the package as a bootstrap-only source. The genuine ADR-0.0.21 content — *runtime resolution is project-first → package-fallback* — is orthogonal to *authoring surface*; one word, "canonical", carries two opposite referents. | blocking |
| **R02** `.gzkit/rules/governance-core.md:50-61` § Required workflow order (OBPI implementation path) — a six-step manual sequence (`gz state --json` → `gz status --table` → *"Implement one OBPI increment"* → `gz implement` → `gz closeout --dry-run` → `gz adr audit-check`) whose only carve-out is *"For in-flight defect fixes meeting the thresholds in AGENTS.md § Defect-fix routing, take the direct `fix(<scope>): … (GHI #N)` path instead"*. `gz obpi pipeline` appears nowhere in the file. **(NEW this run.)** | `AGENTS.md:228` § OBPI Acceptance Protocol — *"**Pipeline mandate (contract-bearing OBPI only):** For OBPI work that adds or changes a CLI/schema/runtime contract, run `uv run gz obpi pipeline <OBPI-ID>` after plan approval — the runtime owns stage sequencing … freeform implementation of such an OBPI without the runtime is a process defect."* Reinforced by `.gzkit/rules/cli.md:99` — *"contract-bearing CLI work runs `gz obpi pipeline`, not a freeform direct fix."* | An agent implements an OBPI that adds a `--json` flag to a `gz` verb. It is a planned OBPI under an active ADR, not a defect fix, so governance-core's scope clause does **not** except it — the rule's literal instruction at step 3 is *"Implement one OBPI increment"*, i.e. edit the source directly. Doing so trips `.claude/hooks/pipeline-gate.py` at exit 2, and `AGENTS.md` § Behavior Rules — Never #6 forbids working around a hook block. The agent is routed by the most-loaded rule in the repo into the exact act two other surfaces call a process defect. | GHI #606 (*"pipeline-gate: lock claimed + no pipeline = src writes pass unblocked"* — the arming path that made the block universal) and GHI #127 (the gate's origin). SHA `29ba38235` (last `AGENTS.md` change) and `ce1d19835` (last `governance-core.md` change — a manpage-alignment fix that did not revisit the workflow list). | **`AGENTS.md` — mechanically, and more strictly than it states.** `.claude/hooks/pipeline-gate.py:159-160` prints the gate message and `sys.exit(2)` for **any** governed OBPI in scope with no active pipeline marker — not only contract-bearing ones. Arming is `receipt_state == "pass"` (`:127`) or `agent_held_obpi_ids` (`:135`); scope is the brief's `## Allowed Paths` (`:146-148`); registered as a PreToolUse hook on Write and Edit at `.claude/settings.json:54` and `:96`. governance-core's sequence has no enforcement arm at all. | Reconcile in one rule: `governance-core.md` § Required workflow order gains the pipeline branch *before* step 1 — *"Contract-bearing OBPI (CLI/schema/runtime contract): run `uv run gz obpi pipeline <OBPI-ID>`; the runtime owns the sequence below. The manual order applies only to non-contract-bearing planned OBPI work."* This is the highest-leverage row in the matrix for the same reason row 14 was in the prior run: `governance-core.md` frontmatter is `paths: "**/*"`, so it is loaded on every edit in every session. Separately, the hook's scope (every governed OBPI) is **wider** than AGENTS.md's stated mandate (contract-bearing only) — reconcile the two or the mandate under-describes the gate. | blocking |
| **R03** `.gzkit/rules/chores.md:141` § Related — *"Manpage — `docs/user/manpages/gz-chores.md`"*. The file on disk is `docs/user/manpages/chores.md`; no manpage in the tree uses a `gz-` prefix. **(NEW this run.)** | `.gzkit/rules/governance-core.md:81` § Operator-doc verb resolution — *"Manpages use the `<verb>.md` convention — never a `gz-` prefix — so a `manpages/gz-<verb>.md` reference is an unresolvable operator-doc pointer of the same class as an unregistered verb. … Recovery is unconditional: drop the `gz-` prefix."* | An agent working a chores task follows `chores.md` § Related to read the chores manpage, opens `docs/user/manpages/gz-chores.md`, and gets nothing — the file is `chores.md`. This is precisely the 174-reference drift class GHI #532 was filed to drain; the drain swept `docs/**`, `features/**`, and `.gzkit/skills/**/SKILL.md` and **left the rule surface behind**, so the one rule that declares the convention binding sits two files away from a rule that violates it. | GHI #532 (*"manpages: 4 brief files reference docs/user/manpages/gz-validate.md (file is validate.md)"*, CLOSED). SHAs: `ce1d19835` (landed the binding, 2026-07-23) and `322f07473` (last `chores.md` change, 2026-07-21 — two days earlier, so the sweep never revisited it). | **Rule B doctrinally, nothing mechanically.** `src/gzkit/governance/trust_audits/cli.py:172-183` (`_manpage_alignment_sources`) enumerates `docs/**/*.md` (minus `docs/releases/`), `features/**/*.feature`, and `.gzkit/skills/**/SKILL.md` — `.gzkit/rules/**` is **not** a source. `uv run gz validate --cli-alignment` exits 0 today with the dead pointer in place; the regex that would catch it is `_MANPAGE_GZ_PREFIX_REF` at `cli.py:24`. | Two-part, both ≤10 lines: (a) direct fix — `chores.md:141` drops the `gz-` prefix (bump to `0.3.1`); (b) mechanical promotion — add `.gzkit/rules/**/*.md` to `_manpage_alignment_sources`, matching the scope `--deprecated-verb-prescription` already uses (`src/gzkit/governance/trust_audits/deprecated_verb_prescription.py:48` lists `.gzkit/rules/**/*.md`). The asymmetry between the two validators over the same rule's two bindings is the real defect: one reads the rule surface, the other does not. | episodic |
| **R04** `.gzkit/rules/task-discovery.md:73-75` § Convention: Commit trailer — *"**Auto-stamped (GHI #731).** `.gzkit/hooks/prepare-commit-msg-task-trailers` appends a `Task:` line per in-progress TASK on `src/**`/`tests/**` commits; an authored trailer of ANY form suppresses it. Witness status unruled — GHI #731."* **(NEW this run.)** | `.gzkit/rules/task-discovery.md:107` § Layer-drift fail-close — *"When a single logical unit of labor surfaces across multiple channels with different TASK IDs … the divergence IS the signal."* — and `:116` § Do Not — *"Do not silently rewrite TASK IDs across channels to 'make the validator happy' — layer-drift is the signal, not the defect to suppress."* | An agent commits a `src/gzkit/**` change while two TASKs are in progress. The hook stamps a `Task:` line per in-progress TASK from runtime state, with no author in the loop and every failure path a silent no-op (hook docstring, `.gzkit/hooks/prepare-commit-msg-task-trailers:23-25`). The commit-trailer channel now agrees with the runtime TASK set *by construction*, so layer-drift between those two channels can no longer arise — the signal § Layer-drift declares load-bearing is eliminated rather than observed. The hook's own docstring states this is the point: the channel was *"empty for 96 of 102 OBPIs and therefore skipped."* A machine-minted trailer is indistinguishable downstream from an authored one. | **GHI #731 — OPEN** (*"task-envelope Signature (c): layer-drift gate compares 6 of 776 OBPIs (two channels always empty)"*). SHA `4b9db7592` (`fix(tasks): stamp Task: trailers from the active TASK set (GHI #731)`, 2026-07-29 — the newest change to the whole audited surface). The rule concedes the gap in four words: *"Witness status unruled."* | **The hook, unconditionally.** `.gzkit/hooks/prepare-commit-msg-task-trailers` is a `prepare-commit-msg` hook, so it runs before the message is finalized on every commit; `has_task_trailer()` / `gz validate --commit-trailers` (`src/gzkit/tasks.py:219`, `_ANY_TASK_TRAILER_RE`) cannot distinguish a stamped trailer from an authored one — it matches on shape only. § Layer-drift's *"OBPI-04 validator"* is the unlanded `gz validate --task-envelope-coherence` scope; it has no arm against a producer-stamped channel. | Rule the witness status rather than leaving it *"unruled"* in a binding rule. Two coherent options, both ≤10 lines: (a) mark stamped trailers distinguishably (e.g. `Task: … (auto)`) so `--task-envelope-coherence` can exclude them from the drift comparison, preserving the signal § Layer-drift depends on; or (b) amend § Layer-drift to say the trailer↔runtime pair is producer-coupled by design and drift detection binds only the `@advances` and frontmatter `tasks:` channels. Do not leave the rule asserting a signal its own producer erases. Route under the already-open GHI #731. | episodic |
| **R05** `AGENTS.md:36` § PRIME DIRECTIVE 4 — *"**SCOPE EXPANSION IS NOT SCOPE CREEP. If fixing requires updating 3 docs, do it.**"* (was: row 2) | `AGENTS.md:292-294` § Defect-fix routing — *"### OBPI ceremony is required when ANY hold"* / *"- Crosses brief boundaries"* | An agent implementing an OBPI under brief A finds a 5-line bug in a module outside brief A's `## Allowed Paths`. PD 4 reads as license to expand. § Defect-fix routing reads as fail-closed: cross-boundary work triggers ceremony. Re-verified this run: **no Allowed-Paths qualifier was added** — `grep -in "allowed.paths" AGENTS.md` returns zero hits. The only narrowing anywhere (`AGENTS.md:56`, § DO IT RIGHT #11 — *"The expansion duty in 1a is for coupled-correctness surfaces only"*) scopes rule **1a**, a different rule, and is silent on brief boundaries. | GHI #195 (*"gz obpi precomplete: pre-Stage-5 checklist not mechanically enforced"*). SHA `29ba38235` (last `AGENTS.md` change, 2026-07-23 — did not touch PD 4). | **Rule B.** `src/gzkit/governance/trust_audits/orphaned_implementation.py:59` — `def _path_covered_by_allowed(event_path: str, allowed: list[str]) -> bool:` — ledger `artifact_edited` paths are tested against the brief's Allowed Paths and out-of-scope edits audit as orphaned. Nothing mechanizes PD 4's expansion duty. The disjunction is unresolved in prose and resolved *against* the PRIME DIRECTIVE in code. | Add one line to PD 4: *"Scope expansion stays within the active brief's `## Allowed Paths`. Cross-boundary fixes route per § Defect-fix routing."* Puts the pointer where the rule lives rather than requiring the agent to have read a section 250 lines away. | blocking |
| **R06** `.gzkit/rules/model-selection.md:61-64` § Subagent effort levels — the enum is exactly `light` → Haiku, `high` → Sonnet, `xhigh` → Opus, `max` → Opus + extended thinking; § Do Not forbids *"Defaulting subagents to `effort: xhigh` when `effort: light` would suffice"* (was: row 9) | `CLAUDE.md:11-13` § Opus 4.7 tuning — *"Default effort: `xhigh` for gzkit agentic work; `high`/`medium` for lookups; reserve `max` for genuinely hard problems."* | An agent dispatches an Explore subagent for a read-only lookup. `CLAUDE.md` says `high`/`medium`. `medium` **is not in model-selection's enum at all** (re-verified: four values only), and `high` maps to Sonnet, the unnamed middle ground model-selection's § Do Not exists to discourage. The agent cannot satisfy both. Worse, it will usually never see the conflict: `model-selection.md` frontmatter `paths:` is exactly `["src/gzkit/pipeline_runtime.py", ".gzkit/skills/**/SKILL.md", ".claude/agents/**"]` — it does **not** load in a general session, which is when most dispatches happen. `CLAUDE.md` loads every turn. | GHI #409 (*"Enforce model-selection routing in skill frontmatter"*). SHAs: `8deb53b1b` (`chore(claude): add Opus 4.7 tuning section to CLAUDE.md (GHI #283)` — the only commit ever to touch that section) and `322f07473` (last `CLAUDE.md` change, which did not revisit it). | **`CLAUDE.md`, by loading posture — neither by enforcement.** Scoping is enforced at `src/gzkit/rules/__init__.py:510` (`paths: list[str] = Field(`) via the frontmatter model that `load_rules` (`src/gzkit/rules/__init__.py:563`) consumes, so the `paths:` exclusion is real, not advisory. No effort→model mapper exists in this repo (`grep -rin "effort" src/gzkit --include=*.py` → no mapper); the mapping lives in the harness. | Split scopes and say so in both files: `CLAUDE.md` § tuning governs the **main session's own** effort; `model-selection.md` § Subagent effort levels governs **dispatched subagents**. Delete `medium` from `CLAUDE.md` or add it to the enum — one or the other, not neither. **Additional drift found this run:** the heading still reads *"Opus 4.7 tuning"* and `model-selection.md:72` still uses `claude-opus-4-7` as its worked example of a forbidden hardcoded model id, while the running model is Opus 5. Fix in the same patch. | blocking |
| **R07** `.gzkit/rules/pythonic.md:35` § Size Limits & Refactoring — *"**Limits:** Functions <=50 lines &#124; Modules <=600 lines &#124; Classes <=300 lines"* (was: row 11) | `.gzkit/rules/complexity-thresholds.md:43-44` § Invariant — *"gzkit publishes one canonical threshold table … A new threshold authority appearing anywhere else is doctrine drift by another name."* | A 45-line function: pythonic.md says compliant (≤50); the canonical table's `lizard_nloc` blocks at 37.0. Inverse: a 700-line module fails pythonic's ≤600 but sits in `radon_raw_nloc`'s **advise** band (block = 1031.9), so the agent refactors toward a number the threshold authority says is not a defect. Two authorities, same quantity, opposite verdicts in both directions. **Changed since 2026-07-16:** `pythonic.md` 0.2.0 now names the conflict in-body and tells the reader which number binds (*"treat <=300 as binding (it gates), <=50 and <=600 as guidance, and cite the table"*). The numbers did not move; the agent-facing ambiguity did — hence the severity drop from `blocking` to `episodic`. | GHI #426. SHA `1ddb407d7` (`fix(rules): reconcile remaining Pass A rows` — the commit that added the naming without changing a number; its message states *"NO number changed: the table has no class-size metric, so 'just cite the table' would delete the only enforced size gate"*). | **`pythonic.md` for classes; the pre-commit hook for cyclomatic; nobody for functions/modules.** Re-verified this run: `src/gzkit/governance/trust_audits/code_quality.py:81` — `limit = 300`, still hardcoded. `src/gzkit/commands/complexity_advise.py:47` — `METRIC_KEY = "radon_cc"`; `:126` — `metrics_checked = 1  # currently only radon_cc`, so the table's length bands still have **no consumer**. `.pre-commit-config.yaml:49` — `uvx xenon --max-absolute C …`, a **third** ceiling (CC 11–20) matching neither authority, since `.gzkit/rules/complexity-thresholds.json:12` blocks `radon_cc` at 11.0. | Unchanged from the prior run and still routed to the operator, not guessable: one authority; repoint `audit_class_size` at `load_threshold_table`; derive the xenon `--max-absolute` from the table. Resolution needs a class-size corpus band the corpus does not carry — a `gz-complexity-distill` pass, not a prose edit. | episodic |
| **R08** `.gzkit/rules/complexity-doctrine.md:109-113` § Percentile + absolute pairing (binding) — *"Every cited boundary MUST appear as **both** a percentile-of-corpus **AND** the absolute-number-at-that-percentile … citing either form alone is a contract violation."* (was: row 24) | `.gzkit/rules/complexity-thresholds.md:84-91` § Bootstrap absolutes (REQ-11 carve-out) — *"Three metrics in the data file carry bootstrap absolutes **not derived from the cited corpus distillation**: `radon_mi`; `lizard_nesting_depth` — parser defect, all-zero corpus; `cohesion_lcom4` — parser defect, all-zero corpus."* | An agent reads `.gzkit/rules/complexity-thresholds.json:48` — `{"metric": "lizard_nesting_depth", "corpus_percentile": 99, "absolute_number": 4.00, "trigger_semantic": "block"}` — and cites it as a corpus boundary, which Rule A's pairing contract obliges. Rule B says `4.00` was never corpus-derived, so the `99` is a synthetic label satisfying the schema's shape while asserting a corpus fact Rule B denies. The tell, re-measured this run: those six bootstrap rows are the **only** rows in the file whose block band sits at `corpus_percentile: 99`; every genuinely corpus-derived metric blocks at 95. `complexity-doctrine.md` § Corpus Disqualifiers #1 independently names *"post-hoc fitting: selecting projects that confirm a pre-decided threshold"* as disqualifying. | GHI #404 (all-zero baselines for `lizard_nesting_depth` and `cohesion_lcom4`), GHI #405 (`radon_mi` polarity), GHI #469. Current JSON: `.gzkit/rules/complexity-thresholds.json:14-16` (`radon_mi`), `:46-48` (`lizard_nesting_depth`), `:54-56` (`cohesion_lcom4`). | **Rule B — the carve-out ships and the pairing contract has no runtime arm.** `src/gzkit/complexity/thresholds.py:63` (`_every_metric_has_block_band`) only checks that a `block` band *exists*; `_check_percentile` range-checks the integer. Nothing cross-checks an absolute against the cited distillation, so a fabricated pairing loads clean. `gz validate --complexity-doctrine-links` checks link integrity, not pairing truth. | Either complexity-doctrine § Percentile + absolute pairing admits the bootstrap carve-out by name with the corpus revision at which each expires, **or** the bootstrap rows null the percentile (`corpus_percentile: null`) so the data stops asserting a corpus fact that is not one. The latter is stronger: it makes the carve-out visible in the data and gives the *"one-shot"* an actual expiry surface. | episodic |
| **R09** `.gzkit/rules/cli.md:74-80` § Output Contracts — *"Default &#124; Human-readable (tables, colors, progress)"*, *"`--json` &#124; Valid JSON to stdout; logs to stderr"* (was: row 6) | `.gzkit/rules/tool-skill-runbook-alignment.md:27-29` § Invariant 3 — *"When a skill's Output Contract declares a required rendering form ('table', **'JSON'**, 'tree', 'plain text', etc.), the CLI verb in the skill's `gz_command` must produce that form as its **default human-readable output**."* | A skill author declares `Output Contract: JSON`. Invariant 3 then requires the destination verb's **default** output to be JSON; cli.md requires the default to be human-readable with `--json` as the opt-in. The two cannot both hold for that skill. Sharpened this run: there is **no skill schema under `src/gzkit/schemas/`**, and `.gzkit/schemas/skill.schema.json` carries no `output_contract` property and no enum at all — Output Contract is a free-text `##` body heading, so nothing prevents an author from declaring JSON. Today's skills happen to comply; that is luck, not a fence. | GHI #327 (the diet pass that last touched both files' surrounding sections). SHA `8aa9b887e` (last `cli.md` change, 2026-07-17 — a `gz cli audit` hardening that did not touch § Output Contracts). | **Rule A by inertia — neither by enforcement.** `grep -rin "output_contract" src/gzkit --include=*.py` finds no validator; the only hits are docstrings (`src/gzkit/commands/complexity_advise.py:10`, `src/gzkit/commands/complexity_distill_cmd.py:10`). Invariant 3 is entirely unmechanized. | Restrict the skill Output Contract vocabulary to `{table, tree, plain, prose}` in `.gzkit/schemas/skill.schema.json` and add the property so the enum can bind. Skills needing machine output invoke the verb with `--json` explicitly and declare the human-readable form as their contract. This is the one row whose fix is a schema change rather than a prose edit. | episodic |
| **R10** `.gzkit/rules/gate5-runbook-code-covenant.md:35-38` § Required updates when behavior changes — *"Update command docs and examples. Update runbook flows and verification commands."* (was: row 7) | `AGENTS.md:194` § Gate Covenant — Lane Rules — *"- **lite**: Gates 1, 2 required"* (Gate 3 = *"Docs updated"* is absent from the lite set) | A Lite-lane fix changes a `gz` verb's output format. gate5-runbook demands docs + runbook updates in the same patch set; the lane matrix marks Gate 3 optional for lite. An agent reading only the lane matrix ships the fix without the doc update. Re-verified this run: **no Gate-3 trigger was added** — the gate5-runbook file *was* bumped to `0.2.0`, but its own header names *"Pass A conflict-matrix rows 15 and 19"*, not row 7, so this pair was never in that patch's scope. | GHI #428. SHA `3a4aacf32` (the bump that fixed rows 15 and 19 in this same file and left this row untouched). | **Rule B.** `src/gzkit/commands/gates.py:380` — `gates_for_lane = manifest.get("gates", {}).get(lane, [1, 2])`, against `.gzkit/manifest.json:68-72` — `"gates": {"lite": [1, 2], "heavy": [1, 2, 3, 4, 5]}`. A lite ADR whose behavior changed never reaches `_run_gate_3` (`src/gzkit/commands/gates.py:168`), so the covenant's duty has no gate to fire in. `AGENTS.md:53` § DO IT RIGHT 1a (coupled-surface coherence) sides with gate5-runbook doctrinally but is likewise unmechanized. | Amend § Lane Rules: *"lite: Gates 1, 2 required; Gate 3 additionally required whenever the change touches a surface documented under `docs/user/manpages/**` or `docs/**/runbook*.md`."* That is the always-on coupled-surface rule expressed in the lane matrix where an agent will actually read it. | episodic |
| **R11** `.gzkit/rules/complexity-thresholds.md:95-97` § Operator-amendable mapping protocol — *"Amendments to `(metric, band, trigger)` mappings flow through the doctrine-amendment-protocol stub (ADR-0.0.27 OBPI-02). **Silent edits are forbidden** by the validator."* (was: row 8) | `.gzkit/rules/skill-surface-sync.md:21` § Non-negotiable rules #2 — *"**Bump the version on every edit.** Increment the version marker before saving."* — whose `paths:` (`:6`) includes `".gzkit/rules/**"`, globbing `complexity-thresholds.json` | An agent corrects a `(metric, band, trigger)` row in `.gzkit/rules/complexity-thresholds.json`. skill-surface-sync #2 demands a version-marker bump on every edit under `.gzkit/rules/**`, and `:39` says rules carry that marker as a body-level `<!-- rule-version: X.Y.Z -->` HTML comment — **which a JSON file cannot hold**. complexity-thresholds says the edit needs the amendment protocol instead. Neither rule tells the agent what to do with the JSON. Sharpened this run: the JSON escapes Rule B *entirely*. | GHI #426. SHA `1ddb407d7` (the commit that landed `gz validate --rule-version-markers`, the mechanical arm — and scoped it to `.md` only). | **Rule A — the JSON escapes Rule B by construction.** `src/gzkit/validators/rule_version_markers.py:78-80` — `rules_root = project_root / ".gzkit" / "rules"` then `for path in canonical_rule_files(rules_root):`, an iterator that is `.md`-only. `complexity-thresholds.json` is never scanned and can be edited with no version bump at all, in direct contradiction of *"every edit."* | Add the scope sentence to § Operator-amendable mapping protocol — *"This protocol governs the JSON data file; editorial changes to this narrative file follow the ordinary `skill-surface-sync` version-bump protocol"* — **and** amend skill-surface-sync #2 to say the `.json` sibling's version discipline is the amendment protocol, not the marker. Fixing only the prose leaves the validator gap. | episodic |
| **R12** `.gzkit/rules/chores.md:52` § Core Principles — *"**Lite by default** &#124; Run `uv run -m unittest -q` (unit tier only); no `behave`, no network, no external services."* (was: row 5) | `.gzkit/rules/tests.md:108` § Two runners, one test surface — *"`gz test` runs `unittest` over `tests/` then `behave` over `features/`. **Both gates must pass for `gz check`.**"* | A Lite-lane chore runs `unittest`, passes its acceptance criteria, and a maintainer reads "chore validated" as "project validated." `gz check` would fail until behave runs. Re-verified this run: `tests.md` reached `0.13.0` and still contains **no** chore-lane carve-out (`grep -in "chore" .gzkit/rules/tests.md` → nothing); it still speaks of one undifferentiated test surface. The `0.3.0` `chores.md` bump added a *duration* pointer to tests.md § Smoke/BVT but obtained no reciprocal behave carve-out. | GHI #276. SHAs `322f07473` (chores.md 0.3.0) and `a7b1c6c1d` (tests.md 0.13.0) — two independent post-audit bumps, neither reconciling the pair. | **Rule B.** `src/gzkit/quality.py:522` — `behave = run_behave(project_root)` inside `run_all_checks` (`:508`), unconditional, no lane parameter. The chore lane's prohibition is metadata only: `src/gzkit/commands/chores.py:244` — *"# The lanes block carries gate-rigor metadata only"* — and `src/gzkit/chores/registry.json:11-13` carries `"allowNetwork": false`, for which `grep -rn "allowNetwork" src/gzkit --include=*.py` finds **zero** Python readers. The lite lane's behave/network prohibition is enforced by nothing. | One sentence in `tests.md` § Two runners: *"Chore-lane validation under `chores.md` is a strict subset and is not equivalent to `gz check`, which gates on both runners."* Removes the lane-scope conflation. Separately worth a GHI: `allowNetwork` is declared in the registry and read by no code. | theoretical |
| **R13** `AGENTS.md:88` § STDLIB-FIRST DOCTRINE, claim 1 — *"**The default is stdlib.** When a capability exists in stdlib, that path is chosen absent named rationale to depart."* (was: row 4) | `.gzkit/rules/models.md:14` — *"Use **Pydantic `BaseModel`** for all data models; no stdlib `dataclasses`."* and `:42` § Do Not — *"stdlib `dataclass` for governance data"* | Authoring a new immutable governance domain model, stdlib offers `@dataclass(frozen=True)` at zero dependency cost. Claim 1 prefers stdlib; models.md forbids dataclasses outright. Canonical reading reconciles — `AGENTS.md:90` claim 2 (*"Departures are foundation-attested"*) and § Existing canonical applications name Pydantic as **the** worked example of a named departure. But re-verified this run: **`models.md` still carries no back-reference** — `grep -n "STDLIB&#124;departure&#124;AGENTS" .gzkit/rules/models.md` (alternation) returns only the two prohibition lines. An agent landing on `models.md` first sees a flat contradiction of the doctrine with no anchor. | SHA `42c91a89` (`fix(chores): enforce evidence-bar at control-surface-rule-conflicts` — the commit that established this row's evidence bar). `models.md` is unchanged since `6026b8015` (2026-05-26); the prior run's suggested one-line cross-reference was never applied. | **`models.md`, correctly** — AGENTS.md's own § Existing canonical applications ratifies it. This is a missing-pointer defect, not a doctrinal disagreement. | One line in `models.md`: *"Pydantic is the single ratified departure from `AGENTS.md` § STDLIB-FIRST DOCTRINE (validation semantics stdlib cannot supply); see § Existing canonical applications."* Bump to `0.1.1`. | theoretical |
| **R14** `.gzkit/rules/security-sensitivity.md:28` § `gz validate --sensitivity` clause 2 — *"A brief MAY NOT omit sensitivity while overlapping (**escape is fail-closed**)."* (was: row 22) | `.gzkit/rules/mx-mode.md:23` § Honor the marker — *"when `.gzkit/mx.json` exists, most guards drop to advisory"*, with only *"`gate5_invariants` remain **fail-closed**"* (`:28`) | An operator enters the MX hangar to repair a brief whose Allowed Paths overlap a registered security surface with no `sensitivity:` declaration. Clause 2 says exit 3, no degradation. mx-mode drops every guard outside `GATE5_INVARIANTS` to advisory, and `sensitivity` is not a member — so the exit-3 errors are dropped and the hangar voids a floor clause 2 calls unconditional. **Changed since 2026-07-16:** `security-sensitivity.md` `0.5.0` added clause 3, which states the demotion out loud, names it *"deliberate, not a defect"* (a fail-closed sensitivity scope would lock the hangar against the very briefs an operator enters it to repair — GHI #682), and warns *"Promoting `sensitivity` into `GATE5_INVARIANTS` would set that trap; do not do it without first discharging #682."* The contradiction between clauses 2 and 3 remains on the page, now annotated rather than silent. | GHI #682 (*"sensitivity: 2 post-cutover briefs fail floor over security overlap"*), GHI #650, GHI #625. SHA `e2d38c3c0` — verified this run to be a **pure path rename** (`.gzkit/mx-active` → `.gzkit/mx.json`) across both files, changing no semantics; the reconciliation was the earlier `0.5.0` prose. | **`mx-mode.md`, live.** `src/gzkit/mx/invariants.py:23` — `GATE5_INVARIANTS: frozenset[str] = frozenset(` with members at `:25-30`: `"gate5-attestation"`, `"secrets"`, `"operator-pii"`, `"ledger"`, `"grader-gaming"`. Five members; **no `sensitivity`**. Additional live nuance found this run: `src/gzkit/mx/invariants.py:54` — `_GATE5_NAMED_NOT_ENFORCED: frozenset[str] = frozenset({"secrets", "operator-pii"})` — two of the five are named-not-enforced even inside the fail-closed set. | Residual only: fold the clause-3 caveat into clause 2 itself so a reader who stops at clause 2 is not misled (*"escape is fail-closed outside the MX hangar — see clause 3"*). Do **not** promote `sensitivity` into `GATE5_INVARIANTS` until GHI #682 is discharged; the rule is explicit that doing so sets a trap. | theoretical |
| **R15** `.gzkit/rules/security-sensitivity.md:21` § Registry contract — *"The registry is self-bootstrapping: edits require the editing brief to declare `sensitivity: security`."* and `:41` § Do Not — *"Editing `data/security_surfaces.json` without declaring `sensitivity: security`"* (was: row 23) | `AGENTS.md:342` § Operator Doctrine (verbatim canon) — *"GHIs are AUTHORIZED for direct repair, always … Never spin up an ADR or OBPI merely to discharge a GHI."* | A GHI reports that `data/security_surfaces.json` omits a surface. Rule A requires an editing **brief** declaring `sensitivity: security`; a brief is an OBPI, which requires a parent ADR. Rule B forbids spinning up an OBPI to discharge a GHI and routes to a direct `fix(security): … (GHI #N)` commit — which has no brief and therefore no declaration channel. **Changed since 2026-07-16:** `0.5.0` added § Registry contract's *"Direct-fix path (no brief exists)"* paragraph, which names the gap, calls the declaration *"a discipline obligation, not a mechanical one"*, and closes *"Do not read the absence of a fail-close as permission."* Residual: **§ Do Not was not reconciled** — it still lists the prohibition flatly, with no mechanism behind it on the path canon mandates. | GHI #682, GHI #625. SHA `e2d38c3c0` (last change to the file — the path rename, which did not revisit § Do Not). | **Rule B — the direct-fix path stays open and unchecked.** Re-verified this run: `src/gzkit/commands/validate_cmd.py:790` — `declared = frontmatter.get("sensitivity")`, inside `_sensitivity_records` (`:747`), whose only input is `_iter_sensitivity_briefs(project_root)` (`:775`). The candidate commit-side channel the rule names is not implemented: `grep -rn "Sensitivity:" src/gzkit --include=*.py` → nothing. | Mechanical promotion: require a `Sensitivity: security` trailer on any commit touching `data/security_surfaces.json`, checked by `gz validate --commit-trailers` — the channel the rule itself already nominates. Until then, add the clause-23 pointer to § Do Not so the prohibition and its actual discharge path sit together. | theoretical |
| **R16** `.gzkit/rules/agents-md-map-doctrine.md:34` § Budget — *"AGENTS.md sits **560 B** under the Codex `project_doc_max_bytes` default — **growth past that boundary fails the default gate closed**."* **(NEW this run.)** | `AGENTS.md:330` § Mechanical scopes that bind here — *"Per-file char budget for AGENTS.md / CLAUDE.md / `.claude/rules/*.md` — `gz validate --instructions-files-budget`; budgets in `data/instructions_files_budget.json`"* — and the same doctrine section two paragraphs above (`:30`) — *"The **live enforced budgets are the values in `data/instructions_files_budget.json`** … This doctrine never duplicates those numbers into prose: a duplicated number drifts from what is enforced."* | An agent lands ~700 B of corpus-sourced doctrine, taking rendered `AGENTS.md` from 32,208 B past 32,768 B. Rule A says the default gate now fails closed, so the agent expects `gz check` to stop it. It does not: the enforced budget is `"AGENTS.md": 50000` (≈17.8 KB of headroom) and the vendor-cap observer never fails. The agent ships a surface Codex silently truncates from `## Architectural Boundaries` down, with a green `gz check` — the exact failure GHI #712 was filed on. Rule A also violates its own adjacent prohibition by hard-coding `32768` into prose. | GHI #712 (*"agents-md: 560 B from silent Codex truncation, no gate observes it"*, CLOSED 2026-07-25 — the origin; its body records *"That enforcement was deliberately removed"*). GHI #533 (OPEN, the 15k destination). SHA `2c57548a1` (`agents-md-map-doctrine.md` `0.3.0`, landed the same day #712 closed and mis-stated the delivered posture). Arithmetic re-measured this run: `wc -c AGENTS.md` = 32208; 32768 − 32208 = 560. | **Rule B / the JSON.** `src/gzkit/governance/trust_audits/surface_delivery_witness.py:130` — `"""Emit the byte-distance observation. Never fail-closed (2026-07-06 ruling)."""` — emission is `emit_advisory(...)` at `:136`. `data/instructions_files_budget.json` `"files": {"AGENTS.md": 50000}`, and its `_doc` states the vendor cap *"is observed, not enforced, by the surface-delivery witness … it reports byte distance to stderr and fail-closes only on survival-declaration drift."* Rule A's sentence is false as written. | Direct fix, ≤3 lines: § Budget's third paragraph drops the fail-close claim and the hard-coded `32768`, per its own second paragraph — *"the delivered-surface witness reports byte distance to stderr as an advisory; it does not fail closed on vendor-cap proximity. The enforced ceiling is the value in `data/instructions_files_budget.json`."* Bump to `0.3.1`. If a fail-close on vendor-cap proximity is actually wanted, that is a separate mechanical-promotion GHI against the 2026-07-06 decoupling ruling — not a prose assertion. | episodic |
| **R17** `.gzkit/rules/agents-md-map-doctrine.md:38` § Shape enforcement — *"`gz validate --agents-md-map-conformance` (OBPI-0.0.54-03, forthcoming) asserts: - No paragraph **in AGENTS.md** is > 5 lines without a binding-bullet anchor …"* **(NEW this run.)** | `.gzkit/rules/skill-surface-sync.md:31` § Non-negotiable rules #5 — *"**Never edit `src/gzkit/<surface>/` directly.** `src/gzkit/skills/`, `src/gzkit/rules/`, `src/gzkit/personas/`, and `src/gzkit/templates/` are derived outputs regenerated by sync. Only the sync mechanism (`sync_pkg_surfaces`) writes to them."* | Two concrete halves. (a) An agent adds an `## Anti-patterns` subsection — prohibited shape #2 — to the content that renders into root `AGENTS.md`; `gz check`'s *"AGENTS.md map conformance"* step passes, because shape criteria run only against the template, never against the rendered file the doctrine names as the subject. (b) When the template *does* trip a criterion, the reported artifact is `src/gzkit/templates/agents.md` — and the validator's docstring calls it *"the operator-edit surface where violations are authored … this is the canonical surface"*. An agent following that error edits the exact path Rule B declares write-only-by-`sync_pkg_surfaces`; the correct target is `.gzkit/templates/agents.md`. Rule A also self-contradicts: § Shape enforcement says *"forthcoming"* while § Budget (`:34`) in the same file says the validator *"ships and binds now"* — the latter is correct. | GHI #533 (OPEN). SHA `2c57548a1` (the `0.3.0` bump, which rewrote § Budget and left § Shape enforcement's *"forthcoming"* and its AGENTS.md-subject framing untouched). | **Split, and neither rule is right about the subject.** `src/gzkit/governance/trust_audits/agents_md_map_conformance.py:91-92` — `_TEMPLATE_REL_PATH = Path("src") / "gzkit" / "templates" / "agents.md"` and `_RENDERED_REL_PATH = Path("AGENTS.md")`. Shape criteria (a)/(b)/(c) run on the template only (`:121-131`); the rendered file gets `_check_budget` alone (`:133-139`). So root `AGENTS.md` shape violations are unenforced by either side. Step registered at `src/gzkit/commands/quality.py:463`. | Three ≤5-line edits, one patch: (a) delete *"(OBPI-0.0.54-03, forthcoming)"* — it shipped; (b) § Shape enforcement states the actual two-layer split (template = shape, rendered = budget) instead of naming AGENTS.md as the shape subject; (c) repoint the validator's `ValidationError.artifact` and docstring at `.gzkit/templates/agents.md`, the surface `skill-surface-sync.md` § Surface layout marks canonical, so the error names an editable path. Verified byte-identical to the package copy today, so (c) is a label fix, not a behavior change. | episodic |

## Run log

| Run | Rows | Files touched by ≥1 row | Notes |
|-----|------|--------------------------|-------|
| 2026-05-11 | 11 | 11 of 22 | Rows 9, 10, 11 carried mechanical-winner claims derived from rule prose, not code. |
| 2026-07-07 | 11 | 11 of 28 | Refresh; no methodology change. |
| 2026-07-16 | 25 | 19 of 28 | Full re-walk, four independent readers. Three prior rows corrected/refuted. |
| **2026-08-01** | **15** | **16 of 28** | Re-walk against a surface that moved on 12 of 28 files. **7 prior rows retired by fix**, 2 downgraded by disclosure, 3 new rows. |

## What changed since the 2026-07-16 run

The dominant fact of this run: **the prior audit was actioned within hours of being
written, and the matrix was never updated to record it.** The proofs were committed at
13:11 on 2026-07-16; `3a4aacf32` landed at 14:48 and `1ddb407d7` at 16:22 the same day,
between them remediating rows 11, 13–23 and 25. The prior matrix therefore described a
surface that had already moved before the day ended — which is exactly the staleness the
`check_proof_freshness.py` gate (GHI #743) now fails closed on.

### Retired — conflict no longer exists

| Prior row | Pair | Retired by |
|---|---|---|
| 13 | `gh-cli.md` § Allowed commands vs `AGENTS.md` Always #13 | `3a4aacf32` → `gh-cli.md` `0.3.0`. `gh issue create` removed from § Allowed commands; retained quarantined in a `/ghi-author`-internal fence. New § heading at `:26` states the prohibition. |
| 14 | `governance-core.md` tracking bullet vs Always #13 + #11 | `3a4aacf32` → `governance-core.md` `0.5.0`. Bullet now reads *"tracked — via `/ghi-author` … or `gz insights remember`"* (`:48`). |
| 15 | `gate5-runbook-code-covenant.md` § Do Not *"heavy/foundation scope"* vs universal attestation | `3a4aacf32` → `0.2.0`. § Do Not now states attestation is universal per ADR-0.0.36. |
| 16 | `governance-core.md` Gate-5 lane conditional vs § Universal OBPI Attestation | `3a4aacf32` → `0.5.0`. Conditional dropped; `:45` now reads *"There is no lane in which Gate 5 is optional."* |
| 17 | `cli.md` *"New Flag (Additive = Lite Lane)"* vs `AGENTS.md` § Lane Rules | `1ddb407d7` → `cli.md` `0.2.0`. Heading is now `### New Flag (Heavy Lane)`; `:99` defers lane authority to AGENTS.md explicitly. |
| 18 | `adr-audit.md` two-branch `@covers` diagnosis vs `tests.md` three-kind taxonomy | `1ddb407d7` → `adr-audit.md` `0.2.0`. Branch (c) added at `:62` routing SUPPORT / STRUCTURAL-FENCE to their own proof channels. |
| 19 | `adr-audit.md` bare-command audit sequence vs `AGENTS.md` § Attestation | `1ddb407d7` → `adr-audit.md` `0.2.0`. § Audit sequence step 2 is now the ARB-wrapped canonical invocations; step 4's `--evidence-json` template carries `arb-*` receipt IDs. The `gate5-runbook` half was closed by `3a4aacf32`. |
| 20 | `task-discovery.md` four-channel disjunction vs `tests.md` trailer floor | `1ddb407d7` → `task-discovery.md` `0.4.0`. § Invariant is now *"cumulative-with-a-floor, not a free choice"* (`:24`, `:26`). |
| 21 | `task-discovery.md` mandatory `-#<ghi>` anchor vs the 2026-06-01 moratorium | `1ddb407d7` → `0.4.0`. `:95` — *"**The `-#<ghi>` anchor is OPTIONAL.**"* `:88` demotes the file to a pointer, not a second authority. |
| 25 | `cli.md` step 5 hand-written release notes vs `changelog-release-notes.md` | `1ddb407d7` → `cli.md` `0.3.0`. `:116` now reads *"do not hand-write release notes"* and cites the ceremony. |
| 1 | `tests.md` § 6f vs `tool-skill-runbook-alignment.md` Invariant 3 | Already mitigated and now explicit: `tests.md:75` § Output-form fixture carve-out states *"The apparent contradiction … dissolves once REQ kind is named"* and adds the `# output-contract:` declaration convention `gz test-shape` reads. |
| 3 | `governance-core.md` § Required workflow order vs § Defect-fix routing | Mitigated by the scope clause at `governance-core.md:52-54`, verified present this run. (Note: a *different* gap in the same section is now row **R02**.) |
| 10 | Never #1 vs § Universal OBPI Attestation | Remains **refuted** — no conflict. `src/gzkit/commands/adr_audit.py` `_requires_human_obpi_attestation` returns `True` unconditionally. Not re-litigated; the prior run's refutation stands and its suggested remediation must never be applied. |

### Downgraded — contradiction disclosed in-rule, mechanically unchanged

Rows **R14** and **R15** (prior 22 and 23) were `episodic`; `security-sensitivity.md` `0.5.0`
named both gaps in the rule body rather than closing them. The text still contradicts, so
they remain rows — at `theoretical`, since an agent reading the current file is told the
truth in the adjacent clause.

Row **R07** (prior 11) dropped `blocking` → `episodic` on the same basis: `pythonic.md`
`0.2.0` names the three-way threshold conflict and tells the reader which number binds,
without moving a number.

### New this run

- **R02** — `governance-core.md` § Required workflow order vs the `AGENTS.md` pipeline mandate. The highest-leverage row in the matrix: `governance-core.md` is the only rule with `paths: "**/*"`, and following its step 3 literally on a governed OBPI trips a PreToolUse hook at exit 2.
- **R03** — `chores.md` § Related's `manpages/gz-chores.md` pointer vs the manpage-filename binding `governance-core.md` `0.7.0` landed six days later. The GHI #532 sweep covered `docs/**`, `features/**`, and `.gzkit/skills/**` and skipped the rule surface.
- **R04** — `task-discovery.md`'s GHI #731 auto-stamp vs its own § Layer-drift fail-close and § Do Not. The only row in this matrix **created by a fix**: the producer-side stamp that closed rows 20/21's successor problem also erased the divergence signal § Layer-drift declares load-bearing. The rule concedes it in four words — *"Witness status unruled"* — and GHI #731 is still open.

## Honest limits of this run

- **Absence of a row is not evidence a pair is clean.** Explicit control cases checked and found clean this run: `cross-platform.md`, `guardrail-feedback-prose.md`, `hexagonal-architecture.md`, `token-block-discipline.md`, `agent-failure-modes.md`, `brief-heading-conventions.md`. Every other silent pair is unreported, not cleared.
- **Every `file:line` in the Mechanical winner column was opened this run.** Several drifted since 2026-07-16 (notably `validate_cmd.py:725` → `:790`, `attestation_receipts.py:165-168` now paired with a new lane predicate at `:211`) and were re-read rather than copied.
- **Off-matrix defects found on the audited surface are recorded in `summary.md` § Off-matrix defects** — they are rule-vs-check or link-hygiene defects, not rule-pair contradictions, and were deliberately kept out of the matrix rather than padding it.
