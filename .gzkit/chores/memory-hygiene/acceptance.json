{
  "criteria": [
    {
      "type": "exitCodeEquals",
      "command": "uv run python .gzkit/chores/memory-hygiene/check_memory_drift.py",
      "expected": 0,
      "description": "no auto-memory file postdates the last recorded hygiene pass"
    },
    {
      "type": "exitCodeEquals",
      "command": "uv run -m unittest -q",
      "expected": 0,
      "description": "unit suite green (precondition, not the chore's discriminator)"
    }
  ]
}
