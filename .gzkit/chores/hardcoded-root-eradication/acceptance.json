{
  "criteria": [
    {
      "type": "outputNotContains",
      "command": "grep -rn \"Path(__file__).*parents\" src/gzkit/eval/",
      "notContains": "parents",
      "description": "No hardcoded root derivations in eval modules"
    },
    {
      "type": "outputNotContains",
      "command": "grep -rn \"Path(__file__).*parents\" src/gzkit/hooks/",
      "notContains": "parents",
      "description": "No hardcoded root derivations in hooks modules"
    },
    {
      "type": "exitCodeEquals",
      "command": "uv run gz lint",
      "expected": 0,
      "description": "Lint passes including parents-pattern lint rule"
    },
    {
      "type": "exitCodeEquals",
      "command": "uv run gz check-config-paths",
      "expected": 0,
      "description": "Config-path audit passes including source path literal scan"
    },
    {
      "type": "exitCodeEquals",
      "command": "uv run -m unittest -q",
      "expected": 0
    },
    {
      "type": "outputNotContains",
      "command": "grep -rn \"Path(__file__).*parents\\[\" src/gzkit/ --include=*.py --exclude=quality.py",
      "notContains": "parents[",
      "description": "No Path(__file__).parents[ patterns anywhere in src/gzkit/ excluding the detector (manifest v2 enforcement)"
    }
  ]
}
