{
  "description": "Constitutional invariant ledger event type definitions (ADR-0.0.37, OBPI-0.0.37-03)",
  "event_types": [
    {
      "id": "composition_rendered",
      "name": "composition_rendered",
      "schema": {
        "required": ["invariant_count", "target", "byte_count", "render_ts"],
        "properties": {
          "invariant_count": {"type": "integer"},
          "target": {"type": "string"},
          "byte_count": {"type": "integer"},
          "render_ts": {"type": "string", "format": "date-time"}
        }
      },
      "required_fields": ["invariant_count", "target", "byte_count", "render_ts"]
    },
    {
      "id": "composition_drift_detected",
      "name": "composition_drift_detected",
      "schema": {
        "required": ["target", "diff_first_50_lines", "render_ts"],
        "properties": {
          "target": {"type": "string"},
          "diff_first_50_lines": {"type": "string"},
          "render_ts": {"type": "string", "format": "date-time"}
        }
      },
      "required_fields": ["target", "diff_first_50_lines", "render_ts"]
    },
    {
      "id": "corpus_entry_appended",
      "name": "corpus_entry_appended",
      "schema": {
        "required": ["surface", "section", "entry_id", "tier"],
        "properties": {
          "surface": {"type": "string"},
          "section": {"type": "string"},
          "entry_id": {"type": "string"},
          "tier": {"type": "string"}
        }
      },
      "required_fields": ["surface", "section", "entry_id", "tier"]
    },
    {
      "id": "brief_reconciled",
      "name": "brief_reconciled",
      "schema": {
        "required": [
          "brief_id",
          "has_drift",
          "allowlist_delta_count",
          "discovery_delta_count",
          "verification_delta_count",
          "req_count_delta",
          "citation_delta_count"
        ],
        "properties": {
          "brief_id": {"type": "string"},
          "has_drift": {"type": "boolean"},
          "allowlist_delta_count": {"type": "integer"},
          "discovery_delta_count": {"type": "integer"},
          "verification_delta_count": {"type": "integer"},
          "req_count_delta": {"type": "integer"},
          "citation_delta_count": {"type": "integer"},
          "applied": {"type": "boolean"},
          "attestor": {"type": ["string", "null"]}
        }
      },
      "required_fields": [
        "brief_id",
        "has_drift",
        "allowlist_delta_count",
        "discovery_delta_count",
        "verification_delta_count",
        "req_count_delta",
        "citation_delta_count"
      ]
    },
    {
      "id": "brief_reconcile_drift_detected",
      "name": "brief_reconcile_drift_detected",
      "schema": {
        "required": [
          "brief_id",
          "allowlist_missing_in_brief",
          "allowlist_missing_on_disk",
          "discovery_unresolved_paths",
          "verification_unresolved_verbs",
          "declared_reqs",
          "acceptance_criteria_count",
          "req_count_delta",
          "citation_stale"
        ],
        "properties": {
          "brief_id": {"type": "string"},
          "allowlist_missing_in_brief": {"type": "array", "items": {"type": "string"}},
          "allowlist_missing_on_disk": {"type": "array", "items": {"type": "string"}},
          "discovery_unresolved_paths": {"type": "array", "items": {"type": "string"}},
          "verification_unresolved_verbs": {"type": "array", "items": {"type": "string"}},
          "declared_reqs": {"type": "integer"},
          "acceptance_criteria_count": {"type": "integer"},
          "req_count_delta": {"type": "integer"},
          "citation_stale": {"type": "array", "items": {"type": "string"}}
        }
      },
      "required_fields": [
        "brief_id",
        "allowlist_missing_in_brief",
        "allowlist_missing_on_disk",
        "discovery_unresolved_paths",
        "verification_unresolved_verbs",
        "declared_reqs",
        "acceptance_criteria_count",
        "req_count_delta",
        "citation_stale"
      ]
    }
  ]
}
