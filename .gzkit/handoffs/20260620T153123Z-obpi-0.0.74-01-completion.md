---
mode: CREATE
adr_id: ADR-0.0.74-mx-mode-maintenance-hangar
branch: main
timestamp: "2026-06-20T15:31:23Z"
agent: claude-code
obpi_id: OBPI-0.0.74-01-mx-marker-file
session_id:
continues_from: .gzkit/handoffs/20260620T003329Z-session-git-sync-ghi634-corpus-doctrine.md
---

<!-- Handoff document for ADR-0.0.74-mx-mode-maintenance-hangar — created by claude-code at 2026-06-20T15:31:23Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

**Register-entry facts (token-block discipline):** last `obpi_lock_claimed` event
timestamp `2026-06-20T14:10:31.428588+00:00` (agent `claude-code-07705ca1`, this
session's claim on OBPI-0.0.74-01-mx-marker-file); last commit SHA
`465d2863f4a3ad37b279bf24152d7eb76a2d0057`; branch `main`, 0 ahead / 0 behind
`origin/main` in committed terms but the **working tree is dirty** (the completed
OBPI-01 deliverables + governance edits are uncommitted — git-sync #1 is the
pending step that commits them). This handoff is the completion register entry
that unblocks the lock release (ADR-0.0.41 coupling).

**OBPI-0.0.74-01-mx-marker-file is COMPLETE (Layer-2 truth: `attested_completed`).**
`uv run gz obpi complete` exited 0 at ~2026-06-20T15:1x UTC; attestor `g0`;
operator-verbatim conversational attestation ("attest completed") accepted as
Gate 5 for the heavy/foundation lane. This is OBPI 1 of 10 under ADR-0.0.74
(MX Mode — Maintenance Hangar), the topmost P0 Magna Carta pull.

The deliverable: `gzkit.mx.marker` — the single MX filesystem truth-source.
`Marker(BaseModel, frozen, extra=forbid)`; `is_active()` returns True on marker
presence (MX==TRUE); `is_valid()` binds `marker.session_id` to a real
`mx_session_opened` ledger event (read raw with stdlib; a later
`mx_session_closed` voids it) so a hand-created marker is void (anti-contrivance);
`marker_path()` resolves the one truth-source path `.gzkit/mx.json`; the module
imports no `gzkit.*` internals (AST-asserted). 12 tests pass; full suite green
(receipt `arb-step-unittest-8c36eb010f774eadb9944bc84d6ed1de`, exit_status 0).

## Important Context

**In-flight correction (operator-caught — the single most important context):**
the ADR-0.0.74 / OBPI-01 brief originally mandated the marker be "stdlib-only."
The operator challenged that premise mid-implementation. Diagnosis: pydantic is a
pinned **core** dependency (`pyproject.toml` `pydantic>=2.13.3`), as present as the
interpreter wherever gzkit runs — it is **not** "the patient." The ADR's real
intent is "the marker reads when gz's own subsystems are broken/mid-repair," whose
correct invariant is **no `gzkit.*`-internal imports**, not "no pydantic." So
`Marker` is now a Pydantic `BaseModel` (project standard, passes
`audit_pydantic_models` with no waiver), and `test_marker_module_imports_no_gzkit_internals`
AST-enforces the no-gzkit-imports invariant. ADR Decision #1 + Checklist and
OBPI-01 REQ-01-01 + Objective were reworded to match; bare "gz is/broken/surface"
prose was rewritten to "gzkit …" to satisfy the brief command-shape validator.

**Two RED full-suite runs this session were caught only by reading the ARB
receipt `exit_status`, not the piped shell exit (which always read 0).** The
exit-code-integrity rule (GHI #589) is load-bearing: never trust a piped
verification exit; read the receipt. The two RED causes were both self-inflicted
and fixed: (1) an insight record with `evidence` as a string (schema wants
`list[str]`); (2) the `gzkit.mx.marker.Marker` `@dataclass` vs `audit_pydantic_models`
(resolved by the pydantic correction above).

**The `@covers` chokepoint needs DECORATORS, not docstring refs.** `gz obpi
complete`'s `_enforce_req_coverage_gate` runs `uv run -m unittest <module>.<qualified_name>`
per discovered ref; a docstring `@covers` resolves to a module-level (empty)
target that errors. Fixed by `@covers("REQ-…")` method decorators
(`from gzkit.traceability import covers`).

## Decisions Made

- **Decision:** `Marker` is a Pydantic `BaseModel`, not stdlib `@dataclass`;
  the marker invariant is "no `gzkit.*` imports," not "stdlib-only."
  **Rationale:** pydantic is a pinned core dep (not "the patient"); models.md
  mandates Pydantic for data; the ADR's intent is decoupling from breakable gz
  internals. **Operator-ratified** ("proceed with the correction … including the
  reword of ADR Decision #1 + REQ-01-01").
  **Alternatives rejected:** stdlib dataclass + `_DATACLASS_WAIVERS` waiver
  (dishonest — fights the project standard for no real robustness gain);
  `NamedTuple` (dodges the audit's spirit).

- **Decision:** brief allowlist amended to add `tests/mx/__init__.py`.
  **Rationale:** every `tests/` test-module subdir carries one;
  `unittest discover tests` needs it. Operator pre-approved via the plan.

- **Decision:** did NOT add a `behave_coverage_waivers.json` entry.
  **Rationale:** it grows the waiver registry 522→523, which the waiver-ratchet
  (ADR-0.0.73 BI#8, shrink-only) fail-closes. Reverted. Completion proceeded via
  the `@covers` chokepoint (the real complete-time gate), which is green. See
  Pending Work — this is a genuine governance deadlock to drain.

## Immediate Next Steps

<!-- ADVISORY ONLY — present to operator, await authorization before executing. -->

1. **Finish Stage 5 of the pipeline (the steps after this handoff).** Release the
   OBPI lock (`uv run gz obpi lock release OBPI-0.0.74-01-mx-marker-file` — full
   slug; do NOT use `--abandon`, this is a completion not a surrender), remove the
   pipeline markers (`.claude/plans/.pipeline-active-OBPI-0.0.74-01-mx-marker-file.json`
   + legacy `.pipeline-active.json`), then **git-sync #1**
   (`uv run gz git-sync --apply`) to commit the OBPI-01 deliverables + governance
   edits, then `uv run gz obpi reconcile OBPI-0.0.74-01-mx-marker-file` +
   `uv run gz adr status ADR-0.0.74 --json`, then **git-sync #2**.

2. **File the behave_req_coverage ⇄ waiver-ratchet deadlock GHI (drainage, E.5).**
   The `precomplete` `behave_req_coverage` gate demands a scenario tag OR a
   `behave_coverage_waivers.json` entry for every BEHAVIOR REQ, but ADR-0.0.73's
   waiver-ratchet (shrink-only) now forbids new entries — so every unit-only OBPI
   (all of ADR-0.0.74's remaining 9) is wedged at precomplete. `gz obpi complete`
   does not enforce behave coverage (only `@covers`), so completion still works
   by bypassing the bypassable pre-flight — but the gate pair is incoherent.
   Route per the campaign's governance-friction-drainage discipline.

3. **Pull OBPI-0.0.74-02 (mx-shared-checkpoint)** — the next increment under the
   P0 ADR, once Stage 5 above is clean and `uv run gz check` is green on `main`.

## Pending Work / Open Loops

- **Stage 5 tail unfinished:** lock still held, markers still present, two
  git-syncs pending (Step 1 above). The OBPI is `attested_completed` in the
  ledger but not yet synced to `origin/main`.
- **behave_req_coverage ⇄ waiver-ratchet deadlock** (Step 2) — unfiled GHI;
  blocks `precomplete` clean-exit for every unit-only OBPI in ADR-0.0.74.
- **OBPI-0.0.74-07 brief drift:** carries the pre-correction "stdlib-only marker
  read" framing; its `awareness.py` reuses this marker (now pydantic). Reconcile
  to "no gzkit-internal imports" when OBPI-07 is pulled (tracked in the OBPI-01
  brief Tracked Defects).
- **Parent ADR-0.0.74 latent scaffold defects** (owned by ADR closeout, not
  OBPI-01): `{persona}` placeholder unfilled; `## Fidelity Assertions` table is
  still the example row (`uv run gz --version | 0`) → will fail
  `gz validate --fidelity-presence` at closeout.
- **Pre-existing:** malformed HEAD commit message on `465d2863` (raw git template)
  — cosmetic, on `origin/main`; rewrite needs force-push (operator call).

## Verification Checklist

- [ ] `git rev-parse HEAD` → `465d2863…` (until git-sync #1 commits)
- [ ] `uv run gz obpi status OBPI-0.0.74-01-mx-marker-file` → `attested_completed`
- [ ] `uv run gz obpi lock list` → lock present until Step 1 releases it
- [ ] `grep obpi_lock_claimed .gzkit/ledger.jsonl | grep OBPI-0.0.74-01` → claim at 14:10:31Z
- [ ] `uv run -m unittest tests.mx.test_marker -v` → 12 tests OK
- [ ] `uv run gz validate --waiver-ratchet` → exit 0 (522 entries; waiver reverted)
- [ ] `uv run python -c "from gzkit.mx import marker; print(marker.is_active())"` → False (no marker)

## Evidence / Artifacts

- `src/gzkit/mx/marker.py` — the MX marker module (Marker BaseModel, is_active/read/write/is_valid, marker_path)
- `src/gzkit/mx/__init__.py` — `gzkit.mx` package init
- `tests/mx/test_marker.py` — 12 tests (@covers-decorated)
- `tests/mx/__init__.py` — test package init
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/ADR-0.0.74-mx-mode-maintenance-hangar.md` — parent ADR (Decision #1 reworded)
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/obpis/OBPI-0.0.74-01-mx-marker-file.md` — completed brief
- `.gzkit/insights/agent-insights.jsonl` — improvement insight (scaffolder omitted test __init__.py)
- `.gzkit/handoffs/20260620T003329Z-session-git-sync-ghi634-corpus-doctrine.md` — predecessor handoff
