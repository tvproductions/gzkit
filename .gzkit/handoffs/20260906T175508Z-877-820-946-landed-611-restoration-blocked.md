---
mode: CREATE
adr_id: null
branch: main
timestamp: '2026-09-06T17:55:08Z'
agent: claude-code
session_id: 9005122b-3bce-4130-9030-7e3435ec4b0f
continues_from: 20260906T155304Z-session-close-970-849-landed-969-971-open-946-next.md
---

## Current State Summary

SESSION-CLOSE. HEAD `1e217fb4`, tree clean, `origin/main` in sync (`git rev-list --left-right --count` -> `0 0`), `uv run gz check` exit 0, no active locks. Five commits: `4967df28` (#611 record), `8d7428ba` (#946 fix), `d25dbe97` (#820 fix), `f8112d35` (insights), `1e217fb4` (#877 fix).

Closed this session: **#946** (ch2 always read empty), **#820** (reopened, then fixed — the drift predicate had been corrected in one consumer only), **#877** (typed union rejected 373 committed ledger rows). Filed **#972** (gh issue list default 30-row cap). Corrected two of my own earlier claims, under #611 and #946.

Open queue measured **43** by `gh issue list --state open --limit 200 --json number --jq 'length'` at close. NOTE the `--limit`: the bare form returns exactly 30, and 30 is the page size, not the count (#972).

## Important Context

=== A LENIENT READER IS NOT A DECLARING CONTRACT (#877) ===

#877 read as "the typed union rejects rows the JSON schema accepts." Measured, ZERO of the 656 offending key-instances are declared by `ledger.json` either: it carries no `additionalProperties` anywhere and `ledger_check.py` does `if field not in entry: continue`, so it accepts by SILENCE. `airlock_in` declares `required: []`, `properties: {}` — the event name and nothing else. Neither contract declared these fields; live producers write them through the `extra` mechanism.

=== MEASURE THE ALTERNATIVE, DO NOT ARGUE IT ===

#877 asked whether `extra="forbid"` is the right posture for a model that must parse append-only history. Subclassing one model with `extra="allow"` and parsing a real row settled it: parses True, but `.extra` returns None for `handoff_path` and `model_dump()` DROPS the key. `_EventBase.extra` is a computed property over DECLARED `model_fields`, so relaxing buys a green suite while breaking the live consumer at `handoff_archive.py:178` and losing the field on round-trip.

=== A FENCE CAN GUARD THE WRONG OBJECT ===

`_EVENT_MODELS` is a hand-maintained dict in `tests/test_schemas.py`. `test_all_schema_events_have_models` passed with all 74 events while `TypedLedgerEvent` carried 72 — two models authored, exported, registered in the map, and never wired into the union. The alignment fence witnessed the map, not the read path. The new tests read `typing.get_args(TypedLedgerEvent)` directly.

=== THE SUBSET CARVE-OUT LIVED ONLY IN A DOCSTRING (#820) ===

#820 corrected `_crossing_channels` on 2026-08-18; `gz task envelope diagnose` kept its own spelling for 19 more days. `.gzkit/rules/task-discovery.md` § Layer-drift fail-close — the canonical source BOTH consumers implement — never said a nested channel is not drift. Two implementations of an unstated rule. Rule 0.8.0 -> 0.9.0 now states it and binds consumers to the one predicate.

=== A COUNT COMMAND THAT PAGES IS NOT A COUNT (#972) ===

`gh issue list` defaults to 30 rows and truncates silently, with no warning and no non-zero exit. Three handoffs in this chain recorded a wrong count from it — and the 155304Z predecessor advised the capped command to future sessions as the CORRECTIVE for those wrong counts. Always pass `--limit`.

## Decisions Made

- [operator-ruled] Correct the ledger explanation under #611 (verbatim: "Read the complete selection and recovery paths, then correct the 'permanent gate failure or history rewrite' claim through a subsequent issue comment and governed insight."). Done: `red_parity._collect` keeps the LAST non-void witness per REQ, so a `none` is superseded — the forced-choice framing was false.
- [operator-ruled] Keep ledger restoration unresolved under #611 (verbatim: "Keep ledger restoration unresolved under #611. Preservation and correction of the explanation are complete; restoration is not.").
- [operator-ruled] Repair the diagnostic/validator disagreement through the matching GHI skill as a direct defect repair (verbatim: "Route through the matching GHI skill as a direct defect repair."). The skill routed it to reopening #820 rather than filing fresh.
- [operator-ruled] Do not assume the typed union should accept everything the JSON schema accepts (verbatim: "Do not assume the typed union should simply accept everything the JSON schema accepts."). Measured instead, and neither contract declared the fields.
- [operator-ruled] Do not weaken validation merely to obtain green (verbatim: "do not rewrite rows, weaken validation merely to obtain green, or start ADR/OBPI machinery."). `extra="allow"` was measured and rejected on evidence.
- [operator-ruled] Stop after #877 (verbatim: "Stop after #877.").
- [agent-chose] Declared the 22 fields in BOTH `events.py` and `ledger.json` rather than the typed models alone, so both contracts document what producers actually write.
- [agent-chose] Read field types off the producers rather than inferring them from committed data — four of the fields appear only as empty lists, so inference would have guessed.
- [agent-chose] Scored advisory-scorecard row 60c **Promotable**, not Mechanical, on the 33a/33b/45b/62c precedent. Summary roll-up Promotable 30 -> 31. Claiming Mechanical would assert the sharing obligation is witnessed when only today's two callers are.
- [agent-chose] Kept `_UNMODELLED_EVENTS` as an EMPTY frozenset rather than deleting it — it is the shrink-only ratchet that makes a new model-less event fail immediately.
- [agent-chose] Wrote #877's tests to assert consumer AGREEMENT, and mutation-checked every new fence; a fence that cannot fail is worse than none.

## Immediate Next Steps

1. Re-establish repository truth BEFORE acting on anything below: `git status --short`, `git log --oneline -5`, `git rev-list --left-right --count origin/main...HEAD`, `uv run gz obpi lock list`. This handoff ADVISES; it does not authorize.
2. Re-derive the open-queue count with an EXPLICIT limit: `gh issue list --state open --limit 200 --json number --jq 'length'`. The bare form returns 30 because that is the page size (GHI #972). Do not copy the number out of this document.
3. Obtain the operator's ruling before drawing work, and book it: `uv run gz handoff decide --handoff <this file> --session-id <id> --decision proceed --operator-text "<their exact words>"`.
4. NEXT CAMPAIGN ITEM is GHI #611. Read the two comments added to it this session first — they narrow its subject considerably, and the second one withdraws part of the first. Campaign order after it: 930, 894, 939, 922, 921, 815, 933, 953, 952, 951, 767, 766, with #972 now also in the queue.
5. Before any SHA enters a close comment, resolve it with `git log -1 --format=%H`. Before citing a receipt, read `exit_status` out of `artifacts/receipts/<id>.json` rather than trusting console output.

## Pending Work / Open Loops

- **GHI #611 — OPEN, and restoration remains BLOCKED by operator ruling.** Three things are separate and must stay separate: (a) PRESERVATION of the discarded event is COMPLETE — the row is quoted verbatim in the issue, recovered from dangling blob `fbf10ccf` before `git gc` could prune it; (b) RESTORATION to `.gzkit/ledger.jsonl` is BLOCKED — no governed writer accepts a caller-supplied historical event (`gz ledger` exposes only `merge-driver`), and the only mechanism that would is a hand-edit, forbidden by AGENTS.md § Never #2; (c) the MISSING MECHANISM is narrower than first claimed — supersession-by-append already exists for this event type, and the real gap is that nothing can mark a SPECIFIC known-false row void.
- **GHI #972 — OPEN with a blocker comment.** The fix is a clause in `.claude/rules/gh-cli.md`, coupled to a rule-version bump and a Coverage Ledger row. Deferred deliberately, not forgotten.
- **GHI #969 / #889 — OPEN and coupled** as producer and consumer of one evidence path; closing either alone leaves the path broken. **GHI #971 — OPEN** by deliberate choice.
- **GHI #968 — OPEN under a standing operator ruling.** Untouched this session. Do not draw it without a fresh ruling.
- **Historical trailer defects, recorded and unrepairable.** `4967df28` and `8d7428ba` both carry the harness-supplied `Claude-Session:` trailer that `.gzkit/rules/task-discovery.md` 0.8.0 says verbatim to strip, and `8d7428ba` touched `src/` and `tests/` with no `Task:` trailer at all. `--commit-trailers` scans HEAD only, so nothing reads red; the break stands in the record and a history rewrite is not on the table.
- Advisory scorecard rules arm now stands at **31 Promotable, 0 Ambiguous** — read the live figure from the fenced Summary table, never this number.
- `gz drift` reports 706 unlinked specs. Advisory only; not addressed.

## Verification Checklist

- `uv run gz check` exits 0 on a clean staged tree. Run `git add -A` FIRST; on a dirty tree the pre-push `--reuse-verified` gate cannot reuse the pass.
- `git rev-list --left-right --count origin/main...HEAD` returns `0 0`.
- `uv run gz obpi lock list` reports no active locks.
- `uv run gz validate --ledger`, `--commit-trailers`, `--red-parity`, `--advisory-scorecard`, `--task-envelope-coherence` each exit 0.
- Reproduce #877's fix: every committed ledger row parses through the typed union — 15922 rows, 0 rejected, down from 373. `uv run -m unittest tests.test_schemas.TestTypedModelParsesEveryCommittedLedgerRow` passes with no allowlist and no unknown-event escape.
- Reproduce #820's fix: `uv run gz task envelope diagnose OBPI-0.35.0-04-section-ownership-and-ratchet` prints `No layer-drift`, and `_crossing_channels` returns `[]` for the same OBPI — the two consumers agree.
- Reproduce #946's fix: the same command renders all 13 declared TASK ids in the `frontmatter tasks: (ch2)` channel.
- The new #877 fences are mutation-live: unwiring one union member trips 3 assertions; dropping one declared field trips the parse fence.

## Evidence / Artifacts

Production surfaces changed this session:

- `src/gzkit/events.py` — two models wired into `TypedLedgerEvent`; 22 optional fields declared across 9 models.
- `src/gzkit/schemas/ledger.json` — the same 22 properties declared, none required.
- `src/gzkit/ontology/corpus.py` — two new discriminators dispositioned OUT with rationale.
- `src/gzkit/commands/task.py` — ch2 resolution via `_obpi_lineage_id`; drift now computed by `_crossing_channels`.
- `src/gzkit/commands/validate_task_envelope.py` — `_frontmatter_channel_for_obpi` accepts either id spelling.
- `src/gzkit/ledger.py`, `src/gzkit/commands/register.py`, `src/gzkit/governance/obpi_slug_rename.py` — `_extract_bare_obpi_id` promoted to `extract_bare_obpi_id`; two private-import baseline edges pruned.

Doctrine and test surfaces:

- `.gzkit/rules/task-discovery.md` — rule 0.8.0 to 0.9.0: the drift carve-out and the shared-predicate obligation.
- `docs/governance/advisory-rules-audit.md` — row 60c added (Promotable); Coverage Ledger and Summary roll-up updated.
- `docs/governance/rule-version-history.md` — the 0.9.0 entry.
- `tests/test_schemas.py`, `tests/governance/test_ledger_reader_parity.py`, `tests/governance/test_task_envelope_coherence.py`, `tests/policy/test_import_boundaries.py`.
- `.gzkit/insights/agent-insights.jsonl` — three records this session.

Commits: `4967df28`, `8d7428ba`, `d25dbe97`, `f8112d35`, `1e217fb4`.

ARB receipts, `exit_status` read directly from `artifacts/receipts/*.json`, each 0 — `arb-ruff-42eabab95ebd4ddf941ede23f9d77381`, `arb-step-typecheck-23fa0feb768b414ba30a4d052bfdd2e7`, `arb-step-unittest-92f533c811584e5f99260d043a743120` (#820); `arb-ruff-d8a58097c9614148a1c642a4fbda917b`, `arb-step-typecheck-46b18c65def24069a0a35cbabea7ffb2`, `arb-step-unittest-8c3de4f728d744768d41579d426a7f47` (#877).

## Settled Rulings

762 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
