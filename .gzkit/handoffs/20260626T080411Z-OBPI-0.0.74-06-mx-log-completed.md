---
mode: CREATE
adr_id: ADR-0.0.74
obpi_id: OBPI-0.0.74-06-mx-log-auto-assembled
branch: main
timestamp: '2026-06-26T08:04:11Z'
agent: claude-code-1e4f27c1
last_lock_event_timestamp: '2026-06-26T05:49:43.728381+00:00'
last_commit_sha: 33610e68a70cb0373dfc28de3a220aac23d29da2
---

<!-- Completion handoff for OBPI-0.0.74-06-mx-log-auto-assembled — register entry for lock release -->

## Current State Summary

OBPI-0.0.74-06-mx-log-auto-assembled is `attested_completed` (attestor g0, lane
heavy). The auto-assembled MX log (ADR-0.0.74 Decision #6) is implemented,
tested (8/8 @covers all 4 REQs), independently adversary-validated
(NOT-REFUTED-WITH-CAVEATS; the one tautological-render caveat fixed and
re-validated RED-under-mutation), and operator-attested. This handoff is the
completion register entry that pairs with the lock release (token-block
discipline § Sub-Invariant 5).

## Important Context

At `gz mx exit`, `mx_cmd.mx_exit_cmd` now calls `log.assemble_and_render` before
writing `mx_session_closed` — the log is built by construction from the ledger
events + git commits in the enter→exit window (bounded by the
`mx_session_opened` ts), names every fix and the ADRs/OBPIs/REQs each touched,
and is rendered for operator review before the close signature. `MxSessionOpenedEvent`
/`MxSessionClosedEvent` typed event types added to `gzkit.events` + the discriminated
union, with paired `schemas/ledger.json` entries and `_EVENT_MODELS` registry entries.

## Decisions Made

Completed OBPI-0.0.74-06 through the full pipeline (plan-audit → TDD implement →
Stage-3 verify → Stage-4 adversarial ceremony → Gate-5 attestation). Four
coupled surfaces the brief under-declared were caught and operator-approved into
the allowlist (mx_cmd.py, schemas/ledger.json, tests/test_schemas.py — events.py
already declared). Two coupled-surface side-fixes landed as separate direct fixes:
GHI #645 (reconcile @covers false-positive) and GHI #646 (pipeline status-flip).
GHI #646 was found post-merge to conflict with `gz frontmatter reconcile` (which
derives frontmatter status from ledger state and reverts Active→Draft) — flagged
to operator for follow-up; it does not affect OBPI-06's completed state.

## Immediate Next Steps

1. Lock release (this register entry pairs it).
2. Git-sync the OBPI-06 deliverable + governance edits.
3. Resolve the GHI #646 frontmatter-reconcile conflict (operator decision pending).
4. Next ADR-0.0.74 MX-kernel OBPI: OBPI-0.0.74-07 (awareness hook).

## Pending Work / Open Loops

- **GHI #646 follow-up (flagged to operator):** the pipeline Draft→Active flip
  conflicts with `gz frontmatter reconcile`, which derives frontmatter status
  from the ledger state machine (resolves in-flight OBPIs to pending/Draft) and
  reverts the flip. The correct fix is at the ledger-state-machine /
  reconcile-derivation layer (resolve `in_progress` for launched OBPIs so
  reconcile derives & keeps `Active`), not a frontmatter-only write. Decision
  pending: fix properly vs. revert GHI #646.
- Remaining ADR-0.0.74 MX-kernel OBPIs (campaign Movement I, release 0.29.0):
  OBPI-07 (awareness hook), OBPI-08 (skill + AGENTS rule), OBPI-13 (proxy-reality
  detector), OBPI-14 (hardening: TTL/max-open, no-release-while-open, debt-aging,
  dangling-state detector). The `gz-mx` skill is a later ADR-0.0.74 deliverable.

## Verification Checklist

- [ ] `git rev-parse HEAD` matches a commit at or after the completion git-sync.
- [ ] Branch matches `main`.
- [ ] OBPI-0.0.74-06-mx-log-auto-assembled shows `attested_completed` in `gz adr status ADR-0.0.74`.

## Evidence / Artifacts

- `src/gzkit/mx/log.py` — `assemble_window`, `parse_artifacts`, `render`, `assemble_and_render`
- `tests/mx/test_mx_log.py` — 8 unit tests (@covers REQ-06-01/02/03/04)
- Receipts: `arb-step-unittest-891fad1a279e4e16a939bafdf5c23ff3`,
  `arb-ruff-e5071f4e47b84810abc10a88a5e458d0`,
  `arb-step-typecheck-07c90783df5a48a5bacee25ce8c6563e`,
  `arb-step-mkdocs-5e84d0e2f63d4cd084aa2af960f9ce26`
