---
mode: CREATE
adr_id: ADR-0.0.37
branch: main
timestamp: '2026-07-18T19:04:10Z'
agent: claude-code
session_id: abe80440-9368-482b-a4b3-254fc75cb415
continues_from: .gzkit/handoffs/20260717T163620Z-corpus-derivation-breach-fixed-mechanism-remains.md
---

## Current State Summary

Design session for the gzkit construction loop (the cadence). No implementation landed and no ADR is booked yet. gz-design Sections 1-3 (command interface, data flow, data model) are operator-approved; Sections 4-5 remain, then the ADR gets booked. Movement A / ADR-0.0.37 audit was deliberately NOT run this session: the orientation-index correction routes under that same ADR by the correction-vs-enhancement doctrine, so auditing first would seal a partial delivery as Validated. Operator ruled yes to the audit but "in a minute" - it is still pending and now sequenced AFTER the correction. One commit landed: b2ef58ab, booking six operator rulings into the campaign Amendments section. Tree is clean and level with origin/main at that commit.

## Important Context

THE OPERATOR CADENCE, verbatim: agents + rules = how we work; magna carta = the story and prioritization of what we are building (short version, referencing long versions); handoff = what happened last time; airlock = how we enter the system for the next move, being obpi, ghi, or permitted move; then the move; then handoff; then git-sync. THE BINDING DESIGN CONSTRAINT, operator verbatim: "you will slightly alter each time" - the loop must not depend on agent comprehension persisting across entries. This was proved live this session: the agent misread canonical-to-vendor mirroring as disposable build artifacts TWICE, with 194KB of guidance loaded. THREE MEASURED FINDINGS. (1) ORPHANED MAPS: of ~75 docs under docs/governance, 2 are DELIVERED, ~24 POINTER-ONLY, ~40 ORPHANED (~450KB). The Agent Orientation Index at agent-control-surface-rendering-substrate.md line 263 opens with "Do not re-derive the rendering architecture from source each session" and has zero referrers in any per-turn surface. The repo diagnosed this on 2026-05-30 in agent-insights.jsonl line 146 ("Capture without re-injection does not bind") and prescribed a HOOK; what shipped under OBPI-0.0.37-27 was a markdown section inside the unread document. (2) THE CMS ALREADY SHIPS INSTALL-TIME ASSEMBLY: gz init copies canonical inputs from the wheel then calls sync_all, which GENERATES vendor surfaces; no pre-rendered AGENTS.md or CLAUDE.md ships. Two facades found: validate_render fires only when project_root is passed and sync_agents_md line 407 does not pass it; temperature is inert per pipeline.py lines 96-101. render_content_surface has zero production callers. Still no Corpus-to-str materializer. (3) THE ACHILLES HEEL: gz git-sync has no --message flag and auto-titles commits from directories touched; 723 such commits in 90 days, one of them carrying 1649 lines of real chore-log content under a chore label. The campaign 2.9-percent feat census is computed from those prefixes, so the measurement that drove the rush disposition reads a broken gauge.

## Decisions Made

1. DISPOSITION CHANGED to TSP routing, not rush-to-1.0 (operator-ratified, booked b2ef58ab). The set is taken as given and the win is in the ordering; re-entry cost is the metric minimized; adjacency is the primary object. Rejected: continuing the finite-gate framing, because its set-shrinking moves (dated census bounds, post-1.0 rulings) were what kept resurfacing as re-adjudication. 2. POOL IS VISITED AS WE INCH FORWARD, amending campaign section 7. Rejected: the post-1.0 wall, because a deferral ruling orphans and resurfaces, whereas a late-routed cluster does not. 3. MOVEMENT C ITEM 1 RETIRES AS ALREADY-DELIVERED, not as wrong. gzkit keeps committing its own mirrors while dogfooding (operator verbatim: "we have to keep on generating/mirroring until the product settles a bit more"). All five copies are wheel-borne product, already mechanized at manifest.json lines 45-59 plus gz validate --distribution. 4. COMPOSE, DO NOT REDESIGN. Thirteen pool ADRs are one neighborhood and get absorbed into a single feature ADR for the construction loop. Rejected: (a) component-complete ordering, because nothing would constrain the agent until the last OBPI; (b) promoting workflow-specification alone and superseding incrementally, because that recreates the one-at-a-time traversal that produced the accretion - retained as the fallback if the composition proves too large. 5. WALKING SKELETON FIRST: OBPI-01 is the thinnest end-to-end cut of all seven cadence steps, so the loop constrains the agent while the rest of the loop is built. 6. NOT A MEMBER: ADR-pool.doctrine-amendment-protocol stays in pool - it governs foundation-doctrine amendment and citation chains, which is Movement D territory, not the loop. 7. REUSE OVER INVENT (Section 3): airlock Door already enumerates exactly the three move types (PIPELINE, MX, PERMITTED_ENTRY); Authority (CAPTAIN or DELEGATED) already models the human-in-the-loop axis. Only LoopSpec and a derived LoopPosition are genuinely new. 8. gz loop is a tool+skill+runbook TRIPLE, never a bare verb, per tool-skill-runbook-alignment Invariant 1 - an orphaned CLI verb with no skill is a defect signal, and the skill lands in the same patch.

## Immediate Next Steps

1. RESUME gz-design at Section 4 (output and rendering: stdout shape, --json, exit codes) for the construction-loop ADR. Sections 1-3 are approved and recorded in Decisions Made above - do not re-derive them. 2. THEN Section 5 (extension points). 3. THEN book the ADR: confirm next available feature semver via gz status --table, lane heavy (new CLI contract), kind feature (release-carrying capability adapting the delivered ADR-0.33.0 airlock port). Co-create OBPIs - never defer them. OBPI-01 is the walking skeleton: three-state spec, gz loop reading it, --move on gz airlock in with legality refusal, PreToolUse refusal hook, handoff records position, git-sync titles from move, plus the gz-loop skill and the runbook operator moment. 4. THEN run gz-adr-evaluate on the booked ADR; any dimension scoring 1 must be revised. 5. THEN and only then return to Movement A: land the orientation-index re-injection correction under ADR-0.0.37, and after that run /gz-adr-audit to take ADR-0.0.37 to Validated. 6. CARRYOVER, still open from the prior handoff: GHI #654 (corpus orchestration gap), GHI #635 (7 duplicate invariant corpus entries), GHI #623 (registry orphan), GHI #533 (the sub-15k AGENTS.md destination), GHI #691 (rules have no aging clock), GHI #682 (security-sensitivity blocked).

## Pending Work / Open Loops

CANDIDATE GHIs captured as insights this session, not yet filed: (a) gz insights remember silently creates a new .gzkit project root in the current working directory instead of resolving upward - running it from docs/design/adr/pool wrote a stray root that had to be removed by hand; sibling chore is hardcoded-root-eradication. (b) gz git-sync has no --message flag, so ceremony commits are auto-titled from directories touched - this is the census-corrupting Achilles heel and ADR-pool.atomic-obpi-commits is its captured fix. (c) The handoff resume gate blocked uv run gz ontology --help, a pure read; the permitted-read allowlist enumerates commands rather than deriving read-ness, which is GHI #574 stated intent. (d) Two CMS facades: fidelity validation fail-open on the one render that ships a surface, and inert temperature. OPEN LOOPS: ADR-0.0.37 audit deferred by design (see Next Steps 5); the campaign story half is still missing and the Magna Carta rethink is authorized but not drafted; Movement C items 1 and 4 need amendment to match the wheel-borne ruling; the 23 airlock_in versus 10 airlock_out accounting gap is unresolved and the new door field will surface it rather than absorb it.

## Verification Checklist

git status -sb shows clean and level with origin/main at b2ef58ab. Read the six booked rulings in the Amendments section of the campaign file before proposing any sequencing - they are settled and must not be re-adjudicated. To re-verify the orphaned-map finding: grep -rn for the substrate doc path across AGENTS.md, CLAUDE.md, .gzkit/rules, .claude/rules, .gzkit/skills and scripts - expect zero referrers. To re-verify the install-time assembly finding: read src/gzkit/commands/init_cmd.py line 894 for the sync_all call and src/gzkit/sync_surfaces.py line 626 for the _pkg_surface_exists adopter no-op guard. To re-verify the Door reuse: read src/gzkit/airlock/exit.py line 72 - three members, PIPELINE, MX, PERMITTED_ENTRY. To re-verify the missing event fields: read src/gzkit/events.py lines 742-751 - AirlockInEvent and AirlockOutEvent carry no door and no target. Do NOT pipe a verifier through tail or head or grep when checking exit codes; capture to a file and read the real exit code.

## Evidence / Artifacts

Commit b2ef58ab (the six booked rulings) touched `docs/governance/build-to-1.0-campaign-2026-07-18.md`, `.gzkit/insights/agent-insights.jsonl`, and `.gzkit/ledger.jsonl`. The binding CMS doctrine, including the headless-Django mapping and the orphaned Agent Orientation Index: `docs/governance/agent-control-surface-rendering-substrate.md`. The pairing contract that corrected the design: `.claude/rules/tool-skill-runbook-alignment.md`. Models to reuse in Section 3: `src/gzkit/airlock/exit.py` and `src/gzkit/airlock/model.py`. Event models needing the additive door and target fields: `src/gzkit/events.py`. The install-time assembly path: `src/gzkit/commands/init_cmd.py` and `src/gzkit/sync_surfaces.py`. The orientation surface to be promoted into gz loop: `scripts/session_orientation.py`. The proven refusal-hook pattern: `.claude/hooks/handoff-resume-gate.py`. The control-surface declaration proving all five copies are wheel-borne: `.gzkit/manifest.json`. The keystone pool fragment: `docs/design/adr/pool/ADR-pool.workflow-specification.md`. The commit-granularity fragment: `docs/design/adr/pool/ADR-pool.atomic-obpi-commits.md`. The retrieval-gap chore whose acceptance criterion is a hyperlink: `.gzkit/chores/instructions-files-diet/CHORE.md`.
