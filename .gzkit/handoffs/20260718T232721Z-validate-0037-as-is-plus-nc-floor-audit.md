---
mode: CREATE
adr_id: ADR-0.0.37
branch: main
timestamp: '2026-07-18T23:27:21Z'
agent: claude-code
continues_from: 20260718T190410Z-construction-loop-design-sections-1-3-approved.md
---

## Current State Summary

Operator ruled: validate ADR-0.0.37 AS-IS. The prior handoff treated the orphaned Agent Orientation Index as a precondition blocking the audit; that read was corrected this session. ADR-0.0.37 established CIC-1 (composition) and CIC-2 (brief-reality coherence) as invariants with structural witness, which is exactly what a foundation ADR does under the taxonomy this ADR itself wrote. The composition ENGINE is a mechanical defense of CIC-1 and is therefore feature-shaped, so it re-homes to Movement A item 2 (closes GHI #623) and carries the orientation-index re-injection with it. No campaign re-sequencing needed. Separately this session ran a full audit of all 47 registered enforcement claims and found 32 do not prove what they claim; GHI #698 (symptom cut) and GHI #699 (class cut) were filed and GHI #648 was updated. Operator is running the ADR-0.0.37 audit. The audit evidence artifact was committed standalone as ca490fc0 so a later git-sync could not file it under a mis-titled commit.

## Important Context

THE DOCTRINAL CORRECTION THAT SETTLED THE 0.0.37 QUESTION. ADR-0.0.37 CIC-1 states verbatim that hand-authoring foundational claims directly into AGENTS.md is the same class of failure as hand-writing ledger entries. The agent twice proposed a one-line pointer edit to AGENTS.md line 346 as the fix for the orphaned orientation index. That proposal is the exact failure class the ADR abolishes and it was retracted. The intent of ADR-0.0.37 is provenance, not content: AGENTS.md must stop being hand-authored canon. In a delivered CIC-1 world the orphan cannot occur, because the pointer is a registry entry with a composition target rather than a sentence someone remembers to type. THE ENFORCEMENT-FLOOR AUDIT. 47 claims, 25 UNDER-SCOPED, 7 TRIVIAL, 14 ADEQUATE, 1 UNCLEAR. gz check reports 47/47 verified. This is not 32 authoring lapses but five structural generators: _command_fails accepts any non-zero exit so it cannot distinguish a caught violation from a tool that never launched; _build_empty supplies a bare directory as the violation; the runner reduces the entrypoint to bool() so a two-arm fixture proves neither arm; one claim per composite validator; and subprocess NCs resolve the installed wheel rather than the working tree. The two claims that certify the apparatus are both compromised: qc-binding registers _check_theater_signatures rather than audit_qc_binding, and its fixture is a set-membership test between two literals in the same module, which is the copy-vs-self tautology that same module defines as a theater signature; enforcement-floor passes registry= explicitly and therefore skips production discovery, making the ORPHAN class invisible to the control that certifies the floor. PRIOR ART. The audit method is hand-run mutation testing. The closest conceptual ancestor to the NC idea is proof testing of safety instrumented systems under IEC 61508/61511; the closest software match is detection engineering validation. The vocabulary is also backwards: a known violation that must produce a detection is a POSITIVE control, and every claim rated ADEQUATE for a strong reason is a two-pole differential carrying both a positive and a negative pole. TOOLING. mutmut is disqualified because it hardcodes pytest with no runner config key; cosmic-ray takes an arbitrary shell command and ships python -m unittest discover tests as its canonical example. BYTE SURFACE. The AGENTS.md budget was decoupled from the Codex cap by operator ruling 2026-07-06, but Codex still truncates at 32768 bytes at runtime and AGENTS.md is 31990 bytes, leaving 778 bytes of real headroom. The budget file narrative is stale: it describes 31800 as current while the enforced value is 50000.

## Decisions Made

1. VALIDATE ADR-0.0.37 AS-IS (operator ruling, this session). The orphaned orientation index is not a blocker. Rejected: hand-editing AGENTS.md to add a pointer, because CIC-1 names that as the failure class the ADR abolishes; rejected: holding the audit until re-injection lands, because re-injection is feature-shaped mechanical defense and belongs to the next artifact. 2. THE ORIENTATION-INDEX RE-INJECTION TRAVELS WITH THE COMPOSITION ENGINE to Movement A item 2's feature ADR (closes GHI #623). Foundation names the invariant; feature names the mechanical defense. This is the taxonomy ADR-0.0.37 itself established, applied to its own delivery. 3. NC REPAIR SPLITS BY THE CORRECTION-VS-ENHANCEMENT INTENT TEST (operator rule, this session): the five generators are correction because the shipped apparatus does not fulfil campaign section 5's own clauses (a)(b)(c), so they route to GHI #699; the negative-control pole, cosmic-ray adoption, predicate-as-data authoring, and the loop attachment point were never declared and are feature, so they route to Movement B's feature ADR extending ADR-0.33.0. 4. NO WORK GOES OUT OF ORDER (operator ruling, verbatim: 'i will not go out of order'). Movement A is topmost and is what gets pulled. The loop ADR is Movement B and waits. No feature semver was claimed. 5. mutmut IS DISQUALIFIED, not deprioritized: it hardcodes pytest, and the operator's standing ruling is that pytest will never be used. cosmic-ray is the only maintained option that runs unittest natively. 6. THE AUDIT METHOD MUST NOT BECOME THE MECHANISM. It was five agent passes making judgment calls, which is a stochastic surface auditing a stochastic surface. It is defensible as a sizing diagnostic and indefensible as a control; cosmic-ray replaces it if Movement B adopts it.

## Immediate Next Steps

1. OPERATOR IS AUDITING ADR-0.0.37 via /gz-adr-audit to take it to Validated. Live state before the run: Completed, attested, 15/15, Closeout READY, QC READY. No precondition blocks it; see Decisions 1 and 2. 2. MOVEMENT A ITEM 2: re-home the registry-to-AGENTS.md composition engine as a feature ADR, closing GHI #623. This is where the orientation-index re-injection lands, as a registry entry with a composition target rather than a hand-typed pointer. 3. MOVEMENT A ITEM 3: the ADR-0.34.0-foundation-sunset capstone, 5 authored OBPIs currently Pending 0/5 (demote the unstarted foundations to pool, populate the grandfather manifest, backfill foundation_grandfathered, gz ontology resense, wire the permanent --taxonomy gate into gz check). 4. MOVEMENT B: the loop ADR extending ADR-0.33.0. It absorbs the feature half of the NC finding, and its item 5 (bind each widened door with a live NC) cannot be discharged honestly until GHI #699 is repaired. 5. GHI #699 repair when its turn comes: fix the five generators first, since they are bounded and mechanical and collapse most of the 32 findings. The correct template already exists in the tree at the unscoped-rules entrypoint, which asserts the specific exit code 3 rather than any non-zero exit.

## Pending Work / Open Loops

OPEN GHIS FILED THIS SESSION: #699 (class cut, the five generators, correction), #698 (symptom cut, validate_sections_populated has no NC). #648 was updated with the finding that its own certifying control, enforcement-floor, is blind to the ORPHAN class it tracks. CARRYOVER GHIS still open from the prior handoff: #654 corpus orchestration gap, #635 seven duplicate invariant corpus entries, #623 registry orphan (now Movement A item 2), #533 the sub-15k AGENTS.md destination, #691 rules have no aging clock, #682 security-sensitivity blocked, #696 handoff decision-state loss. UNSETTLED MEASUREMENT: the lint claim is the single UNCLEAR verdict in the audit; settle it by running the fixture command in a scratch dir with a clean py file, where exit 0 confirms ADEQUATE-narrow and non-zero makes it TRIVIAL. SEMVER SEQUENCE: ADR-0.44.0-vendor-alignment-codex sits out of sequence above ADR-0.34.0 and the operator has ruled against it. ADR-pool.feature-adr-semver-discipline already carries the diagnosis and two dispositions (backfill attestation versus renumber). It is Housekeeping in the campaign and gates nothing. MINOR: the budget file narrative in data/instructions_files_budget.json describes 31800 as the current AGENTS.md ceiling while the enforced value is 50000. AGENTS.md sits 778 bytes below the Codex runtime truncation point.

## Verification Checklist

The audit evidence artifact is committed at ca490fc0 and present at docs/governance/enforcement-claim-nc-audit-2026-07-18.md. Re-verify the orphan finding with a single command: grep -rl for the string agent-control-surface-rendering-substrate across AGENTS.md, CLAUDE.md, .gzkit/rules/, .claude/rules/, .gzkit/skills/ and scripts/ returns nothing. Re-verify the CIC-1 prohibition by reading the Decision section of the ADR-0.0.37 package; the sentence naming hand-authoring as a failure class is in the CIC-1 paragraph. Re-verify the qc-binding finding by reading register_qc_binding_claim near line 262 of src/gzkit/governance/trust_audits/qc_binding.py and confirming the third argument is _check_theater_signatures rather than audit_qc_binding. Re-verify the enforcement-floor finding by reading _ep_enforcement_floor near line 276 of src/gzkit/governance/trust_audits/_qc_nc_entrypoints.py and confirming it passes registry=records. Re-verify the binary-provenance finding by resolving which gz from outside the project tree. Do not pipe a verifier through head or tail or grep when checking exit codes; capture to a file and read the real exit code.

## Evidence / Artifacts

The audit evidence artifact produced this session, committed at ca490fc0: `docs/governance/enforcement-claim-nc-audit-2026-07-18.md`. The ADR whose Decision section settled the validate-as-is ruling: `docs/design/adr/foundation/ADR-0.0.37-constitutional-invariant-composition/ADR-0.0.37-constitutional-invariant-composition.md`. The campaign that governs sequencing, including Movement A and Movement B item 5: `docs/governance/build-to-1.0-campaign-2026-07-18.md`. The NC fixture table and the _build_empty helper: `src/gzkit/governance/trust_audits/_qc_negative_controls.py`. The NC entrypoints including _command_fails and _ep_enforcement_floor: `src/gzkit/governance/trust_audits/_qc_nc_entrypoints.py`. The meta-validator, the known-claims loader, and the bool() reduction: `src/gzkit/enforcement.py`. The qc-binding self-registration: `src/gzkit/governance/trust_audits/qc_binding.py`. The orphaned document carrying the Agent Orientation Index: `docs/governance/agent-control-surface-rendering-substrate.md`. The byte budget whose narrative has drifted from its enforced value: `data/instructions_files_budget.json`. The pool ADR that already diagnoses the feature-semver gap: `docs/design/adr/pool/ADR-pool.feature-adr-semver-discipline.md`. The predecessor handoff this one continues: `.gzkit/handoffs/20260718T190410Z-construction-loop-design-sections-1-3-approved.md`.
