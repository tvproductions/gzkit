---
mode: CREATE
adr_id: null
branch: main
timestamp: '2026-09-15T01:56:20Z'
agent: claude-code
session_id: 6cc9dfd9-da90-4bb4-a765-20351dcbe1fe
continues_from: 20260915T005456Z-session-close-935-refresh.md
---

## Current State Summary

Session 6cc9dfd9 resumed `20260915T005456Z-session-close-935-refresh.md`; every claim verified (level with origin, #935 closed, no locks, 36 overdue/1 due/3 unmeasured, 45 chore tests OK). Ruling booked via `gz handoff decide` (proceed; a-d set aside). Fixed GHI #1010 in `4c9774686`, synced (`c2135c6b0`), closed #1010 with a cause-to-test table.

- Entrypoint: `src/gzkit/cli/main.py` now calls `configure_logging` after parse (debug > verbose > quiet > normal); `_apply_debug_mode` removed. structlog no longer writes to stdout.
- 13 `--json` document sites switched from Rich `console.print`/`print_json` to `print(json.dumps(...))` (chores doctor; task start/start --req/complete/block/escalate/list x2/fanout; ledger corrections; preflight; handoff rulings; obpi adversary workspace).
- `src/gzkit/cli/logging.py`: colour only a terminal via `gzkit.color_env`, never forced (Windows SystemError), None stderr guarded.
- Independent spec review: FAIL (3 blocking, all fixed) then PASS. Mutation sweep 22/22 killed, conclusive. `gz check` exit 0.

At authoring: HEAD `c2135c6b0` level with origin/main, no OBPI locks.

## Important Context

### Campaign workflow fronts (source: `docs/governance/build-to-1.0-campaign-2026-08-16.md` § Workflow fronts, active per `data/active_campaign.json`)
- **handoff system:** unchanged.
- **ghi triage:** #1010 closed. Still open, verified at resume: #934, #1008, #1009, #1011; #983 and #894 await rulings.
- **adr/obpi campaign:** no OBPI work, no locks. ADR-0.35.0 still topmost.
- **new R&D:** not worked.

### Found this session
- `tests/commands/common.py` now has `hostile_console()` (12-col, forced truecolor terminal) and `parse_json_document()`; use them for any future `--json` test.
- `gzkit.mutation_witness` only credits assertion-class failures: a JSONDecodeError or AttributeError in the test body grades inconclusive. Route the property through `self.fail`.
- `FORCE_COLOR=3` is set in this shell environment; Rich output probes are confounded unless run with `env -u FORCE_COLOR`.
- zsh does not word-split `$v`; a loop over command strings passes one argument (exit 2 usage).
- `CliRunner` in `tests/commands/common.py` merges stdout and stderr, so it cannot witness which stream a line reached.

## Decisions Made

- [operator-ruled] Resume ruling on `20260915T005456Z`: "e — fix #1010". (a) frontier scan, (b) overdue board, (c) staleness-gate-class-fit ruling, (d) R&D skill design set aside.
- [agent-chose] Widened #1010 to the Rich-rendered `--json` mechanism: the contract's own acceptance named `chores doctor --json`, and the same mechanism sat at 12 more sites. Amendments posted on the issue before widening.
- [agent-chose] Left the nine `--dry-run` event previews unchanged: their handlers take no JSON output flag.
- [agent-chose] Kept `VERBOSITY_TO_LEVEL` (default INFO) rather than the spec's WARNING: cli.md § Flag Conventions and OBPI-0.0.3-07's attested tests back INFO/DEBUG; the conflict is recorded as insight `cli/logging-verbosity-levels` for a ruling, not resolved.

## Immediate Next Steps

1. Put the next move to the operator with the standing fronts: (a) the frontier-model-card-currency scan (#934 drift); (b) the 36-overdue chores board; (c) a ruling on insight `chores/staleness-gate-class-fit`; (d) R&D skill design per `20260912T235557Z` step 4; (e) a short direct fix, #1008; (f) a ruling on insight `cli/logging-verbosity-levels` (spec WARNING vs adapter INFO default).
2. If (e): #1008 is the verifier-pipe gate not recognizing a verifier inside a ( ) or { } group; start from `gzkit.verifier_pipe_gate.decide`.
3. If (f): the losing surface is either the `docs/design/cli-standards-v3.md` § Verbosity Levels table or `VERBOSITY_TO_LEVEL` plus `tests/test_logging.py::TestVerbosityLevels`.

## Pending Work / Open Loops

- GHI #934 (frontier drift), #1008 (verifier-pipe groups), #1009 (accumulated-work counters), #1011 (CHORE.md drift), #997 and #808 (chores); #983 and #894 ruling-gated; #969 open by ruling; #968 open.
- Insights awaiting operator calibration: `cli/logging-verbosity-levels` (new), `cli/json-error-paths` (new: `--json` failure paths still print Rich text to stdout, including the entrypoint error boundary), `chores/staleness-gate-class-fit`, `chores/staleness-signal-fit`, `cli/manpage-flag-claims`.
- Carried: permission-consent-drift lacks `proofs/settings-patch.md`; `src/gzkit/commands/obpi_stages.py` BASELINE_VERIFICATION prescribes a non-canonical unittest step; three live justify-binding violations; 83 enforcement claims population-undeclared and 55 exemption-undeclared.

## Verification Checklist

```bash
git status -sb
git rev-list --left-right --count origin/main...HEAD
gh issue view 1010 --json state,stateReason
uv run gz obpi lock list
uv run -m unittest tests.cli.test_json_stdout_log_isolation tests.commands.test_json_document_rendering tests.test_logging tests.commands.test_preflight > /tmp/v.log 2>&1; echo "exit $?"
uv run gz handoff rulings --search "fix #1010"
```
Expected: level with origin/main; #1010 CLOSED COMPLETED; no locks; tests exit 0; the resume ruling in the store. Re-run rather than trust.

## Evidence / Artifacts

- `.gzkit/handoffs/20260915T005456Z-session-close-935-refresh.md` (predecessor).
- `src/gzkit/cli/main.py`, `src/gzkit/cli/logging.py`, `src/gzkit/commands/task.py`, `src/gzkit/commands/chores.py`, `src/gzkit/commands/preflight.py`, `src/gzkit/commands/ledger_correct.py`, `src/gzkit/commands/handoff.py`, `src/gzkit/commands/obpi_adversary_workspace.py`.
- `tests/cli/test_json_stdout_log_isolation.py`, `tests/commands/test_json_document_rendering.py`, `tests/commands/common.py`, `tests/test_tasks.py`, `tests/commands/test_preflight.py`, `tests/test_logging.py`.
- `docs/design/cli-standards-v3.md` (Logging status row and banner).
- `artifacts/receipts/arb-ruff-349da2a997df4121a875958749b11550.json`, `artifacts/receipts/arb-step-typecheck-46ae2f150ed24d41855eecf8cfaa5ef5.json`, `artifacts/receipts/arb-step-unittest-2b95a9b9de2344e39d1e8085bf37d99e.json`.
- Commits `4c9774686` and `c2135c6b0`; GHI #1010 amendment and close comments.

## Settled Rulings

867 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
