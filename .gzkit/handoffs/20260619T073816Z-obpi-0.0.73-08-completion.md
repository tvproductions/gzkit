---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-19T07:38:16Z"
agent: claude-code
obpi_id: OBPI-0.0.73-08-fidelity-presence-enforcement
session_id:
continues_from:
last_lock_event_timestamp: "2026-06-19T06:32:25.752996+00:00"
last_commit_sha: 0593cacd
---

## Current State Summary

OBPI-0.0.73-08-fidelity-presence-enforcement is COMPLETE and operator-attested
("attest completed", g0). It mechanizes ADR-0.0.73 Boundary Invariant #4: the new
`gz validate --fidelity-presence` scope fails closed (exit 3) on any non-pool ADR
Decision lacking a parseable `## Fidelity Assertions` block, wired into `gz check`.
101 pre-existing block-less ADRs are grandfathered in
`data/fidelity_presence_grandfather.json` (the sensitivity-floor cutover precedent);
ADR-0.0.73 itself is compliant; a NEW block-less ADR fails closed. The `gz obpi
complete` ran with `--accept-security-floor` (additive-only discharge, brief-sanctioned).
This handoff is the register entry that pairs with the Stage-5 lock release.

## Important Context

- The security floor fired at completion because the allowlist overlaps
  `validate_cmd.py`/`quality.py` (registered security surfaces). The change is
  additive only (a new scope + a new gz-check step + its negative control), so the
  floor was discharged via `--accept-security-floor` per the brief's stated
  rationale (precedent OBPI-0.0.73-02). No existing security logic was modified.
- The new bound `gz check` step "Fidelity presence" carries a GENUINE negative
  control (block-less fixture ADR it must flag) registered in
  `_PRODUCTION_NEGATIVE_CONTROLS`; it is NOT in `_NEGATIVE_CONTROL_DEBT`, so
  `gz validate --qc-binding` stays green.
- The brief allowlist was amended during Stage 5 to honestly declare coupled
  surfaces the plan mis-located: `parser_maintenance.py` (argparse home),
  `quality.py` (wrapper home), `tests/commands/test_skills.py` (the gz-check
  mock-list), the `src/gzkit/templates/adr.md` sync mirror, `traceability.py`
  (shared @covers import, sibling convention), and `data/behave_coverage_waivers.json`.
- ADR-0.0.73 recovery freeze was operator-lifted this session: the real gate
  (OBPI-02 repair) was satisfied; the two failing Fidelity Assertions (OBPI-08/09)
  were circular and the NO-GO scorecard was authored pre-repair.

## Decisions Made

- **Decision:** Discharge the security floor via `--accept-security-floor` rather
  than narrow the allowlist.
  **Rationale:** The overlap is incidental (new scopes register in validate_cmd.py
  and quality.py); the change introduces no new security surface.
  **Alternatives rejected:** Relocating the scope registration to avoid the overlap
  (would fight established CLI-registration convention).

- **Decision:** Declare `req_atomic:` for all 5 REQs instead of subdividing TASKs.
  **Rationale:** This was one coherent validator-scope delivery in a single inline
  pass; each REQ is a proof-axis slice (one indivisible acceptance unit), not
  separately-tracked multi-session labor.
  **Alternatives rejected:** Minting seq=02 sub-TASKs after the fact.

- **Decision:** Waive behave coverage for REQ-08-02/03/04/05; only REQ-08-01 carries
  a behave scenario.
  **Rationale:** 08-02/03 are unit-proven BEHAVIOR (registry wiring + grandfather
  fixtures need an in-repo ADR corpus the behave workspace lacks); 08-04 is SUPPORT;
  08-05 is STRUCTURAL-FENCE. `gz covers` reports behavior_uncovered_reqs=0.
  **Alternatives rejected:** Forcing synthetic behave scenarios for unit-channel REQs.

## Immediate Next Steps

1. Release the lock: `uv run gz obpi lock release OBPI-0.0.73-08-fidelity-presence-enforcement` (this handoff is the paired register entry).
2. Remove pipeline markers `.claude/plans/.pipeline-active-OBPI-0.0.73-08-fidelity-presence-enforcement.json` and `.pipeline-active.json`.
3. Git-sync #1: `uv run gz git-sync --apply` (commits governance edits + lock release + marker cleanup).
4. Reconcile: `uv run gz obpi reconcile OBPI-0.0.73-08-fidelity-presence-enforcement` and refresh parent ADR status `uv run gz adr status ADR-0.0.73-verification-layer-binding-audit --json`.
5. Git-sync #2: `uv run gz git-sync --apply` (commits reconcile output + ADR status refresh).

## Pending Work / Open Loops

- ADR-0.0.73 OBPI-09 (waiver-ratchet honesty contract) remains a Draft/recovery item; its Fidelity Assertion (`uv run gz validate --waiver-ratchet`) is still red. Not in scope here.
- `data/behave_coverage_waivers.json` (521→522 entries) and `data/fidelity_presence_grandfather.json` (101 entries) are unratcheted waiver/grandfather surfaces — exactly the class OBPI-09 is meant to ratchet. The fidelity-presence grandfather is fail-closed on NEW ADRs by construction; OBPI-09 would add the meta-ratchet.
- The full `gz check` now runs the new "Fidelity presence" step; future ADR authors get the template stub by construction.

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.0.73-08-fidelity-presence-enforcement` reports attested-complete.
- [ ] `uv run gz validate --fidelity-presence` exits 0.
- [ ] `uv run gz validate --qc-binding` exits 0 (new bound step honest).
- [ ] `uv run gz adr fidelity ADR-0.0.73-verification-layer-binding-audit` shows the OBPI-08 row PASS.
- [ ] `uv run gz obpi lock list` no longer lists OBPI-0.0.73-08 after release.

## Evidence / Artifacts

- `src/gzkit/governance/trust_audits/fidelity_presence.py` — the validator.
- `data/fidelity_presence_grandfather.json` — 101 grandfathered block-less ADR ids.
- `tests/governance/test_fidelity_presence.py` — 13 unit tests.
- `features/fidelity_presence.feature` — 3 scenarios tagged REQ-0.0.73-08-01.
- `features/steps/fidelity_presence_steps.py` — behave fixture steps.
- `docs/user/manpages/validate.md` — `--fidelity-presence` manpage section.
- `.gzkit/templates/adr.md` — seeded `## Fidelity Assertions` stub.
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-08-fidelity-presence-enforcement.md` — completed brief with evidence.
