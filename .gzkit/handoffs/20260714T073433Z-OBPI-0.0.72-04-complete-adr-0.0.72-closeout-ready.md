---
mode: CREATE
adr_id: ADR-0.0.72
branch: main
timestamp: '2026-07-14T07:34:33Z'
agent: claude-code-057172e6
obpi_id: OBPI-0.0.72-04-security-floor-overridden-event
---

## Current State Summary

OBPI-0.0.72-04-security-floor-overridden-event is COMPLETE, attested (operator g0, "attest completed"), and synced to origin/main. The gz-obpi-pipeline ran all five stages. The new `security_floor_overridden` ledger event is live: completing this OBPI itself tripped the auth_boundaries security floor, so `--accept-security-floor` fired the emission and the ledger census went 0 -> 1 (recorded surfaces: 'auth_boundaries, ledger_integrity', attestor g0). Parent ADR-0.0.72 (meta-governance-coherence) is now Closeout READY at 3/3 OBPIs (this was its sole remaining blocker); OBPI-0.0.72-01 is withdrawn/Abandoned, 02/03/04 attested_completed. Last action: two guarded git-syncs; tree clean, main == origin/main.

## Important Context

The override witness is emitted by `_emit_security_floor_override_best_effort` in src/gzkit/commands/obpi_complete.py, called AFTER the atomic completion transaction returns -- structurally OUTSIDE its rollback boundary. This shape was reached after FIVE Codex Step-4b adversarial rounds, each of which cracked an inside-the-transaction variant (phantom-before-transaction, before-receipt orphan, hard-append gating completion, throwing warning path). The invariant: a best-effort ledger side-effect must live outside the atomicity boundary, not merely be try/except-wrapped inside it. Round 5 surfaced a PRE-EXISTING, system-wide `Ledger.append` non-atomicity (a partial mid-write corrupts any append; ledger_integrity security surface) -- operator-ruled out-of-scope for this OBPI and tracked in GHI #687. Also this session: ADR-0.29.0 (precise-auth-boundaries) was a burned-release-number orphan (release v0.29.0 shipped MX/ADR-0.0.74) and was demoted to pool under GHI #686 (closed). The OBPI-04 brief Allowed Paths were amended (operator-approved) to add sensitivity.py (public detect_brief_security_surfaces helper) plus the two mechanically-forced event-type fences trust_audits/events.py and ontology/corpus.py.

## Decisions Made

1. ADR-0.29.0 orphan -> demote to pool via GHI #686 (operator directive 'fix this bullshit via ghi now! pool + adr'); no foundation ADR. 2. Source the event's `surfaces` via a public detect_brief_security_surfaces helper in sensitivity.py + Allowed-Paths amendment (Option A, operator-approved) rather than inline-duplicating the extractor. 3. Accept OBPI-0.0.72-04 as complete and route the Ledger.append non-atomicity to GHI #687 (Option A, operator-ruled) rather than expand this OBPI into a ledger_integrity security-surface primitive rewrite. 4. Gate-5 recorded with adversary verdict refuted-with-caveats (rounds 1-4 fixed; round-5 caveat pre-existing and tracked). Rejected alternative for (3): expanding scope to fix Ledger.append now (higher blast radius, security surface, separate concern).

## Immediate Next Steps

1. Run the ADR-0.0.72 closeout ceremony: `/gz-adr-closeout-ceremony ADR-0.0.72` -> attest -> audit -> Validated (ADR is Closeout READY, 3/3 OBPIs).
2. Pick up GHI #687 (Ledger.append failure-atomicity): its own scoped fix on the ledger_integrity security surface -- serialize-then-single-write + truncate-on-failure + encoding='utf-8'; needs broad append regression testing.
3. Return to the Magna Carta campaign topmost pull: the Foundation Sunset (ADR-0.34.0) execution -- the Class-2 closeouts then the capstone.
Each of these ADVISES only; obtain operator authorization before executing.

## Pending Work / Open Loops

- GHI #687 OPEN: Ledger.append not failure-atomic (system-wide, ledger_integrity security surface).
- ADR-0.0.72 closeout pending (READY but not yet Validated).
- Campaign: Foundation Sunset (ADR-0.34.0, 5 OBPIs) awaiting execution; validate() registry collapse (#618); oversized-module census; deferred Phase 4 RECALL.
- GHI #686 CLOSED (ADR-0.29.0 demoted to pool, commit 45ee3a44).

## Verification Checklist

- `git rev-parse --short HEAD` resolves to 5196a76f on main; tree clean.
- `uv run gz adr status ADR-0.0.72` shows 3/3, Closeout READY.
- `uv run gz obpi status OBPI-0.0.72-04-security-floor-overridden-event` -> attested_completed.
- Census: `python3 -c "import json,pathlib; print(sum(1 for l in pathlib.Path('.gzkit/ledger.jsonl').read_text(encoding='utf-8').splitlines() if l.strip() and json.loads(l).get('event')=='security_floor_overridden'))"` -> 1.
- `uv run gz obpi lock list` -> no lock on OBPI-0.0.72-04 (surrendered mechanically at completion).

## Evidence / Artifacts

- `docs/design/adr/foundation/ADR-0.0.72-meta-governance-coherence/obpis/OBPI-0.0.72-04-security-floor-overridden-event.md` -- completed brief (Step-4b section records the 5-round verdict).
- `tests/test_security_floor_overridden.py` -- 11 adversary-hardened tests.
- `src/gzkit/commands/obpi_complete.py` -- `_emit_security_floor_override_best_effort`.
- `src/gzkit/events.py`, `src/gzkit/ledger_events.py`, `src/gzkit/schemas/ledger.json` -- the event triple.
- `.gzkit/handoffs/20260714T004256Z-OBPI-0.0.72-04-security-floor-overridden-event-complete.md` -- the mechanical completion handoff.
- `.gzkit/ledger.jsonl` -- completion receipt + the live security_floor_overridden event.
