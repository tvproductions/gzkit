---
mode: CREATE
adr_id: ADR-0.33.0
obpi_id: OBPI-0.33.0-01-airlock-data-model-and-events
branch: main
timestamp: '2026-07-09T01:02:00Z'
agent: claude-code-4472fe2c
last_lock_event_timestamp: '2026-07-08T23:42:22.675182+00:00'
last_commit_sha: b786fb8
---

<!-- Completion handoff for OBPI-0.33.0-01-airlock-data-model-and-events — mechanical register entry (GHI #619) -->

## Current State Summary

OBPI OBPI-0.33.0-01-airlock-data-model-and-events completed and attested by `g0` under ADR-0.33.0. The work lock (if held) was surrendered mechanically at completion; the `obpi_lock_released` ledger event is the surrender audit.

## Important Context

Written mechanically at `gz obpi complete` as the token-block exit-edge register entry (token surrender at the section's end; see `.gzkit/rules/token-block-discipline.md`). Auto-drafted from completion evidence and may be terse.

## Decisions Made

- attest completed — Gate 5 recorded by g0 for OBPI-0.33.0-01-airlock-data-model-and-events after independent adversarial validation (Codex, job task-mrcrhhaq-dambrd) returned REFUTED on exact enum closure, the refutation was closed with membership assertions on REQ-03/REQ-04, and the adversary's identical mutation (Authority.MATE + Decision.DEFER + Verdict.REVIEW) was re-run and now FAILS. Green bar: 6817 tests receipt arb-step-unittest-47804087a4d54b989f041ff003c74c7b; ruff receipt arb-ruff-8f09af87b3cc47e48ff2c000dfb5a69f; typecheck receipt arb-step-typecheck-8e43d17394114bdd9de43b37d5551325;

## Immediate Next Steps

1. Continue the parent ADR-0.33.0 checklist, or open the next OBPI.

## Pending Work / Open Loops

- Implementation summary: - Files created: `src/gzkit/airlock/__init__.py`, `src/gzkit/airlock/model.py`, `src/gzkit/schemas/seam_map.json`, `tests/test_airlock_model.py`, `tests/test_airlock_events.py` - Files modified (additive only): `src/gzkit/events.py` (AirlockInEvent/AirlockOutEvent + TypedLedgerEvent union append), `src/gzkit/ontology/corpus.py` (registry-coupled fence disposition), `src/gzkit/schemas/ledger.json` (closed events registry), `tests/test_schemas.py` (_EVENT_MODELS coherence) - Delivered: the airlock's pure additive data layer — five closed `enum.StrEnum` vocabularies (SeamKind, Provenance, Authori

## Verification Checklist

- [ ] `git rev-parse HEAD` resolves to `b786fb8` (or operator explains drift).
- [ ] Branch matches `main`.
- [ ] Key proof: The shape is live in the runtime, not merely declared in a schema file: ``` $ uv run python -c "from gzkit.events import parse_typed_event; print(type(parse_typed_event({'event':'airlock_in','id':'x','ts':'2026-07-08T00:00:00+00:00'})).__name__)" AirlockInEvent ``` Falsifiability is witnessed, not asserted. Independent adversarial validation (Codex, job `task-mrcrhhaq-dambrd`) returned **REFUTED**: the tests proved a specific out-of-enum value was rejected, but never that the vocabularies were closed to exactly their declared members. Its refuting mutation — `Authority.MATE` + `Decision.DEFER`

## Evidence / Artifacts

- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/obpis/OBPI-0.33.0-01-airlock-data-model-and-events.md` — the completed OBPI brief.
- `.gzkit/ledger.jsonl` — completion receipt and lock-release event.
