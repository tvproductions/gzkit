---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-18T05:45:42Z"
agent: claude-code
obpi_id: OBPI-0.0.73-05-absorb-dispatch-attestation-pool
session_id:
continues_from: .gzkit/handoffs/20260617T120034Z-obpi-0-0-73-04-completion.md
last_lock_event_timestamp: "2026-06-18T01:12:51.010307+00:00"
last_commit_sha: 241ca582c80a7f2c06f1f2cf1149e68d503cc6aa
---

<!-- Handoff document for ADR-0.0.73 — created by claude-code at 2026-06-18T05:45:42Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. On resume — at every freshness level, Fresh included — present the
advised steps and obtain explicit operator authorization before executing any of
them. You advise; the operator rules; you note variance and stop.

## Current State Summary

OBPI-0.0.73-05-absorb-dispatch-attestation-pool is **attested-complete** (Gate-5
operator-verbatim attestation by `g0`, 2026-06-18). The gz-obpi-pipeline ran the
full 5 stages: plan-audit PASS → implement → verify (all ARB receipts exit 0) →
operator attestation → completion.

The dispatch-attestation concern (ADR Decision item #5) is now folded into the
ADR-0.0.73 bound-checker mechanism:
- `src/gzkit/quality.py` — `run_dispatch_attestation_audit` reads the pool ADR
  and fails closed (exit 3) when the `absorbed_into: ADR-0.0.73` marker is absent.
- `src/gzkit/commands/quality.py` — the audit is wired into `_build_check_steps()`
  as `("Dispatch attestation", run_dispatch_attestation_audit)`.
- `src/gzkit/qc_binding.py` — classified `("audit", "docs/", "bound", "python_function")`.
- `docs/design/adr/pool/ADR-pool.obpi-pipeline-dispatch-attestation.md` —
  status `Pool` → `Superseded`, `absorbed_into: ADR-0.0.73` frontmatter,
  `## Absorption Note` body section.
- `tests/governance/test_dispatch_attestation_absorption.py` — 7 unit tests, all pass.

`gz obpi complete` exited 0 with completion type `operator-verbatim-conversational`.
The lock is **still held at handoff-author time** (release is step 1 below — the
handoff-before-release coupling per ADR-0.0.41 requires this entry to exist first).
The working tree has uncommitted governance edits awaiting git-sync #1.

## Important Context

- **Lane mismatch (non-blocking):** the brief frontmatter declares `lane: Lite`
  but the parent ADR-0.0.73 is `lane: heavy`, so the runtime correctly treated
  this OBPI as heavy. The `Lite` label in the brief body is a pre-existing author
  drift, not corrected here (surgical-change discipline; it did not block any gate).
- **Two documented overrides applied at completion** (both genuine false-positives,
  each with a recorded `--reason`):
  1. `--accept-stale-reconciliation` — the lone allowlist drift was
     `src/gzkit/traceability.py`, flagged only because the new test imports the
     cross-cutting `@covers` decorator and `traceability.py` shares the
     `src/gzkit/` top-level neighborhood with the allowlisted `quality.py`. It is
     test infrastructure, NOT a subject-under-test; per Gate-Friction anti-pattern
     it was deliberately NOT added to the allowlist.
  2. `--accept-security-floor` — the parent ADR's security-scan canonical slot is
     unfilled; the change is structurally additive (adds a QC audit step + a doc
     annotation), so the floor was discharged with rationale.
- **`req_atomic` exemption declared** for all 3 REQs (each is a single indivisible
  unit; no labor below the REQ to subdivide).

## Decisions Made

- **Decision:** Annotate the pool ADR as `status: Superseded` (not keep `Pool`).
  **Rationale:** Superseded accurately reflects absorption-into-ADR-0.0.73; the
  item is no longer a free unpromoted backlog candidate.
  **Alternatives rejected:** Keep `status: Pool` with only a body note — rejected
  because the status field would then misrepresent the item's lifecycle.
- **Decision:** Implement `run_dispatch_attestation_audit` as an inline Python
  audit (pattern of `run_handoff_document_audit`) rather than a new
  `gz validate --dispatch-attestation` scope.
  **Rationale:** A single doc-annotation check does not warrant a new CLI surface;
  inline Python matches the existing bound-step convention and stays Lite-shaped.
  **Alternatives rejected:** New validator scope — over-engineered for one marker check.
- **Decision:** Do NOT add `traceability.py` to the allowlist to clear the reconcile
  drift; use `--accept-stale-reconciliation` instead.
  **Rationale:** Gate-Friction doctrine forbids polluting the allowlist with the
  false positives the gate over-flags; the override keeps the allowlist honest.

## Immediate Next Steps

1. **Release the OBPI lock:** `uv run gz obpi lock release OBPI-0.0.73-05-absorb-dispatch-attestation-pool`
   (this handoff is the register entry the release coupling requires; do NOT use `--abandon`).
2. **Remove pipeline markers:** delete
   `.claude/plans/.pipeline-active-OBPI-0.0.73-05-absorb-dispatch-attestation-pool.json`
   and `.claude/plans/.pipeline-active.json`.
3. **Git-sync #1:** `uv run gz git-sync --apply` to commit all governance edits.
4. **Reconcile + ADR status:** `uv run gz obpi reconcile OBPI-0.0.73-05-absorb-dispatch-attestation-pool`
   then `uv run gz adr status ADR-0.0.73-verification-layer-binding-audit --json`.
5. **Git-sync #2:** `uv run gz git-sync --apply` to commit the reconcile output.

## Pending Work / Open Loops

- **Parent ADR-0.0.73 remaining checklist item:** #7 — "Evaluator truth-binding"
  (replace `gz adr evaluate` dim-1/dim-2 heuristics in `src/gzkit/adr_eval_scoring.py`
  with decision-substance checks; add a seventh `shape-graded-not-substance` theater
  signature). Items #1–#6 are now complete. ADR closeout (Stage 5 of the parent ADR)
  follows once #7 lands.
- **Brief `lane: Lite` vs ADR `lane: heavy` label drift** in this OBPI brief body —
  cosmetic; could be reconciled in a future sweep but did not block completion.

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.0.73-05-absorb-dispatch-attestation-pool` shows attested-complete
- [ ] `uv run gz obpi lock list` — confirm lock state before relaying "release the lock"
- [ ] `uv run -m unittest tests.governance.test_dispatch_attestation_absorption -v` passes (7 tests)
- [ ] `uv run python -c "from gzkit.qc_binding import build_qc_registry; print([s.id for s in build_qc_registry() if 'dispatch' in s.id])"` → `['dispatch-attestation']`
- [ ] Branch matches: `git branch --show-current` → `main`

## Evidence / Artifacts

- `src/gzkit/quality.py` — `run_dispatch_attestation_audit` function (bound enforcement step)
- `src/gzkit/commands/quality.py` — `_build_check_steps()` wiring of the new step
- `src/gzkit/qc_binding.py` — `_STEP_CLASSIFICATION` entry for "Dispatch attestation"
- `docs/design/adr/pool/ADR-pool.obpi-pipeline-dispatch-attestation.md` — Superseded + absorption note
- `tests/governance/test_dispatch_attestation_absorption.py` — 7 unit tests
- `tests/commands/test_skills.py` — mock step-list coupled-consumer fix
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-05-absorb-dispatch-attestation-pool.md` — completed brief with evidence

## Environment State

Python 3.13 + uv; branch `main`; ARB receipts under `artifacts/receipts/`:
`arb-step-unittest-ed0d4d6302d048ba94882b502aeb6d96` (6249+ pass),
`arb-ruff-35d3188b29ce469f8a20df1338be50aa`,
`arb-step-typecheck-fcca3f21c3a048a090d4a3bbe6f2ff4b`,
`arb-step-mkdocs-8f76902c22fe4c4da098d0109ce7dcfd` (all exit 0).
