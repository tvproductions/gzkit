---
mode: CHECKPOINT
adr_id: ADR-0.35.0
branch: main
timestamp: '2026-09-11T08:27:02Z'
agent: claude-code-50fd14ae
obpi_id: OBPI-0.35.0-06-validate-rendition-lineage
session_id: 50fd14ae-a31e-4881-9f48-7d8badfaf9c5
continues_from: 20260911T001043Z-obpi-0.35.0-05-completion.md
---

## Current State Summary

OBPI-0.35.0-06 (gz validate --rendition-lineage) Stage 2 is COMPLETE; Stages 3, 4b, 4, 5 remain. Implementation, wiring, negative control, BDD and docs all landed. NOTHING IS COMMITTED — the tree is dirty with ~16 modified + 4 new files. Acceptance at input_digest 45cf01ca84735e24dd917c36bbff3dc1924873ee9ac9bc81d25632632ca99319: all 8 proofs valid, 10 killed assertion-class mutations across the 6 BEHAVIOR REQs, spec accepted 8/8 (receipt arb-step-specreview-31f47107410b41ccaeecf5743b705a38), quality accepted 8/8 with the merge finding closed by evidence (receipt arb-step-qualityreview-f282d3545e1b45e9ab0f76ce5b47fc0f). EXACTLY ONE BLOCKER: finding req-03-unowned-content-mutation-gap needs a closure bound to the CURRENT proof id proof-562c35f6a66c47a9ab7cc51d5abb77a5. The OBPI lock is still HELD by agent claude-code-50fd14ae (elapsed 178m of 1440m ttl) — it was deliberately NOT released because this is a mid-flight checkpoint, not an abandonment.

## Important Context

THE ONE BLOCKER IS AN OPERATOR RE-RULING, NOT A DEFECT. The operator already ruled req-03-unowned-content-mutation-gap an 'accepted disclosed residual' on 2026-09-11 and it was recorded via gz obpi acceptance human-review (tier 3). That closure was then SUPERSEDED when the proofs were re-executed to pick up a repaired merge test, because acceptance closures bind to a specific proof id. The judgment is unchanged; only the binding is stale. The exact ruling text is preserved at .gzkit/evidence/obpi-0.35.0-06/req03-residual-ruling.txt — pass it through verbatim; do not recompose it. WHY THE RESIDUAL EXISTS: REQ-03's claim that unowned bytes never fail the gate is CORRECT, and its in-module half is witnessed by two killed mutations. Its cross-module half is structurally unwitnessable from inside this OBPI because composer.py's generate_candidate (a Denied Path belonging to already-landed OBPI-0.35.0-05) carries unowned bytes forward byte-verbatim from the same committed-rendition file _owned_drift compares — so an unowned section can never textually mismatch, and the filter's discriminating power depends on code this OBPI may neither touch nor mutate. Two independent rounds named that same root, which triggered the design-escalation rule and produced the operator ruling. A third repair attempt would surface it one layer deeper; do not attempt one. OPERATOR HAD AN UNANSWERED CLARIFICATION at handoff time: they declined the re-ruling prompt and asked to clarify something first. Four candidate objections were put to them and none was answered — (a) whether the re-prove should have been sequenced BEFORE the ruling so the closure landed once, (b) whether the residual should route to OBPI-07 for a cross-module witness instead of being closed, (c) whether human-review (documented as the degraded tier-3 ADVERSARY floor) is the right instrument for closing a SPEC-review finding, (d) whether they want to dictate their own ruling wording. ASK WHICH BEFORE ACTING.

## Decisions Made

- [operator] Ruled the never-published-lineage case DISCLOSED rather than fail-closed, and ruled the caller be wired (2026-09-11). Recorded in the brief's § Audit Contract as an inline AMENDED paragraph plus a § Change Log entry. This resolved a genuine conflict: GHI #785 requires a newly authored gate to have an automatic caller, data/uncalled_gate_grandfather.json forbids grandfathering a NEW gate verbatim, and fail-closing on a never-published lineage would have held gz check red on every commit until OBPI-07 lands.
- [operator] Approved three successive allowlist amendments, all consequent on that ruling: first data/check_scope_membership.json + _qc_nc_entrypoints.py; then src/gzkit/quality.py + src/gzkit/commands/quality.py; then qc_binding.py + check_step_concurrency.json + test_validate_registry_parity.py + _qc_claim_exemptions.py. Registering ONE gz check step cascaded into six coupled surfaces, each an anti-laundering fence that only fires once the step is bound.
- [operator] Ruled req-03-unowned-content-mutation-gap an accepted disclosed residual (needs re-binding, see Important Context).
- [agent-chose] Fixed a real interaction defect found by the acceptance prover: the gate's always-on stderr advisory interleaved into unittest -v's result line, breaking test-id extraction so 5 of 6 proofs read baseline-not-green and every mutation came back inconclusive. Added a _validate() stderr-capturing helper to the test fixture mixin. The quality reviewer noted this localizes the hazard but is NOT a systemic guard — any future consumer calling the gate directly can reproduce it.
- [agent-chose] Repaired the quality reviewer's merge finding rather than arguing it: added a multi-operand LineageCoverage.merge test with twelve mutually-distinct values whose six pairwise sums are also distinct, so a transposed or self-summed field cannot coincidentally pass. Observed RED: AssertionError: 94 != 340.
- [agent-ERROR] Dispatched the merge repair WHILE a spec review was in flight. That review returned a clean accepted 8/8 against a digest the repair had already invalidated, and the re-prove also superseded the operator's REQ-03 closure. Cost: one wasted review round plus the re-ruling now outstanding. The skill's 'sequence shared acceptance/schema writes outside review windows' covers this; it was misread as applying only to parallel sessions.

## Immediate Next Steps

1. ASK THE OPERATOR which of the four clarification points they wanted to raise (see Important Context) BEFORE re-recording anything.
2. Then, if they confirm: uv run gz obpi acceptance OBPI-0.35.0-06-validate-rendition-lineage human-review --attestor g0 --ruling "$(cat .gzkit/evidence/obpi-0.35.0-06/req03-residual-ruling.txt)" — verbatim, no recomposition. Confirm stage2 ready becomes true with open_findings empty.
3. CRITICAL: make NO further edits to src/** or tests/** after that closure lands, or the digest moves and supersedes it again. If an edit becomes unavoidable, re-prove all 8 specs from .gzkit/evidence/obpi-0.35.0-06/proof-specs/ and redo BOTH reviews plus the closure.
4. Stage 3 verify: ARB-wrap the canonical gates (gz arb ruff, gz arb typecheck, gz arb step --name unittest, plus Heavy-lane mkdocs and scoped behave on tags @REQ-0.35.0-06-01/-02/-03). Read each receipt's exit_status; do not trust an aggregate.
5. Stage 3 Phase 1b: uv run gz covers OBPI-0.35.0-06-validate-rendition-lineage --json — expect behavior_uncovered_reqs 0 (REQ-07 SUPPORT and REQ-08 STRUCTURAL-FENCE carry no @covers by design).
6. Step 4b: MANDATORY tier-1 Codex adversary. Build the disposable writable checkout with uv run gz obpi adversary-workspace, then dispatch via node .../codex-companion.mjs task --write --cwd <checkout> --prompt-file <prompt>, ARB-wrapped. Codex was ready:true earlier this session. Import with acceptance review --receipt.
7. Stage 4: narrator-composed evidence packet to .gzkit/evidence/<OBPI>.stage4a.md, then gz obpi verify-packet, then present 4a + 4b together and WAIT for operator attestation.
8. Stage 5: precomplete, closure-narrative preview, gz obpi complete, completion handoff BEFORE lock release, two git-syncs.

## Pending Work / Open Loops

- Nothing is committed. Tree carries ~16 modified + 4 new files plus 7 session-exit bookmark handoffs. No commit has been made this entire session.
- The OBPI lock is held by agent claude-code-50fd14ae. A new session will have a different agent id and may fail to claim it. Do not force-release casually — read the lock, then either reclaim as the same logical operator workflow or release and re-claim deliberately.
- Pipeline markers are live at .claude/plans/.pipeline-active-OBPI-0.35.0-06-validate-rendition-lineage.json and .pipeline-active.json; leave them in place during implementation.
- REQ-03 residual is disclosed, not proven. A cross-module witness for composer.py's carry-forward behaviour is the honest long-term fix and most naturally lands with OBPI-0.35.0-07, which consumes this gate. Not filed as a GHI — the operator ruled it a residual rather than a work order.
- Quality reviewer's three non-blocking observations stand and are NOT defects: validate_rendition_lineage is ~68 lines against a 50-line GUIDANCE band (cite .claude/rules/complexity-thresholds.json, never a restated number); this gate emits no ledger event on its fail-closed path where sibling rendition_floor_coherence.py emits composition_drift_detected; and the always-on advisory's stderr footprint is wider than the sibling's.
- On the LIVE repo the new gate exits 0 and discloses 0/22 sections owned, 0/47851 bytes owned, 12 sections / 41846 bytes UNGRADED. That is the correct bootstrap state pending OBPI-07, NOT a regression to chase.
- gz arb red returns failure_class=error for all six BEHAVIOR REQs because the module does not exist at HEAD (import failure). That is a WEAK red by the tool's own definition and must not be presented as an assertion RED. The in-tree mutation kills are the substantive evidence.

## Verification Checklist

- uv run gz obpi acceptance OBPI-0.35.0-06-validate-rendition-lineage status --stage stage2 --json -> ready false, open_findings ['req-03-unowned-content-mutation-gap'], exactly 1 blocker
- uv run python -m unittest tests.governance.test_rendition_lineage -> exit 0, 12 tests
- uv run python -m behave features/rendition_lineage.feature -> exit 0, 3 scenarios, 20 steps
- uv run gz validate --rendition-lineage -> exit 0 (discloses the ungraded bootstrap state)
- uv run gz validate --gate-callers -> exit 0
- uv run gz validate --exemption-controls -> exit 0
- uv run gz cli audit -> exit 0
- uv run gz lint -> exit 0; uv run gz typecheck -> exit 0
- uv run unittest-parallel -t . -s tests --buffer -> exit 0, 10148 tests, 0 failures

## Evidence / Artifacts

- Proof specs (re-runnable): .gzkit/evidence/obpi-0.35.0-06/proof-specs/REQ-0.35.0-06-0{1..8}.json
- Operator ruling text for the REQ-03 residual, verbatim: .gzkit/evidence/obpi-0.35.0-06/req03-residual-ruling.txt
- Final spec review: arb-step-specreview-31f47107410b41ccaeecf5743b705a38 (accepted 8/8, zero findings)
- Final quality review: arb-step-qualityreview-f282d3545e1b45e9ab0f76ce5b47fc0f (accepted 8/8, closed rendition-lineage-merge-untested-multi-operand)
- Earlier rounds retained as history: arb-step-specreview-3c195166fc7c432789ca49635c910a54 (raised the REQ-03 finding), arb-step-specreview-09feec6798f146fa8b5db3cc7059466a (REFUSED to close it), arb-step-qualityreview-2495e093c23d4d77b745328eb5fbadde, arb-step-specreview-72138a4f7eb94dfb828e7d2679d4551c, arb-step-qualityreview-30eb5ca7771048f48010d0d8eee427ec (raised the merge finding)
- Plan: .claude/plans/virtual-kindling-piglet.md; plan-audit receipt PASS: .claude/plans/.plan-audit-receipt-OBPI-0.35.0-06-validate-rendition-lineage.json
- Brief § Audit Contract AMENDED paragraph and § Change Log 2026-09-11 entries carry the operator rulings and all three allowlist amendments

## Settled Rulings

801 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
