---
mode: CREATE
adr_id: null
branch: main
timestamp: '2026-08-29T01:07:24Z'
agent: claude-code
session_id: 3a3dcd7c-4d28-4645-b5e6-7ee2cd19de1e
continues_from: .gzkit/handoffs/20260828T235337Z-ghi-909-908-fixed-906-measured.md
---

## Current State Summary

A handoff review that turned into five closed defects and a measured gate speedup. The operator ruled twice by bounded-choice prompt: once on the resumed handoff's advised steps (booked `proceed`, session `3a3dcd7c`), once on GHI #906 and #911 after the evidence was gathered.

Five commits beyond the prior handoff, HEAD `d1f6b644`, main synced (`0 0`), tree clean. Full gate green throughout: `uv run gz check` exit 0 at **45.64s**, down from **65.07s** at session start.

CLOSED fixed: #909 (cwd corruption), #908 (init leaves an unsynced tree), #906 (Behave sharding), #911 (adopter path leak), #910 (missing README). FILED open: #912. Open-issue count 35 -> 32.

Each fix exposed the next. #909's repair made #908's test fixture trustworthy; #908's repair unmasked #911 by rendering the mirrors that had been missing; #911's repair shrank #908's fixture accommodation from 23 seeded paths to 7 and isolated #912 as the remainder; #910 surfaced while verifying #909, because the probe tree could not host a `uv run` at all.

## Important Context

BEHAVE NOW SHARDS x4 AND THE COUNT IS DECLARED, NOT COMPILED IN. The concurrency declaration under data/ carries a `shards` key on the Behave entry, read from the root passed in. FOUR IS THE SATURATION POINT: eight shards halve the step again and move the gate not at all, because past roughly 34s Behave stops being the critical path and further shards contend with `Test`. Re-measure before changing it, and measure IN-GATE -- Behave costs 29.33s standalone and 47-49s inside `gz check`, so the standalone figure overstates the available gain by more than half.

`.gzkit/` IS ADOPTER-REAL AND THAT IS THE TRAP IN THE #911 CLASS. A first classification matched the literal "gzkit" and reported four rules as wholly framework-internal; three were false, because the exchange-lock and handoff directories under `.gzkit/` belong to whoever runs gz. The committed predicate keys on the package source tree, `scripts/` and `data/`. Anyone touching delivery scoping must not re-derive it by string match.

THE TEST_AUDIT FIXTURE SEEDING IS AN ACCOMMODATION WITH A SHELF LIFE. It seeds 7 paths so the readiness assertion stays meaningful on a young tree, and its comment says so. It was 23 before #911. When #912 lands it should go to 0 -- that shrinkage is the honest measure of each fix, and it is how #911's end-to-end property was verified rather than by a circular test.

TWO CAUSES WORE ONE SYMPTOM IN THE READINESS AUDIT, and separating them was most of #911's work. An unreachable `applyTo` pattern can mean the glob is UNSATISFIABLE (a delivery leak, 19 of them, now fixed) or merely UNPOPULATED (a young project with no ADRs authored, 14 of them, now #912). The audit cannot tell them apart. Do not read a reachability count as a leak count.

A GREEN TEST CAN BE GREEN BECAUSE THE SURFACE IS ABSENT -- three instances in one session. The reachability audit returns early when the instructions directory does not exist; a first-entry-of-iterdir assertion is correct while the directory holds one entry; the sync-parity check had nothing to compare. Fixing the absence is what made all three speak.

THE TAUTOLOGICAL-TEST AUDIT CAUGHT A TEST I WROTE AND WAS RIGHT. A pin asserting the concurrency declaration says 4 restated the file it read and could not fail on a behaviour change. It was deleted, not silenced. The declaration's own `why` field carries the measurement record instead.

`uv run` RESOLVES ITS PROJECT FROM THE PROCESS CWD. Any gzkit code shelling out to it against a tree it is not standing in has #909's bug shape. Both new surfaces this session derive from the root passed in for that reason.

A `uv`-UNBUILDABLE FIXTURE IS A LEGITIMATE TEST INSTRUMENT -- and after #910 it must be constructed deliberately, because a scaffolded tree now builds. The hook-emitter test names a build backend that does not exist with no requirements to resolve: local, offline, ~30ms.

THE `verifier-pipe-gate` HOOK PARSES ONLY THE FIRST STATEMENT of a compound command. A heredoc followed by a piped verifier trips it even with `set -o pipefail` leading. Split the write and the run.

DO NOT `git stash --keep-index` TO GET A BASELINE. It keeps the working tree as-is, so it produces no baseline, and popping it against a since-edited file yields an add/add conflict. Reason from the code or use a worktree.

THE HANDOFF GATE READS PATH-SHAPED TOKENS AS FILE REFERENCES, dotted key paths included. Write bare paths, and describe keys in prose.

## Decisions Made

- [operator-ruled] Work #909, #908, and measure #906 (verbatim: "Fix GHI #909 (cwd corruption) (Recommended), Fix GHI #908 (gz init fails --surfaces), Measure GHI #906 (Behave sharding)"). Booked `proceed`; #907 and campaign Movement B recorded as set-asides.
- [operator-ruled] Proceed on the one-line Claude hook-emitter touch despite two Draft briefs declaring that path (verbatim: "Proceed — no ownership (Recommended)"), after the disposition was surfaced.
- [operator-ruled] Resolve the readiness-audit fallout by seeding the fixture and filing the finding separately (verbatim: "Seed the fixture + file a GHI (Recommended)").
- [operator-ruled] Build the Behave sharding at four shards (verbatim: "Build it at 4 shards (Recommended)").
- [operator-ruled] Fix #911 with a delivery-time path classifier (verbatim: "Delivery-time path classifier (Recommended)"), explicitly NOT by scrubbing canonical frontmatter and explicitly NOT by making the audit adopter-aware. That second exclusion is why #912 exists as its own issue rather than being folded in.
- [operator-ruled] Take #910 (verbatim: "take 910").
- [agent-chose] Created the README rather than dropping the `readme` key. Both close the build failure; `readme` is real packaging metadata the adopter would otherwise re-add, and the scaffolder's direction is more skeleton, not less.
- [agent-chose] Took #906 down the direct-fix path. It is labelled `enhancement` so the Trigger criterion does not strictly hold, but no mandatory-ceremony trigger fires -- no CLI surface, no schema, no runtime contract, two source files, no live brief owning the quality module. § Defect-fix routing says ambiguous does not default to ceremony.
- [agent-chose] Corrected my own classification mid-ruling and said so, rather than building on it. The four-rules-wholly-internal figure I gave the operator was wrong; the real figure is one. The ruling stood, but the correction is what made the classifier the right mechanism instead of a file-level partition.
- [agent-chose] Repaired both coupled consumers inside #908's commit -- the flaky first-entry assertion and the readiness fixture -- rather than leaving them to fail later.
- [agent-chose] Wrote #910's durable witness against the CLASS (every path the scaffolded manifest declares exists) rather than the readme instance, per the GHI's own recommendation.
- [agent-chose] Deleted a test the tautological-test audit flagged instead of arguing with it.
- [agent-chose] Filed #912 rather than extending #911's fix into the audit's semantics, because the operator had just declined that exact option.

## Immediate Next Steps

1. GHI #912 is the natural next take and is fully specified: three candidate predicates with a recommendation to measure the first -- a pattern is reachable if its longest non-glob directory prefix exists -- against the current corpus before committing. It is the last thing standing between a fresh `gz init` tree and a green `gz readiness audit`, and landing it should take the test_audit seeding from 7 paths to 0.
2. The other readiness failure on a fresh tree is a missing manpages index under docs/user/, seen throughout this session and never investigated. It is the same adopter-boundary family as #911 [settled] and has no issue of its own yet.
3. Consider drawing campaign work. Nothing has been drawn in FOUR sessions; the plan sits at 7/25 with Movement B (calibrate the airlock gate before widening it) topmost. Only the operator can initiate it.
4. GHI #907 remains scope-blocked by #611 for the ledger tier. GHI #894 remains carried forward untouched.
5. The suppressed-failure residual in the hook formatter still has no tracking home -- see Pending.

## Pending Work / Open Loops

- GHI #912 OPEN, fully specified, no blocker. Its fix should also retire the last 7 seeded paths in the test_audit fixture.
- GHI #907, #894 carried forward untouched; #611 remains the only mechanism that can correct ledger identity rows without breaching append-only.
- The suppressed-failure half of #909 [settled] is UNFIXED and deliberately so: the hook formatter still swallows every subprocess error, so a formatter that cannot run is indistinguishable from one that ran. The cwd binding made the outcome deterministic per root; it did not make the failure visible. Offered to the operator for filing and not yet ruled on -- still no tracking home.
- The fresh-tree missing-manpages-index readiness failure has no issue.
- `gz check` advisory drift is 697 findings (696 unlinked specs, 1 unjustified code change), down from 699. Advisory, does not affect exit code, untouched this session.
- The foreign-reference check in the instruction audit still scans only for airlineops and opsdev. #911 [settled] fixed the gzkit-to-adopter leak by classifier; the foreign-reference check itself remains one-directional and cannot see a future extraction doing the same thing.

## Verification Checklist

- `uv run gz check` exits 0 in roughly 45-49s (was 65.07s at session start; the Behave shard runner is the difference).
- `git rev-list --left-right --count origin/main...HEAD` reports `0 0` and `git status --porcelain` is empty.
- `uv run python -m unittest tests.governance.test_behave_sharding tests.governance.test_rule_delivery_scoping tests.commands.test_init tests.commands.test_audit` exits 0.
- Behave conservation: 68 of 68 feature files partition into 4 shards, summing to 408 scenarios passed, 0 failed, 35 skipped -- the single-process baseline exactly.
- To reproduce #911's outcome: `gz init --yes` a tmp tree, then `gz readiness audit`. 14 unreachable patterns, none naming the gzkit package source (was 33).
- To reproduce #910's outcome: `gz init --yes` a tmp tree, then run any `uv run` command in it. Exit 0 (was exit 1, hatchling readme error).
- `gh issue view` reports CLOSED for 906, 908, 909, 910, 911 and OPEN for 912. Open-issue count is 32.

## Evidence / Artifacts

Commits this session, all on main:
- `542e2567` fix(hooks): bind the hook formatter to the synced root, not the caller's cwd (GHI #909)
- `87e88314` fix(init): leave a synced tree so the scaffolder passes its own surface gate (GHI #908)
- `f8f3a01d` chore(handoffs): record the GHI #909/#908 repair session
- `4ae44ded` feat(check): shard Behave across four processes (GHI #906)
- `515c95dc` fix(rules): scope delivered rules to paths an adopter can have (GHI #911)
- `d1f6b644` fix(init): create the README the scaffolded manifest names (GHI #910)

New surfaces:
- `src/gzkit/quality.py` — the shard count reader, the LPT planner, the parallel launcher and the result aggregator
- `src/gzkit/rules/__init__.py` — the framework-internal path predicate, the framework-tree discriminator and the delivery-scoping filter
- `src/gzkit/hooks/core.py` — the ruff formatter now takes the project root and binds the subprocess cwd
- `src/gzkit/commands/init_cmd.py` — a final sync after every scaffolder; README.md in the skeleton
- `data/check_step_concurrency.json` — the Behave entry gains a shard count of four, plus the measurement record in its `why`

New tests:
- `tests/governance/test_behave_sharding.py` — partition conservation, LPT placement, failure ordering, the one-writer fence
- `tests/governance/test_rule_delivery_scoping.py` — the framework/adopter predicate and delivery filtering
- `tests/hooks/test_hook_emitter_root_binding.py` — the two-directory property and the cwd seam
- `tests/commands/test_init.py` — the synced-tree parity test and the self-consistent-manifest class witness

Measurements (10-core host, 2026-08-28):
- Gate: 65.07s -> 45.64s
- Behave in-gate: 47-49s single-process, 33.85s at four shards, 22-23s at eight; gate 62.07s -> 50.45s at four and 50.54s at eight
- Reachability on a fresh tree: 33 unreachable -> 14
- test_audit fixture seeding: 23 paths -> 7

GHIs: #906, #908, #909, #910, #911 CLOSED fixed; #912 filed OPEN.
Predecessor: `.gzkit/handoffs/20260828T235337Z-ghi-909-908-fixed-906-measured.md`
Active campaign: `docs/governance/build-to-1.0-campaign-2026-08-16.md` (7/25, Movement B topmost, unchanged).

## Settled Rulings

578 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
