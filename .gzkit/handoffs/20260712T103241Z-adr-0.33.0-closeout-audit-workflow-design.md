---
mode: CREATE
adr_id: ADR-0.33.0
branch: main
timestamp: "2026-07-12T10:32:41Z"
agent: claude-code
obpi_id:
last_lock_event_timestamp:
last_commit_sha: c62e7b59
session_id:
continues_from:
---

<!-- Handoff document for ADR-0.33.0 — created by claude-code at 2026-07-12T10:32:41Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

OBPI-0.33.0-06-airlock-doctrine-lawful (the airlock ADR's one-way door) is
**completed, attested by g0, and synced to main** (HEAD `c62e7b59`). It promoted
`work-phases-and-airlock.md` and `four-phases-of-work.md` from Draft to BINDING,
widened the section-2 seam definition to BODY-and-BOUNDARY, and discharged the
campaign section-8 (Phase 3 HATCH) gate. A doc-content-grep regression test was
authored, used for the Step-4b Codex falsification, then removed pre-push after
the `gz check` tautological-test audit correctly flagged it (STRUCTURAL-FENCE /
SUPPORT REQs are proven by parent-ADR Boundary Invariants + ledger/validator, so
no unit test was needed).

**ADR-0.33.0 is now 6/6 OBPIs Completed** but the ADR frontmatter is still
`Draft` — it sits exactly at the closeout boundary. No work lock is held (released
at completion). Tree clean, branch synced (ahead=0, behind=0).

A four-part read-only survey of the whole ADR lifecycle (design → authoring →
validation → OBPI completion → closeout → audit) was run and is summarized under
Important Context — it is the input to the planned workflow-design ADR.

## Important Context

The survey found the ADR lifecycle is **segment-complete but macro-unorchestrated**,
and worse, the surfaces have **actively diverged**:

- **No macro-orchestrator.** No skill drives the full lifecycle; no `gz validate`
  scope checks the whole sequence; the `gz-workflow` "design through release"
  router truncates at `plan audit`. The two unrouted seams:
  last-OBPI-complete → ADR-closeout, and closeout → audit → Validated.
- **Order documented ~9 times, authoritatively zero.** The candidate authoritative
  page (`docs/user/concepts/workflow.md` § "ADR Closeout Loop") is an unmarked
  concepts page nothing enforces or cites.
- **Active divergence.** A Validated ADR (ADR-0.19.0) + the shipped ceremony
  skills folded `gz attest` INTO `gz closeout`, added a second Gate-5 attestation
  moment at audit, and bound a fidelity gate — none absorbed by the older canon
  docs. Concept-doc followers and skill followers run different command sequences.
  Consequence for the closeout below: follow the SKILL sequence (attest subsumed),
  not the drifted docs.
- **Enforcement is piecewise, never a whole-chain gate.** `gz closeout` blocks on
  incomplete OBPIs; `gz audit` refuses without prior attestation — but these are
  independent single-hop preconditions in command handlers; no tool re-derives the
  full `OBPIs→closeout→attest→Completed→audit→Validated` chain from the ledger.
- **ADR carries no completion-process checklist.** Its `## Checklist` is the OBPI
  WBS, plus a scalar `status` and `## Attestation Block` table.
- **The campaign is the WRONG model** for the operator's "ledger-validated
  checklist" idea — it is hand-maintained markdown with zero ledger binding. The
  right precedents are `gz obpi precomplete` (9 frozen `CheckResult` preconditions,
  fail-closed) and `gz adr audit-check` (ledger `get_artifact_graph()` binding).

## Decisions Made

- **Decision:** Sequence the remaining work as (1) ADR-0.33.0 closeout, (2)
  ADR-0.33.0 audit, (3) design the macro-workflow + ADR-completion-checklist.
  **Rationale:** Close the finished airlock ADR before designing its successor;
  the design is informed by having just walked the (broken) lifecycle end-to-end.
  **Alternatives rejected:** designing the workflow first (leaves ADR-0.33.0
  hanging); folding the workflow fix into ADR-0.33.0 (wrong scope — it is a new
  design, not an airlock OBPI).
- **Decision:** The workflow design is an **interview-driven `gz-design` ADR**
  (operator review + interview), not agent-drafted-in-a-vacuum.
  **Rationale:** Operator directive; the design touches Magna Carta / canon
  surfaces and the divergence resolution needs operator judgment.
  **Alternatives rejected:** direct-fix / GHI repair (too large; genuine design).
- **Decision:** Model the ADR-completion checklist on `gz obpi precomplete` +
  `gz adr audit-check`, NOT the campaign.
  **Rationale:** Those are already ledger-bound fail-closed; the campaign is not.
  **Alternatives rejected:** porting the campaign checkbox pattern (no ledger
  binding — would reproduce the hand-maintained drift).

## Immediate Next Steps

1. **ADR-0.33.0 closeout** via `/gz-adr-closeout-ceremony ADR-0.33.0` — follow the
   SKILL sequence (attest subsumed into closeout per Validated ADR-0.19.0), which
   includes `gz adr audit-check` + `gz adr fidelity` as bound gates. Requires
   operator Gate-5 attestation.
2. **ADR-0.33.0 audit** via `/gz-adr-audit ADR-0.33.0` → COMPLETED → VALIDATED;
   emit the `validated` receipt. This earns the Phase 3 HATCH campaign box.
3. **Design the workflow** via `/gz-design` — an interview-driven session with the
   operator producing an ADR for: (a) a macro-orchestration skill/driver across
   the lifecycle, (b) a schema-mandated ADR completion-process section, (c) a
   `gz adr precomplete` / `--adr-completion-fresh` ledger-validated checklist
   validator, and (d) reconciling the ADR-0.19.0-vs-canon-doc divergence.
4. After (3) design lands, decompose into OBPIs and run the pipeline per stage.

## Pending Work / Open Loops

- ADR-0.33.0 is `Draft` in frontmatter despite 6/6 OBPIs Completed — closeout
  (step 1) transitions it to Completed, audit (step 2) to Validated.
- The Phase 3 HATCH campaign box is checked as the section-8 discharge but its own
  bar ("6/6 attested + a `/gz-adr-audit` Validated receipt") is not fully met
  until step 2's Validated receipt lands.
- Residual campaign caveat (Codex Step-4b): the pre-existing aspirational
  design-description prose in the Phase-3 bullet ("judgment-grade", "mature exit
  surfaces") overstates the delivered diagnostic-only reality — belongs to the
  ADR-0.33.0 closeout narrative pass, not FC-6.
- The four survey subagent transcripts hold the full evidence for the step-3
  design; the synthesis is captured in Important Context above.

## Verification Checklist

- [ ] `git branch --show-current` returns `main`
- [ ] `uv run gz adr status ADR-0.33.0` shows 6/6 OBPIs complete, ADR Draft
- [ ] `uv run gz obpi lock list` shows no lock on any OBPI-0.33.0-*
- [ ] `uv run gz adr audit-check ADR-0.33.0` passes (OBPI completeness + coverage)
- [ ] `uv run gz validate --qc-binding` exits 0 (airlock NC live)
- [ ] No uncommitted changes conflict with handoff state (`git status -sb`)

## Evidence / Artifacts

- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/ADR-0.33.0-airlock-membrane.md` — parent ADR (6-OBPI checklist, Boundary Invariants #2/#4)
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/obpis/OBPI-0.33.0-06-airlock-doctrine-lawful.md` — completed doctrine-lawful brief (Step-4b evidence, closure narrative)
- `docs/governance/work-phases-and-airlock.md` — now BINDING; section-2 BODY-and-BOUNDARY widening
- `docs/governance/four-phases-of-work.md` — now BINDING doctrine
- `docs/governance/build-to-1.0-campaign-2026-06-30.md` — Phase 3 HATCH box checked; coherent 6/6 narrative
- `docs/user/concepts/workflow.md` — the orphaned "ADR Closeout Loop" order (survey subject)
- `src/gzkit/commands/obpi_precomplete.py` — the precedent CheckResult fail-closed pattern for the ADR-completion-checklist design
- `src/gzkit/commands/adr_audit.py` — `gz adr audit-check` ledger `get_artifact_graph()` binding (precedent)

## Environment State

Python 3.13 via uv; branch `main` synced to origin (ahead=0, behind=0) at HEAD
`c62e7b59`. Codex CLI available (`gpt-5.6-sol`) for Step-4b adversarial validation
— dispatch with `--model gpt-5.6-sol --effort high` and be patient (xhigh-effort
turns can run several minutes; do not mistake a slow turn for a hang).
