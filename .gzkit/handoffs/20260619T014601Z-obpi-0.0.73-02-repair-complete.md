---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-19T01:46:01Z"
agent: codex
obpi_id: OBPI-0.0.73-02-qc-binding-validate-scope
session_id:
continues_from:
last_lock_event_timestamp: "2026-06-19T01:11:46.426186+00:00"
last_commit_sha: d13ee2d3
---

## Current State Summary

OBPI-0.0.73-02-qc-binding-validate-scope was repaired after the original Completed state proved invalid. The recovery removed acknowledged QC negative-control debt from the passing path, registered production negative controls for every existing bound QC step, updated the scoped tests, kept the operator-approved recovery governance surfaces in the brief allowlist, and emitted a new OBPI completed receipt with operator attestation from this session.

The active lock was still held at the time this handoff was authored so the normal release path could pair the release event to this register entry. The intended immediate follow-through is `uv run gz obpi lock release OBPI-0.0.73-02-qc-binding-validate-scope`.

## Important Context

`uv run gz obpi complete OBPI-0.0.73-02-qc-binding-validate-scope` cannot be the repair accounting path because the brief was already marked Completed by the earlier bad state and the command refuses to re-complete a Completed brief. The repair accounting therefore used `uv run gz obpi emit-receipt OBPI-0.0.73-02-qc-binding-validate-scope --event completed` with a full evidence payload and operator-verbatim attestation.

The recovery allowlist amendment was intentional and narrow. It includes the durable governance surfaces that were actually changed while repairing OBPI-02: the plan-audit receipt, ledger, insight record, ADR evaluation scorecard, OBPI-06 recovery context, OBPI-09 recovery draft, and ADR status view. Transient pipeline marker paths were removed from the allowlist after the runtime purged them as attested-completed.

## Decisions Made

- **Decision:** Keep the recovery docs and amend OBPI-02's allowlist narrowly for the recovery governance surfaces.
  **Rationale:** The operator approved this route because it preserves the real repair history rather than hiding the coupled governance edits that made precomplete honest.
  **Alternatives rejected:** Reverting the recovery documents or widening the allowlist with transient marker files.

- **Decision:** Emit a new OBPI completed receipt instead of forcing `gz obpi complete`.
  **Rationale:** The brief was already Completed, but the repair still needed Layer-2 receipt accounting tied to the operator's attestation and the fresh recovery evidence.
  **Alternatives rejected:** Hand-editing the ledger, trying to recreate pipeline markers, or treating the prior bad completion as sufficient.

## Immediate Next Steps

1. Release the lock with `uv run gz obpi lock release OBPI-0.0.73-02-qc-binding-validate-scope`; this handoff is the register entry the command should pair with the release.
2. Reconcile OBPI-02 with `uv run gz obpi reconcile OBPI-0.0.73-02-qc-binding-validate-scope` and inspect any authored-vs-ledger drift.
3. Refresh parent status with `uv run gz adr status ADR-0.0.73-verification-layer-binding-audit --json`.
4. Run the guarded git sync required by Stage 5 after the release and reconciliation surfaces are settled.
5. Continue ADR-0.0.73 only through operator-authorized next work, likely the remaining recovery/governance follow-up around OBPI-09.

## Pending Work / Open Loops

- Parent ADR-0.0.73 still has broader recovery context beyond OBPI-02. OBPI-09 exists as a recovery-context draft and should not be silently treated as completed.
- The final repository sync is still pending at the time this handoff is authored.
- If `gz obpi reconcile` changes ADR status or brief cache surfaces after lock release, include those generated surfaces in the guarded sync rather than discarding them.

## Verification Checklist

- [ ] `uv run gz validate --qc-binding` exits 0 with no QC theater detected.
- [ ] `uv run -m unittest -q tests.governance.test_qc_binding_scope tests.governance.test_qc_binding_self_check` reports 29 tests OK.
- [ ] `uv run gz obpi precomplete OBPI-0.0.73-02-qc-binding-validate-scope` reports READY with all eight preconditions met.
- [ ] `uv run gz obpi lock list` no longer lists OBPI-0.0.73-02 after the release command runs.
- [ ] `uv run gz validate --documents` exits 0.

## Evidence / Artifacts

- `src/gzkit/governance/trust_audits/qc_binding.py` - production negative controls and empty debt set for the bound QC steps.
- `tests/governance/test_qc_binding_scope.py` - scoped CLI and clean-path coverage for the QC-binding audit.
- `tests/governance/test_qc_binding_self_check.py` - self-check assertions proving OBPI-02 has no acknowledged debt and the audit is clean.
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-02-qc-binding-validate-scope.md` - recovery evidence and narrow allowlist amendment.
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/EVALUATION_SCORECARD.md` - recovery scorecard replacing the invalid green scorecard.
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-06-self-check-facade-regression-corpus.md` - coupled recovery context reconciled during the repair.
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-09-waiver-ratchet-honesty-contract.md` - recovery-context draft kept in the allowlist by operator approval.
- `.gzkit/ledger.jsonl` - meta-receipt-bind and OBPI completed receipt emitted for the repair attestation.
- `.gzkit/insights/agent-insights.jsonl` - course-correction insight for the allowlist-friction gate.
- `artifacts/receipts/arb-ruff-c7d5da51b0dc46a1a16f9258a6ace677.json` - lint receipt cited in the attestation.
- `artifacts/receipts/arb-step-typecheck-7e836f4f47cd4510bcc36f05b4078248.json` - typecheck receipt cited in the attestation.
- `artifacts/receipts/arb-step-unittest-e7b0a9f5117c4106b7544d5038256326.json` - unittest receipt cited in the attestation.
- `artifacts/receipts/arb-step-mkdocs-f7ced42cd8f4499584636a1a6bf6e0b6.json` - mkdocs receipt cited in the attestation.

## Environment State

Repository branch was `main`; HEAD was `d13ee2d3` when the handoff was authored. The session used the governed `uv run gz` surfaces; sandboxed `uv` invocations may need escalation when they touch the user-level uv cache outside the workspace.
