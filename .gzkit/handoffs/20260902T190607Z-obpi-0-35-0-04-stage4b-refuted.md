---
mode: CHECKPOINT
adr_id: ADR-0.35.0
branch: main
timestamp: '2026-09-02T19:06:07Z'
agent: claude-code
obpi_id: OBPI-0.35.0-04-section-ownership-and-ratchet
session_id: 66d15068-b58b-4c7b-86b4-d8e072092658
continues_from: .gzkit/handoffs/20260902T152037Z-obpi-0-35-0-04-stage2-savepoint.md
---

## Current State Summary

Mid-Stage-4 CHECKPOINT for OBPI-0.35.0-04-section-ownership-and-ratchet (parent
ADR-0.35.0-canon-entry-corpus-landing, Heavy lane), running under gz-obpi-pipeline.
Lock HELD (agent=claude-code-9de7e7a3, elapsed 377m of 1440m ttl). Pipeline marker
active at current_stage=implement. NOT departing — bookmarking mid-flight, so this
CHECKPOINT does not discharge the lock release.

Stage 2 CLOSED. Task 4 landed (day-one declaration artifact, manpage `### unown`
section, five Gate-4 behave scenarios, brief Demo repair), reviewed two-stage
(spec CONCERNS -> one major fixed -> re-review confirmed all four fixes; quality
PASS with two minor, one fixed and one tracked), one fix cycle of a permitted two.
All three mandated Stage-2 roles are receipted: `gz obpi dispatch` reports
"DISPATCHED — 3 of 3 mandated roles produced receipted independent input".

Stage 3 CLOSED GREEN. arb ruff clean (arb-ruff-fa0254a3498343a19f9c5897070ef556);
arb typecheck clean (arb-step-typecheck-5922df12c0324073badce450451958d0);
arb unittest 9198 tests OK (arb-step-unittest-7577c7a1b02944e8a6826ce849964b78);
arb mkdocs --strict clean (arb-step-mkdocs-e74e3d08b1b54ebab5831855456cd330);
arb behave scoped to @REQ-0.35.0-04-04,@REQ-0.35.0-04-05 -> 5 scenarios, 29 steps,
0 failed (arb-step-behave-3546e7a32f794bcaa2c105ba967734ea). `gz validate
--documents` exit 0; `gz validate --req-kind-discipline` exit 0; `gz cli audit`
141/141; `gz covers` 7/8 with behavior_uncovered_reqs 0.

Stage 4a DONE and ATTESTABLE — generated by `gz obpi present-evidence`, NOT by any
agent. Packet at `.gzkit/evidence/OBPI-0.35.0-04-section-ownership-and-ratchet.evidence.json`.
Demo 3/3 exit 0, three required ARB receipts exit 0, 0 uncovered of 8 REQs.

Stage 4b RETURNED **REFUTED**. Tier-1 Codex adversary (job review-mtkgb5bu-sil0hv),
dispatched through the plugin (`codex-companion.mjs adversarial-review --wait --scope
working-tree`) and ARB-wrapped: receipt arb-step-codexadversary-f7a101da3ba3498e94249f2bdb39969f,
exit_status=0. Four [high] findings and one [medium], each backed by pasted probe
output. Per gz-obpi-pipeline, a REFUTED verdict returns the work to Stage 2.

THIS OBPI IS NOT COMPLETABLE AS IT STANDS. Stage 5 was never entered. No attestation
was solicited and none must be until the refutation is resolved and the adversary's
own checks are re-run.

## Important Context

FOUR DEFECTS OF ONE FAMILY WERE FOUND THIS SESSION, and the family is the session's
real subject: A SIGNAL THAT DOES NOT MEAN WHAT ITS SURFACE SAYS IT MEANS. Three were
filed (#940, #941, #942); the fourth is the operator's own open #919. All four are
cases where a control that could not run, or did not run, reported something other
than "did not run". Two produce CONFIDENT GREEN rather than an error, which is the
worse half.

GHI #940 — verifier-pipe-gate misses the backgrounded-SEQUENCE case. The gate
(`src/gzkit/verifier_pipe_gate.py:96`) treats a statement separator as ending the
masking risk, on the reasoning that `gz check; ls | head` masks nothing. TRUE in
the foreground; FALSE when backgrounded, where the only signal surfaced is the
final statement's exit status. Measured live: `arb step --name unittest -- <suite> > log`
/ `echo` / `tail -6 log` run via run_in_background reported "exit code 0" while
receipt arb-step-unittest-7b33ce61 recorded exit_status=1 over 9198 tests with one
failure. THE OBVIOUS FIX DOES NOT WORK — requiring `$?` be read immediately after
would have PERMITTED that exact command, which had `echo "exit=$?"` on the next
line. Capturing status into stdout is no defense when nobody reads stdout. The
discriminator is BACKGROUNDING, and the payload carries it: the Claude Code hooks
reference gives a named per-tool example showing Bash `tool_input` as
{command, description, timeout, run_in_background}. Operator ruled: build the
background-aware predicate. Gate must test TRUTHINESS not key presence — the docs
do not say whether the key is always emitted or only when set.

GHI #941 — reviewer personas are `tools: Read, Glob, Grep` with no Bash, so a
review dispatch asking them to verify by RUNNING yields findings about the
reviewer's own coverage, which then land in a verdict that gates advancement.
Measured: an all-info, no-code-findings review returned CONCERNS; re-derived by the
orchestrator, both checks passed. Operator ruled: READ-ONLY IS THE INTENT; separate
the finding channels. Do NOT grant reviewers Bash.

GHI #942 — Stage-4a pasted command output is unverified and Step 4b does not check
it. The `narrator` dispatch fabricated `$ uv run gz covers --json` output with an
invented flat shape and two invented key names (`obpi_id` for `identifier`,
`coverage_pct` for `coverage_percent`), and cited a REQ-08 proof command
(`gz state --json` piped to `jq`) that returns NOTHING when run. The FIGURES were correct
because they were supplied in the dispatch prompt; the EVIDENCE was constructed
around them. GHI #643 named this exact surface and is CLOSED — its remedy was Step
4b, but 4b re-derives the CLAIM from the repository and is never handed the PACKET,
so a fabricated packet passes an adversary that never looks at it. Cross-linked as
#643's residual.

THE CORRECTION THAT MATTERS MOST, and the reason the narrator path must not be
retried: after catching the narrator fabricating, the orchestrator proposed to
"rebuild the 4a packet from verified output" — i.e. to author the evidence itself.
The operator flagged that immediately. Swapping which agent types the evidence
fixes nothing; the orchestrator is the same fabrication surface, a point #942's own
body makes in as many words. THE ANSWER IS NO AUTHOR: `uv run gz obpi
present-evidence <OBPI>` generates the packet "from observables the agent cannot
author" — it runs the brief's ## Demo live, reads on-disk ARB receipts, and runs
gz covers. It existed the whole time. Dispatching a narrator to compose evidence
was the wrong path from the start. NEVER hand-compose a Stage-4a packet.

THE DEMO CONTRACT IS ASSERT-SHAPED, and the brief violated it. `gz obpi
present-evidence` first exited 3 NOT-ATTESTABLE because the brief's Demo carried a
deliberate refusal probe (`gz content unown AGENTS.md --section attestation --attestor ""`) that exits 1 when
the system is HEALTHY. `src/gzkit/governance/stage4_evidence.py:19` requires the
Demo be "assert-shaped — exit non-zero on a bad state", so that probe inverted the
contract: it was a demonstration wearing an assertion's clothes. Repaired at the
BRIEF, not the tool, and made to assert BOTH of REQ-04's claims (non-zero exit AND
declaration byte-unchanged), so it now exits non-zero only if the guard ever stops
refusing.

THE FENCE CONFLICT WAS PRE-EXISTING FROM TASK 3, NOT TASK 4.
`tests/content/test_tui_affordances.py` pins a hardcoded `gz content` subcommand
roster under REQ-0.0.34-05-05. Task 3 registered `unown` and tripped it; the full
suite was not re-run at Task 3 (only the scoped modules), so it went undetected
until Stage 3. The test's own docstring rules the disposition — "the fence is
updated to admit it, not relaxed" — and six sibling verbs were admitted the same
way, each with provenance named in the comment.

RED WITNESS IS WEAK ON ALL SEVEN BEHAVIOR REQs, and that is expected, not a finding.
All returned failure_class=error, never the blocking `none`. An `error` red proves
only that a symbol was absent — NEVER report it as an assertion RED. The production
code landed in Tasks 1-3, so the experiment had no premise. Do NOT weaken any
passing assertion to make the witness quieter.

SUBAGENT TURN CEILINGS remain the dominant cost: 5 of 11 dispatches hit the 25-turn
(implementer) or 15-turn (reviewer) limit, and one spec-review agent died on a
600s stream-watchdog stall with no verdict. Front-loading VERIFIED state — exact
symbols, measured figures, what is already green, what NOT to re-check — is what
got every resumed dispatch through in one pass.

COVERAGE HONESTY IS BINDING (brief Requirement 8). 81.6% is span-based and INFLATES:
four of the ten owned sections carry exactly ONE corpus entry, and
governance-doctrine-surfaces' single entry is compressible tier, so it is not on the
invariant floor at all. Read it as six real sections plus four tokens. Never round,
average, or present it as stronger.

THE STEP-4b REFUTATION IS THE SESSION'S MOST IMPORTANT FINDING, and it vindicates
the gate. Two Claude reviews (spec + quality) and the orchestrator all passed work
that a different-vendor adversary refuted on five counts with pasted probe output.
Read the findings before touching the code; each names a file and line.

1. [high] `record_unowned_total` EMITS A LEDGER EVENT BUT NEVER PERSISTS THE FLOOR
   (`src/gzkit/content/ownership.py:215-253`). It returns an in-memory `model_copy`
   and emits `unowned_ratchet_updated`; nothing writes the declaration. Probe:
   {"ledger_new_floor":40,"persisted_after_record":100,"returned_floor":40}. REQ-03's
   covering test asserts the RETURNED MODEL and the LEDGER, so it passes while
   durable state contradicts Layer-2. This is a hollow test of exactly the kind the
   two-stage review exists to catch and did not.
2. [high] `gz content unown` IS NON-ATOMIC AND LOSES CONCURRENT TRANSITIONS
   (`src/gzkit/commands/content/unown.py:116-203`). Unlocked whole-file
   read-modify-write. A forced concurrent run had both operations exit 0 and emit
   success events while final state retained only one. Ledger failure after the
   declaration write also leaves an unwitnessed raise that retry then rejects as
   already-unowned.
3. [high] REQ-08's CLAIMED VALIDATOR NEVER LOOKS AT THE ARTIFACT
   (`src/gzkit/commands/validate_cmd.py:923-949`). `gz validate --documents`
   validates manifest-declared Markdown and the exemplar corpus, never
   `.gzkit/ownership/*.json`. A probe containing `{ definitely-not-json` returned
   documents_error_count 0. So REQ-08's SUPPORT proof channel reports `pass` without
   validating the artifact against `section_ownership.json` — the validator arm is
   hollow, which is the same family as GHI #942.
4. [high] DIRECT DECLARATION EDITS BYPASS THE ATTESTED RAISE PATH
   (`src/gzkit/content/ownership.py:145-208`). `load_declaration` checks the enum and
   section-key coverage but never that the stored floor matches measured unowned
   spans or follows an attested transition. An unattested direct raise 100 -> 101
   reloaded cleanly. REQ-02's "an increase is only reachable through the attested
   raise-path" is therefore false at the durable layer; `gz content unown` is only
   the supplied CLI writer, not the only accepted raise path.
5. [medium] REQ-07's COVERING TEST CANNOT DETECT A STORED SNAPSHOT
   (`tests/content/test_ownership.py:444-536`). It derives its expectation from the
   same primitives as production, so replacing day-one computation with a stored
   snapshot still passed the whole class. The adversary names this its weakest point
   — an evidence-quality defect, not proof the arithmetic is wrong.

ALLOWLIST NOTE ON THE REFUTATION, needs an operator ruling before repair starts.
Findings 1, 2, 4 and 5 touch `src/gzkit/content/ownership.py`,
`src/gzkit/commands/content/unown.py` and `tests/content/test_ownership.py` — all
already inside this brief's Allowed Paths, so they are repairable in place. Finding 3
requires a NEW validator scope and touches `src/gzkit/commands/validate_cmd.py`,
which is NOT in the allowlist; it may also belong to OBPI-0.35.0-06 (the
`--rendition-lineage` gate). Do not widen the allowlist for it without an operator
ruling, and surface the OBPI-0.35.0-06 overlap when asking.

THE ADVERSARY ALSO CONFIRMED REAL POSITIVES, which should not be re-litigated: the
BDD scenarios genuinely prove all three REQ-04 effects; collision handling works;
undeclared sections fail closed; and the current 8,637-byte floor remeasures
correctly.

## Decisions Made

- [operator-ruled] Resume the handoff and work Task 4 through all five stages (verbatim: "continue with task 4 (#1) do all 5 stages in task 4."). Booked via `gz handoff decide --decision proceed`.
- [operator-ruled] Add `tests/content/test_tui_affordances.py` to this brief's Allowed Paths so the `gz content` subcommand fence can admit `unown` (verbatim: "3. add to allowed"). Booked via `gz obpi unblock`. The fence was widened by exactly one id with provenance named in its comment — never relaxed.
- [operator-ruled] GHI #941: reviewers stay READ-ONLY; the remedy is to separate the finding channels so a reviewer's own verification gap cannot contaminate a verdict (verbatim: "2. read-only is the intent, separate the fundung channels" — spelling preserved). Granting reviewers Bash is REJECTED.
- [operator-ruled] GHI #940: build the background-aware predicate rather than the declare-only fallback (verbatim: "1. recommend?" answered with that recommendation and accepted). Rejected alternative: requiring `$?` be read after the verifier — it would have permitted the exact command that failed.
- [operator-ruled] The orchestrator must NOT rebuild the Stage-4a evidence packet itself (verbatim: 'this is bad: "I''ll rebuild the 4a packet from verified output only, and present it with the Codex verdict."'). Swapping which agent authors the evidence keeps the fabrication surface; the answer is that NO agent authors it — `gz obpi present-evidence` generates it from observables an agent cannot author.
- [agent-chose] Repair the brief's `## Demo` rather than the Stage-4 evidence tool when `present-evidence` exited 3. The tool's assert-shaped contract (`stage4_evidence.py:19`) is correct; the brief's refusal probe, which exits 1 on a HEALTHY system, was the defect. The replacement asserts BOTH of REQ-04's claims.
- [agent-chose] Do NOT relocate the `no ledger event <event> was emitted` behave step into `features/steps/gz_steps.py`, against the quality reviewer's suggestion. Behave raises on ambiguous duplicates, so relocation means deleting it from `content_retire_steps.py` — Gate-4 evidence for the attested-completed OBPI-0.35.0-02. Recorded as tracked debt in the brief instead.
- [agent-chose] Raise the quality reviewer's `minor` REQ-05 ledger-field finding to a required fix. REQ-0.35.0-04-05 states the event carries five fields and the scenario asserted three; that is a coverage hole against the literal REQ text, not a style nit.
- [agent-chose] File three separate GHIs (#940, #941, #942) rather than one bundled issue. One GHI, one class of failure — bundling couples routing decisions the matrix cannot resolve.
- [agent-chose] Cross-link #942 onto the CLOSED GHI #643 as its residual rather than reopening. #643's remedy (Step 4b) did ship; what it never covered is the packet itself.
- [agent-chose] Finish the OBPI before landing the #940/#941/#942 fixes. #941's remedy edits `ReviewResult` in `src/gzkit/pipeline_dispatch.py` — the review machinery this pipeline run is using. Editing the tool mid-run is how a clean run becomes unexplainable.

## Immediate Next Steps

1. PRESENT THE REFUTATION TO THE OPERATOR AND WAIT. Stage 4b returned REFUTED with four [high] findings; `gz-obpi-pipeline` routes a REFUTED verdict back to Stage 2. Do not solicit attestation, do not run `gz obpi precomplete`, and do not run `gz obpi complete` — a REFUTED verdict additionally requires `--adversary-resolution` naming what was fixed and how the adversary's own check was re-run. Present findings 1-5 from Important Context with their file:line anchors and let the operator rule on scope.
2. OBTAIN AN OPERATOR RULING ON FINDING 3's ALLOWLIST. Findings 1, 2, 4, 5 are repairable inside the current Allowed Paths. Finding 3 needs a new validator scope in `src/gzkit/commands/validate_cmd.py`, which is NOT allowlisted and may belong to OBPI-0.35.0-06. Surface the overlap; never widen the allowlist unilaterally.
3. REPAIR FINDING 1 FIRST — it is the one that makes a passing test lie. `record_unowned_total` must persist the declaration, not only return a copy and emit the event. Re-run the adversary's own probe and assert DECLARATION BYTES in both the success and induced-failure tests, never only the returned model.
4. REPAIR FINDINGS 2 AND 4. Make `unown` atomic (locking or versioned compare-and-swap plus atomic file replacement) with a recoverable transaction tying declaration version to ledger event; and make `load_declaration` fail closed when the stored floor is inconsistent with measured unowned spans, so a direct edit cannot launder an unattested raise.
5. RE-RUN STAGE 3 AND RE-DISPATCH STEP 4b AFTER THE REPAIRS. Codex tier 1 was confirmed READY this session (`ready: true`, runtime mode `direct`, prop clear), so tiers 2 and 3 remain FORBIDDEN. Dispatch through `/codex:adversarial-review` or `codex-companion.mjs` — never `codex exec` — and ARB-wrap it so the new receipt can be cited. Only after a clean or caveat-resolved verdict may Stage 4a be regenerated with `gz obpi present-evidence` and attestation solicited.

## Pending Work / Open Loops

- STAGE 4b REFUTED — the OBPI cannot complete until findings 1-5 are resolved and the adversary's checks re-run. Receipt arb-step-codexadversary-f7a101da3ba3498e94249f2bdb39969f. Stage 5 never entered.
- GHI #940 OPEN (verifier-pipe-gate backgrounded-sequence masking). Operator ruled: build the background-aware predicate, testing truthiness of `run_in_background`, not key presence. Direct-fix routed; not yet implemented. `UNWITNESSABLE` should gain the foreground-sequence limit either way.
- GHI #941 OPEN (reviewers cannot execute; unrunnable asks degrade the verdict). Operator ruled: read-only is the intent, separate the finding channels. Touches `ReviewResult` in `src/gzkit/pipeline_dispatch.py`; deliberately deferred until this pipeline run ends.
- GHI #942 OPEN (Stage-4a pasted output unverified; Step 4b does not check the packet). Remedy not yet chosen; remedy 1 — never let a composing agent render command output — is the one that removes the surface rather than policing it. Cross-linked onto closed #643 [settled] as its residual.
- GHI #919 (operator's, OPEN) is the fourth member of the same family and was cross-linked from #941.
- FINDING 3's HOME IS UNRESOLVED — a ratchet-coherence/ownership-artifact validator may belong to OBPI-0.35.0-06 rather than this brief. Needs an operator ruling before any allowlist change.
- TRACKED DEBT already recorded in the brief's § Tracked Defects: content-layer ledger write deferred to OBPI-0.35.0-05; `emit_unowned_ratchet_updated` constructs `LedgerEvent` inline; the `no ledger event <event> was emitted` behave step lives in `content_retire_steps.py` rather than `gz_steps.py`.
- DEFERRED OPERATOR QUESTION, logged as an `improvement` insight, not acted on: whether Stage-2 dispatch grain should be finer (per-RGR-phase or per-behavior subagents) given context rot. Five of eleven dispatches hit turn ceilings this session and one reviewer died on a 600s stream-watchdog stall.
- PARENT-ADR COUPLING STILL UNRESOLVED from the prior session: ADR-0.35.0 § Decision item 4 and § Consequences Positive #4 still carry the retired entry-witness figures (31.2%, 354 B -> 22,378 B). Scoped to OBPI-0.35.0-05/-06 and outside this brief's allowlist.

## Verification Checklist

Run these before acting on anything above; every claim here is Layer-1 narrative and
is unverified until checked.

    uv run gz obpi status OBPI-0.35.0-04-section-ownership-and-ratchet
    uv run gz obpi lock list
    uv run gz obpi brief-drift OBPI-0.35.0-04
    uv run gz covers OBPI-0.35.0-04-section-ownership-and-ratchet --json
    uv run gz obpi present-evidence OBPI-0.35.0-04-section-ownership-and-ratchet
    uv run -m unittest tests.content.test_ownership
    uv run -m unittest tests.commands.test_content_unown
    uv run -m unittest tests.content.test_tui_affordances
    uv run ruff check .
    uv run gz cli audit

Expected at checkpoint: lock HELD by session 9de7e7a3; brief-drift clean on all five
dimensions; covers 7/8 with behavior_uncovered_reqs 0; present-evidence ATTESTABLE;
ownership 24 pass; unown 9 pass; tui-affordances 10 pass; ruff clean; cli audit
141/141.

BEWARE: `present-evidence` reporting ATTESTABLE does NOT mean the OBPI may complete.
Stage 4b REFUTED it, and that verdict lives in the adversary receipt, not in the
evidence packet. The packet asserts the Demo runs and the receipts are green; the
adversary asserts the durable state is wrong. Both are true at once.

NEVER pipe a verifier through tail/head/grep, and NEVER background a verifier that is
not the final statement of its command — the shell reports the last statement's exit
status, which is the defect GHI #940 names. Redirect to a file, then read the ARB
receipt's `exit_status`.

If a unittest failure contradicts source you have just read, clear stale bytecode
before diagnosing: `find src tests -name __pycache__ -type d -exec rm -rf {} +`.

## Evidence / Artifacts

Brief, plan and evidence packet:
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-04-section-ownership-and-ratchet.md`
- `.claude/plans/section-ownership-and-ratchet-OBPI-0.35.0-04.md`
- `.claude/plans/.plan-audit-receipt-OBPI-0.35.0-04-section-ownership-and-ratchet.json`
- `.claude/plans/.pipeline-active-OBPI-0.35.0-04-section-ownership-and-ratchet.json`
- `.gzkit/evidence/OBPI-0.35.0-04-section-ownership-and-ratchet.evidence.json`

Production and artifacts authored across this OBPI:
- `src/gzkit/content/ownership.py`
- `src/gzkit/commands/content/unown.py`
- `src/gzkit/commands/content/__init__.py`
- `src/gzkit/schemas/section_ownership.json`
- `src/gzkit/governance/events.py`
- `.gzkit/ownership/AGENTS.md.json`
- `config/doc-coverage.json`
- `docs/user/manpages/content.md`

Tests and scenarios:
- `tests/content/test_ownership.py`
- `tests/commands/test_content_unown.py`
- `tests/content/test_tui_affordances.py`
- `features/content_unown.feature`
- `features/steps/content_unown_steps.py`

Precedents the remaining work must mirror:
- `features/content_retire.feature`
- `features/steps/content_retire_steps.py`
- `src/gzkit/commands/content/retire.py`
- `src/gzkit/governance/stage4_evidence.py`

Surfaces the Step-4b refutation names:
- `src/gzkit/commands/validate_cmd.py`
- `src/gzkit/verifier_pipe_gate.py`
- `src/gzkit/pipeline_dispatch.py`

Governance surfaces:
- `.gzkit/ledger.jsonl`
- `.gzkit/insights/agent-insights.jsonl`
- `.gzkit/handoffs/rulings.jsonl`

## Settled Rulings

665 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
