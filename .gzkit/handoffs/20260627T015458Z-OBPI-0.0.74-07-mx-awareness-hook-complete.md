---
mode: CREATE
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-27T01:54:58Z"
agent: claude-code
obpi_id: OBPI-0.0.74-07-mx-awareness-hook
last_lock_event_timestamp: "2026-06-27T01:34:31.401892+00:00"
last_commit_sha: d541e9b1345b50ce5c7759d92c88944ab90bac68
session_id: main-2026-06-27
continues_from:
---

<!-- Completion handoff for OBPI-0.0.74-07 — created by claude-code at 2026-06-27T01:54:58Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

This is a completion register entry paired with the lock release for a
*completed* OBPI (not an abandonment). The pending follow-up below ADVISES the
next session; it is not authorization to execute.

## Current State Summary

OBPI-0.0.74-07-mx-awareness-hook is **completed and attested** (Gate 5 operator
attestation `g0`, lane heavy). The MX awareness hook substrate landed:

- `src/gzkit/mx/awareness.py` — stdlib+json-only shared logic: `MX_BANNER`,
  `get_banner()`, `LivenessResult`, `check_hook_liveness()`
- `.claude/hooks/mx-awareness.py` — UserPromptSubmit hook adapter, fail-open,
  stdlib fallback for the MX-patient case
- `scripts/session_orientation.py` — secondary SessionStart banner (backup only)
- `tests/hooks/test_mx_awareness.py` — 8 RGR-derived unit tests, all green

All quality gates passed: ruff, typecheck, full unittest suite (516 tests),
mkdocs --strict, `gz validate --documents`. ARB receipts emitted for each.

## Important Context

- `awareness.py` carries NO gzkit-internal imports by design — it is the
  fallback nerve that must import cleanly when gzkit itself is the patient (the
  MX premise). It duplicates `_find_project_root` from `gzkit.mx.marker`
  deliberately rather than importing it.
- The hook is a `UserPromptSubmit` type (fires every turn), NOT `PreToolUse`.
  `merge_settings()` in `src/gzkit/hooks/claude.py` preserves `UserPromptSubmit`
  as a user-only phase across `gz agent sync control-surfaces` runs.
- The tool-output banner (session_orientation.py) is explicitly secondary; the
  per-turn hook is the load-bearing guarantee per ADR-0.0.74 Decision item #7.

## Decisions Made

- **Decision:** Defer `.claude/settings.json` UserPromptSubmit registration to a
  follow-up.
  **Rationale:** `.claude/settings.json` and `src/gzkit/hooks/claude.py` were not
  in the brief's allowed paths. The gap was surfaced to the operator in the
  Stage 4 ceremony; the operator attested completed, accepting the deferral. The
  liveness check (`check_hook_liveness()`) correctly reports the unwired state as
  a detected defect — which is the intended REQ-07-02 behavior, not a failure.
  **Alternatives rejected:** Amending the brief allowlist mid-pipeline to wire
  settings.json now — declined in favor of the operator's attestation decision.

- **Decision:** stdlib `LivenessResult` class instead of pydantic.
  **Rationale:** A simple result carrier does not justify a pydantic model in the
  one module that must stay import-light for the MX-patient case.

## Immediate Next Steps

1. **Wire the hook in settings.json (follow-up OBPI or direct-fix):** Add
   `src/gzkit/hooks/claude.py` to scope, modify `generate_claude_settings()` to
   emit the `UserPromptSubmit` mx-awareness hook, run
   `uv run gz agent sync control-surfaces`. After wiring,
   `check_hook_liveness()` returns `ok=True`.
2. **Continue ADR-0.0.74 MX-kernel OBPIs:** OBPI-0.0.74-08 (the `gz-mx` skill +
   `mx-mode` rule companion) is the next checklist item.
3. Present these to the operator for authorization before executing.

## Pending Work / Open Loops

- settings.json UserPromptSubmit registration (deferred — see Decisions Made).
- Remaining ADR-0.0.74 MX-kernel checklist items (per build-to-1.0 campaign
  Movement I): the gz-mx skill/rule, staging-flag retirement, and the
  enforcement-claim meta-validator items.

## Verification Checklist

- [ ] `uv run -m unittest tests.hooks.test_mx_awareness -v` — 8 tests pass
- [ ] Branch matches: `git branch --show-current` → main
- [ ] `uv run gz obpi reconcile OBPI-0.0.74-07-mx-awareness-hook` — receipt and brief agree

## Evidence / Artifacts

- `src/gzkit/mx/awareness.py` — shared banner/liveness logic
- `.claude/hooks/mx-awareness.py` — UserPromptSubmit hook adapter
- `tests/hooks/test_mx_awareness.py` — 8 unit tests
- `scripts/session_orientation.py` — secondary banner fold-in
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/obpis/OBPI-0.0.74-07-mx-awareness-hook.md` — completed brief with evidence
