---
mode: CREATE
adr_id: ADR-0.0.72
branch: main
timestamp: "2026-06-14T22:34:40Z"
agent: claude-code
session_id:
continues_from: .gzkit/handoffs/20260614T222414Z-OBPI-0.0.72-02-complete.md
---

<!-- Campaign session handoff for the ADR-0.0.65 + ADR-0.0.72 handoff-system /
     meta-governance-coherence campaign — created by claude-code at 2026-06-14T22:34:40Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is NOT clearance to execute. On resume — at every freshness level,
Fresh included — present the advised steps, obtain explicit operator authorization
before executing any of them, and treat human-as-final-witness as binding. The
plan is the destination; operator authorization is the ignition.

## Current State Summary

Session-end handoff for the operator-directed **"build BOTH ADRs, FULL SEND"**
campaign across **ADR-0.0.65-handoff-system-consolidation** and
**ADR-0.0.72-meta-governance-coherence**. Working tree is **clean**, `origin/main`
in sync (`ahead=0 behind=0`), HEAD `81ae707b`. No active OBPI lock, no active pipeline.

**Completed this session (both full pipelines, Gate-5 attested by g0):**
- **OBPI-0.0.37-23-invariant-tier** — invariant-tier 0-Kelvin floor (`tier_policy`); composer re-pointed onto it (operator path-b).
- **OBPI-0.0.72-02-handoff-frontmatter-reconcile** — `HandoffFrontmatter` reconciled (explicit-superset model), `validate_handoff_document` made shape-aware (register entries a distinct doc class), `_write_reaping_handoff` adr_id fixed for full-slug locks, gate-wired into `gz check`. A spec-review caught a masked REQ-05 (test validated synthetic docs); fixed via the operator-approved FULL coherence fix. The 5 pre-existing `--lock-handoff-coupling` violations were repaired RED→GREEN.

## Important Context

- **`--lock-handoff-coupling` is now a LIVE `gz check` gate** (OBPI-0.0.72-02 REQ-06).
  Every release-paired session handoff MUST carry `last_lock_event_timestamp`,
  `last_commit_sha`, `branch`, and a `## Decisions Made` section. The systemic
  recurrence-prevention is **OBPI-0.0.65-02's `scaffold_handoff`** (pre-fills these
  deterministically) — Phase C below.
- **Handoff shape-awareness:** frontmatter `abandoned: true` ⇒ register entry
  (relaxed contract); CREATE/RESUME ⇒ full 7-section + referenced-file contract.
  Handoff-document audit cutover is `2026-06-15T00:00:00Z` (pre-cutover entries
  grandfathered; enforced forward).
- **Campaign rules sequencing** (operator ruling 2026-06-10) — but this campaign is
  an explicit operator override pulling both ADRs; the sequence below is dependency-locked.
- **The 8-OBPI campaign was decided after discovering** that OBPI-0.0.72-02 owns the
  handoff-frontmatter fix that OBPI-0.0.65-02's API depends on (cross-ADR dependency).

## Decisions Made

- **Decision:** Build BOTH ADR-0.0.65 + ADR-0.0.72 (operator: "FULL FIX FULL SEND FULL WIN"),
  dependency-ordered (Phases A→B→C below).
  **Rationale:** 72-02 (frontmatter coherence) is a prerequisite for 65-02 (API).
  **Alternatives rejected:** 65-02+03 only; keeping REQ-07 in 65-02 (fragments the fix).
- **Decision:** Resume the remaining 7 OBPIs in a FRESH session (operator-directed).
  **Rationale:** context budget after two full pipelines + a major coherence fix.
- **Decision (open-item #1):** TRACK the two pre-existing `gz check` blockers rather than
  fix them in-campaign (operator: "both 1 and 2 — handoff"). See Pending Work.

## Immediate Next Steps

ADVISORY ONLY — present to the operator before acting. Resume the campaign in this order:

1. **Phase A — OBPI-0.0.72-03** (insight-record-reconcile): single mechanical
   `InsightRecord` append helper; reconcile AGENTS.md Rule-11 prose to the model's
   required fields incl. `evidence: list[str]` (closes the C4 contradiction — the exact
   bug hit twice this session when hand-appending insights). Start: `/gz-plan-audit OBPI-0.0.72-03`.
2. **Phase A — OBPI-0.0.72-04** (security-floor-overridden-event): first-class
   `security_floor_overridden` ledger event on `gz obpi complete --accept-security-floor`.
3. **Phase B — OBPI-0.0.72-01** (writer-model-roundtrip-validator): `gz validate
   --writer-model-roundtrip`; depends on the adapters (02/03/04) existing.
4. **Phase C — OBPI-0.0.65-02** (programmatic-api): FIRST remove the REQ-07 I authored
   into its brief (the obpi_id regex now owned by 72-02), then build `create_handoff` /
   `scaffold_handoff` (grounded pre-population) / `list_handoffs` / `resume_handoff` /
   `load_handoff_chain`. `scaffold_handoff` fixes the systemic min-info-field gap.
5. **Phase C — OBPI-0.0.65-03** (`gz handoff` verb) → **65-04** (orientation single-scan) →
   **65-05** (`gz handoff archive`).

> Each is Heavy/foundation → mandatory Gate-5 human attestation. Pre-authored briefs
> (72-01/03/04, 65-03/04/05) likely carry scaffold-quality drift (glob paths, ALWAYS/NEVER
> Requirements instead of `REQUIREMENT [kind]:` lines, missing `req_atomic`) — expect a
> brief-remediation pass at each Stage 1 reconcile, same as 72-02 needed.

## Pending Work / Open Loops

- **OPEN-ITEM #1 (operator: track): two pre-existing, campaign-orthogonal `gz check` blockers.**
  `gz check` is RED until these are addressed:
  (a) 3 behave scenario failures in `features/constitutional_invariants.feature` (ADR-0.0.37
  composition fixtures — the REAL `gz validate --invariant-coherence` PASSES; these are
  fixture/step-level). Run `PYTHONIOENCODING=utf-8 uv run -m behave features/constitutional_invariants.feature` to reproduce.
  (b) behave Windows cp1252 `✓` (`✓`) `UnicodeEncodeError` in the pretty formatter under
  stdout redirection — environmental tooling; fix = force UTF-8 stdout for the behave subprocess
  in the `gz test --bdd` runner. NOTE: do NOT diagnose `gz check` with a piped `tee`/`tail` —
  it masks the real exit (GHI #589); redirect to a file and read `$?` or the ARB receipt.
- **Sibling bug (tracked):** `list_locks` adr_filter at `src/gzkit/lock_manager.py:189` has the
  same `rsplit("-",1)` full-slug bug fixed in `_write_reaping_handoff`. Direct-fix route; test
  home `tests/test_lock_manager.py`.
- **Reconcile heuristic gap (tracked):** `_compute_missing_in_brief` (`src/gzkit/governance/brief_reconcile.py:287`)
  flags `gzkit.traceability` (the `@covers` source) as a false positive for top-level-`src/gzkit/`
  OBPIs; add an explicit `_CROSS_CUTTING_TEST_INFRA` exclusion. Recurs across the campaign.
- **REQ-07 removal in OBPI-0.0.65-02 brief** (pending) — authored there, now owned by 72-02.
- ADR-0.0.72 closeout awaits OBPIs 01/03/04; ADR-0.0.65 awaits 02/03/04/05.

## Verification Checklist

- [ ] `git status --short` empty; `git branch --show-current` → main; `ahead=0 behind=0`
- [ ] `uv run gz obpi lock list` → no active locks
- [ ] `uv run gz validate --lock-handoff-coupling` → exit 0 (GREEN)
- [ ] `uv run gz obpi status OBPI-0.0.72-02-handoff-frontmatter-reconcile` → ATTESTED COMPLETED
- [ ] `uv run gz obpi status OBPI-0.0.37-23-invariant-tier` → ATTESTED COMPLETED
- [ ] Before each new OBPI: `/gz-plan-audit <OBPI-ID>` then `/gz-obpi-pipeline <OBPI-ID>`

## Evidence / Artifacts

- `.gzkit/handoffs/20260614T222414Z-OBPI-0.0.72-02-complete.md` — the OBPI-72-02 completion handoff this continues from
- `docs/design/adr/foundation/ADR-0.0.72-meta-governance-coherence/ADR-0.0.72-meta-governance-coherence.md` — ADR-0.0.72 (OBPIs 01/03/04 remain)
- `docs/design/adr/foundation/ADR-0.0.65-handoff-system-consolidation/ADR-0.0.65-handoff-system-consolidation.md` — ADR-0.0.65 (OBPIs 02/03/04/05 remain)
- `docs/design/adr/foundation/ADR-0.0.65-handoff-system-consolidation/obpis/OBPI-0.0.65-02-programmatic-api-implementation.md` — sharpened brief (REQ-07 to be removed)
- `src/gzkit/handoff_validation.py` — shape-aware validator + reconciled model (72-02)
- `.gzkit/insights/agent-insights.jsonl` — course-correction insights from this session

## Environment State

Python 3.13, uv-managed. Platform win32, branch `main`, synced with `origin/main`.
Full unittest sweep at session end: 6157 pass / 1 skipped
(receipt `arb-step-unittest-e7bc1c9b05d74eb8abe0f2d101cb8acc`).
`gz check` is RED only on the two pre-existing campaign-orthogonal blockers above.
