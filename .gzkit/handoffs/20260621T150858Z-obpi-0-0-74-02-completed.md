---
mode: CREATE
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-21T15:08:58Z"
agent: claude-code
obpi_id: OBPI-0.0.74-02-mx-shared-checkpoint
session_id:
continues_from: .gzkit/handoffs/20260621T143820Z-vibes-observability-design.md
---

<!-- Completion handoff for OBPI-0.0.74-02 — created by claude-code at 2026-06-21T15:08:58Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

**OBPI-0.0.74-02-mx-shared-checkpoint is COMPLETED and attested.** `gz obpi
complete` succeeded (exit 0): attestor `g0`, operator-verbatim conversational
attestation ("attest completed"), completion `attested_completed`, parent ADR
ADR-0.0.74, lane heavy. This is the register entry that authorizes the OBPI-02
lock release (ADR-0.0.41 coupling); the lock is released immediately after this
handoff is written.

What landed:
- `src/gzkit/mx/checkpoint.py` — the shared MX checkpoint: `GATE5_INVARIANTS`
  frozenset (`ledger`, `gate5-attestation`, `operator-pii`, `secrets`) +
  `is_advisory(guard_name, project_root) -> bool` (advisory in-hangar for
  non-gate5 guards; strict no-op out-of-hangar; gate5_invariants never relaxed).
- `tests/mx/test_checkpoint.py` — 7 unit tests (TestInHangar, TestOutsideHangar,
  TestValidateCmdWiring), all green.
- `src/gzkit/commands/validate_cmd.py` — `_run_scope_checks` lazy-imports the
  checkpoint and consults `is_advisory(scope)` before extending the error list
  (the first wired fail-closed funnel).
- Brief allowlist amended (g0-attested reconcile) to declare the two genuine
  mx-package import dependencies: `src/gzkit/mx/__init__.py`,
  `src/gzkit/mx/marker.py`.

**Stage 5 is now COMPLETE.** All steps closed: lock released (handoff-coupled);
both pipeline markers removed; git-sync #1 committed + pushed; `gz obpi reconcile`
PASS (runtime state + proof coherent, ATTESTED COMPLETED); git-sync #2 clean.
Final repo state: `main`, ahead=0 behind=0 diverged=False dirty=False. The
pipeline is fully discharged — nothing for a resuming agent to finish on OBPI-02.

**Git-sync incident (resolved):** git-sync #1 first blocked on a stranded
`.git/COMMIT_EDITMSG` left by a prior session (subject "chore: update .gzkit (3
files), docs/governance/build-to-1.0-campaign-2026-06-10.md (gz git-sync)").
Verified that subject had already landed as commit `3f23d785` (pulled ff-only at
session start), so the message was a stale leftover — cleared the guard (the
sanctioned recovery the guard itself names) and re-ran; commit + push succeeded.
No work lost.

## Important Context

- **This is the BINARY checkpoint.** A multi-turn design dialogue (captured in
  the predecessor handoff `20260621T143820Z-vibes-observability-design.md`)
  produced a richer severity/fidelity + V.I.B.E.S.-observability model. Per the
  operator's correction-vs-enhancement doctrine, the binary checkpoint
  **fulfills OBPI-02's declared intent as written** ("drops guards to advisory
  except gate5_invariants; strict no-op outside the hangar") — the severity model
  is *new design* (a sibling foundation ADR + re-derivation), NOT a correction of
  an OBPI-02 gap. Landing OBPI-02 as-is is coherent; the redesign is future work.
- **DEFECT (tracked, out of OBPI-02 scope): `gz obpi precomplete`'s
  `behave_req_coverage` check is stale.** `obpi_precomplete.py:324-396`
  (`_check_behave_req_coverage_scoped`) only looks for `@REQ-*` scenario tags in
  `features/**` — it predates GHI #636 / tests.md v0.9.0 and was never reconciled
  with REQ-kind-awareness, unlike its own chokepoint `obpi_complete.py:581-593`
  (which IS kind-aware: skips SUPPORT/STRUCTURAL-FENCE, accepts `@covers`). This
  is a coupled-surface coherence defect from GHI #636's landing (the canonical
  gate + `obpi_complete` were updated; the `obpi_precomplete` mirror was not). It
  produced a FALSE-POSITIVE block on this OBPI's completion (3 REQs flagged though
  REQ-01/02 have `@covers` and REQ-03 is structural-fence). Resolution taken:
  proceeded to the kind-aware chokepoint (the doctrinally clean route — precomplete
  is the bypassable pre-flight); did NOT add a waiver (shrink-only ratchet forbids
  it; the rule says this shape needs none) or author throwaway scenarios (that
  would be the proxy-gaming the V.I.B.E.S. work targets). **Recommended fix:**
  make `_check_behave_req_coverage_scoped` REQ-kind-aware to match
  `obpi_complete.py` — a clean GHI-tracked direct fix. NOT yet filed (operator
  moratorium on reflexive GHI-filing — surface for operator decision).
- **Reconcile sequence gotcha (for future completions):** `gz brief reconcile
  --apply` writes amendments but its emitted receipt records the PRE-amendment
  `has_drift=True`. After `--apply`, run a PLAIN `gz brief reconcile` to emit a
  fresh `has_drift=False` receipt before `gz obpi complete`, or completion blocks
  on `has_drift=True`.

## Decisions Made

- **Decision:** Land OBPI-02's binary checkpoint as-is (attested), treating the
  severity/fidelity model as future sibling-ADR work.
  **Rationale:** binary checkpoint fulfills the brief's declared intent;
  correction-vs-enhancement doctrine routes the level model as new design, not an
  OBPI-02 correction.
  **Alternatives rejected:** block OBPI-02 to re-derive against the level model
  now (the operator chose to land the in-flight run: "continue with OBPI-02").

- **Decision:** Amend the OBPI-02 allowlist to declare `src/gzkit/mx/__init__.py`
  + `src/gzkit/mx/marker.py` (g0-attested via `gz brief reconcile --apply`).
  **Rationale:** the checkpoint and its test genuinely import `marker` — these are
  real coupled surfaces the brief under-declared; the brief adjusts to reality
  (Gate Friction protocol). Surgical: only the two mx-package files actually
  touched/imported.
  **Alternatives rejected:** suppress the reconcile check (gaming a true signal).

- **Decision:** Proceed past the stale `precomplete` behave block to the kind-aware
  chokepoint rather than waiver/scenario-pad.
  **Rationale:** precomplete is the bypassable pre-flight; `gz obpi complete` is
  the authoritative gate and enforces the correct kind-aware rule (it passed).

## Immediate Next Steps

<!-- ADVISORY ONLY — present to operator, await authorization before acting. -->

1. **OBPI-02 is fully discharged — no action needed** (verified: attested-completed,
   lock released, markers cleaned, two git-syncs done, tree clean/synced). Listed
   only so a resuming agent does not re-open it.
2. **Decide the V.I.B.E.S.-observability redesign route** (the big open thread):
   confirm Option A vs **B** (B = ADR-0.0.74 keeps severity-vocabulary +
   checkpoint + integrity-floor incl. `grader-gaming`; sibling foundation ADR owns
   the proxy-reality instrument + detector bus + chore-drain), NOTICE band naming,
   and whether the fidelity facet is wanted now. Then `/gz-design` to crystallize.
   Full design is in the predecessor handoff.
3. **Authorize the precomplete-staleness fix** (the tracked defect above): make
   `_check_behave_req_coverage_scoped` REQ-kind-aware to match `obpi_complete.py`.
   Clean GHI-tracked direct fix; awaiting operator go (moratorium on reflexive
   filing).
4. **Continue ADR-0.0.74 OBPIs** per the Build-to-1.0 campaign (it sequences
   0.0.74 as topmost P0): next un-built increments are OBPI-03 (gate5_invariants —
   under the redesign this must also carry `grader-gaming`), then 04/05 (gz mx
   enter/exit), 06 (MX log), 07 (awareness hook), 08 (gz-mx skill + AGENTS.md
   rule), 09 (retire staging flags — becomes per-guard baseline severity under the
   level model).

## Pending Work / Open Loops

- **ADR-0.0.74 is far from done** — OBPI-02 is the 2nd of 9 active OBPIs. 01
  (marker) + 02 (checkpoint) landed; 03–09 remain.
- **V.I.B.E.S.-observability sibling ADR is unbuilt** — squawk bus, `gz vibe
  squawk` capture verb, proxy-reality distance correlation (the north-star
  instrument), MX-awareness-hook generalization, chore-drain. Full design in the
  predecessor handoff; superpowers prior art at
  `docs/governance/prior-art-scaffold-firewall-2026-06-14.md`.
- **`.gzkit/rules/agent-failure-modes.md` should be updated against Opus 4.8** —
  add Instruction-following-failure, grader-gaming, the diligence family
  (uncritically-reporting-flawed-results, code-summary-honesty, lazy-investigation,
  overconfidence). Currently 4.7/GPT-5.5-based.
- **Tracked defect:** stale `precomplete` behave check (see Important Context).
- **B.1 (ADR-0.0.37 corpus rebuild) remains PAUSED** behind ADR-0.0.74 per Magna
  Carta; OBPI-0.0.74-09 subsumes its `_FRESHNESS_FAIL_CLOSED` flip.

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.0.74-02-mx-shared-checkpoint` → attested-completed
- [ ] `uv run gz obpi lock list` → no OBPI-0.0.74-02 lock
- [ ] `uv run -m unittest tests.mx.test_checkpoint -v` → 7/7 green
- [ ] `git status` → clean (two git-syncs completed)
- [ ] `git branch --show-current` → main
- [ ] `uv run gz adr status ADR-0.0.74 --json` reflects OBPI-02 completed

## Evidence / Artifacts

- `src/gzkit/mx/checkpoint.py` — the shared MX checkpoint (landed)
- `tests/mx/test_checkpoint.py` — 7 passing unit tests
- `src/gzkit/commands/validate_cmd.py` — first wired funnel
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/obpis/OBPI-0.0.74-02-mx-shared-checkpoint.md` — brief (Completed, evidence sections written)
- `.gzkit/handoffs/20260621T143820Z-vibes-observability-design.md` — predecessor: full V.I.B.E.S. design dialogue + 4.8 findings
- `.gzkit/insights/agent-insights.jsonl` — improvement record for the fabricated-card-quote correction (2026-06-21)
- `src/gzkit/commands/obpi_precomplete.py` — site of the tracked stale-behave-check defect (lines 324-396)
- `docs/governance/build-to-1.0-campaign-2026-06-10.md` — Magna Carta campaign (sequences 0.0.74)

## Environment State

- Platform: win32; Python 3.13; `uv run` invocation throughout. Branch `main`
  (operator directive: no feature branches). Repo synced ff-only at session start.
