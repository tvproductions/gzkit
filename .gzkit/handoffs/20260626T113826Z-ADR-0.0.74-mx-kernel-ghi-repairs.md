---
mode: CREATE
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-26T11:38:26Z"
agent: claude-code
obpi_id:
last_lock_event_timestamp:
last_commit_sha: cf7b35c9
session_id:
continues_from: .gzkit/handoffs/20260626T080411Z-OBPI-0.0.74-06-mx-log-completed.md
---

<!-- Handoff document for ADR-0.0.74 — created by claude-code at 2026-06-26T11:38:26Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

Clean tree, synced with `origin/main` at `cf7b35c9` (ahead=0 behind=0,
diverged=False, dirty=False). This session continued from the
OBPI-0.0.74-06-mx-log completion handoff and closed the two GHI follow-ups that
handoff flagged as open loops — both now **CLOSED**:

- **GHI #646** (ledger: launch never flips brief status Draft→Active) — resolved
  at the ledger-state-machine / reconcile-derivation layer, not a frontmatter
  write. Commit `cf7b35c9` `fix(ledger): derive in_progress->Active for launched
  OBPIs`. This supersedes the earlier frontmatter-only flip, which was reverted
  (`8a555db3`) after it was found to conflict with `gz frontmatter reconcile`.
- **GHI #647** (req_kind: SUPPORT proof ledger arm matches event TYPE, ignores
  cited path — hollow gate) — drained in stages: path-aware SUPPORT proof ledger
  arm + grandfather cutover (`61ca2847`), 62 hollow SUPPORT proofs drained via
  artifact-existence (`64a6a350`), grandfather 19→7 (`b0d004b5`), 7→2 via
  stale-citation repair (`366bab2a`), final-2 rationale doc correction
  (`d4c4703d`).

No active OBPI locks. No in-progress ADR pipeline. OBPI-0.0.74-06 remains
`ATTESTED COMPLETED` / Completion COMPLETE (verified via `gz obpi status`).

## Important Context

- **GHI #646 fix layer matters.** The correct fix derives frontmatter status
  from the ledger state machine (resolve `in_progress` → `Active` for launched
  OBPIs) so `gz frontmatter reconcile` *keeps* the Active status rather than
  reverting it. A frontmatter-only write is the wrong layer and was reverted —
  do not reintroduce it.
- **GHI #647 grandfather is drained to 2, by design.** The final 2 SUPPORT
  grandfather entries are retained with corrected rationale (`d4c4703d`), not a
  defect. The SUPPORT proof ledger arm is now path-aware: it matches the cited
  artifact path, not just the event type.
- **Campaign governs sequencing (Magna Carta).** Active plan:
  `docs/governance/build-to-1.0-campaign-2026-06-20.md`, Movement I — MX lean
  kernel + hardening, release `0.29.0`. The remaining MX-kernel OBPIs under
  ADR-0.0.74 are the topmost unchecked substrate work.

## Decisions Made

- **Decision:** Fix GHI #646 at the ledger-state-machine / reconcile-derivation
  layer (`in_progress`→`Active` derivation), reverting the prior frontmatter-only
  flip.
  **Rationale:** The frontmatter-only write conflicted with `gz frontmatter
  reconcile`, which derives status from ledger state and reverted the flip. The
  durable fix is at the derivation source.
  **Alternatives rejected:** Keep the frontmatter-only flip (loses to reconcile);
  disable reconcile derivation (breaks Layer-2-as-source-of-truth doctrine).
- **Decision:** Make the SUPPORT proof ledger arm path-aware and drain the
  grandfather set to its irreducible 2 (GHI #647).
  **Rationale:** Matching event TYPE while ignoring the cited path is a hollow
  gate — it passes proofs whose cited artifact does not exist. Path-awareness
  closes the gap; the remaining 2 entries have legitimate retained rationale.
  **Alternatives rejected:** Drain to 0 (the final 2 have valid grandfather
  rationale, documented in `d4c4703d`); leave the gate type-only (hollow).

## Immediate Next Steps

<!-- ADVISORY ONLY — present to operator and await authorization before acting. -->

1. **Next ADR-0.0.74 MX-kernel OBPI: OBPI-0.0.74-07-mx-awareness-hook**
   (`pending`/`draft`). This is the topmost unchecked Movement-I substrate item.
   Reconcile its brief (`gz brief reconcile` / `gz-brief-reconcile`) before
   Stage 2, then run the OBPI pipeline.
2. After 07: **OBPI-0.0.74-08-mx-skill-and-agents-rule** (`pending`/`draft`) —
   the `gz-mx` skill + AGENTS rule deliverable.
3. Then **OBPI-0.0.74-13-mx-proxy-reality-detector** and
   **OBPI-0.0.74-14-mx-hardening** (TTL/max-open, no-release-while-open,
   debt-aging, dangling-state detector) to complete the MX kernel for the
   `0.29.0` release.
4. When all MX-kernel OBPIs are `attested_completed`, lock the `0.29.0` release
   per the campaign (lean kernel + hardening).

## Pending Work / Open Loops

- **Remaining ADR-0.0.74 MX-kernel OBPIs** (campaign Movement I, release
  `0.29.0`): OBPI-07 (awareness hook), OBPI-08 (skill + AGENTS rule), OBPI-13
  (proxy-reality detector), OBPI-14 (hardening). All currently `pending`/`draft`.
- **Campaign-named cuts/fixes still pending** (from session orientation): CUT the
  doc-type taxonomy (OBPI-10); fix ADR-0.0.74's stub-fidelity assertions
  and reconcile its drifted Q&A transcript (GHI #640); repair the hollow antibody
  + inert rendition gates and delete the tests that certify their inertness
  (state-of-gzkit cut #1).
- **Release discipline:** no normal release while MX is open; `0.29.0` lock is
  the Movement-I milestone, not yet reached.

## Verification Checklist

- [ ] `git rev-parse --short HEAD` is `cf7b35c9` or later on `main`.
- [ ] `git branch --show-current` is `main`.
- [ ] `uv run gz git-sync` reports ahead=0 behind=0 dirty=False.
- [ ] `gh issue view 646` and `gh issue view 647` both show `CLOSED`.
- [ ] `uv run gz obpi lock list` reports no active locks.
- [ ] `uv run gz adr status ADR-0.0.74` shows OBPI-07/08/13/14 as `pending`/`draft`.

## Evidence / Artifacts

- `.gzkit/handoffs/20260626T080411Z-OBPI-0.0.74-06-mx-log-completed.md` —
  predecessor handoff (`continues_from`).
- `docs/governance/build-to-1.0-campaign-2026-06-20.md` — active campaign plan
  (Magna Carta), Movement I MX kernel.
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/` — parent
  ADR package and the pending OBPI briefs (07/08/13/14).

## Environment State

Python 3.13 / uv toolchain. No database or external service state relevant to
resumption. All gates green at the synced HEAD (`cf7b35c9`).
