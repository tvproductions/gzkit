---
mode: CREATE
adr_id: null
branch: main
timestamp: '2026-08-29T18:47:57Z'
agent: claude-code
session_id: ce100bdb-8286-498c-9ef0-228bb77115b3
continues_from: .gzkit/handoffs/20260829T173244Z-instruction-surface-cleansing.md
---

## Current State Summary

Continuation of the instruction-surface cleansing under GHI #921 (OPEN). Three
commits landed on main and the tree is clean; `uv run gz check` exits 0 and all
9,119 unit tests pass. **Three commits are UNPUSHED** (`git rev-list
--left-right --count origin/main...HEAD` returns `0 3`) — pushing was not
authorized this session.

Landed:
- `40035686` fix(content): the composer resolved every surface's content type to
  the literal "AgentContract" and never called `content_type_for_surface`. It now
  resolves from the surface registry and fails closed on an unmapped surface.
- `0ed24c13` fix(sync): the v2 manifest schema declares a `vendors` block that
  `generate_manifest` never emitted, so the sync gate read False on every project
  and each disabled vendor tree was written regardless of config.
- `65001830` feat(vendors)!: Copilot dropped — 154 files / 1,015,407 B removed.

The predecessor handoff's claims were verified at resume and two were STALE: it
expected HEAD `1c547c64` with roughly 92 modified paths carrying an uncommitted
17-rule changelog lift, and `--advisory-scorecard` exiting 1 with 15 errors.
Actual at resume: HEAD `c7a1adc2`, one modified path (the ledger), scorecard
exit 0. **The 17-rule lift is gone** — not stashed, not committed, absent from
reflog, with `.gzkit/rules/` last touched at `1c547c64`. The operator was told it
was preserved; it was not. That is the second data loss of the day, after an
earlier session deleted the local clone.

## Important Context

The Copilot drop was far larger than the predecessor handoff's five-line plan,
because that plan could not work. `_has_manifest_vendors` read
`.gzkit/manifest.json` for a `vendors` key while `generate_manifest` never wrote
one, so adding a `vendors` block to `.gzkit.json` would have changed nothing.

The predicate now reads `.gzkit.json` canon via `has_vendor_declaration(config)`,
using `model_fields_set` rather than the field value — `VendorsConfig` defaults
every vendor, so a parsed config cannot otherwise tell "declared" from
"defaulted". This is the GHI #891 correction (canon first; a derived view must
not decide what gets derived) applied to a second predicate in the same module.

Four consumers validated the vendor tree and had to move in the same commit
(coupled-surface coherence, DO IT RIGHT 1a):
- `audit_skills` demanded mirror roots sync had stopped writing — one blocking
  SKA-MIRROR-DIR-MISSING per canonical skill, unclearable by any sync.
- `readiness` required a disabled vendor's control surface.
- `audit_generated_surface_drift` judged `.claude/rules/` by
  `.github/instructions/` — derived against derived. Guarded canon-first.
- `GzkitConfig.save` materialized a DEFAULTED `vendors` block, so `gz init`
  would have silently opted every new adopter into gating and switched off their
  Codex surfaces. Verified fixed against a fresh `gz init` in a temp dir.

Two `.github` surfaces were deliberately KEPT against the predecessor's list.
`.github/discovery-index.json` is vendor-neutral and merely sat inside the
copilot branch; seven live OBPI briefs cite it in Discovery Checklists, and
deleting it broke `gz validate --brief-reconcile`. Its generation moved out of
that branch. `.github/AGENTS.md` is the Codex projection of `gh-cli.md` for the
surviving `workflows/` and `ISSUE_TEMPLATE/`, not a Copilot artifact.

**The design spike's central premise does not hold, and this changes the
sequencing.** The predecessor described 22 nested AGENTS.md as authored,
hand-maintained, and divergence-prone. Every one carries `<!-- Generated by
gzkit -->` and is written by `sync_nested_agents_md`
(`src/gzkit/rules/__init__.py:482`), which concatenates the bodies of
`.gzkit/rules/*.md` whose `paths:` frontmatter matches the subtree — the same
canon that produces `.claude/rules/*.md`. One generator writes both, so they
cannot diverge. The Claude-does-not-load-nested-AGENTS.md half DID reproduce: a
Read of `src/gzkit/config.py` pulled six `.claude/rules/*.md` and not
`src/gzkit/AGENTS.md`. Consequence: corpus onboarding is NOT blocked by the
spike — onboarding the 26 canonical rules governs both projections at once. The
operator has ruled the spike still open for discussion; do not close it.

Measured for item 4: 18 rules still carry version-chain prose totalling 44,240 B;
11 rules already carry a lifted pointer. `data/advisory_scorecard_grandfather.json`
holds 12 pinned rules while its own `baseline_count` reads 23.

## Decisions Made

- [operator-ruled] Do the composer fix (verbatim: "do the composer fix"). Routed
  direct-fix, not ceremony, on canon rather than agent judgment: "GHIs are
  AUTHORIZED for direct repair, always" makes GHI #921 the work order.
- [operator-ruled] The design spike is NOT answered (verbatim: "spike is not
  answered, we'll discuss"). Booked as a set-aside on the resumed handoff. The
  agent's finding that the spike's premise is wrong is evidence for that
  discussion, never a substitute for it.
- [operator-ruled] Drop Copilot effective immediately (verbatim: "we will drop
  copilot, effective immediately").
- [operator-ruled] Corpus onboarding is held for discussion (verbatim: "we need
  to discuss your #3"). No corpora were seeded.
- [operator-ruled] Redo the changelog lift (verbatim: "4. yes redo, I was told it
  was preserved").
- [agent-chose] Snapshotted the repo outside the working tree before any edit,
  after the operator reported losing the local clone earlier in the day. A full
  `git bundle --all` plus a worktree tarball now sit under
  `~/.gzkit-safety-snapshots/`.
- [agent-chose] Kept `.github/discovery-index.json` and `.github/AGENTS.md`
  rather than deleting them as the predecessor handoff listed, on the grounds
  that neither is a Copilot artifact and the first is cited by seven live briefs.
  Editing those briefs would have been agent-initiated OBPI work, which the Iron
  Law forbids.
- [agent-chose] Repointed the vendor predicate at `.gzkit.json` canon instead of
  the generated manifest. Flagged as a Boundary 6 concern in `0ed24c13` and then
  fixed when it blocked correct behavior, following the GHI #891 precedent
  already present in the same file.
- [agent-chose] Retargeted the retired copilot-mirror REQ test at the rendered
  mirror set rather than deleting it, keeping REQ-0.0.57-03-06's assertion true
  and its proof channel attached, and renamed its tautological-audit baseline
  entry 1:1 rather than growing the baseline.
- [agent-chose] Added `tests/vendor_surfaces.py` so twelve test modules and one
  BDD step derive mirror roots from the project's declaration instead of each
  restating a hardcoded three-vendor tuple.

## Immediate Next Steps

1. Push the three unpushed commits (`git push origin main`). The tree is clean
   and `gz check` exits 0, but nothing is on the remote yet — this is the
   outstanding data-loss exposure and it should be the first action.
2. Redo the changelog lift under GHI #921. Estimated 2 to 3.5 hours and
   indivisible: 18 rules carry 44,240 B of version-chain prose to lift into
   `docs/governance/rule-version-history.md`, each needs a version bump, and each
   bump requires its Coverage Ledger re-score in the SAME commit because
   `advisory_scorecard` fails closed once a rule moves past its scored version. A
   partial landing leaves the tree red, and lifting without a bump is the silent
   lift the operator already ruled against.
3. Run the mechanical content-neutrality proof after each lift pass. The prior
   attempt sliced from the rule-version blockquote to the next heading and
   silently deleted 24 lines from `mx-mode.md` plus content from six other rules;
   only the proof caught it. Operate on contiguous blockquote lines only.
4. Give the 12 still-grandfathered rules real first scores, per the standing
   ruling that nothing stays grandfathered, and correct `baseline_count` in
   `data/advisory_scorecard_grandfather.json` from 23 to the true count.
5. Hold the design-spike discussion and the corpus-onboarding discussion with the
   operator. Both are operator-held; neither is agent work until they rule.

## Pending Work / Open Loops

DESIGN SPIKE — still open by operator ruling, with its premise now corrected.
The four questions the predecessor posed largely dissolve once nested AGENTS.md
are recognized as generated projections of `.gzkit/rules/*.md`: the shared-source
shape already exists, the owning content type is `Rule` (which exists, routes to
claude, temperature heavy), survival is decided by a rule's `paths:` frontmatter
rather than per-file, and regrowth is bounded by the generator. What genuinely
survives is narrower — the nested projection is unmeasured
(`agents-md-map-doctrine.md` scopes `paths: "AGENTS.md"` root-only and
`agents_md_map_conformance.py` sets `_RENDERED_REL_PATH = Path("AGENTS.md")`),
and real byte duplication now sits in the surviving vendor mirrors rather than in
the nesting. Present this to the operator; do not close it unilaterally.

CORPUS ONBOARDING — held. `surface_content_types` still declares only
`AGENTS.md`, so `gz content compose` now fails closed on every other surface.
That is honest and louder than the previous silent mis-grading, but it means no
rule surface can be composed until the registry gains entries. Assigning content
types to the 26 canonical rules is the held discussion, not agent work.

GHI #921 title is misframed: "22 of 23 AGENTS.md are ungoverned by the CMS
corpus" is true that no corpus backs them, but they are not ungoverned, and the
title invites repair at the wrong surface. The CMS gap is on `.gzkit/rules/*.md`.

Pre-existing and untouched: `OBPI-0.37.0-05-session-entry-door` carries an
allowlist drift on `src/gzkit/hooks/scripts/handoff.py`, which is absent from
disk. Not caused by this session and not repaired, because amending an OBPI brief
is operator-initiated work.

Boundary 6 residue: `.gzkit/manifest.json` still carries a `vendors` projection
that nothing gates on any more. It satisfies the schema and is a faithful
projection of config; it is not a second source of truth, but a future reader
could mistake it for one.

The retired "<90% sure" framing still renders at AGENTS.md line 123 while the
superseding entry sits at line 363. Both remain live in the corpus. Retiring the
superseded one touches `.gzkit/corpus/AGENTS.md.jsonl`, which is in the allowlist
of `OBPI-0.35.0-03` (Active), so it is operator-initiated work.

## Verification Checklist

Run these before trusting any claim above.

`git log --oneline -4`
  Expect `65001830`, `0ed24c13`, `40035686`, then `c7a1adc2`.

`git status --short`
  Expect empty. The tree was clean at handoff time.

`git rev-list --left-right --count origin/main...HEAD`
  Expect `0	3` until the push happens. Anything else means the remote moved.

`uv run gz check`
  Expect exit 0. This is the gate the three commits already pass.

`uv run gz validate --advisory-scorecard`
  Expect exit 0. It goes red the moment a rule version bumps past its Coverage
  Ledger entry, which is the constraint that makes item 2 indivisible.

`uv run gz validate --brief-reconcile`
  Expect exit 3 with one error: the pre-existing
  `src/gzkit/hooks/scripts/handoff.py` allowlist drift on
  `OBPI-0.37.0-05-session-entry-door`. Seven `.github/discovery-index.json`
  errors appeared when that file was briefly deleted and are resolved.

`gh issue view 921 --json state,title`
  Expect OPEN. Confirms the work order still stands.

`ls .github/`
  Expect exactly `AGENTS.md`, `discovery-index.json`, `ISSUE_TEMPLATE`,
  `workflows`.

`uv run gz agent sync control-surfaces`
  Expect exit 0 and no resurrection of any Copilot tree.

`uv run gz obpi lock list`
  Expect no active locks.

Reproduce the nested-AGENTS.md finding with the Read tool on any file under
`src/gzkit`, observing which instruction files load. Bash reads do not trigger
directory-scoped memory, so the Read tool is required.

## Evidence / Artifacts

Source changed this session:
- `src/gzkit/content/composer.py` — content type resolves from the surface registry
- `src/gzkit/sync_surfaces.py` — `has_vendor_declaration` reads canon; vendors block emitted; discovery-index moved out of the copilot branch
- `src/gzkit/skills_audit.py` — mirror roots gated on the same predicate sync uses
- `src/gzkit/commands/readiness.py` — a disabled vendor's surface is no longer required
- `src/gzkit/instruction_audit.py` — canon-first guard on the generated-surface drift audit
- `src/gzkit/config.py` — `save` no longer materializes a defaulted vendors block

Test surfaces:
- `tests/vendor_surfaces.py` — new shared helper deriving mirror roots from the project declaration
- `tests/test_manifest_v2.py`, `tests/test_skills_audit.py`, `tests/test_instruction_audit.py` — new failing-first coverage
- `data/tautological_test_baseline.json` — one entry renamed 1:1 for the retargeted REQ test

Configuration:
- `.gzkit.json` — vendors block: claude and codex enabled, copilot/gemini/opencode disabled

Outstanding-work surfaces for item 2:
- `docs/governance/rule-version-history.md` — the lift destination
- `data/advisory_scorecard_grandfather.json` — 12 pinned rules, stale `baseline_count`
- `docs/governance/advisory-rules-audit.md` — the Coverage Ledger the re-scores land in

Predecessor:
- `.gzkit/handoffs/20260829T173244Z-instruction-surface-cleansing.md`

Off-repo safety snapshot taken before any edit: a full `git bundle --all` and a
worktree tarball under `~/.gzkit-safety-snapshots/`, outside the working tree.

## Settled Rulings

603 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
