---
mode: CREATE
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-24T11:48:00Z"
agent: claude-code
obpi_id: OBPI-0.0.74-16-meta-validator-runner
last_lock_event_timestamp: "2026-06-24T11:48:00Z"
last_commit_sha: 55d929da
session_id: bcb8332a-6e8c-4ac7-8241-4c988949bcc0
continues_from:
---

<!-- Handoff for ADR-0.0.74 — two intertwined threads left GREEN and UNCOMMITTED. -->

## Important Context

This handoff documents a long single-session traversal of ADR-0.0.74 (the enforcement-claim meta-validator). OBPI-0.0.74-16-meta-validator-runner was locked at 11:48:00Z and has been in-flight for 254 minutes. The lock has expired (24h TTL) but the work is complete and ready for final ceremony: GHI #643 (Stage 4 interface) landed as a direct fix, and the OBPI is at Stage 4 with both 4a (present-evidence) and 4b (adversary) outcomes in hand. The tree is GREEN (all tests pass, lint clean, typecheck clean) with 33 uncommitted changes spanning two logical threads. Operator must decide: commit strategy (separate or bundled) and whether to re-run the Codex adversary on the fixed state before attestation.

## Current State Summary

Tree is GREEN, nothing committed (33 dirty files). Full suite **6459 pass** (receipt
`arb-step-unittest-5161455631b2462693a234e114a0f573`, exit_status 0); lint clean;
typecheck clean; `gz cli audit` 110/110.

**Two threads in flight:**

### Thread 1 — OBPI-0.0.74-16-meta-validator-runner (the original pipeline)
- Implemented + verified. The engine was genuinely lifted: `run_meta_validator()`
  discovers + verifies **37** production `@enforces` claims (0 facades, 0 test_bugs);
  qc_binding routes through the shared `_run_single_claim`; the old
  `_NEGATIVE_CONTROLS`/`_check_negative_control`/`_NEGATIVE_CONTROL_DEBT` engine is gone
  (BI#6/#8); 2 forced NCs un-forced; new `enforcement_claim_verified` event + schema.
- Sibling OBPI-0.0.73 tests re-expressed against the lifted engine; ADR-0.0.73 fidelity row
  updated; ADR-0.0.73 fidelity 8/8.
- **At Stage 4.** Both outcomes in hand: 4a `gz obpi present-evidence` = ATTESTABLE;
  4b Codex adversary = REFUTED-WITH-CAVEATS → the one caveat (no no-arg production-discovery
  test) is FIXED (`TestProductionRegistryDiscovery`).
- **Lock is FREE** (120m TTL expired over this long session). Re-claim to resume.

### Thread 2 — GHI #643 (Stage-4 adversarial validation) — direct-fix repair, landed
- Operator-ratified redesign: Stage 4 = 4a (agent presents) + 4b (independent adversary
  refutes using the tools); operator attests holding both. Adversary is an INTERFACE
  (independent + refute-framed + evidence-backed); ladder Codex → Claude subagent →
  human-degraded. Proven live on OBPI-16.
- Landed: `src/gzkit/governance/stage4_evidence.py` (+test), `gz obpi present-evidence`
  command + parser/manifest registration + `config/doc-coverage.json` entry; Stage 4b in
  `.gzkit/skills/gz-obpi-pipeline/SKILL.md` (6.21.0→6.22.0, synced). The deterministic
  `gz obpi complete` gate was REVERTED (it broke 20 completion tests).

## Pending Work / Open Loops

1. **OBPI-16 completion.** RECOMMENDED next step: re-run 4b (dispatch Codex adversary)
   against the *fixed* state for a clean NOT-REFUTED verdict, then operator attests holding
   4a+4b, then Stage 5 (`gz obpi complete` + two git-syncs + reconcile). Re-claim the lock
   first.
2. **Commit strategy.** Thread 1 (OBPI-16) and Thread 2 (GHI #643) are mixed in the tree.
   Consider committing GHI #643 as `fix(...) (GHI #643)` and OBPI-16 via its Stage-5
   ceremony, or bundle — operator's call.
3. **GHI #643 follow-ups (logged, not blockers):** wire `SubagentDispatchRecord(role="Adversary")`
   into `pipeline_runtime`; consider making "a REQ with only injected-registry tests" a
   brief-authoring smell.
4. **Sibling open GHIs cross-linked:** #516, #620, #642 (same passive-presenter root).

## Decisions Made

Decided to defer OBPI-16 completion until the Stage-4b adversary is re-run against the fixed state (TestProductionRegistryDiscovery now in place). GHI #643 (Stage 4 interface redesign) has landed as a direct fix — the deterministic completion gate was reverted due to test breakage and will be re-evaluated post-completion. Both threads are GREEN and uncommitted; committing strategy is operator's choice (separate commits or bundled).

## Immediate Next Steps

1. Re-claim OBPI-0.0.74-16-meta-validator-runner lock (TTL expired; claim fresh)
2. Re-run Stage 4b adversary dispatch via Codex against the fixed state
3. If NOT-REFUTED verdict is clean, operator attests holding both 4a (present-evidence) and 4b (adversary)
4. Run Stage 5 ceremony: `gz obpi complete --attestation-text "<operator attestation>"` + two git-syncs + ledger reconcile

## Verification Checklist

- [ ] Full test suite GREEN (6459 pass, arb-step-unittest receipt available)
- [ ] Lint clean (ruff check + format)
- [ ] Typecheck clean (ty)
- [ ] CLI audit 110/110 (gz cli audit)
- [ ] Codex adversary re-run: NOT-REFUTED or REFUTED-WITH-CAVEATS-FIXED on Stage 4b
- [ ] `gz obpi present-evidence` command runs and produces attestable output
- [ ] `.gzkit/evidence/` artifacts exist and match brief scope
- [ ] No lock-handoff coupling defects (will verify on next git-sync)

## Evidence / Artifacts

- Test receipt: `arb-step-unittest-5161455631b2462693a234e114a0f573` (6459 pass, exit 0)
- Lifted engine: `run_meta_validator()` discovers + verifies 37 production `@enforces` claims (0 facades, 0 test_bugs)
- New schema: `enforcement_claim_verified` event + schema in ledger
- GHI #643 implementation: `src/gzkit/governance/stage4_evidence.py` (+test), `gz obpi present-evidence` command, skill SKILL.md § 6.22.0
- ADR-0.0.73 fidelity reconciled: 8/8 rows
- Git state: 33 dirty files (OBPI-16 + GHI #643 mixed), last commit 55d929da

## Advised Next Step (advisory — operator rules)

Re-claim the OBPI-16 lock, re-run the Stage-4b adversary on the fixed state, then attest
and complete. Cross-reference this session's insights at
`.gzkit/insights/agent-insights.jsonl` (last 2 entries: the engine-lift near-false-completion
and the GHI #643 course-correction).
