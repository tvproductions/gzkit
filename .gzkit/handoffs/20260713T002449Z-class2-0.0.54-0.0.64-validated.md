---
mode: CREATE
adr_id: ADR-0.34.0
branch: main
timestamp: "2026-07-13T00:24:49Z"
agent: claude-code
obpi_id:
last_lock_event_timestamp:
last_commit_sha: 01b14c27
session_id:
continues_from: .gzkit/handoffs/20260712T223248Z-adr-0.34.0-foundation-sunset-authored.md
---

<!-- Handoff document for ADR-0.34.0 (Foundation Sunset Class-2 track) — created by claude-code at 2026-07-13T00:24:49Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

**The two trivial Class-2 foundations are CLOSED and VALIDATED** — the first
step of the Foundation Sunset execution sequence (handoff `20260712T223248Z`
step 1) is complete. Tree clean, synced (`git rev-list --left-right --count
origin/main...HEAD` → `0	0`; HEAD `01b14c27`).

- **`ADR-0.0.54-agents-md-map-not-encyclopedia-doctrine`** → **Validated**,
  verdict **Completed - Partial**. The map-*shape* enforcer shipped and gates in
  `gz check`; the headline *15k weight-halving* was NOT delivered (AGENTS.md is
  ~31,741 bytes; the enforced budget was raised to 50000) and is deferred to
  **GHI #533 / ADR-0.0.37** (kept open by design). Closeout surfaced and fixed:
  a 12-tag REQ-kind retrofit, three truthfulness corrections (false
  enforced-budget surfaces repointed to live JSON; rule `agents-md-map-doctrine.md`
  v0.2.0; OBPI-02's false "15,079 under budget" attestation line annotated not
  overwritten), and 6 git-verified covers-backfill overlays cleared with
  operator-attested `# audit-exempt: regression-invariant-overlay` markers.
- **`ADR-0.0.64-task-envelope-and-planning-decomposition`** → **Validated**,
  verdict **Completed** (full). Independent closeout review found a *hollow
  foundation* behind green gates: OBPI-02/03 were attested Completed with
  scaffold-default REQs (no `[kind]` tags) proven by cosmetic `.is_file()`
  tests, and `gz task envelope diagnose` read only 2 of its 4 documented
  channels. Per operator ruling **Correct-then-close (thorough)** all of it was
  remediated in-place before Gate-5 (see § Decisions Made). GHI #553 closed
  (envelope delivered).

Both ADRs were taken through the full two-ceremony arc: `/gz-adr-closeout-ceremony`
(→ Completed, operator `attest completed`) then `/gz-adr-audit` (→ Validated,
operator `accept audit`). No OBPI locks held; no ceremony markers left.

## Important Context

- **Closeout messiness correlates with how much doctrine post-dates the ADR.**
  0.0.54 predates ADR-0.0.59 (REQ-kind discipline) → its briefs needed retrofitting
  and its tests carried pre-discipline covers-backfill timing. 0.0.64 post-dates
  0.0.59 → its briefs were born `[kind]`-tagged (closeout-proof passed clean), but
  its OBPI-02/03 were closed through the *hollow-gate* path the ADR itself exists
  to close. Expect the remaining Class-2 members (`0.0.65`, `0.0.72`) to sit on the
  same spectrum — pre-flight both gates before assuming "trivial".
- **Green gates were necessary but not sufficient; the independent reviewers were
  the actual signal.** closeout-proof / req-kind-discipline / audit-check were all
  green on 0.0.64's hollow state (untagged REQ defaults to BEHAVIOR; a cosmetic
  `@covers` satisfies that default; all-untagged briefs are grandfathered).
  Dispatch spec-reviewer + quality-reviewer at closeout and READ their findings.
- **covers-backfill (GHI #309) mechanics, now known cold.** The timing heuristic
  flags a `@covers` decorator introduced ≤ threshold before its OBPI receipt. Two
  legitimate escape hatches: (1) the inline `# audit-exempt:
  regression-invariant-overlay <reason>` marker — operator-attested, for a genuine
  decorator-overlaid-onto-pre-existing-test (used on 0.0.54); (2) ceremony bundling
  — a remap that lands in a `gz git-sync` commit is exempt via the
  `Task: TASK-gz-git-sync` trailer in `_EXEMPT_CEREMONIES` (relied on for 0.0.64's
  7-test REQ-02-01→02-02 remap). Source: `src/gzkit/commands/adr_audit_covers_backfill.py`.
- **Heavy-lane validated-receipt gate needs resolvable ARB receipt IDs *in the
  `attestation_text`*** (not just prose). First `gz adr emit-receipt … --event
  validated` attempt failed "(no resolvable receipts cited)"; re-emit with the
  `arb-*` IDs embedded in the text.
- **Commit convention:** substantive work lands as `chore: … (gz git-sync)`;
  significance lives in content + ledger events. `gz git-sync --apply --lint --test`
  then a bare `gz git-sync --apply` (two-sync) after each ceremony.
- **Bash cwd persists across calls** — use absolute paths / repo-root `gz` calls.

## Decisions Made

**0.0.54 — verdict Completed-Partial (operator-selected).** Rejected: flat
"Completed" (would attest a 15k weight-halving that was never delivered);
"pause and deliver 15k first" (that work is legitimately deferred/tracked under
GHI #533/ADR-0.0.37). Truthfulness corrections were applied regardless of verdict
because the false surfaces were untrue independent of the attestation value; the
OBPI-02 false attestation line was *annotated with a dated correction, never
overwritten* (a recorded attestation is Layer-1 history — annotate, don't tamper).

**0.0.64 — Correct-then-close (thorough) (operator-selected).** Rejected:
"Completed-Partial + carve-outs" and "file GHIs + pause" — both would have shipped
the anti-hollow-gate ADR *with a hollow gate in its own decomposition*. The
remediation, all under the owning ADR (operator canon: correction under owning ADR,
never a fresh pool ADR): OBPI-02 REQs re-authored to 2 BEHAVIOR (11 genuine
`test_advances_decorator.py` tests, 7 remapped 02-01→02-02) + 1 SUPPORT; OBPI-03
REQs re-authored to 1 BEHAVIOR + 1 SUPPORT + 1 STRUCTURAL-FENCE (cites ADR
§ Boundary Invariants #1); both cosmetic `.is_file()` test surfaces deleted (the
whole `test_obpi_0_0_64_03_subdivision_cli.py` file); `task_envelope_diagnose_cmd`
wired to the validator's own `_channel_declarations_for_obpi` (4 channels + drift),
and REQ-04-05's weak "is callable" test replaced with a genuine 4-channel
assertion; 8→12 event-type and 3→4 signature drifts reconciled in
`task-discovery.md` v0.3.0 + a new ADR § Post-authoring reconciliation note;
runbook gap for `gz task fanout` / `envelope diagnose` / `--seq` closed. Kept the
composite BEHAVIOR REQ where one cohesive feature had many test cases rather than
force-splitting.

## Immediate Next Steps

<!-- ADVISORY ONLY — present for operator review; do not execute without authorization. -->

The Foundation Sunset execution continues in dependency order (the campaign
governs sequencing; the operator selects the pull):

1. **Finish the partial Class-2** — `ADR-0.0.65` (build its 4 designed OBPIs,
   incl. a new `gz handoff` verb) and `ADR-0.0.72` (build its 3 OBPIs) via
   `gz-obpi-pipeline`, then closeout → audit → Validated. Pre-flight
   `gz validate --closeout-proof` + `gz adr audit-check` first (learned from
   0.0.54/0.0.64) and dispatch the two independent reviewers at closeout.
2. **Closeout `ADR-0.0.37`** — `gz obpi withdraw` the 4 fabricated composition
   OBPIs (02/03/21/22) → 15/15 → Validated; re-home the real registry→AGENTS.md
   composition as a **feature** ADR (closes GHI #623). This is also the tracked
   home for 0.0.54's deferred 15k weight-halving (GHI #533).
3. **Implement `ADR-0.34.0`** (Foundation Sunset capstone) OBPIs 01–05 — the
   capstone sequences LAST (OBPI-05 gate-wiring only after the tree is terminal).
4. **Also owed:** GHI #682 (sensitivity-floor per-brief decision on 0.32.0-06 /
   0.44.0-01); and 0.0.64's still-open runtime/spec GHIs **#563** (seq=01-only
   TASKs + missing task_id at runtime), **#561** (OBPI-05 REQ-05-06 SUPPORT
   citation), **#564** (orphan preflight receipt) — none resolved by this closeout.

## Pending Work / Open Loops

- **GHI #533 (OPEN)** — 0.0.54's deferred 15k weight-halving; home is ADR-0.0.37.
- **GHI #623 (OPEN)** — ADR-0.0.37 composition facade; step-2 feature re-home closes it.
- **GHI #682 (OPEN, open-with-blocker)** — sensitivity floor on two pre-existing briefs.
- **GHI #563 / #561 / #564 (OPEN)** — 0.0.64 runtime + OBPI-05 + preflight defects, left open per dead-letter discipline (not resolved by the closeout).
- **Foundation Sunset execution (in progress)** — Class-2 trivial done (0.0.54/0.0.64); partial Class-2 (0.0.65/0.0.72), 0.0.37, and the 0.34.0 capstone remain.
- **Deferred (non-1.0-blocker):** Phase 4 — RECALL; the Movement IV `validate()` registry collapse (#618); oversized-module census.
- No active OBPI locks. No blockers.

## Verification Checklist

- [ ] `uv run gz adr status ADR-0.0.54` → Lifecycle `Validated`, 4/4, `validated`
- [ ] `uv run gz adr status ADR-0.0.64` → Lifecycle `Validated`, 5/5, `validated`
- [ ] Branch: `git branch --show-current` → `main`
- [ ] Tree clean + synced: `git status --short` empty; `git rev-list --left-right --count origin/main...HEAD` → `0	0`
- [ ] Gates green: `uv run gz validate --req-kind-discipline --closeout-proof` → pass
- [ ] `gh issue view 553 --json state` → `CLOSED`; `gh issue view 533 --json state` → `OPEN`
- [ ] Full check: `uv run gz check` → pass

## Evidence / Artifacts

- `docs/design/adr/foundation/ADR-0.0.54-agents-md-map-not-encyclopedia-doctrine/audit/AUDIT.md` — 0.0.54 audit record
- `docs/design/adr/foundation/ADR-0.0.64-task-envelope-and-planning-decomposition/audit/AUDIT.md` — 0.0.64 audit record
- `docs/design/adr/foundation/ADR-0.0.64-task-envelope-and-planning-decomposition/ADR-0.0.64-task-envelope-and-planning-decomposition.md` — carries the new § Post-authoring reconciliation note
- `.gzkit/rules/task-discovery.md` — v0.3.0 (event-count reconciliation)
- `.gzkit/rules/agents-md-map-doctrine.md` — v0.2.0 (budget-surface truthfulness correction)
- `tests/governance/test_advances_decorator.py` — OBPI-02 genuine tests (cosmetic class removed; 02-01/02-02 remap)
- `tests/governance/test_task_envelope_coherence.py` — strengthened REQ-04-05 four-channel diagnose test
- `src/gzkit/commands/task.py` — `task_envelope_diagnose_cmd` four-channel fix
- `docs/governance/build-to-1.0-campaign-2026-06-30.md` — active campaign (Magna Carta)
