---
mode: CREATE
adr_id: ADR-0.0.65
branch: main
timestamp: '2026-07-16T00:36:22Z'
agent: claude-code
continues_from: .gzkit/handoffs/20260715T222217Z-demo-gz-handoff-create.md
---

## Current State Summary

ADR-0.0.65-handoff-system-consolidation is now VALIDATED (foundation / heavy). The /gz-adr-audit ceremony ran end-to-end: L2 ledger proof shows 5/5 OBPIs attested_completed; the bound fidelity gate passes 3/3 (rewritten in-audit to exercise the shipped `gz handoff` verb instead of the pre-audit WEAK stub row that falsely claimed the verb was unbuilt). Two independent persona reviews concurred: spec-reviewer PASS-WITH-CONCERNS, quality-reviewer COHERENT-WITH-CONCERNS. The validated receipt is in the ledger (attestor g0, attestation_type operator-verbatim-conversational, verbatim ack 'accept audit'). ADR frontmatter status is now Validated, `adr-status.md` regenerated, and `gz validate --documents --adr-status-fresh` is clean. All session work is uncommitted on main.

## Important Context

This was a read-only Gate-5 audit, not implementation. Code defects surfaced by the reviews were NOT fixed inside the audit (skill doctrine: no implementer in the audit ceremony) and were routed to GHIs. Only documentation drift inside the ADR itself (stale fidelity prose + unchecked checklist boxes) was remediated in-flight. The heavy/foundation validated receipt required fresh resolvable ARB receipt IDs (arb-ruff / arb-step-typecheck / arb-step-unittest / arb-step-mkdocs) -- prose claims of green are structurally inert at that gate, so the four canonical `gz arb` commands were run at validation time even though the L2 audit logic already trusted the ledger. Attestor is recorded as g0 per the 2026-06-10 operator directive (attestor fields use g0, never the personal email).

## Decisions Made

1) Split-brain confirmed genuinely CLOSED (not relocated): write surface (`SKILL.md`), CREATE API (`handoff_api.py`), orientation reader (single-scan; dual-scan deleted), and archive all agree on `.gzkit/handoffs/`. 2) GHI #688 (orientation boot-hook crash on non-UTF-8 read) was widened to the full failure class -- it covers BOTH read sites, including the `except OSError` at `session_orientation.py:72` that cannot catch `UnicodeDecodeError`. Filed fresh with Related: #582 (a sibling-cut) rather than reopening #582, so #582 keeps its clean closed subprocess scope. 3) GHI #689 filed for the `continues_from` resolver duplicated across the OBPI-02/-05 boundary with no coherence test (Invariant 1a). 4) The campaign edit was scoped to an append-only Amendments entry; the historical 1/5 reading in the 2026-07-12 amendment was left as-authored and the new entry is the live correction. Rejected alternative: rewriting the historical number in place (would violate append-only amendment discipline).

## Immediate Next Steps

1. git-sync the session work on main (ledger validated receipt, ADR-0.0.65 update, regenerated `adr-status.md`, campaign amendment, and the `audit/` artifacts) via the git-sync-repo path. 2. Optionally land GHI #688 and #689 as a bundled direct-fix pass (both pass the direct-fix thresholds: <=10 / <=100 lines, single surface). 3. Resume the Foundation Sunset sequence step 1: finish ADR-0.0.72 (1/4), then closeout 0.0.54 / 0.0.64, then 0.0.37, then the ADR-0.34.0 capstone.

## Pending Work / Open Loops

Open direct-fix work orders: GHI #688 (orientation non-UTF-8 read guard, both sites) and GHI #689 (resolver coherence test / shared helper). Non-blocking notes carried in AUDIT.md section 6: S4 (`handoff_validation.py` 732 > 600-line ceiling) is owned by the campaign oversized-module census; S5 (REQ-0.0.65-01-02 says 'exactly 34' while the test asserts a >=34 floor) and S6 (two by-design register writers bypass the CREATE gate) are noted as defensible. All session work is uncommitted on main.

## Verification Checklist

- `uv run gz adr report ADR-0.0.65` -> Lifecycle Validated, closeout phase validated, 5/5 attested_completed. - `uv run gz adr fidelity ADR-0.0.65-handoff-system-consolidation` -> 3 pass, 0 fail. - `uv run gz validate --documents --adr-status-fresh` -> clean. - `gh issue view 688` and `gh issue view 689` -> both open. - `git status --short` -> the five pending paths listed in Evidence.

## Evidence / Artifacts

Git-tracked surfaces modified this session (the untracked audit package -- AUDIT.md, AUDIT_PLAN.md and the proofs directory under the ADR folder -- lands with the next git-sync): - `docs/design/adr/foundation/ADR-0.0.65-handoff-system-consolidation/ADR-0.0.65-handoff-system-consolidation.md` - `docs/governance/build-to-1.0-campaign-2026-06-30.md`
