---
mode: CREATE
adr_id: ADR-0.0.37
branch: main
timestamp: "2026-06-14T20:10:48Z"
agent: claude-code
obpi_id: OBPI-0.0.37-23-invariant-tier
session_id:
continues_from: .gzkit/handoffs/20260614T141050Z-OBPI-0.0.37-22-complete-skill-followup.md
---

<!-- Handoff document for ADR-0.0.37 — created by claude-code at 2026-06-14T20:10:48Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

OBPI-0.0.37-23-invariant-tier is **attested-complete** (Layer-2 truth:
`gz obpi complete` emitted `attested_completed`, attestor g0, Heavy/foundation
Gate 5, operator-verbatim conversational attestation "attest completed"). This handoff
is the Stage-5 completion register entry that unlocks the OBPI lock release.

The OBPI delivered the invariant-tier 0-Kelvin floor as a first-class guarantee:
`src/gzkit/content/tier_policy.py` is the single composer-consumable enforcement surface
(`invariant_entries` + `assert_invariant_verbatim`; stdlib + Pydantic, no LLM). The
OBPI-21 composer was re-pointed onto it (operator-approved path b), removing its
duplicated inline invariant-floor check. Full suite green: 6149 tests pass, 1 skipped.

This handoff continues the ADR-0.0.37 corpus-rendition track from the OBPI-0.0.37-22
completion handoff.

## Important Context

- **21↔23 enforcement-wiring seam (resolved).** The parent ADR sequences OBPI-21
  (composer) before OBPI-23 but says 21's invariant floor is "deepened by OBPI-23."
  OBPI-21 had landed first with an inline check at `composer.py:53-62`. Spec-review
  FAIL on REQ-03 ("no duplicated inline check") surfaced the two-parallel-implementations
  problem. The operator chose path (b): re-point the composer onto `tier_policy` as a
  coupled-surface edit. The brief allowlist was amended to add `src/gzkit/content/composer.py`.
- **corpus_store fixture-import false positive.** The composer-routing test originally
  imported `append_entry` from `corpus_store`, which tripped the brief-reconcile
  neighborhood heuristic (corpus_store shares the `src/gzkit/content/` parent with the
  allowlisted `tier_policy.py`). Resolved by seeding the test corpus JSONL directly —
  corpus_store is a consumed prerequisite, not an edited surface.
- **REQ-04 is SUPPORT-kind** — proven by the substrate-doc edit + `validate --documents`
  + `artifact_edited` ledger event, NOT a `@covers` test. Behave coverage is waived via
  `data/behave_coverage_waivers.json` (no Gherkin-observable CLI surface; ships no verb).
- **Parent ADR-0.0.37 remains Draft** (Attestation Block `0.0.37 | Pending`). Individual
  OBPI receipts are valid; no ADR-level ratification exists yet.

## Decisions Made

- **Decision:** Resolve the 21↔23 seam via path (b) — re-point the composer onto `tier_policy`.
  **Rationale:** REQ-03 literally requires "single enforcement surface, no duplicated
  inline check"; leaving the composer's inline check would leave two parallel
  implementations. DO IT RIGHT 1a coupled-surface coherence.
  **Alternatives rejected:** Keep composer untouched (interpret REQ-03 as "consumable" only)
  — rejected by the operator because it leaves the duplicated check REQ-03 forbids.
- **Decision:** Eliminate the `corpus_store` import in the routing test by seeding the
  corpus JSONL directly.
  **Rationale:** corpus_store is consumed, not edited; adding it to the allowlist would
  misrepresent scope. Keeps the brief honest and the reconcile clean (no override event).
  **Alternatives rejected:** Add corpus_store to the allowlist (misrepresents edit scope);
  use `--accept-stale-reconciliation` override (an override event for a pure fixture import
  is over-ceremony).

## Immediate Next Steps

ADVISORY ONLY — present to the operator before acting.

1. Release the OBPI lock: `uv run gz obpi lock release OBPI-0.0.37-23-invariant-tier`
   (now satisfiable — this handoff is the register entry). Do NOT use `--abandon` —
   this is a completed OBPI.
2. Run git-sync #1: `uv run gz git-sync --apply` to commit the governance edits
   (completion, brief, waiver, insight, handoff, marker cleanup).
3. Reconcile + ADR status: `uv run gz obpi reconcile OBPI-0.0.37-23-invariant-tier` then
   `uv run gz adr status ADR-0.0.37 --json`, then git-sync #2.
4. Consider the next ADR-0.0.37 OBPI in sequence (24/25/26/27 remain — see Pending Work).

## Pending Work / Open Loops

- **ADR-0.0.37 remaining active OBPIs:** 24 (advisor-panel QC loop), 25 (bullet-retention
  tier-scoped validator + coupled ADR-0.0.33 amendment), 26 (#519 Codex-root setpoint),
  27 (migration/disposition + doctrine refresh + orientation index). OBPI-23's `tier_policy`
  is now available for OBPI-25's tier-scoped retention validator to consume.
- **OBPI-25 coupling:** the ADR-0.0.33 bullet-retention validator must flip to tier-aware
  (verbatim on invariant tier; advisor-QC + attestation on compressed tiers) in the same
  commit-window as the ADR-0.0.33 Invariant-1 amendment.
- **Parent ADR-0.0.37 closeout** awaits all active OBPIs (24-27) completing.

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.0.37-23-invariant-tier` shows ATTESTED COMPLETED
- [ ] `uv run gz obpi lock list` — confirm lock state before release
- [ ] `uv run -m unittest tests.content.test_tier_policy tests.content.test_composer` passes
- [ ] Branch matches: `git branch --show-current` → `main`
- [ ] `uv run gz obpi reconcile OBPI-0.0.37-23-invariant-tier` confirms receipt/brief agree

## Evidence / Artifacts

- `src/gzkit/content/tier_policy.py` — the single invariant-tier enforcement surface
- `tests/content/test_tier_policy.py` — 18 `@covers` tests across 5 classes
- `src/gzkit/content/composer.py` — re-pointed onto `tier_policy` (duplicated check removed)
- `docs/governance/agent-control-surface-rendering-substrate.md` — 0-Kelvin floor subsection
- `data/behave_coverage_waivers.json` — OBPI-level behave waiver
- `docs/design/adr/foundation/ADR-0.0.37-constitutional-invariant-composition/obpis/OBPI-0.0.37-23-invariant-tier.md` — completed brief + evidence
- `.gzkit/insights/agent-insights.jsonl` — improvement insight (course-correction record)

## Environment State

Python 3.13, uv-managed. Platform win32. Full suite: 6149 pass / 1 skipped
(receipt `arb-step-unittest-a37f8b15eb97439299bd6f94703b08ec`).
