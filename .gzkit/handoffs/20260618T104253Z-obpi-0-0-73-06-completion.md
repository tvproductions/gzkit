---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-18T10:42:53Z"
agent: claude-code
obpi_id: OBPI-0.0.73-06-self-check-facade-regression-corpus
session_id: 2835335b-74bb-4381-8144-110265a5ac92
continues_from: .gzkit/handoffs/20260618T061124Z-obpi-0-0-73-05-lock-release.md
last_lock_event_timestamp: "2026-06-18T10:30:32.702745+00:00"
last_commit_sha: ce3413d5e1ba757cad880549437a98a3628a1da4
---

<!-- Completion handoff for ADR-0.0.73 / OBPI-0.0.73-06 — created by claude-code at 2026-06-18T10:42:53Z. This is the register entry required by ADR-0.0.41 to release the completed OBPI's lock (handoff-before-release coupling). -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

A handoff records a *proposed* plan and its context. On resume, present advised
steps to the operator and obtain explicit authorization before executing any of
them. You advise; the operator rules.

## Current State Summary

OBPI-0.0.73-06-self-check-facade-regression-corpus is **completed and attested**.
`gz obpi complete` ran successfully with operator-verbatim-conversational
attestation by `g0` ("attest completed", 2026-06-18). The brief frontmatter is
`status: Completed`; the completion receipt and ADR-level audit ledger entry were
written atomically. Branch `main`, in sync with origin (ahead=0, behind=0) as of
the pre-completion commit `ce3413d5`.

This handoff is the register entry that ADR-0.0.41 handoff-before-release coupling
requires to release the (completed, not abandoned) OBPI lock. The lock was claimed
at 2026-06-18T10:30:32Z by agent `claude-code-2835335b`.

## Important Context

- The implementation was largely already present from a prior session; this run
  verified it green, fixed two ADR drift defects discovered during Stage 3, and
  completed the governance ceremony.
- Two in-flight ADR fixes were made: (1) the Decomposition Scorecard
  `Baseline Selected` was `6` but the Final Target formula expects `6 + 1 split = 7`
  while the real target is `8` (OBPI-08 was an operator-directed addition) — fixed
  to `Baseline Selected: 7` so `7 + 1 = 8` reconciles; (2) `gz validate --documents`
  flagged the mismatch (exit 1) before the fix and exits 0 after.
- A `req_atomic:` exemption was declared in the brief frontmatter for all six REQs
  (each REQ's labor was one indivisible authoring unit — single test methods, one
  cohesive corpus, one guard-wiring change; none was subdivided across labor
  sessions). This satisfied the task-envelope coherence gate (Signature b).
- Do NOT use `--abandon` for the lock release — the OBPI is completed, not
  surrendered. A plain `gz obpi lock release` is correct.

## Decisions Made

- **Decision:** Declare `req_atomic:` for all six REQs rather than minting
  `seq=02+` subdivision TASKs.
  **Rationale:** Each REQ corresponds to one cohesive, indivisible deliverable
  authored in a single pass (REQ-01/03 single test methods, REQ-02 one corpus,
  REQ-04 SUPPORT with no separate labor, REQ-05 STRUCTURAL-FENCE invariant,
  REQ-06 one guard-wiring change). There was no labor below the REQ to attribute.
  **Alternatives rejected:** `gz task start --seq next` subdivision — would invent
  labor distinctions that did not exist.
- **Decision:** Fix the ADR Decomposition Scorecard `Baseline Selected` to 7 in
  flight rather than filing a GHI.
  **Rationale:** The mismatch is an in-scope coupled-surface defect (the parent ADR
  is the self-check subject and is on the allowlist); `gz validate --documents`
  fail-closed on it, blocking completion. Fixed it where it surfaced.

## Immediate Next Steps

1. **Release the lock:** `uv run gz obpi lock release OBPI-0.0.73-06-self-check-facade-regression-corpus`
   (this handoff is the register entry; the OBPI is completed, not abandoned).
2. **Git-sync #1:** `uv run gz git-sync --apply` to commit the completion governance
   edits, lock release, and marker cleanup.
3. **Reconcile:** `uv run gz obpi reconcile OBPI-0.0.73-06-self-check-facade-regression-corpus`
   to confirm receipt and brief agree.
4. **ADR status + Git-sync #2:** `uv run gz adr status ADR-0.0.73 --json`, then
   `uv run gz git-sync --apply`.
5. **Advance the campaign:** the next unchecked ADR-0.0.73 checklist items are #7
   (Evaluator truth-binding) and #8 (Fidelity-presence enforcement). Consult
   `docs/governance/build-to-1.0-campaign-2026-06-10.md` for the gate check.

## Pending Work / Open Loops

- **ADR-0.0.73 checklist item #7:** "Evaluator truth-binding" — replace
  `gz adr evaluate` dim-1/dim-2 heuristics in `src/gzkit/adr_eval_scoring.py` with
  decision-substance checks; add a seventh `shape-graded-not-substance` theater
  signature to the facade corpus.
- **ADR-0.0.73 checklist item #8:** "Fidelity-presence enforcement" —
  `gz validate --fidelity-presence` fails closed on block-less non-pool ADRs.
- **OBPI-02 correction — negative-control coverage debt:** the 33 bound steps in
  `_NEGATIVE_CONTROL_DEBT` (`src/gzkit/governance/trust_audits/qc_binding.py`) still
  need honest negative controls authored. That constant is the tracking surface;
  the self-check fails if a step leaves it unwired.
- ADR closeout for ADR-0.0.73 follows once #7 and #8 land.

## Verification Checklist

- [ ] `uv run gz obpi lock list` — confirm lock released after Step 1
- [ ] `uv run gz obpi reconcile OBPI-0.0.73-06-self-check-facade-regression-corpus` — confirm ATTESTED COMPLETED
- [ ] `git status --short` — confirm tree clean after git-sync #2
- [ ] `git branch --show-current` → `main`
- [ ] `uv run gz adr fidelity ADR-0.0.73-verification-layer-binding-audit` → 5 pass, 0 fail

## Evidence / Artifacts

- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-06-self-check-facade-regression-corpus.md` — completed brief with attestation
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/ADR-0.0.73-verification-layer-binding-audit.md` — parent ADR (Fidelity Assertions table + scorecard fix)
- `src/gzkit/governance/trust_audits/qc_binding.py` — green-by-emptiness guard + genuine qc-binding negative control
- `tests/governance/test_facade_regression_corpus.py` — six-signature corpus + behavioral-catch tests
- `tests/governance/test_qc_binding_self_check.py` — self-check + honest-accounting tests
- `tests/governance/fixtures/facade_corpus/` — six calibration fixtures

## Environment State

Python 3.13 + uv; branch `main` (ahead=0, behind=0 at commit `ce3413d5`). ARB
receipts from this session: `arb-ruff-e6a1f1f3d0a842d7bf5e006d6a536d88`,
`arb-step-typecheck-e2b947094c954e8dbf0427e0e7fbf455`,
`arb-step-unittest-d7ef711909cd469d8d87e9231356cafc` (6265/6265 pass) — all exit 0.
