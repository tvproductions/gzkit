---
mode: CREATE
adr_id: ADR-0.0.37
branch: main
timestamp: "2026-06-14T20:17:46Z"
agent: claude-code
obpi_id:
session_id:
continues_from: .gzkit/handoffs/20260614T201048Z-OBPI-0.0.37-23-complete.md
---

<!-- Handoff document for ADR-0.0.37 — created by claude-code at 2026-06-14T20:17:46Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

Session-end handoff for the ADR-0.0.37 corpus-rendition track. The
`gz-obpi-pipeline` run for **OBPI-0.0.37-23-invariant-tier** completed through
Stage 5: attested-complete (`gz obpi complete` → `attested_completed`, attestor
g0), lock released, two git-syncs pushed, reconcile PASS. The working
tree is **clean** and `origin/main` is in sync (`ahead=0 behind=0`).

No OBPI lock is currently held. No pipeline is active. This is a clean stopping
point — the next session starts fresh on the next ADR-0.0.37 OBPI or any
operator-directed campaign item.

## Important Context

- **OBPI-23 delivered the invariant-tier 0-Kelvin floor.** `src/gzkit/content/tier_policy.py`
  is now the single composer-consumable enforcement surface (`invariant_entries` +
  `assert_invariant_verbatim`). The OBPI-21 composer was re-pointed onto it
  (operator-approved path b); the previous duplicated inline check is gone. This
  surface is available for **OBPI-25** to consume.
- **`tier_policy` is deterministic** (stdlib + Pydantic, no LLM/network) — keep it that way;
  the brief's NEVER clause forbids introducing an LLM into the policy.
- **Layer-2 truth over frontmatter (Never #7).** OBPI brief statuses below are read from
  frontmatter (Layer-1). Before acting on any "completed" claim, verify with
  `uv run gz obpi status <OBPI-ID>` (ledger).
- **Parent ADR-0.0.37 is still Draft** (Attestation Block `0.0.37 | Pending`) — no
  ADR-level ratification exists; individual OBPI receipts are valid.
- **The campaign RULES sequencing** (operator ruling 2026-06-10): work the topmost
  unchecked campaign item whose gate is met via its governed path. Handoffs ADVISE;
  the campaign governs what is pulled next. See
  `docs/governance/build-to-1.0-campaign-2026-06-10.md`.

## Decisions Made

- **Decision:** Resolve the 21↔23 enforcement-wiring seam via path (b).
  **Rationale:** REQ-03 requires "single enforcement surface, no duplicated inline check";
  re-pointing the composer onto `tier_policy` was the only way to satisfy it. DO IT RIGHT 1a.
  **Alternatives rejected:** Leave composer untouched (would keep the forbidden duplicate).
- **Decision:** Seed the routing test's corpus JSONL directly rather than importing
  `corpus_store.append_entry`.
  **Rationale:** corpus_store is consumed, not edited; the import tripped a brief-reconcile
  neighborhood false-positive. Direct seeding keeps the brief scope honest and reconcile clean.
  **Alternatives rejected:** Add corpus_store to allowlist (misrepresents edit scope);
  `--accept-stale-reconciliation` override (over-ceremony for a fixture import).

## Immediate Next Steps

ADVISORY ONLY — present to the operator before acting.

1. Consult the campaign (`docs/governance/build-to-1.0-campaign-2026-06-10.md`) for the
   topmost unchecked item whose gate is met — the campaign rules sequencing, not this handoff.
2. If continuing the ADR-0.0.37 track, the remaining Draft OBPIs are **24, 25, 27**
   (26 is frontmatter-Completed — verify via `uv run gz obpi status` before relying on it).
   Suggested order per the ADR's sequencing notes: 24 (advisor-QC loop) → 25 (bullet-retention
   tier-scoped, coupled to the ADR-0.0.33 Invariant-1 amendment) → 27 (migration/disposition).
3. For any chosen OBPI, start with `/gz-plan-audit <OBPI-ID>` then `/gz-obpi-pipeline <OBPI-ID>`
   — do not skip the plan-audit gate.
4. Before Stage 2 of any OBPI, run `uv run gz brief reconcile <OBPI-ID>` (the Stage 1
   fail-close gate requires a fresh receipt).

## Pending Work / Open Loops

- **ADR-0.0.37 remaining Draft OBPIs:** 24 (advisor-panel info-retention QC loop, per
  ADR-0.0.39 llm-as-judge advisory-never-gating), 25 (ADR-0.0.33 bullet-retention
  tier-scoped validator + coupled Invariant-1 attested amendment), 27 (migration/disposition
  + doctrine refresh + orientation index).
- **OBPI-25 coupling (load-bearing):** the bullet-retention validator must flip to tier-aware
  (verbatim on invariant tier; advisor-QC receipt + attestation on compressed tiers) in the
  SAME commit-window as the ADR-0.0.33 Invariant-1 amendment — never a silent validator edit.
- **Parent ADR-0.0.37 closeout** awaits 24/25/27 completing (26 already Completed).
- **Known surface friction (not a blocker):** `validate_handoff_document`'s `_OBPI_ID_RE`
  rejects full OBPI slugs in handoff frontmatter; the full slug is nonetheless required for
  `find_handoff_for_release` release-pairing. Completion handoffs use the full slug by design.

## Verification Checklist

- [ ] `git status --short` is empty (tree clean) and `git branch --show-current` → `main`
- [ ] `uv run gz obpi lock list` shows no active locks
- [ ] `uv run gz obpi status OBPI-0.0.37-23-invariant-tier` → ATTESTED COMPLETED
- [ ] `uv run -m unittest tests.content.test_tier_policy tests.content.test_composer` passes
- [ ] `uv run gz check` is green before starting new work

## Evidence / Artifacts

- `.gzkit/handoffs/20260614T201048Z-OBPI-0.0.37-23-complete.md` — the OBPI-23 completion handoff this continues from
- `src/gzkit/content/tier_policy.py` — the invariant-tier enforcement surface delivered this session
- `tests/content/test_tier_policy.py` — 18 `@covers` tests across 5 classes
- `src/gzkit/content/composer.py` — re-pointed onto `tier_policy`
- `docs/design/adr/foundation/ADR-0.0.37-constitutional-invariant-composition/ADR-0.0.37-constitutional-invariant-composition.md` — parent ADR (checklist + sequencing)
- `docs/governance/build-to-1.0-campaign-2026-06-10.md` — the campaign that rules sequencing

## Environment State

Python 3.13, uv-managed. Platform win32, branch `main`, synced with `origin/main`.
Full suite at session end: 6149 pass / 1 skipped
(receipt `arb-step-unittest-a37f8b15eb97439299bd6f94703b08ec`).
