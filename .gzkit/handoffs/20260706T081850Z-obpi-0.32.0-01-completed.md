---
mode: CREATE
adr_id: ADR-0.32.0
branch: main
timestamp: "2026-07-06T08:18:50Z"
agent: claude-code
obpi_id: OBPI-0.32.0-01-ontology-model-and-purity
last_lock_event_timestamp:
last_commit_sha: db432ae6
session_id:
continues_from: .gzkit/handoffs/20260706T001255Z-adr-0.32.0-revised-to-go.md
---

<!-- Completion handoff for OBPI-0.32.0-01 — created by claude-code at 2026-07-06T08:18:50Z. This is the Stage-5 completion register entry that couples to the lock release (ADR-0.0.41). -->

## Current State Summary

`OBPI-0.32.0-01-ontology-model-and-purity` (ADR-0.32.0 Movement III Phase 2,
HULL) is **COMPLETED and operator-attested** (Gate 5 operator-verbatim, attestor
g0, "attest completed"). This is the first landed increment of ADR-0.32.0 — the
keel-up ontology model layer + Harness-Purity fence.

Delivered:
- `gzkit.ontology.model` — frozen `extra="forbid"` `OntologyNode`/`OntologyEdge`;
  closed `Ownership`/`Plane`/`LinkType`/`ObjectType` StrEnums; total
  `OBJECT_TYPE_REGISTRY` two-axis classification; `ontology_node_json_schema()`.
- `gzkit.ontology.purity.audit_ontology_purity` + `harness_purity_violations`.
- Committed `src/gzkit/schemas/ontology_node.json` (== model projection).
- Additive `gz validate --ontology-purity` scope (fails closed exit 3 on a
  product object in `ownership:harness`).
- 18 REQ-`@covers` tests (15 model + 3 purity); full suite 6799/6799.

Verification: lint/typecheck/mkdocs-strict clean (ARB receipts
arb-ruff-9189c035fa8141569407bdcffd697767,
arb-step-typecheck-3a406f22a1d84830b46cf857113e65d9,
arb-step-unittest-6f66d02a946b4b14a57057dc127118cf,
arb-step-mkdocs-bb8c15d81536413fbc3656b63e80f012). Independent Step 4b Codex
adversary: verdict REFUTED-WITH-CAVEATS, sole scope caveat resolved.

## Important Context

- **Attestation is recorded — this OBPI is DONE.** `gz obpi complete` succeeded
  (exit 0, `attested_completed`, attestor g0, operator-verbatim "attest
  completed"). The lock was released atomically by completion; no orphan.
- The `ontology_purity` scope is explicit-tier (not in default `gz check`).
- Phase-2 continuation (OBPI-02+) is operator-gated and NOT authorized by this
  handoff.

## Decisions Made

- `Plane` members reproduced verbatim from `.gzkit/governance/ontology.schema.json`
  `$defs.plane` (product/process), pinned by a runtime schema-load test
  (continuity-of-naming, parent ADR § Decision).
- Two-axis seating: corpus/GovZero-universal types → `(harness, process)`;
  gzkit product types → `product` ownership (CliVerb/Validator on the product
  plane, Skill/Chore on the process plane). Harness purity holds.
- Brief coherence fixes made in-flight under this OBPI (operator-ratified where
  they changed the declared contract): dropped a duplicate FAIL-CLOSED item to
  align req-count with the 6 acceptance REQs; declared `req_atomic:` for all 6
  REQs (each one indivisible RGR cycle); declared the coupled parity fence
  `tests/cli/test_validate_registry_parity.py` in the allowlist (DO IT RIGHT
  §1a); added the GHI #432 command-shape speculative marker for the planned
  OBPI-03 `gz ontology` verb reference.
- Pre-existing ADR-0.31.0 frontmatter drift (Completed→Validated) that the
  precomplete reconcile-idempotence gate surfaced was isolated into its own
  reconcile commit `db432ae6`, kept out of this OBPI's substantive diff.

## Immediate Next Steps

- None required for this OBPI — it is complete, attested, and being synced.
- Do NOT start OBPI-02+ without explicit operator authorization (phase-gated).

## Pending Work / Open Loops

- **ADR-0.32.0 remaining OBPIs (NOT started; operator-gated per phase sequencing):**
  OBPI-02 (networkx substrate + corpus projection), OBPI-03 (gz ontology operator
  verbs), OBPI-04 (ownership/plane doctrine + Boundary Invariants, Lite),
  OBPI-05 (OKF open-absorption), OBPI-06 (work-domain L2 schema — the one-way
  door, gated last), OBPI-07 (source-domain tree-sitter anchors). Per § Target
  Scope, the deferred breadth (05/06/07) is gated behind OBPI-02's fidelity
  fence being proven live.
- The `ontology_purity` scope is additive/explicit-tier; it is NOT in the
  default `gz check` scope. Later OBPIs / the parent ADR Fidelity Assertions
  bind against it.
- REQ-0.32.0-01-06 (STRUCTURAL-FENCE) is anchored at parent ADR
  `## Boundary Invariants` #4 and audited at ADR closeout, not per-OBPI.

## Verification Checklist

- [x] `gz obpi complete` exit 0 — `attested_completed`, attestor g0
- [x] 18 scoped REQ-`@covers` tests green; full suite 6799/6799
- [x] lint / typecheck / mkdocs --strict clean (ARB receipts below)
- [x] `gz validate --ontology-purity` exit 0 clean; exit 3 on real breach (Step 4b)
- [x] precomplete 8/8 READY; brief reconcile drift-free
- [x] Step 4b Codex adversary: REFUTED-WITH-CAVEATS, sole caveat resolved

## Evidence / Artifacts

- ARB receipts: `arb-step-unittest-6f66d02a946b4b14a57057dc127118cf` (6799/6799),
  `arb-ruff-9189c035fa8141569407bdcffd697767`,
  `arb-step-typecheck-3a406f22a1d84830b46cf857113e65d9`,
  `arb-step-mkdocs-bb8c15d81536413fbc3656b63e80f012`.
- Isolated reconcile commit: `db432ae6` (ADR-0.31.0 frontmatter).
- Brief: `docs/design/adr/pre-release/ADR-0.32.0-gzkit-ontology/obpis/OBPI-0.32.0-01-ontology-model-and-purity.md`.
- Source: `src/gzkit/ontology/`, `src/gzkit/schemas/ontology_node.json`.

## Notes

A handoff ADVISES; it does not authorize. Phase-2 continuation (OBPI-02+) remains
operator-gated. Do not begin sibling OBPI work without explicit operator
authorization.
