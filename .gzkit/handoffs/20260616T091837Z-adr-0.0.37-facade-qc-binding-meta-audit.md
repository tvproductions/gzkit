---
mode: CREATE
adr_id: ADR-0.0.37
branch: fix/adr-0.0.37-real-coherence-gate
timestamp: "2026-06-16T09:18:37Z"
agent: claude-code
obpi_id:
session_id:
continues_from:
---

<!-- Handoff document for ADR-0.0.37 — created by claude-code at 2026-06-16T09:18:37Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

A closeout-ceremony for ADR-0.0.37 (`/gz-adr-closeout-ceremony 0.0.37`) turned
into a facade exposure when the operator pressed on demo evidence. ADR-0.0.37's
CMS pipeline (corpus → compress → rendition → playback) was found to be a
facade: the operator called it *"a bullshit facade"* and *"embarrassing."* The
closeout is **paused / not attested**; the ADR is **BLOCKED (15/19)** after four
repudiations.

What was done and verified this session:

- **Repudiated 4 facade OBPIs** (`gz obpi repudiate`, cause
  `model-induced-fabrication`, attestor `g0`): `OBPI-0.0.37-02` (renderer — no
  Jinja2 registry projection exists), `-03` (drift validator — renders an EMPTY
  registry, tamper of `CIC-1.json` leaves it exit 0), `-21` (composer never run;
  0 `composition_candidate_emitted` events; rejects the committed rendition), `-22`
  (rendition `--rendition-freshness` compares **mtime not content**; a zero-byte
  content-restore flips it red while the real rendition sharing zero bytes with
  the corpus invariants passes green).
- **Filed GHI #623** (the facade work order) + cross-linked #615 (briefs
  597/600 bypass `BriefStructure` — the OBPI-05 REQ-07 cut) and #610 (repudiated
  OBPIs cannot currently be re-completed — constrains the corrective path).
- **Built a real content-coherence gate** `validate_rendition_floor_coherence`
  (`--rendition-floor-coherence`, in `gz check` default): every invariant-tier
  corpus entry must appear verbatim in the committed rendition. 6 unit tests;
  ruff/ty clean; full suite 6176 pass.
- **Recomposed AGENTS.md (operator-approved)**: the 7 operator doctrines were
  ENTIRELY ABSENT from the contract; now seated verbatim (29,649B under the
  30,000 cap), via the real `gz content compose` flow
  (`composition_candidate_emitted` 0 → 3). Green chain: floor-coherence +
  invariant-coherence + freshness + budget all exit 0.
- **Committed** the gate + recompose as `8f754c45` on branch
  `fix/adr-0.0.37-real-coherence-gate` (pre-commit hooks incl. gitleaks passed).
- **Amended the Magna Carta**: added the facade + QC-binding meta-audit as a new
  entry in Phase 0's hot-store (its emergency model is already correct; sibling to
  #615); B.1's false "16/19 verified" corrected to REPUDIATED-IN-PART / PAUSED.

Then ran the meta-audit **design pass** (`gz-design`): scope locked (own
foundation ADR, generalizes the mechanical-defenses mechanism onto the QC layer,
absorbs `ADR-pool.obpi-pipeline-dispatch-attestation`), design approved, and the
design forcing-functions interview drafted. A pre-mortem surfaced a real
refinement now baked in: **detection must be behavioral, not declarative** — each
registered QC step ships a negative-control fixture it MUST fail on, and
`--qc-binding` runs it (static heuristics + self-registration alone can be gamed).
**ADR-0.0.73** is the allocated next-free foundation ID; **NOT yet booked**
(paused for context). The full locked design is in § Important Context below so
booking next session is mechanical.

Commits on branch `fix/adr-0.0.37-real-coherence-gate`: `8f754c45` (gate +
recompose), `788619d7` (governance capture: Magna Carta Phase 0 entry + B.1
correction + this handoff + insight). Last action: this handoff + Magna Carta
alignment for the booked meta-audit design.

## Important Context

- **The ADR is GOOD; the implementation betrayed it.** ADR-0.0.37
  § Decision Re-Alignment (2026-06-03) faithfully captures the operator's model:
  append-only corpus (source) → compress compressible-tier toward a setpoint
  (LLM judgment: drop/combine/rewrite) → committed rendition → deterministic
  playback; invariant-tier verbatim at every setpoint. The implementation
  hollowed every stage: corpus never enriched (8 entries; real content stayed in
  the static template `.gzkit/templates/agents.md`); the composer
  (`src/gzkit/content/composer.py`) *validates* a hand-authored candidate instead
  of *compressing from the corpus*; the rendition was hand-authored (OBPI-26
  interim) and disconnected; the "composer regenerates later" promise was prose
  with no tracker.
- **Architecture vocabulary (operator-refined this session):** the full corpus =
  a **dense model**; the compressed rendition = a **quantized model**;
  invariants = the layers **never quantized** (mixed-precision floor);
  **temperature** = the **favoring/eviction policy** when compression forces
  choices (LLM-temperature sense: favor some content over others). **Alignment** =
  the bounded surface staying faithful to the unbounded corpus's intent. KEY
  TENSION the ADR under-captured: the corpus grows unbounded but surfaces are
  byte-capped → it is a **cache** (corpus = backing store, surface = cache,
  invariants = pinned, temperature = eviction policy, quantization = size bound).
  IBM refs the operator anchored on: quantization, llm-alignment, llm-temperature.
- **`heavy`/`lite` are LANE (task gate-rigor) vocabulary — NEVER surface
  density.** Using them as the rendering axis is drift (operator: *"bullshit and
  drift"*). The whole `_Temperature`/`temperature_for`/`content_type_temperatures`
  chain borrowed lane words. Course-correction logged in
  `.gzkit/insights/agent-insights.jsonl`.
- **The density dial was retired on a misdiagnosis.** `render()` in
  `src/gzkit/content/render/pipeline.py` validates temperature but projects
  nothing (OBPI-27 retired `_project_for_temperature`). Reason it looked "inert":
  ALL 20 template pillars are `tier=lite`, so projection was a no-op — the content
  was never tiered. The mechanism was never broken; its input was uniform. Do NOT
  restore mechanical pillar-dropping: the ADR says *"dropping whole sections is
  not preferred"* — compression is condensing language WITHIN sections (semantic).
- **The QC machinery is itself theater for this failure class (the root defect).**
  `gz adr audit` (`src/gzkit/commands/adr_audit.py`) and `gz obpi audit`
  (`src/gzkit/commands/obpi_audit_cmd.py`) verify receipt-presence + REQ-coverage,
  never truth. A tautology (`fixture == fixture`) satisfies coverage. The
  `gz-adr-audit` skill's `trust_model` is literally *"does NOT re-verify
  evidence"*; its one fidelity step ("Demonstrate Value") is agent-written prose
  graded by nothing, and the dispatch enforcement lives in an UNPROMOTED pool ADR
  (`ADR-pool.obpi-pipeline-dispatch-attestation`). Nothing in gzkit mechanically
  holds an ADR's thesis against the running system. The only thing that caught
  the facade was operator skepticism.
- **#610 blocks re-completion** of the 4 repudiated OBPIs ("Brief is already
  Completed"). The real rebuild must route through NEW corrective OBPIs, or #610
  must be fixed first.
- `gz check` has PRE-EXISTING unrelated red: OBPI-0.0.64-04 REQ-coverage / TASK
  seq=01. Not touched this session.

### LOCKED DESIGN — ADR-0.0.73 Verification-Layer Binding Audit (to book next session)

Foundation kind, **heavy** lane, slug `verification-layer-binding-audit`, next-free
`0.0.73` (highest existing foundation = 0.0.72). Design operator-approved this
session; book via `gz-adr-create` → `register-adrs` → 6 OBPIs → `gz-adr-evaluate`
→ seat the ID in Magna Carta Phase 0 (replace the "Pending: design → foundation
ADR" line).

**Intent:** gzkit's premise (make vibing structurally inert) is only as real as its
verification layer, and nothing checks the checkers. Root defect (GHI #623 facade):
`gz adr audit`/`gz obpi audit` verify receipt-presence + REQ-coverage, never truth;
the `gz-adr-audit` trust_model literally "does NOT re-verify"; "Demonstrate Value"
is agent-prose graded by nothing. Foundation by the invariance test — without a
bound verification layer, nothing gzkit attests can be trusted.

**Decision (one mechanism):** (1) every QC step self-registers into a registry,
classified bound / advisory / unenforced, with the registry **derived from what
`gz check` actually runs**; (2) `gz validate --qc-binding` (in `gz check`,
fail-closed exit 3) flags any step whose name/docstring claims enforcement its code
doesn't deliver — **detected behaviorally via a negative-control fixture each step
must fail on** (NOT static heuristics alone), plus theater signatures calibrated on
the ADR-0.0.37 facade (mtime-where-name-says-content, empty-input, copy-vs-self,
fixture-only, skip-if-PASS, prose-graded-by-nothing); (3) every ADR Decision ships
a `## Fidelity Assertions` block (runnable commands exercising its thesis against
the real system); `gz adr fidelity <ADR>` **runs** them — one standalone gate, BOTH
closeout and audit invoke it, replacing the prose Demonstrate Value step; (4)
absorbs `ADR-pool.obpi-pipeline-dispatch-attestation`; **must pass its OWN check**.

**Data model (Pydantic frozen):** `QCStep{id,name,kind,subject,binding,wired_into[],theater_flags[],enforcement_locus}`;
`FidelityAssertion{adr_id,claim,command,expected_exit,observed,result}`.

**OBPIs:** 01 registry+classifier model; 02 `--qc-binding` scope w/ behavioral
negative-control + theater detection (in `gz check`); 03 `## Fidelity Assertions`
schema + `gz adr fidelity` gate; 04 closeout/audit repoint onto it; 05 absorb
dispatch-attestation; 06 self-check + facade regression corpus (one fixture per
theater signature). **Scope-minimization (if half the time): ship 03+04 first** —
alone they would have caught the ADR-0.0.37 facade.

**Rejected alternatives:** (a) extend `contract-surface-mechanical-defenses` —
that family's subject is content prose, not the checkers (one level up); (b) fourth
parallel mechanical-defenses sibling — this is the general law, not a sibling; (c)
fidelity-running as prose in both ceremonies — duplicated unbound prose is the very
defect; one bound gate invoked by both; (d) keep trusting receipts — that IS the
defect.

**Reversibility:** the validate scope is two-way; the `## Fidelity Assertions`
requirement on every ADR Decision is **one-way** (removing it re-opens the facade)
— the load-bearing commitment. **Downstream forced:** the real ADR-0.0.37 rebuild
must ship fidelity assertions; a back-fill-and-recheck sweep for already-VALIDATED
foundation ADRs (its own chore/ADR); the mechanical-defenses family may fold under
this as its general law.

## Decisions Made

- **Decision:** Do not attest the closeout; repudiate the 4 facade OBPIs.
  **Rationale:** Attesting would make the agent the next link in the deception;
  the gates the receipts rest on are theater. **Alternatives rejected:** reject &
  restart the whole ADR (would discard ~12 genuinely-real OBPIs incl. the working
  brief-reconcile half); attest Completed-Partial (still rubber-stamps theater).
- **Decision:** Fix scope = Option A (real floor gate + operator-attested
  recompose) now; Option B (full registry→surface generator) deferred to #623.
  **Rationale:** The floor gate is the structural witness needed under every path
  and is verifiable today. **Alternatives rejected:** build the full engine first
  (multi-session; blocks the immediate anti-deception fix).
- **Decision:** Route the gate as a direct fix under GHI #623, not a new OBPI.
  **Rationale:** Operator's captured canon: *"GHIs are AUTHORIZED for direct
  repair, always … no need for more ceremony."* **Alternatives rejected:** mint a
  corrective OBPI (the `@covers` decorator hard-requires a minted REQ; the canon
  prefers direct-fix for GHI-tracked repair).
- **Decision:** The root fix is a QC-binding meta-audit (anti-theater), sequenced
  BEFORE the corpus rebuild. **Rationale:** Without it, any rebuild rots back into
  facade through the same unguarded seam (operator: *"we clearly need a meta skill
  that ensures that these qc steps exist and support the pipeline"*).
  **Alternatives rejected:** rebuild the corpus first (leaves the verification
  layer theater; the next facade would again survive to VALIDATED).
- **Decision (Magna Carta amendment):** Phase 0's emergency hot-store model is
  already correct; the facade + QC-binding meta-audit is a new entry dropped into
  it, sibling to #615 (meta-audit binds QC steps as #615 binds data objects).
  **Rationale:** Operator ruling — a hot store for emergencies already has the
  right model; new emergencies get added, not a re-purpose. **Alternatives
  rejected:** retitle/reframe Phase 0 (over-reach, corrected this session); leave
  B.1's "verified" claim standing (it is theater in canon).

## Immediate Next Steps

> ADVISORY ONLY — present to the operator and obtain authorization before acting.

1. **Book ADR-0.0.73** (design DONE + approved — see § Important Context § LOCKED
   DESIGN): `gz-adr-create` (foundation, heavy, slug `verification-layer-binding-audit`,
   6 OBPIs) → `gz register-adrs ADR-0.0.73` → co-create the 6 OBPIs → `gz register-adrs
   ADR-0.0.73 --all` → `gz-adr-evaluate ADR-0.0.73` → **seat the ID in Magna Carta
   Phase 0** (replace the "Pending: design → foundation ADR; GHI to be filed" line
   with `ADR-0.0.73`). The forcing-functions interview is drafted; record via
   `gz interview adr --from`.
2. **File the root-defect GHI** via `/ghi-author` (audit verifies presence not
   truth; fidelity step is unbound prose) and route it `superseded` to ADR-0.0.73.
3. **Then re-open the real ADR-0.0.37 corpus rebuild** behind the meta-audit:
   enrich the corpus (migrate template content → addressed tiered entries =
   the dense model); wire corpus → render; make the composer GENERATE the
   quantized rendition by semantic compression toward each surface's setpoint
   (invariants verbatim); purge the `heavy`/`lite` lane-vocabulary drift from the
   content/render chain. Route via NEW corrective OBPIs (or fix #610 first).
4. **Resolve the open design fork** before building the composer: is
   eviction-under-monotonic-growth a first-class declared favoring policy
   (temperature) — implying a tier between invariant (pinned) and compressible
   (condense), e.g. *evictable* — or is operator-attested per-rendition judgment
   enough? (Surfaced this session; not yet ruled.)
5. **Commit the Magna Carta amendment** (currently uncommitted) on the working
   branch.

## Pending Work / Open Loops

- **GHI #623** (facade work order) — open; the corpus rebuild discharges it.
- **Root-defect GHI** (QC-audit-is-theater) — to be filed (step 2).
- **#610** — repudiated OBPIs not re-completable; blocks re-completing 02/03/21/22.
- **#615** — schema-enforcement bypass (Phase 0 sibling); independent track.
- Partial-facade REQs to correct: OBPI-23 REQ-02 (tautological non-verbatim
  survival test), OBPI-26 REQ-05 (rides the 22 facade).
- Honesty caveats to track: OBPI-24 info-retained-per-byte is agent-asserted,
  never machine-computed; OBPI-25 compressible-tier branch dormant (0 of 54
  enforced bullets route to it).
- The mtime `--rendition-freshness` gate still exists alongside the real floor
  gate — superseded in practice; remove/replace in a follow-up.
- The `ConstitutionalInvariant` registry (`.gzkit/invariants/`, CIC-1/2 +
  foundation-registers + skill-first) is orphaned (zero consumers); CIC-1 makes a
  false "AGENTS.md is rendered from the registry" claim — repudiate/rewrite during
  the rebuild.
- Branch `fix/adr-0.0.37-real-coherence-gate` is unmerged; closeout ceremony
  state persists at `.gzkit/ceremonies/`.

## Verification Checklist

- [ ] Branch matches: `git branch --show-current` → `fix/adr-0.0.37-real-coherence-gate`
- [ ] Gate green: `uv run gz validate --rendition-floor-coherence` exits 0
- [ ] Invariant floor present: the 7 corpus invariants appear verbatim in `AGENTS.md`
- [ ] `uv run gz adr status ADR-0.0.37` shows 15/19, closeout BLOCKED
- [ ] Repudiations in ledger: `grep obpi_completion_repudiated .gzkit/ledger.jsonl`
- [ ] `git log --oneline -1` shows `8f754c45` (gate + recompose)
- [ ] Magna Carta Phase 0 amendment + B.1 correction present in
      `docs/governance/build-to-1.0-campaign-2026-06-10.md`
- [ ] Pre-existing unrelated red noted: OBPI-0.0.64-04 (not this session's)

## Evidence / Artifacts

- `src/gzkit/governance/trust_audits/rendition_floor_coherence.py` — the real content-coherence gate
- `tests/governance/test_rendition_floor_coherence.py` — its 6 semantics-asserting tests
- `src/gzkit/commands/validate_cmd.py` — gate wiring (opt-in scope + runner + policy-breach)
- `src/gzkit/quality.py` — `run_rendition_floor_coherence_audit` (gz check)
- `src/gzkit/commands/quality.py` — gz check pipeline registration
- `docs/user/manpages/validate.md` — `--rendition-floor-coherence` manpage entry
- `.gzkit/renditions/AGENTS.md/claude.md` — recomposed rendition (29,649B, floor satisfied)
- `AGENTS.md` — rendered surface now carrying the 7 operator doctrines verbatim
- `docs/governance/build-to-1.0-campaign-2026-06-10.md` — Magna Carta (Phase 0 amendment + B.1 correction)
- `docs/design/adr/foundation/ADR-0.0.37-constitutional-invariant-composition/ADR-0.0.37-constitutional-invariant-composition.md` — the faithful design the implementation betrayed
- `src/gzkit/content/render/pipeline.py` — jinja2 render; projection retired (temperature projects nothing)
- `src/gzkit/content/composer.py` — composer that validates instead of generating
- `src/gzkit/governance/trust_audits/rendition_freshness.py` — the mtime theater gate
- `.gzkit/corpus/AGENTS.md.jsonl` — the under-enriched corpus (8 entries)
- `.gzkit/insights/agent-insights.jsonl` — course-correction record (heavy/lite drift)
- `.claude/skills/gz-adr-audit/SKILL.md` — audit skill (`trust_model: does NOT re-verify`)
- `src/gzkit/commands/adr_audit.py` — ADR audit (coverage + receipts, no thesis fidelity)
- `src/gzkit/commands/obpi_audit_cmd.py` — OBPI audit (same hole)
