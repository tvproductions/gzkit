---
mode: CREATE
adr_id: ADR-0.0.37
branch: main
timestamp: "2026-06-14T09:00:00Z"
agent: claude-code
obpi_id: OBPI-0.0.37-21-authoring-time-compression-composer-tool-skill
session_id: 57fa1640
continues_from: .gzkit/handoffs/20260613T232200Z-OBPI-0.0.37-21-stage1-blocked-reconcile-bug.md
last_lock_event_timestamp: "2026-06-14T08:48:45.300676+00:00"
last_commit_sha: 56da365776ca5bdabb9dc83928e4c11cbdd05a30
---

<!-- Handoff document for ADR-0.0.37 OBPI-21 — created by claude-code at 2026-06-14T09:00:00Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

## Current State Summary

OBPI-0.0.37-21-authoring-time-compression-composer-tool-skill was completed this
session through the full `gz-obpi-pipeline` (Stages 1→5). This session resumed from
the prior handoff that left Stage 1 BLOCKED on a brief-reconcile engine bug; that
bug was fixed in-flight, then the OBPI was implemented, verified, attested, and
completed.

- **Stage 1 unblock (in-flight fix):** `src/gzkit/governance/brief_reconcile.py`
  `_REQ_LINE_RE` regex changed from `r"REQUIREMENT:"` to
  `r"REQUIREMENT\s*(?:\[\w+\])?\s*:"` so it matches the `REQUIREMENT [BEHAVIOR]:` /
  `REQUIREMENT [SUPPORT]:` kind-tag format (ADR-0.0.59). Covering test added at
  `tests/governance/test_brief_reconcile.py::TestReqCountDimension.test_delta_zero_on_kind_tag_format`.
  After the fix, `gz brief reconcile` showed `req_count: 0, has_drift: false`.
- **Plan-audit:** PASS receipt at
  `.claude/plans/.plan-audit-receipt-OBPI-0.0.37-21-authoring-time-compression-composer-tool-skill.json`
  (plan `.claude/plans/OBPI-0.0.37-21-compose-plan.md`).
- **Stage 2 implement:** `gz content compose` deterministic compress stage — new
  `composer.py` engine (NO LLM, NO network), `rendition.py` models, `compose.py` CLI
  handler, `composition_candidate_emitted` event (events + factory + schema +
  NO_GRAPH_IMPACT waiver), `gz-content-compose` skill, manpage, runbook, BDD.
- **Stage 3 verify:** ruff / typecheck / unittest / mkdocs all exit 0; `gz check`
  all-green; `gz covers` shows `behavior_uncovered_reqs: 0` (REQs 06/07/08 are
  SUPPORT, proof channel ledger + structural validators).
- **Stage 4:** operator attested "attest completed" (Gate 5, universal ADR-0.0.36).
- **Stage 5:** `gz obpi complete` succeeded — runtime_state `attested_completed`,
  attestor g0, `attestation_type: operator-verbatim-conversational`.

This handoff IS the required register entry (ADR-0.0.41 token-block discipline) for
the OBPI-0.0.37-21 lock; release follows immediately after it is written.

## Important Context

- **Two completion overrides applied (both surfaced + justified):**
  - `--accept-stale-reconciliation`: reconcile `missing_in_brief` flagged two
    READ-ONLY consumed deps imported only by this OBPI's tests, neither modified
    (`src/gzkit/content/corpus_store.py` via `load_corpus`; `src/gzkit/traceability.py`
    `@covers` decorator — the engine's own `_compute_missing_in_brief` docstring
    intends to exclude `traceability` as a false positive, but the neighborhood
    filter catches it because allowlisted `events.py`/`ledger_events.py` sit directly
    under `src/gzkit/`).
  - `--accept-security-floor`: the only registered security surface touched is
    `src/gzkit/ledger_events.py` (`ledger_integrity`); the change is a pure-additive
    `composition_candidate_emitted_event` factory mirroring `corpus_entry_appended_event`
    exactly — no credential/subprocess/crypto/auth logic.
- **behave waiver:** REQs 06/07/08 are SUPPORT-kind; a waiver was registered in
  `data/behave_coverage_waivers.json` (rationale key
  `obpi-0.0.37-21-req060708-support-kind-ledger-skill-docs-validators`).
- **Distribution manifest regenerated** to include the new `gz-content-compose` skill.

## Decisions Made

- **Decision:** Fixed the brief-reconcile `_REQ_LINE_RE` bug as a direct in-flight
  fix rather than a separate OBPI.
  **Rationale:** It was the documented Stage-1 blocker for this OBPI; the fix is
  2 source files (regex + test), in-flight trigger, unit-covered — squarely within
  AGENTS.md § Defect-fix routing direct-fix thresholds.
- **Decision:** Used `--accept-stale-reconciliation` rather than amending the brief
  allowlist to add the two read-only deps.
  **Rationale:** The allowlist is the OBPI's MODIFICATION scope; both deps are
  read-only consumed (git status clean), so adding them would misrepresent scope.
- **Decision:** Used `--accept-security-floor` rather than declaring `sensitivity: security`.
  **Rationale:** The ledger_events.py change is structurally additive (a new event
  factory) with no security-relevant logic; GHI #462 names this exact case.

## Immediate Next Steps

<!-- ADVISORY ONLY — present to operator, await authorization before executing. -->

1. Complete the pipeline Stage 5 tail (this session): lock release (this handoff
   is the register entry), marker cleanup, git-sync #1, reconcile, ADR status
   refresh, git-sync #2.
2. Continue ADR-0.0.37 build-out — remaining OBPIs (22 playback, 23 invariant tier,
   24 advisor-QC) per the campaign sequencing.

## Pending Work / Open Loops

- ADR-0.0.37 is not yet closeout-ready; sibling OBPIs (22/23/24) remain.
- Consider a follow-up to the brief-reconcile neighborhood filter so
  `traceability.py` (and cross-cutting test infra generally) is excluded as the
  engine's docstring already intends — would remove the read-only-import false
  positive class without an override.

## Verification Checklist

- [ ] `uv run gz obpi reconcile OBPI-0.0.37-21-authoring-time-compression-composer-tool-skill` agrees (receipt ↔ brief)
- [ ] `uv run gz adr status ADR-0.0.37 --json` reflects OBPI-21 attested_completed
- [ ] Branch: `git branch --show-current` → `main`
- [ ] `uv run gz check` all-green

## Evidence / Artifacts

- Brief: `docs/design/adr/foundation/ADR-0.0.37-constitutional-invariant-composition/obpis/OBPI-0.0.37-21-authoring-time-compression-composer-tool-skill.md` (status: Completed)
- `src/gzkit/content/composer.py`, `src/gzkit/content/rendition.py`, `src/gzkit/commands/content/compose.py`
- `.gzkit/skills/gz-content-compose/SKILL.md`, `docs/user/skills/gz-content-compose.md`
- `tests/commands/test_content_compose.py`, `tests/content/test_composer.py`
- `features/content_compose.feature`, `features/steps/content_compose_steps.py`
- ARB receipts: `arb-step-unittest-9811d24e6ddf41d9a2655d3acd9816e4`, `arb-ruff-82e7f018e982449b81abf3e75663eee9`, `arb-step-typecheck-ad004027fe0a4591bcc80e0fd16cc6c7`, `arb-step-mkdocs-0aa58dd44e1c443b855ed06042338827`

## Environment State

- Python 3.13, uv, macOS (Darwin). Full unittest suite exit 0.
