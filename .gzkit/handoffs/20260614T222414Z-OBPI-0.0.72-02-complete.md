---
mode: CREATE
adr_id: ADR-0.0.72
branch: main
timestamp: "2026-06-14T22:24:14Z"
agent: claude-code
obpi_id: OBPI-0.0.72-02-handoff-frontmatter-reconcile
session_id:
continues_from: .gzkit/handoffs/20260614T201048Z-OBPI-0.0.37-23-complete.md
last_lock_event_timestamp: "2026-06-14T21:01:58.960447+00:00"
last_commit_sha: 6dbf601d47fcc3558962319d6a514a9d3f1c8fe8
---

<!-- Handoff document for ADR-0.0.72 — created by claude-code at 2026-06-14T22:24:14Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at every freshness level, Fresh included — present the advised steps, obtain
explicit operator authorization before executing any of them, and treat the
human-as-final-witness doctrine as binding.

## Current State Summary

OBPI-0.0.72-02-handoff-frontmatter-reconcile is **attested-complete** (`gz obpi
complete` → `attested_completed`, attestor g0, Heavy/foundation Gate 5,
`--accept-security-floor` for the additive `run_handoff_document_audit` runner in
the security-registered `quality.py`). This handoff is the Stage-5 completion
register entry; it itself carries the Sub-Invariant 2 min-info fields the OBPI
just made gate-enforced.

OBPI-72-02 reconciled `HandoffFrontmatter` (explicit-superset model: slug
`obpi_id` + min-info + degenerate/reaping fields, typo-defense intact), made
`validate_handoff_document` shape-aware (register entries a distinct doc class),
fixed `_write_reaping_handoff`'s `adr_id` derivation for full-slug locks, and
gate-wired the validator into `gz check`. Spec-review round-2 PASS after a
masked-REQ-05 critical (synthetic-doc validation) was fully fixed to drive the
real writers. The 5 pre-existing `--lock-handoff-coupling` violations were
repaired (RED→GREEN). Full sweep 6157 pass.

## Important Context

- **`--lock-handoff-coupling` is now a live `gz check` gate** (REQ-06). Every
  release-paired session handoff from here on MUST carry `last_lock_event_timestamp`,
  `last_commit_sha`, `branch`, and a `## Decisions Made` section, or `gz check`
  fail-closes. The recurrence-prevention is OBPI-0.0.65-02's `scaffold_handoff`
  (Phase C) which pre-fills these deterministically.
- **Shape-awareness:** handoffs with frontmatter `abandoned: true` are register
  entries (relaxed contract); CREATE/RESUME handoffs keep the full 7-section +
  referenced-file contract.
- **`gz check` is NOT fully green** — two pre-existing, campaign-orthogonal
  blockers remain (see Pending Work).

## Decisions Made

- **Decision:** Resolve the spec-review critical via the FULL coherence fix
  (operator-approved) rather than narrowing the gate.
  **Rationale:** REQ-05 demanded real emitted documents round-trip clean; the
  writers (read-only) emit terse register entries that failed the full-document
  validator. Shape-awareness + the adr_id fix close it genuinely.
  **Alternatives rejected:** narrowing the gate (leaves writers non-conforming);
  re-interpreting REQ-05 as frontmatter-only (the masking the review caught).
- **Decision:** Repair all 5 pre-existing `--lock-handoff-coupling` violations now
  (operator-approved FULL WIN) to restore the gate.
  **Rationale:** OBPI-72-02's model fix is what lets the min-info fields validate;
  repairing made the newly-live gate green.
  **Alternatives rejected:** fix only mine + defer (leaves the gate RED).
- **Decision:** Track (not fix-here) the sibling `list_locks` adr_filter bug, the
  reconcile traceability heuristic gap, and the pre-existing behave failures.
  **Rationale:** surgical discipline — they are out of REQ scope / campaign-orthogonal.

## Immediate Next Steps

ADVISORY ONLY — present to the operator before acting.

1. Continue the campaign Phase A: **OBPI-0.0.72-03** (insight-record-reconcile) and
   **OBPI-0.0.72-04** (security-floor-overridden-event) — both adapters, independent.
2. Then Phase B: **OBPI-0.0.72-01** (writer-model-roundtrip-validator), after the adapters.
3. Then Phase C: **OBPI-0.0.65-02** (programmatic-api, REQ-07 removed → owned by this OBPI;
   `scaffold_handoff` fixes the systemic min-info-field gap) → **65-03** → **65-04** → **65-05**.
4. Decide on the two pre-existing `gz check` blockers (Pending Work) — fix or formally track.

## Pending Work / Open Loops

- **`gz check` two pre-existing, campaign-orthogonal blockers (operator decision pending):**
  (a) 3 behave scenario failures in `features/constitutional_invariants.feature` (ADR-0.0.37
  composition fixtures — the *real* `gz validate --invariant-coherence` passes; fixture-level);
  (b) behave Windows cp1252 `✓` encoding crash under stdout redirection (environmental tooling).
- **Sibling bug (tracked):** `list_locks` adr_filter at `src/gzkit/lock_manager.py:189` has the
  same `rsplit("-",1)` full-slug bug fixed in `_write_reaping_handoff`; route as a direct-fix
  between OBPIs (proper test home is `tests/test_lock_manager.py`).
- **Reconcile heuristic gap (tracked):** `_compute_missing_in_brief` flags `gzkit.traceability`
  as a false positive for top-level-`src/gzkit/` OBPIs; add an explicit cross-cutting exclusion.
- **REQ-07 removal pending in OBPI-0.0.65-02 brief:** I authored 65-02 with a REQ-07 (obpi_id
  regex) now owned by this OBPI; remove it before building 65-02.
- ADR-0.0.72 closeout awaits OBPIs 01/03/04; ADR-0.0.65 awaits 02/03/04/05.

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.0.72-02-handoff-frontmatter-reconcile` → ATTESTED COMPLETED
- [ ] `uv run gz validate --lock-handoff-coupling` → exit 0 (GREEN)
- [ ] `uv run -m unittest tests.test_handoff_frontmatter_coherence` → 8/8 pass
- [ ] `uv run gz obpi lock list` → no active lock on this OBPI
- [ ] `git branch --show-current` → main

## Evidence / Artifacts

- `src/gzkit/handoff_validation.py` — explicit-superset model + widened `_OBPI_ID_RE` + shape-aware `validate_handoff_document`
- `src/gzkit/quality.py` — `run_handoff_document_audit` gate
- `src/gzkit/commands/quality.py` — gate registration in `_build_check_steps`
- `src/gzkit/lock_manager.py` — `_write_reaping_handoff` adr_id semver derivation fix
- `tests/test_handoff_frontmatter_coherence.py` — 8 `@covers` tests (real-writer round-trip + shape-awareness)
- `data/behave_coverage_waivers.json` — OBPI-level behave waiver
- `docs/design/adr/foundation/ADR-0.0.72-meta-governance-coherence/obpis/OBPI-0.0.72-02-handoff-frontmatter-reconcile.md` — completed brief

## Environment State

Python 3.13, uv-managed. Platform win32, branch `main`. Full unittest sweep:
6157 pass / 1 skipped (receipt `arb-step-unittest-e7bc1c9b05d74eb8abe0f2d101cb8acc`).
