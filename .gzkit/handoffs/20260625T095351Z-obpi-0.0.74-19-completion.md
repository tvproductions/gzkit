---
mode: CREATE
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-25T09:53:51Z"
agent: claude-code
obpi_id: OBPI-0.0.74-19-floor-wiring
last_lock_event_timestamp: "2026-06-25T09:19:57.525456+00:00"
last_commit_sha: d9606b06
session_id: 8b3568d5-ef34-472d-af58-ee7321ed5a30
continues_from: .gzkit/handoffs/20260625T084000Z-obpi-0.0.74-18-completion.md
---

<!-- Handoff document for ADR-0.0.74 — created by claude-code at 2026-06-25T09:53:51Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

OBPI-0.0.74-19-floor-wiring is **attested-complete** (operator `g0`, Gate-5
verbatim "attest completed", 2026-06-25). The enforcement-claim meta-validator
runner (OBPI-16) is now wired into the floor as the LAST organ of the §5
mechanism, landing only after OBPI-17 (gate5 floor migration, released
2026-06-25T01:23) and OBPI-18 (structural-fence proof upgrade, released
2026-06-25T08:39) completed — strict-no-debt sequence honored.

What landed:
- `src/gzkit/quality.py` — `run_enforcement_floor_audit(project_root)` wrapper;
  calls `run_meta_validator(root=None)` (READ-ONLY, no ledger mutation on clean).
- `src/gzkit/commands/quality.py:450` — `("Enforcement floor", run_enforcement_floor_audit)`
  registered in `_build_check_steps()`.
- `src/gzkit/hooks/guards.py` — `_run_enforcement_floor(root)` wired into `main()`
  (pre-push guard), READ-ONLY on clean.
- `src/gzkit/governance/trust_audits/_qc_negative_controls.py` — `("enforcement-floor", ...)`
  NC table entry + `_build_enforcement_floor()` synthetic-FACADE fixture.
- `src/gzkit/governance/trust_audits/_qc_nc_entrypoints.py` — `_ep_enforcement_floor()` entrypoint.
- `src/gzkit/qc_binding.py` — `"Enforcement floor"` classification entry.
- `tests/governance/test_enforcement_floor_wiring.py` — 10 tests (created).
- `tests/commands/test_skills.py` — stub added to existing gz-check test.

Last action: `gz obpi complete` succeeded (exit 0), `--accept-security-floor`
applied for the unfilled security-scan canonical slot (GHI #462). Remaining
Stage-5 steps (lock release, marker cleanup, git-sync ×2, reconcile, ADR status)
are in progress in this same session.

## Important Context

- **Security-scan floor is unfilled.** `CANONICAL_STEP_COMMANDS["security"]` is `[]`
  (reserved for `pool.agentic-security-review` promotion). Any `sensitivity: security`
  brief completion must pass `--accept-security-floor 'REASON'` until that slot is
  filled. This OBPI's override reason: additive wiring of an existing read-only
  audit, no new subprocess call site at the three overlapped surfaces.
- **ADR checklist boxes are NOT checked per-OBPI.** OBPI-17, -18, -19 are all
  complete but their `- [ ]` boxes in the ADR Feature Checklist (lines 267-269)
  remain unchecked — they are checked at ADR closeout, not per-OBPI. Follow this
  convention; do not check item 19's box now.
- **The §5 enforcement-claim chain (items 15-19) is now COMPLETE.** OBPI-15
  (@enforces decl), -16 (runner), -17 (gate5 floor migration), -18 (fence-proof
  upgrade), -19 (floor wiring) all have completion receipts in the ledger.
- The NC for the floor step is "enforcement-floor" — it plants a synthetic FACADE
  claim and asserts the meta-validator detects it (`facade_count + test_bug_count > 0`).
  This makes the floor that proves enforcement claims itself enforcement-claim-covered.

## Decisions Made

- **Decision:** `run_enforcement_floor_audit` always passes `root=None` to `run_meta_validator`.
  **Rationale:** brief mandates READ-ONLY on a clean run; `root=None` is the
  runner's no-ledger-mutation branch.
  **Alternatives rejected:** passing `project_root` (would emit ledger receipts on
  every `gz check` / pre-push — violates the READ-ONLY contract).
- **Decision:** Register the enforcement-floor NC in the existing `_QC_NEGATIVE_CONTROL_TABLE`
  rather than a new self-registration file like `qc_binding.py`.
  **Rationale:** the table is the canonical home; a new file would duplicate the
  import-chain machinery without justification (simplicity-first).
  **Alternatives rejected:** new `enforcement_floor.py` with its own `register_*` fn.
- **Decision:** Added a runtime NC test (`test_enforcement_floor_nc_detects_facade_at_runtime`)
  exercising fixture→entrypoint→detection.
  **Rationale:** Stage-4 adversary (Codex) named the gap — registration/callability
  tests alone don't prove `_ep_enforcement_floor` actually catches the FACADE.
  **Alternatives rejected:** leaving the NC tested by registration alone.

## Immediate Next Steps

<!-- ADVISORY ONLY — present for operator review; do not execute without authorization. -->

1. Complete the remaining Stage-5 accounting for this OBPI (in progress this
   session): release the lock (`uv run gz obpi lock release OBPI-0.0.74-19-floor-wiring`),
   remove pipeline markers, git-sync #1, `gz obpi reconcile`, `gz adr status ADR-0.0.74 --json`,
   git-sync #2.
2. Pull the next Build-to-1.0 campaign item. The §5 enforcement-claim chain
   (15-19) is now complete; remaining ADR-0.0.74 work includes the proxy-reality
   distance detector (item 13 / grader-gaming live NC), MX hardening (item 14), the
   `gz check` step-layer checkpoint seam (item 20 / GHI #638 — ~30 steps still
   self-decide fatality), and the core MX-mode organs (items 4-10: gz mx enter/exit,
   marker, checkpoint, gate5_invariants floor constant).
3. Consult `docs/governance/build-to-1.0-campaign-2026-06-20.md` for the
   operator-ratified pull order before starting the next item.

## Pending Work / Open Loops

- **ADR-0.0.74 is not yet closed out.** Many checklist items remain unbuilt
  (items 4-10 core MX organs; 13 proxy-reality detector; 14 MX hardening; 20
  step-layer checkpoint seam). ADR closeout ceremony comes after all OBPIs land.
- **GHI #638** — the `gz check` step layer (~30 steps) + ~5 solo `validate_cmd`
  paths still self-decide fatality outside the checkpoint (item 20 / OBPI-0.0.74-20).
- **Security-scan canonical slot (GHI #462)** stays unfilled until
  `pool.agentic-security-review` promotes; every `sensitivity: security` completion
  needs `--accept-security-floor` until then.

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.0.74-19-floor-wiring` shows attested-complete
- [ ] `uv run gz obpi lock list` — lock released (no active lock for this OBPI)
- [ ] `uv run -m unittest -q` passes (6489 tests at completion)
- [ ] Branch matches: `git branch --show-current` → `main`
- [ ] `uv run gz obpi reconcile OBPI-0.0.74-19-floor-wiring` clean

## Evidence / Artifacts

- `src/gzkit/quality.py` — `run_enforcement_floor_audit` wrapper (READ-ONLY)
- `src/gzkit/commands/quality.py` — "Enforcement floor" gz check step registration
- `src/gzkit/hooks/guards.py` — `_run_enforcement_floor` pre-push wiring
- `src/gzkit/governance/trust_audits/_qc_negative_controls.py` — enforcement-floor NC
- `src/gzkit/governance/trust_audits/_qc_nc_entrypoints.py` — `_ep_enforcement_floor`
- `src/gzkit/qc_binding.py` — "Enforcement floor" classification entry
- `tests/governance/test_enforcement_floor_wiring.py` — 10 covering tests
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/obpis/OBPI-0.0.74-19-floor-wiring.md` — completed brief with evidence

## Environment State

Python 3.13, uv. Test suite: 6489 tests passing at completion. ARB receipts:
`arb-step-unittest-780211e5138240819d5132468a8e912b`,
`arb-ruff-cd8da11076e24673b340a25caaf29c7c`,
`arb-step-typecheck-0d1c9e8335394eb0a26d48bee35964da`,
`arb-step-mkdocs-fff53a9c10ed42c4a1c6ba3d114c5f0d`.
