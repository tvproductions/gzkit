---
mode: RESUME
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-25T01:23:13Z"
agent: claude-code-362d8209
obpi_id: OBPI-0.0.74-17-gate5-invariants-floor-migration
last_lock_event_timestamp: "2026-06-25T00:55:11.526962+00:00"
last_commit_sha: 7fd1b805
session_id: 362d8209-4ac7-48ed-ae1c-ed6f62bbb0f6
continues_from: 20260624T121411Z-obpi-0.0.74-16-completion.md
---

<!-- Completion register entry for OBPI-0.0.74-17-gate5-invariants-floor-migration. -->

## Current State Summary

OBPI-0.0.74-17 is COMPLETE (attested_completed, attestor g0, operator-verbatim
"attest completed"). Tree GREEN. Full suite 6471/6471 pass (receipt
`arb-step-unittest-87fbfb05b85841f894d109c37bb2f5ef`, exit 0); lint clean
(`arb-ruff-baaff749264f472598a95d3bf2e19317`); typecheck clean
(`arb-step-typecheck-f92749a720b94379969c79485d88bc43`); mkdocs --strict clean
(`arb-step-mkdocs-f547129b3959452ebc4cea98ea681ef9`). Covers parity:
behavior_uncovered=0 (4/5 REQ @covers-proven; REQ-17-05 is structural-fence,
proved at ADR closeout via parent ADR Boundary Invariants #9). `gz obpi
reconcile` PASS (runtime/proof/attestation coherent). `gz adr status ADR-0.0.74`
shows OBPI-17 completed=true / ledger_completed=true / file_completed=true /
evidence_ok=true.

## Important Context

OBPI-0.0.74-17-gate5-invariants-floor-migration reached Stage 5 and was
completed via `gz obpi complete` with operator-verbatim attestation. This
handoff is the completion register entry that couples the lock release
(ADR-0.0.41 token-block discipline § Sub-Invariant 5). Implementation,
verification, the Stage 4a evidence ceremony, Stage 4b independent adversarial
validation, and completion all happened in this session through the
`gz-obpi-pipeline` skill.

Four `GATE5_INVARIANTS` floor members were migrated onto the `@enforces`
surface in `src/gzkit/mx/invariants.py`:

- **`gate5-ledger`** (REQ-17-03): live un-forced NC builds a schema-corrupted
  ledger (the gzkit ledger has NO cryptographic hash chain — the brief's
  "broken hash chain" is realized concretely as JSONL schema corruption) and
  runs the real `validate_ledger` integrity path; PASS.
- **`gate5-attestation-absence`** (REQ-17-04): live un-forced NC presents a
  missing-attestation evidence payload to BOTH real production gates —
  `_requires_human_obpi_attestation` AND `_validate_obpi_human_attestation_fields`
  (the validator `gz obpi complete` invokes, which raises `GzCliError` on empty
  attestation_text); PASS. Forgery-detection is OUT (canon: operator-verbatim
  relayed attestation IS Gate 5).
- **`secrets`** (REQ-17-01) and **`operator-pii`** (REQ-17-02): HONEST NEGATIVE —
  no gate5 production entrypoint exists today, surfaced as named-not-enforced
  via `_GATE5_NAMED_NOT_ENFORCED`, with NO narrower proxy bound (tests enforce
  the prohibition).

## Decisions Made

- **D1 — Honest negative (secrets, operator-pii):** No gate5 production gate
  exists; `validate_no_secrets` is handoff-scoped and `_EMAIL_RE` is
  insights-scoped. Surfaced as `_GATE5_NAMED_NOT_ENFORCED` rather than binding
  either as a narrower proxy (forbidden by ADR-0.0.74 § Consequences/Neg #7).
- **D2 — Claim IDs are hyphen-namespaced** (`gate5-ledger`,
  `gate5-attestation-absence`): `_CLAIM_ID_RE` (`^[a-z][a-z0-9-]*$`) disallows
  the colon the brief's Demo aspirationally used.
- **D3 — Known-claims extension stays in `invariants.py`** (operator chose
  Option A at plan-audit): `_ensure_gate5_claims_registered()` calls
  `set_known_claims(_KNOWN_QC_CLAIM_IDS | _GATE5_CLAIM_IDS)`. `enforcement.py`
  was NOT added to the allowlist — avoids a brief amendment; OBPI-19 will do the
  cleaner production-discovery wiring.
- **D4 — Attestation NC binds the REAL field validator** (seam fix from Stage
  4b): the entrypoint now invokes `_validate_obpi_human_attestation_fields`
  (which raises `GzCliError` on empty text) rather than re-implementing a
  private `.strip()` check. Mutation-verified seam-closed.
- **D5 — No module-import auto-registration:** `invariants.py` does NOT call
  `_ensure_gate5_claims_registered()` at import, keeping the widely-imported
  module lightweight; OBPI-19 wires production discovery.
- **In-flight brief amendments (operator-attested by silent acceptance):**
  (1) added the missing `REQUIREMENT:` line for REQ-17-05 (sibling OBPI-16
  convention maps every acceptance criterion, including structural-fence, to a
  REQUIREMENT line) — cleared the req_count=-1 reconcile drift;
  (2) removed the redundant `from gzkit.mx import invariants` test import (kept
  the specific `from gzkit.mx.invariants import ...`) — the package import
  resolved `gzkit.mx/__init__.py`, a reconcile-allowlist neighborhood
  false-positive for a file this OBPI does not modify.

## Immediate Next Steps

1. Git-sync #1 amended commit `ea4c7011` (handoff section-contract fix) and push
   — the pre-push `gz check` gate must pass clean.
2. There is no Git-sync #2 content: `gz obpi reconcile` produced no drift and
   `gz adr status` is a read-only Layer-3 view; the tree is clean.
3. Next OBPI in ADR-0.0.74 land-order: **OBPI-0.0.74-18** (structural-fence
   proof upgrade — `resolve_fence_proof` requires a live `@enforces` NC).

## Pending Work / Open Loops

- **ADR-0.0.74 is NOT closed.** Meta-validator land-order status:
  - OBPI-0.0.74-15 (`@enforces` decl + registry) — landed (prerequisite).
  - OBPI-0.0.74-16 (meta-validator runner) — COMPLETE.
  - OBPI-0.0.74-17 (gate5 floor migration) — COMPLETE (this handoff).
  - OBPI-0.0.74-18 (structural-fence proof upgrade) — NEXT.
  - OBPI-0.0.74-19 (floor wiring — strict-no-debt; wires
    `_ensure_gate5_claims_registered` into `_ensure_production_claims_registered`
    and enumerates `GATE5_INVARIANTS` membership; proves REQ-17-05's structural
    fence at closeout). Cannot land until 17 + 18 complete (BI#8 sequencing
    window, operator-acknowledged).
- **Note for OBPI-19:** `src/gzkit/mx/invariants.py` deliberately does NOT
  auto-register the gate5 claims at module import. OBPI-19 extends
  `_ensure_production_claims_registered` in `src/gzkit/enforcement.py` to call
  `_ensure_gate5_claims_registered()` so the meta-validator runner discovers
  them in production.
- Earlier campaign items (gates-as-sensors step layer / GHI #638, OBPI-20
  `gz check` step-layer seam) remain open per the Build-to-1.0 campaign.

## Verification Checklist

- [x] `uv run gz arb ruff` — clean (`arb-ruff-baaff749264f472598a95d3bf2e19317`)
- [x] `uv run gz arb typecheck` — clean (`arb-step-typecheck-f92749a720b94379969c79485d88bc43`)
- [x] `uv run gz arb step --name unittest -- uv run -m unittest -q` — 6471/6471 (`arb-step-unittest-87fbfb05b85841f894d109c37bb2f5ef`)
- [x] `uv run gz arb step --name mkdocs -- uv run mkdocs build --strict` — clean (`arb-step-mkdocs-f547129b3959452ebc4cea98ea681ef9`)
- [x] `uv run gz covers OBPI-0.0.74-17-gate5-invariants-floor-migration --json` — behavior_uncovered=0
- [x] `uv run gz obpi reconcile OBPI-0.0.74-17-gate5-invariants-floor-migration` — PASS
- [x] Stage 4b adversary (independent, refute-framed) — NOT-REFUTED; weakest-point seam fixed in-flight + mutation-verified
- [x] Gate 5 human attestation recorded (operator-verbatim, attestor g0)

## Evidence / Artifacts

- `src/gzkit/mx/invariants.py` — gate5 `@enforces` registration (2 bound claims + `_GATE5_NAMED_NOT_ENFORCED`)
- `tests/mx/test_gate5_invariants_live_nc.py` — 12 live un-forced NCs + genuineness guards
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/obpis/OBPI-0.0.74-17-gate5-invariants-floor-migration.md` — brief (Completed; evidence sections recorded)
- `.claude/plans/OBPI-0.0.74-17-gate5-invariants-floor-migration.md` — approved plan
- ARB receipts (gitignored under `artifacts/receipts/`): unittest `arb-step-unittest-87fbfb05b85841f894d109c37bb2f5ef`, ruff `arb-ruff-baaff749264f472598a95d3bf2e19317`, typecheck `arb-step-typecheck-f92749a720b94379969c79485d88bc43`, mkdocs `arb-step-mkdocs-f547129b3959452ebc4cea98ea681ef9`
