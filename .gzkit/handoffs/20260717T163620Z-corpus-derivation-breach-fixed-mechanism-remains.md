---
mode: CREATE
adr_id: ADR-0.0.37
branch: main
timestamp: '2026-07-17T16:36:20Z'
agent: claude-code
session_id: d29833d4-a0e9-4875-a620-bddbd70c932c
continues_from: .gzkit/handoffs/20260717T015912Z-handoff-surface-mechanization-574-689-692.md
---

## Current State Summary

Operator ruled: close ADR-0.0.37's canon->AGENTS.md inversion VIA THE CORPUS (not the .json registry — the 2026-06-03 Re-Alignment already superseded that spine). This session verified the ADR's real state and fixed the most urgent hole; the mechanism and migration remain. VERIFIED STATE: 3 of GHI #623's 4 findings are STALE — the mtime theater is gone (rendition_freshness now compares content fingerprints), the composer HAS run (11 composition_candidate_emitted events since the 2026-06-16 repudiation), and assert_invariant_verbatim PASSES on the committed rendition. Tamper-tested all three live gates in isolated temp roots: hand-edit AGENTS.md -> invariant-coherence CAUGHT it; mutate corpus -> rendition-freshness CAUGHT it (fingerprint divergence); mtime-bump-identical-content -> correctly stayed green. The gates are genuinely load-bearing now. What survives #623 is the REGISTRY ORPHAN: load_invariants() result is discarded at render_agents_md's boundary (its own docstring says the param is unused; test_compose.py:125-128 PINS it ignored); reconcile_invariant has zero non-test callers. Two spines share the word 'invariant': the LIVE corpus spine (corpus->composer->rendition->playback, gated) and the DEAD registry spine (.gzkit/invariants/*.json, orphaned). BREACH FIXED (GHI #694, commit dd221531, pushed to main): committed rendition bytes were unprotected between commit and playback — RenditionProvenance froze the corpus fingerprint but never the rendition's own digest, and gz content commit is a byte copy, so claude.md was hand-edited to 31990B under a sidecar attesting 31741B (a correct-but-out-of-seam rewrite of Behavior Rule 11), live in AGENTS.md, all gates green. Added rendition_fingerprint (optional only so pre-field sidecars load; None reads as drift never skip), froze it at commit, added a rendition_integrity arm to --rendition-freshness. Both renditions reconciled content-preserving (AGENTS.md byte-unchanged) and re-attested by operator g0. gz check exit 0, 7151 unittests OK, behave rendition_playback 9/9.

## Important Context

READ FIRST: the resume gate (.claude/hooks/handoff-resume-gate.py) is ARMED by this handoff and will BLOCK every mutating tool call until the operator rules and you book it via 'uv run gz handoff authorize' — run it BARE (a leading cd with ; makes it compound and the gate refuses). Reading is permitted. || THE HOUSE FAILURE MODE FIRED AGAIN THIS SESSION AND WAS CAUGHT BY READING CODE, NOT DESCRIPTIONS: GHI #623's body (a 4-reviewer audit) was 75% stale because the code moved after it was filed; the budget JSON _doc claimed 'compressible 21957' when the real compressible entry is 354B (measured); the ADR's Fidelity Assertions claim registry-byte-match that does not exist. Every DESCRIPTIVE layer in this ADR has drifted from the code beneath it, always claiming MORE than exists — which is exactly the inversion CIC-1 was written to end. Trust the code. || THE CORE STRUCTURAL FACTS (measured, not narrated): AGENTS.md is 31990B; corpus witnesses 9966B (9612 invariant across 43 UNIQUE entries — 7 are #635 duplicates — + 354 compressible); 22.3% witnessed, ~70%+ UNWITNESSED hand-authored prose whose only 'source' is the rendition itself. The composer does NOT materialize a document from the corpus: gz content compose VALIDATES a hand-supplied candidate (agent authors it; there is NO Corpus->str function anywhere), and composer.py:65 IMPUTES compressible bytes as total-minus-invariant, so invented prose is silently booked as 'compressed' (the claude compose printed compressible=354B->22378B — the 22378 is unwitnessed prose). The floor only checks corpus SUBSET candidate; there is NO candidate SUBSET corpus (superset) check, so unwitnessed prose is structurally invisible. SECTION-VOCABULARY CIRCULARITY: the ADR says 'sections are defined by the Jinja2 template' but claude.md.j2 is a bare {% for pillar in pillars %} loop — section names are parsed from AGENTS.md's OWN H2 headings then kebab-slugged; the corpus validates section against structure extracted from the very file it is supposed to be canon for. APPEND-ONLY IS HONOR-SYSTEM: no shrink ratchet exists (commit dc2bc605 hand-deleted a corpus entry and gates went green). anchor/witness fields exist on CorpusEntry but gz content remember cannot set them (all 51 entries have anchor=None). || MIGRATION IS ATOMIC BY MECHANICS: the instant entry #1 is appended, both committed renditions go stale and gz check reds until BOTH consumers (claude=heavy, codex=lite) are composed AND committed under Gate 5 — dc2bc605 is that failure recorded. So capture-all -> compose-both -> attest-both is one arc (~100+ entries, 2 Gate 5s). Migrate mechanism-first or the migration lands into a composer that still cannot materialize, still cannot detect invented prose — ceremony, not canon.

## Decisions Made

1. BREACH ROUTED DIRECT-FIX not OBPI (operator canon: 'GHIs are AUTHORIZED for direct repair, always... never spin up an ADR or OBPI merely to discharge a GHI'). Commit dd221531, Task: TASK-rendition-integrity-#694 (the -#694 anchor is legitimate — #694 already existed; no GHI filed to mint a trailer). 2. rendition_fingerprint is OPTIONAL (str|None=None) — REQUIRED would break loading the two pre-field sidecars (extra='forbid'). But the GATE reads None as DRIFT, never as skip: an optional-field-that-reads-absent-as-pass would be bypassable by deleting one JSON line, which is the same unhashed-link class the fix closes. 3. Integrity check is an ARM on --rendition-freshness, NOT a new gz validate scope — reuses the existing per-rendition loop and sidecar read, avoids adding to the 70-scope surface #618 is collapsing; distinct 'rendition_integrity' error type preserves attribution. 4. Rule 11 breach edit was REGULARIZED not reverted — the text is CORRECT (names the governed gz insights remember path, matches current AGENTS.md, passes the floor); reverting would have re-broken a correct rule. Reconciliation was content-preserving: AGENTS.md byte-unchanged, only the sidecars gained digests. 5. WITHDRAW-VS-RECOMPLETE for the 4 repudiated OBPIs (02/03/21/22) DEFERRED until the corpus genuinely derives — the split (withdraw 02/03 as registry-spine-superseded, re-complete 21/22 as the live spine) is defensible but should be ruled AFTER close-via-corpus lands, so the withdrawal is earned on evidence not asserted on the unratified Re-Alignment paragraph. Operator ruled tier default = per-entry, defaulting compressible (reserve invariant for genuine operator doctrine).

## Immediate Next Steps

1. BUILD THE MECHANISM (the natural next pull; likely OBPI ceremony — new CLI surface + superset gate + materializer — not direct-fix). Four producer pieces named in #694's analysis + two more found this session: (a) MATERIALIZER Corpus->str grouped by section, ordered by anchor, as the default candidate_text when --candidate omitted; (b) SUPERSET ASSERTION candidate SUBSET corpus in tier_policy.py alongside assert_invariant_verbatim, so invented prose fails closed; (c) ATTRIBUTED byte accounting replacing composer.py:65's total-minus-invariant subtraction; (d) anchor/witness CLI channel on gz content remember (Heavy — CLI contract change); (e) SHRINK RATCHET for the corpus (append-only is currently honor-system). Overlaps sibling GHI #654's 'candidate generator' — consider one shared home. 2. THEN THE MIGRATION: ~21600B across 12 zero-coverage pillars (of 20) into corpus entries, per-entry tier defaulting compressible, atomic capture->compose-both->attest-both. This IS the GHI #533 <15k corpus-split path. 3. THEN rule withdraw-vs-recomplete on OBPI 02/03/21/22 and close #623. 4. Non-urgent carryovers from the PRIOR handoff, still open: GHI #691 (rules have no aging clock — route ruling), GHI #682 (security-sensitivity blocked until discharged), Pass A rows 1-8 re-verification. GHI #693 from the prior handoff is now CLOSED (commit 8aa9b887, before this session).

## Pending Work / Open Loops

GHI #694 CLOSED (dd221531). GHI #654 OPEN — sibling of #694, the orchestration gap: gz content remember is a footgun (leaves gz check red with no warning), no corpus->candidate generator, no atomic multi-consumer landing; its fix-shape item 2 (candidate generator) IS the materializer step 1 above — do not double-build. GHI #635 OPEN — 7 of 50 invariant corpus entries are exact duplicates (operator doctrine captured twice with divergent quotes), inflating apparent coverage. GHI #623 OPEN — parent finding; do NOT close until the corpus genuinely derives (superset gate + real coverage), because 3 of its 4 findings are stale but the registry-orphan cut survives. GHI #533 OPEN — the <15k AGENTS.md destination that the migration realizes. Repudiated OBPIs 02/03/21/22 remain repudiated (ADR-0.0.37 at 15/19, closeout BLOCKED) — that is correct and must NOT be force-cleared to a green number; the Foundation Sunset capstone (ADR-0.34.0) sequences AFTER this ADR's closeout. UNTRACKED OBSERVATION (noted in #694 body, not bundled): src/gzkit/commands/content/commit.py is a Gate-5 attestation gate but is NOT registered under auth_boundaries in data/security_surfaces.json alongside obpi_complete.py. Budget JSON _doc drift (names a nonexistent _budget_within_codex_cap fn, cites compressible 21957) — cosmetic, trackable if touched.

## Verification Checklist

git status -sb shows clean, level with origin/main at dd221531. uv run gz check exits 0 (all gates incl. the new rendition_integrity arm). uv run -m unittest -q = 7151 OK. uv run gz validate --rendition-freshness exits 0 with rendition_fingerprint frozen in BOTH sidecars (grep rendition_fingerprint .gzkit/renditions/AGENTS.md/*.corpus.json). uv run -m behave features/rendition_playback.feature = 9/9. cmp AGENTS.md .gzkit/renditions/AGENTS.md/claude.md exits 0 (surface byte-unchanged by the reconciliation). NEVER pipe a verifier through tail/head/grep (tests.md Verification exit-code integrity) — capture to a file and read the real exit. To re-prove the gate is load-bearing: in an ISOLATED temp root only (never the live ledger — prior session's disclosed violation), edit a committed rendition post-commit and expect a rendition_integrity ValidationError; the tamper script pattern is in scratchpad tamper_37.py. To re-measure coverage: uv run python -c to load_corpus and sum invariant_entries text bytes vs AGENTS.md total (expect ~22.3%).

## Evidence / Artifacts

Fix (commit dd221531): `src/gzkit/content/rendition_store.py` (rendition_fingerprint field + digest helper), `src/gzkit/commands/content/commit.py` (freezes digest at commit), `src/gzkit/governance/trust_audits/rendition_freshness.py` (rendition_integrity arm), `tests/governance/test_rendition_freshness.py` (6 gate tests), `tests/commands/test_content_commit.py` (digest semantic assertion), `features/steps/rendition_playback_steps.py` (BDD step freezes digest). Reconciled sidecars: `.gzkit/renditions/AGENTS.md/claude.corpus.json`, `.gzkit/renditions/AGENTS.md/codex.corpus.json`. The ADR under correction: `docs/design/adr/foundation/ADR-0.0.37-constitutional-invariant-composition/ADR-0.0.37-constitutional-invariant-composition.md`. The dead spine: `src/gzkit/governance/compose.py` (render_agents_md ignores its invariants param), `src/gzkit/governance/invariants.py` (reconcile_invariant orphaned). The live spine: `src/gzkit/content/composer.py`, `src/gzkit/content/tier_policy.py`, `src/gzkit/content/corpus_store.py`. Binding doctrine: `docs/governance/agent-control-surface-rendering-substrate.md`. Corpus source of truth: `.gzkit/corpus/AGENTS.md.jsonl`.
