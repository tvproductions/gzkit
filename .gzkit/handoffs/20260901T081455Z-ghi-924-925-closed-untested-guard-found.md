---
mode: CREATE
adr_id: null
branch: main
timestamp: '2026-09-01T08:14:55Z'
agent: claude-code
session_id: ec4ed25a-5a87-4a15-bead-ddda55adee66
continues_from: .gzkit/handoffs/20260831T141123Z-copilot-declarations-retired-mirror-exclusion-honoured.md
---

## Current State Summary

Tree is clean and fully synced: `git rev-list --left-right --count origin/main...HEAD` returns `0 0`, HEAD is `41646443`. No active locks, no pipeline marker, no OBPI scope, no open TASK.

The session resumed the predecessor handoff, reviewed it against the tree rather than trusting it, booked one operator ruling, landed two commits and closed two GHIs:

- `8ac7d418` chore: update .gzkit (4 files) (gz git-sync) — carried the three loose governance artifacts and pushed `3c255459` to origin with them
- `41646443` fix(sync-skills): bind the mirror's foreign-root exclusion to tests (GHI #925)

GHI #924 and GHI #925 both closed `fixed`, separately, one disposition and one comment each.

THE HEADLINE FINDING IS A NEW FAMILY, NOT THE PREDECESSOR'S REPRODUCING. Two sessions running have found already-fixed-but-open issues; this session found the inverse shape — a fix that landed correct and observably working, with **no covering test for the guard it added**. `3c255459` touched twelve test files and not one of them drove `_forbidden_mirror_names`. `grep -rn "_forbidden_mirror_names" tests/` returned nothing at close time. Every pre-existing `find_stale_mirror_paths` test exercises a different cause (retired skills, non-destructiveness). The guard was correct, so nothing failed — which is exactly how an untested guard reaches main and how it later gets refactored away by someone who sees no test protecting it. Caught only because the `ghi-close` class-of-failure step forces the question *which input in this family does no test drive?*

## Important Context

THE FIXTURE NOTE, AND IT COST A FAILED FIRST PASS. In `tests/test_sync.py` the canonical `CLAUDE.md` is written **without** the generated-surface marker on purpose. A marker-bearing redirect is reaped by the stale sweep in a tree that small: no rule is scoped to the canonical skills dir, so the nested writer emits no `AGENTS.md` there and therefore no redirect beside it. My first pass used the marker text (`<!-- Generated by gzkit ... -->`) and failed its own overshoot assertion — the fixture was testing an artifact of itself rather than the mirror. The unmarked form is preserved (`test_hand_written_claude_md_is_preserved`), and that is what puts a `CLAUDE.md` in the canonical tree for `rglob` to pick up: the precondition the defect needs. A three-variant probe settled it; do not "tidy" the fixture back to the marker form.

RED WAS DEMONSTRATED, NOT ASSERTED. Both new tests were run against the pre-fix behaviour by neutering `source_file.name in forbidden_names` and dropping the sweep's `and rel.name not in forbidden`. Each failed on its own cause — `AssertionError: True is not false : a foreign vendor's surface_root must not receive a CLAUDE.md` and `AssertionError: '.agents/skills/CLAUDE.md' not found in []`. Guards restored from a backup copy and verified byte-identical (`git diff --stat src/gzkit/sync_skills.py` empty) before committing. If you add a guard here, do the same; a test that cannot fail when the guard is removed is not a witness.

`render_rule_for_copilot` IS STILL LOAD-BEARING AND STILL LOOKS LIKE DEAD CODE. Carried forward from the predecessor unchanged, because the temptation survives its retirement. It renders no Copilot SURFACE any more, but `_classify_canonical_rules` is documented as equivalent to it BY CONSTRUCTION and the nested `AGENTS.md` generation runs on the `applyTo` / `.instructions.md` vocabulary that renderer defines. The `renderer` argument on `render_rules_to_dir` stays for the same reason. Read the classifier docstring before deleting either.

`.github/skills` SURVIVES DELIBERATELY as a bootstrap candidate so an adopter migrating an old tree is still seeded — a literal in `test_sync_bootstraps_canonical_from_legacy_github_skills_mirror`, precisely because nothing declares it any more. `.github/CLAUDE.md` also exists deliberately: `.github` left the declared vendor set, so it takes its sibling redirect like any other rule-scoped subtree.

THE PREDECESSOR'S VERIFICATION CHECKLIST HAD TWO DEFECTS, both found by running it. It asserted a clean tree when three governance artifacts were uncommitted, including the handoff document itself (untracked). And its `find .agents .gemini .opencode -name CLAUDE.md` check does not run in this repo — `.gemini` and `.opencode` are declared vendor roots that are not materialized, so `find` errors rather than passing. The invariant holds on `.agents`; the command cannot witness it. A checklist line that errors is not a green check.

INHERITED CAUTIONS THAT STILL BIND, all exercised this session. `uv run gz validate --transcribed-adr-counts` refuses a live ADR count transcribed into a handoff; cite `uv run gz adr status` instead. Any commit touching source or tests needs a `Task:` trailer. A verifier piped into another process is refused by the verifier-pipe-gate hook — it fired twice this session, on `gz validate` and on `unittest`; use `set -o pipefail`, read `${PIPESTATUS[0]}`, or capture to a file and echo the real exit.

COMMIT TRAILER CONVENTION, STILL UNRULED after three sessions. The harness asks for a `Claude-Session:` trailer; recent repo commits carry none and the repo uses `Task:` in slug form. This session followed repo convention, as its two predecessors did.

## Decisions Made

- [operator-ruled] Execute the resumed handoff's step 1 only — push, then close the two implemented GHIs separately (verbatim: "Step 1 only"). Booked to Layer 2 via `gz handoff decide`, with steps 2 through 5 recorded individually as `--set-aside` rather than left silent.
- [agent-chose] Widened the push to carry the three loose governance artifacts rather than pushing `3c255459` bare, because the handoff document itself was UNTRACKED. Pushing the fix while leaving its own handoff uncommitted is the same invisibility that let GHI #836 sit fixed-but-open for eleven days — the predecessor's own headline finding, applied to the predecessor's own output.
- [agent-chose] Wrote the missing covering tests instead of closing GHI #925 on the observably-working fix. The `ghi-close` class-of-failure step says an instance fix with no coverage of adjacent inputs is a DO IT RIGHT #1 violation: expand the fix or file a follow-up and keep the issue open. Expanding was cheap (one test file, 462 `fix(` commits of precedent in 60 days) so the direct-fix route applied and the gap closed in the same motion.
- [agent-chose] Pinned the overshoot direction as a second assertion in the same test, not a separate one. An exclusion that reached every mirror root — including Claude's own — would satisfy the leak assertion alone, so the two assertions are one semantic and belong together.
- [agent-chose] Corrected the fixture rather than weakening the assertion when the first pass failed. The failure was real information about the writer/sweep interaction, not noise to assert around.
- [agent-chose] Did NOT file a GHI for the untested-guard class, and did NOT record an insight. The instance was fixed in flight and is tracked by `41646443`; Behavior Rule 11's trigger is an operator course-correction, which did not occur. The broader question — whether anything witnesses that a new guard ships with a test — is parked in Pending as a candidate for the operator, not filed unilaterally under a "step 1 only" ruling.
- [agent-chose] Closed GHI #924 and #925 with per-cause family tables rather than prose coverage claims, and re-derived every cited `#N` against live issue state at close time per `ghi-close` step 7f. #923 was annotated `closed 2026-08-31, COMPLETED` in both comments rather than carried forward as open.

## Immediate Next Steps

1. RE-DERIVE ANY FAMILY GHI PREMISE BEFORE WORKING IT. Measured four times over two prior sessions and not weakened by this one. The open family is GHI #921, #907, #873, #815 and #922. Read the surface each body cites and confirm the defect is still present before planning around it. This session adds a second reason beyond staleness: a premise can also be UNDERSTATED, as #922's is.
2. Decide whether GHI #873's ownership paragraph gets corrected on the issue. Unchanged from the predecessor and still nothing downstream will force it. Its body says the replacement-row producer is scoped to OBPI-0.35.0-02 and that the question should be ruled before that brief lands. That brief HAS landed attested-completed and ships retraction rows only; no brief in ADR-0.35.0 owns a replacement-row producer, and the corpus holds zero such rows. The gate the issue set has already passed unremarked.
3. Rule on whether the untested-guard finding deserves a mechanical witness or a GHI. This session fixed the instance; nothing yet asks the question at the class level — no control observes that a commit adding a guard also adds a test that fails without it. The nearest existing surface is the advisory spec-test-code drift report, which counts REQs with no test (697 today) and would not have flagged this, because the guard carried no REQ. Agent recommendation: worth a GHI, but it was not filed under a "step 1 only" ruling.
4. Extend ADR-0.35.0's Feature Checklist with the witness-repoint item and its matching brief, if and when you want GHI #922 to move. Under the IRON LAW only the operator initiates that, via the gz-obpi-pipeline skill. Until it exists, #922 stays open with its blocker recorded on the issue.
5. Hold the long-standing operator-held items: the design spike, and whether a delivery-cap breach on must-survive canon stays advisory (GHI #815). That is the one family item that has DEGRADED — 385 bytes over the codex cap when filed, 14,108 bytes over as re-measured this session, with a second must-survive section now affected.
6. Rule the commit-trailer question or retire it. Three sessions have now carried it forward unchanged, which is itself the signal: an unruled convention that keeps arriving is cheaper to settle than to re-explain.

## Pending Work / Open Loops

NOTHING UNPUSHED. `0 0` against origin, tree clean, no locks. This session leaves no in-flight work.

OPEN, live and untouched: GHI #907. The authorship arm is 112 lines reading one git config value; no file-content witness exists in the trust-audit package.

OPEN, live and WIDENING: GHI #815. Re-measured this session at 46,876 bytes rendered against the 32,768-byte codex cap — 14,108 bytes over, against the 385 its body records. Two must-survive sections are affected: `operator-doctrine-verbatim-canon` straddles the cap (spans 30020-43941) and `architectural-boundaries` starts entirely past it (46281). Both remedies remain owned elsewhere and both are unavailable, one parked post-1.0 and the other blocked on the registry-projection migration. `uv run gz validate --instructions-files-budget` exits 0 with these three advisory warnings; the figures there are the live measurement, never a number transcribed here.

OPEN, partially discharged: GHI #921. Unchanged this session. The grandfather debt named in its Observed section is DRAINED and the version-chain lift is COMPLETE. Remaining: the bullet-narrative compression pass, gated at the instructions-files-diet chore's consult gate, plus the corpus half held for the ADR-0.35.0 discussion. The 90-percent contradiction it names is STILL LIVE, with both statements rendering in the root contract.

OPEN by deliberate disposition from two sessions back: GHI #873, ruled an operator-led fold-algebra amendment. Nothing further is agent-workable. Its reachability is still latent, with zero replacement rows and no producer in the source tree, but for a different reason than its body states — see Immediate Next Steps item 2.

OPEN with its routing ruling recorded as a comment: GHI #922. It cannot close as superseded until ADR-0.35.0 carries a checklist item that absorbs the scope, and the dead-letter prohibition makes open-with-blocker the honest state. Its premise is UNDERSTATED rather than stale: the shape witness audits the adopter bootstrap template, which is templated on the project name, while calling it the canonical Layer-1 surface. No trust audit shape-checks the Layer-1 corpus at all.

UNFILED CANDIDATE, deliberately not filed under a narrow ruling: no control observes that a commit adding a guard also adds a test that fails without it. Found this session when `3c255459`'s mirror guard reached main untested across twelve touched test files. The instance is closed by `41646443`; the class is not. Agent recommendation is to file it; the operator's ruling scoped this session to step 1, so it is parked here rather than filed. See Immediate Next Steps item 3.

PRE-EXISTING and untouched: the quality gate reports 697 unlinked specs as advisory drift. Note for whoever works the untested-guard question — that report counts REQs with no test, so it structurally could not have caught this defect, whose guard carries no REQ.

UNRULED and carried forward for the third session: whether commits should carry the harness-requested session trailer alongside the repo's task trailer.

## Verification Checklist

Run these before trusting any claim above.

`git rev-list --left-right --count origin/main...HEAD` expects `0 0`. Anything else means work landed after this document was written.

`git status --short` expects empty output.

`git log --oneline -1` expects `41646443` at authoring time.

`uv run gz obpi lock list` expects `No active locks.`

`uv run gz check` expects exit 0, with 697 unlinked specs reported as advisory drift and three surface-delivery-witness warnings. Pipe it only with `set -o pipefail` or read `${PIPESTATUS[0]}`; the verifier-pipe-gate hook refuses a bare pipe and fired twice this session.

`uv run gz arb step --name unittest -- uv run unittest-parallel -t . -s tests --buffer` expects 9130 tests OK — 9128 before this session's two additions.

`uv run python -m unittest tests.test_sync` expects OK, 75 tests. This is the covering module for the mirror exclusion.

To re-demonstrate RED rather than trust it: neuter `if source_file.name in forbidden_names` in `src/gzkit/sync_skills.py` and drop `and rel.name not in forbidden` from `find_stale_mirror_paths`, then run the two named tests. Both must fail, each on its own cause. Restore from a backup copy and confirm `git diff --stat src/gzkit/sync_skills.py` is empty.

`uv run gz agent sync control-surfaces` expects exit 0 with no stale mirror-only recovery block and no content drift. That block is what the GHI #925 fix made visible; its absence now is the repaired state.

`ls -1 .claude/skills/*.md .agents/skills/*.md` expects exactly `.agents/skills/AGENTS.md`, `.claude/skills/AGENTS.md`, `.claude/skills/CLAUDE.md` — no `CLAUDE.md` under `.agents`. Do NOT use the predecessor's `find .agents .gemini .opencode -name CLAUDE.md`: those two roots are declared but not materialized here, so the command errors instead of passing.

`uv run gz validate --instructions-files-budget` expects exit 0 with three advisory warnings; the byte figures there are the live GHI #815 measurement.

`gh issue list --state open --limit 40` re-derives family membership rather than trusting the ids listed above.

`uv run gz handoff rulings --search "copilot"` checks the settled corpus before re-arguing any vendor question.

## Evidence / Artifacts

Commits landed this session:

- `8ac7d418` chore: update .gzkit (4 files) (gz git-sync) — the staged session-exit bookmark, five carried-forward lines in `.gzkit/handoffs/rulings.jsonl`, the ledger's two `handoff_resume_decided` rows, and the previously-untracked predecessor handoff document
- `41646443` fix(sync-skills): bind the mirror's foreign-root exclusion to tests (GHI #925) — tests only, no production behaviour changed

Surface changed:

- `tests/test_sync.py` — `TestSyncControlSurfaces::test_generated_claude_redirect_is_not_mirrored_into_a_foreign_root` and `::test_forbidden_nested_surface_in_a_foreign_root_is_reported_stale`, inserted after the existing non-destructive stale-path test

Surfaces read and verified, not changed:

- `src/gzkit/sync_skills.py` — `_forbidden_mirror_names` (line 359), its consumers in `sync_skill_mirror` (331) and `find_stale_mirror_paths` (554)
- `src/gzkit/rules/__init__.py` — `_foreign_vendor_roots` (491) and `_within_any`, the derivation the mirror now asks
- `src/gzkit/skills_mirror.py` — `_validate_mirror_skill_assets`, the coupled surface that only a run would have found
- `src/gzkit/config.py`, `src/gzkit/schemas/manifest.json`, `data/distribution_baseline_manifest.json` — confirmed clean of Copilot declarations
- `.gzkit/skills/ghi-close/SKILL.md` — the four-phase protocol followed for both closes

ARB receipt emitted this session, `exit_status` read from disk rather than presence counted:

- `arb-step-unittest-6570d1d64a0d4bb881323afc4dc94573` — exit_status 0, 9130 tests

Receipts inherited from `3c255459` and re-resolved on disk at close time, all exit_status 0:

- `arb-ruff-be191a0a0b00449cbdad312a6ec7e50f`
- `arb-step-typecheck-809aa2f8ae3b4f70a9bc01e3c9326ecd`
- `arb-step-unittest-59b358a2db3a483b8ffd33ed5ca4b782`

GHIs closed this session, each `fixed`, separately, with a per-cause family table:

- GHI #924 — vendors: gz init regenerates the whole Copilot surface the drop removed; resolved by `3c255459`
- GHI #925 — skill mirror: generated CLAUDE.md is copied into Codex's own surface root; resolved by `3c255459` plus `41646443`

Handoff chain and Layer-2 records:

- `.gzkit/handoffs/20260831T141123Z-copilot-declarations-retired-mirror-exclusion-honoured.md` — the resumed predecessor
- `.gzkit/ledger.jsonl` — one `handoff_resume_decided` row booking this session's ruling with four set-aside steps
- `.gzkit/handoffs/rulings.jsonl` — the append-only settled-ruling store, read with `gz handoff rulings`

## Settled Rulings

640 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
