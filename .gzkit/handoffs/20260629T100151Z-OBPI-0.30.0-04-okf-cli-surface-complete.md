---
mode: CREATE
adr_id: ADR-0.30.0
branch: main
timestamp: "2026-06-29T10:01:51Z"
agent: claude-code
obpi_id: OBPI-0.30.0-04-okf-cli-surface
last_lock_event_timestamp: "2026-06-29T09:35:59.147682+00:00"
last_commit_sha: 4ad0c092
session_id:
continues_from:
---

<!-- Handoff document for ADR-0.30.0 — created by claude-code at 2026-06-29T10:01:51Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

OBPI-0.30.0-04-okf-cli-surface is **COMPLETE and operator-attested** (Gate 5
operator-verbatim conversational, attestor `g0`, completed via `gz obpi complete`
exit 0). The OBPI shipped the `gz knowledge generate` / `gz knowledge refresh`
CLI surface verification chain over pre-existing scaffolding: REQ-derived
unittests (`tests/commands/test_knowledge.py`, 6 cases across 4 classes) and a
behave smoke feature (`features/knowledge.feature`, 2 scenarios). All four REQs
are `@covers`-parity at 4/4. Full suite 6644/6644 pass. Stage 5 ceremony is
in-flight: completion is recorded; this handoff is authored to satisfy the
ADR-0.0.41 lock-release coupling, after which the lock is released and two
git-sync cycles commit the governance edits.

## Important Context

- The CLI command/parser/manpage scaffolding (`src/gzkit/commands/knowledge.py`,
  `src/gzkit/cli/parser_artifacts.py` `_register_knowledge_parser()`,
  `src/gzkit/cli/parser_handler_manifest.py`, `docs/user/manpages/knowledge.md`)
  was authored in a PRIOR session; this OBPI added only the test/behave
  verification surface plus two manpage path-corrections.
- **Step-4b adversarial validation (Codex) returned REFUTED-WITH-CAVEATS.** Two
  real gaps were fixed before attestation: (a) the idempotency test now runs
  `refresh` TWICE to match the REQ-02 acceptance criterion semantics; (b) the
  subcommand manpages `knowledge-generate.md`/`knowledge-refresh.md` cited a
  stale `.gzkit/knowledge/` output path — corrected to the real
  `.gzkit/governance/knowledge/` (the generator's `BUNDLE_OUTPUT`). A new
  regression test `test_manpages_cite_the_real_bundle_output_path` guards the
  class — `gz cli audit` checks structural coverage, not documented-path
  correctness, so the adversary caught what the mechanical gate is blind to.
- `req_atomic:` was declared in the brief frontmatter (all 4 REQs) — the labor
  was genuinely one indivisible unit per REQ; matches sibling OBPI-0.30.0-01.

## Decisions Made

- **Decision:** Declare `req_atomic:` for all four REQs rather than minting
  seq=02+ TASKs.
  **Rationale:** Each REQ was satisfied by a single coherent test-authoring unit
  over pre-existing scaffolding; no labor subdivides below the REQ (GHI #590).
  **Alternatives rejected:** `gz task start --seq next` subdivision — there was
  no multi-step labor to attribute.
- **Decision:** Fix the stale manpage paths in `knowledge-generate.md` /
  `knowledge-refresh.md` even though only `knowledge.md` was in the explicit
  allowlist.
  **Rationale:** Coupled-surface coherence (DO IT RIGHT 1a); correction not scope
  creep (Prime Directive #4). The stale path would mislead operators.
  **Alternatives rejected:** Filing a GHI to defer — the defect was in-flight,
  ≤2 files, trivially fixable now.

## Immediate Next Steps

1. Continue the parent **ADR-0.30.0** checklist: OBPI-0.30.0-05 (docs/runbook
   progressive-disclosure wiring — the success-defining path) and OBPI-0.30.0-06
   (content-boundary doctrine doc under `.gzkit/`). OBPI-01/02/03/04 are now done.
2. When all six OBPIs complete, run the ADR-0.30.0 closeout ceremony
   (`gz closeout ADR-0.30.0`) which audits the STRUCTURAL-FENCE Boundary
   Invariants (1-4) at the ADR layer.
3. Per the active Build-to-1.0 campaign (Magna Carta), confirm the topmost
   unchecked item whose gate is met before pulling the next unit of work.

## Pending Work / Open Loops

- OBPI-0.30.0-05 (docs/runbook wiring) — not started.
- OBPI-0.30.0-06 (content-boundary doctrine doc) — not started.
- ADR-0.30.0 closeout (STRUCTURAL-FENCE audit) — blocked on all six OBPIs.
- The docs/→`.gzkit/` wholesale relocation declared by OBPI-06 is a forced
  subsequent decision, NOT performed in this ADR (operator: "a delicate matter").

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.30.0-04-okf-cli-surface` → ATTESTED COMPLETED
- [ ] `uv run -m unittest tests.commands.test_knowledge -v` passes (6 tests)
- [ ] `uv run gz covers OBPI-0.30.0-04 --json` → 4/4 covered
- [ ] Branch matches: `git branch --show-current` → main
- [ ] `uv run gz obpi lock list` → no lock held on OBPI-0.30.0-04 (after release)

## Evidence / Artifacts

- `tests/commands/test_knowledge.py` — 6 REQ-derived unittest cases (4 classes), all @covers-decorated
- `features/knowledge.feature` — 2 behave smoke scenarios (@REQ-0.30.0-04-04)
- `docs/user/manpages/knowledge-generate.md` — corrected output path
- `docs/user/manpages/knowledge-refresh.md` — corrected output path
- `docs/design/adr/pre-release/ADR-0.30.0-okf-documentation-knowledge-structure/obpis/OBPI-0.30.0-04-okf-cli-surface.md` — completed brief (status Completed, req_atomic declared)

## Environment State

Python 3.13 via uv; macOS (darwin). No environment-specific state affects resumption.
