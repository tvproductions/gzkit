---
obpi_id: OBPI-0.0.54-03-agents-md-map-conformance-validator
parent_adr: ADR-0.0.54-agents-md-map-not-encyclopedia-doctrine
lane: heavy
kind: foundation
abandoned: false
created_at: 2026-05-25T18:00:00+00:00
last_lock_event_ts: 2026-05-25T16:20:43.001216+00:00
last_lock_event: obpi_lock_claimed
agent: claude-code-595954b5
session_id: 595954b5-e85f-4e7d-a148-54e283529e24
last_commit_sha: 30acf5a0
branch: main
branch_state: main, 0 ahead 0 behind origin/main, working tree dirty (~10 modified + 7 untracked, all in scope)
pipeline_stage: stage-2-implement
pipeline_resume_point: stage-2-task-2.6-template-lift
ttl_remaining_minutes_at_handoff: ~20
---

# OBPI-0.0.54-03 — R1+expansion, Task 2.5 complete, stopped at Task 2.6 entry

## Named decision context (Token-Block Sub-Invariant 2 § 3)

Session paused for token-budget reasons (operator request). Lock retained — pipeline is mid-Stage-2 with concrete resume work. Three operator course-corrections were absorbed in flight during this session and the OBPI scope was expanded under R1+expansion (see § Scope amendments below). Three of seven implementation tasks are done; four remain. Next agent picks up at Task 2.6 (template lift) with the audit receipt PASS and the validator working at the correct layer.

## What landed this session

| # | Task | Status | Evidence |
|---|---|---|---|
| 1 | RED tests + validator stub | DONE | `src/gzkit/governance/trust_audits/agents_md_map_conformance.py` (stub-then-impl); `tests/governance/test_agents_md_map_conformance.py` (11 tests) |
| 2 | Validator logic (made 10/11 tests GREEN) | DONE_WITH_CONCERNS | surfaced 7 hard findings against rendered AGENTS.md — operator corrected to template layer |
| 2.5 | Retarget validator at template (shape) + rendered (budget only) | DONE_WITH_CONCERNS | `_TEMPLATE_REL_PATH` + `_RENDERED_REL_PATH` split landed; current real output 6 hard on template, 1 hard on rendered, 0 advisory |

Course corrections (per Behavior Rule 11 — `improvement` record at `.gzkit/insights/agent-insights.jsonl` line 104, 2026-05-25T17:30:00Z):

1. Validator was authored against rendered AGENTS.md. Operator: *"AGENTS.md is template-constructed."* Corrected by retargeting at `src/gzkit/templates/agents.md`.
2. 5k recovery-plan target forgotten in favor of OBPI-01's 15k compromise. 5k requires ADR-0.0.37 completion + a follow-up registry-migration pool ADR. Tracked in **GHI #533** (blocker comment names the routing).
3. Insights record `evidence` field was a string; canonical schema is `list`. Fixed; `gz validate --insights-shape` green.

## What remains (4 tasks)

| # | Task | Why | Approximate scope |
|---|---|---|---|
| 2.6 | Template lift pass | The validator's 6 hard template findings (2 prohibited titles at AGENTS.md lines 113 `Anti-patterns` + 278 `Worked example`; 4 long paragraphs at lines 19, 188, 266, 288) live in `src/gzkit/templates/agents.md`. Lift verbatim to `docs/governance/agent-contract-rationale.md` per OBPI-02 convention; tighten template to fit 15k post-render. Brief REQ-10 absorbs this work under PRIME DIRECTIVE Rule 4. | `src/gzkit/templates/agents.md` + `docs/governance/agent-contract-rationale.md`; ~6 lift sections + targeted tightening. Estimated ~600-1000 chars removed from template by lift; another ~5000 chars by tightening to reach 15k rendered. Opus-complexity. |
| 2.7 | Re-render AGENTS.md | `uv run gz governance render --target agents-md`; verify `gz validate --invariant-coherence` passes + `gz validate --agents-md-map-conformance` passes within 15k budget. | One CLI invocation + verification. |
| 3 | Wire validator into validate_cmd / parser_maintenance / quality | `--agents-md-map-conformance` flag + `_explicit_scope_runners` entry + `_build_check_steps` entry; mirror sibling `instructions_files_budget` at exact line citations in plan. | `src/gzkit/commands/validate_cmd.py` (~4 sites), `src/gzkit/cli/parser_maintenance.py` (1 site), `src/gzkit/commands/quality.py` (2 sites). Sonnet-complexity. |
| 4 | Manpage entry with observed CLI output | New H3 subsection `### \--agents-md-map-conformance` in `docs/user/manpages/validate.md` mirroring the `--chores-layout` shape; EXAMPLES block must use OBSERVED `uv run gz validate` output, not invented. | `docs/user/manpages/validate.md` (one section). Haiku-complexity. |
| 5 | Present OBPI Acceptance Ceremony (Stage 4) | Narrator-dispatched evidence packet → operator attestation → `gz obpi complete` with attestation text. | Per `gz-obpi-pipeline` § Stage 4 template. |

## Outstanding craft concern (must address in Task 2.6 or before)

The keystone test `test_happy_path_against_lifted_agents_md` was designed as "expected to fail until Task 2.6." That is a designed-to-fail unit test, which violates `.gzkit/rules/tests.md` § Rule 6 (*"a test that can't fail when business logic changes is wrong"* — corollary: *"a test designed to be RED is wait-for-someone-else dressed as TDD"*). Two right-shape options for the next agent:

1. **Split:** unit test asserts "validator passes on a synthetic conforming template fixture" (always green); a separate `tests/governance/test_real_template_conformance.py` (or a new `gz validate` scope) verifies the real template + rendered output, allowed to fail until the lift lands. The validator failure becomes a `gz check` exit-3 forcing function, not a unit-test ambiguity.
2. **Sequence:** do Task 2.6 (lift) FIRST so the keystone naturally turns green at unit-test time, then keep the unit test as-is. Task 2.5 was already done; this is the simpler path.

Recommendation: sequence (option 2). Lift → re-render → keystone goes green naturally. If the lift cannot reach 15k in one pass, split the keystone (option 1) at that point.

## Scope amendments this session (binding for resume)

The original brief authored Allowed Paths assuming AGENTS.md was direct-edit; OBPI-02 was assumed to have delivered doctrine-conformant AGENTS.md. Both assumptions broke. Brief amended:

| Section | Change |
|---|---|
| Allowed Paths | Added `src/gzkit/templates/agents.md` (audit-surface for shape) + `docs/governance/agent-contract-rationale.md` (lift target). |
| Denied Paths | Made explicit: rendered `AGENTS.md` is never hand-edited (state-doctrine Layer-3); `.gzkit/invariants/*.json` is ADR-0.0.37 territory; budget JSON stays OBPI-01/04. |
| REQ-01 | Reframed: shape criteria (a)(b)(c) against template; budget criterion (d) against rendered. |
| REQ-02 | Reframed: happy path is template + lifted rationale doc conforming, plus rendered ≤ 15k post-render. |
| REQ-07 / REQ-08 | Adjusted to match the layer split. |
| REQ-10 (new) | Absorbs the lift OBPI-02 didn't finish under PRIME DIRECTIVE Rule 4. |
| STOP-on-BLOCKERS | Revised to acknowledge OBPI-02's attested-but-incomplete state. |

5k destination explicitly deferred to **GHI #533** as ADR-0.0.37-dependent. Today's target is 15k.

## Resume recipe (for next agent)

```bash
# 1. Confirm lock still claimed by this agent (TTL was 120m; check elapsed):
uv run gz obpi lock list

# 2. Re-read the plan + brief + this handoff:
cat C:/Users/Jeff/.claude/plans/quiet-dazzling-starfish.md
cat docs/design/adr/foundation/ADR-0.0.54-agents-md-map-not-encyclopedia-doctrine/obpis/OBPI-0.0.54-03-agents-md-map-conformance-validator.md
cat .gzkit/handoffs/20260525T180000Z-obpi-0.0.54-03-r1-expansion-task-2.5-complete.md

# 3. Re-orient on validator's current behavior against real surface:
uv run python -c "
from pathlib import Path
from gzkit.governance.trust_audits.agents_md_map_conformance import audit_agents_md_map_conformance
errors = audit_agents_md_map_conformance(Path('.'))
hard = [e for e in errors if e.type == 'agents_md_map_conformance']
print(f'expected: 6 template-hard + 1 rendered-hard = 7 total')
print(f'actual: {len(hard)}')
"

# 4. If lock TTL expired, claim fresh and resume:
uv run gz obpi lock claim OBPI-0.0.54-03-agents-md-map-conformance-validator

# 5. Refresh plan-audit receipt (handoff edit may have invalidated freshness):
uv run gz plan audit OBPI-0.0.54-03

# 6. Resume Stage 2 at Task 2.6 (template lift):
#    Dispatch implementer (opus) to lift the 6 template-side hard findings
#    to docs/governance/agent-contract-rationale.md verbatim + tighten.
#    Then 2.7 (re-render), 3 (wire), 4 (manpage), 5 (ceremony).

# 7. The full plan + per-task dispatch prompts are in:
#    C:/Users/Jeff/.claude/plans/quiet-dazzling-starfish.md
```

## Cross-references

- Parent ADR: `docs/design/adr/foundation/ADR-0.0.54-agents-md-map-not-encyclopedia-doctrine/`
- Brief (amended): `.../obpis/OBPI-0.0.54-03-agents-md-map-conformance-validator.md`
- Plan: `C:/Users/Jeff/.claude/plans/quiet-dazzling-starfish.md`
- Plan-audit receipt: `.claude/plans/.plan-audit-receipt-OBPI-0.0.54-03.json` (PASS)
- Pipeline marker: `.claude/plans/.pipeline-active-OBPI-0.0.54-03.json`
- Insights course-correction record: `.gzkit/insights/agent-insights.jsonl` line 104
- Filed in this session: GHI #532 (manpage filename drift — 4 brief files), GHI #533 (5k → ADR-0.0.37 dependency)
- Lock claim event: `.gzkit/ledger.jsonl` `obpi_lock_claimed` ts `2026-05-25T16:20:43.001216+00:00`
- Lock file: `.gzkit/locks/obpi/OBPI-0.0.54-03-agents-md-map-conformance-validator.lock.json`

## Token-block discipline statement

This handoff satisfies Token-Block Sub-Invariant 2 § Register-Entry Minimum-Information Rule (1) last lock-event timestamp, (2) last commit SHA, (3) named decision context, (4) branch state. Lock is **retained** (not released) for next-session resume. If TTL expires before resume (24h default per Sub-Invariant 4, currently 120m configured), the SessionStart hook will reap with an `abandoned_by_reaper` register entry; this handoff is the antecedent route for the next agent to find context.
