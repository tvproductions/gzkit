---
mode: CREATE
adr_id: ADR-0.0.72
branch: main
timestamp: "2026-06-13T15:30:06Z"
agent: claude-code
obpi_id:
session_id:
continues_from:
---

<!-- Handoff document for ADR-0.0.72 — created by claude-code at 2026-06-13T15:30:06Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume,
at every freshness level, present the advised next steps to the operator and
obtain explicit authorization before acting. You advise; the operator rules.

## Current State Summary

Session collapsed `ADR-0.0.72-meta-governance-coherence` as over-construction
(operator decision). The ADR proposed catching validator-versus-artifact
incoherence by adding another always-on validator
(`gz validate --writer-model-roundtrip`). A pipeline run on the ADR proved the
regress: landing that meta-validator tripped four more incoherences it does not
address (brief-reconcile glob-blindness, InsightRecord-vs-Rule-11 drift,
plan-audit self-receipt, lock-release coupling). The ADR is tombstoned in place
with a DO-NOT-IMPLEMENT banner (committed `e8ee2624`); OBPIs 01 through 04 are
dead. `main` is green and synced. Two real commits landed this session
(`e0de3e67`, `e8ee2624`).

## Important Context

- The cure identified is coherence-by-construction: one declaration projected
  deterministically to its derived surfaces, so the contradiction class cannot
  recur. This is the OPPOSITE direction from the collapsed ADR (which added a
  validator to police drift after the fact). It is already a booked workstream:
  Config-first SSOT, return-to-health Tier-2 section 2.5.
- The campaign `docs/governance/build-to-1.0-campaign-2026-06-10.md` is the one
  canonical plan. Phase A is done; the road is B (CMS) then C (MOTD) then
  D-E-F-G-H to 1.0, then I to reduce. The campaign is NOT up to date: it does
  not mention ADR-0.0.71 or ADR-0.0.72 (both authored off-plan after it), and
  its GHI register snapshot reads 37 open versus 44 actual.
- This handoff was created as a LIVE TEST of the handoff system, which the
  operator flagged as janky. Findings recorded under Decisions Made.

## Decisions Made

- **Decision:** Collapse ADR-0.0.72; route its real bugs to lightweight GHIs.
  **Rationale:** It answered over-auditing with more auditing; the session was
  empirical proof of the regress.
  **Alternatives rejected:** Build the meta-validator (the metastasis); rewrite
  four briefs to satisfy a string-counter (gaming a validator, caught as VIBES);
  formally withdraw via more machinery (more cards).
- **Decision:** File one new GHI (#612) and route the rest to existing homes.
  **Rationale:** Prior-art lookup showed the backlog already tracked C4 (#575)
  and brief-reconcile crudeness (#581); filing duplicates is the failure the
  lookup prevents.
  **Alternatives rejected:** File four fresh GHIs.

## Immediate Next Steps

These are ADVISORY — present to the operator and await authorization.

1. Decide whether to start campaign Phase B (CMS, `ADR-0.0.37` build-out, B.1 at
   13/19), or first inspect B.0 (`ADR-0.0.70`) whose Gate-5 was repudiated
   (GHI #608) and may not be clean.
2. Treat GHI #612 (HandoffFrontmatter superset model) as a direct-fix: widen the
   model to accept the fields its own writers emit and the slug-form `obpi_id`,
   preserving typo-defense.
3. Consider whether some subtraction moves before 1.0 rather than all deferred
   to Phase I — the one campaign knob today's evidence argues for revisiting.

## Pending Work / Open Loops

- GHI #612 (new), #575 and #581 (evidence added this session) carry the real
  bugs; none is fixed yet.
- The campaign register needs a triage regen (37 to 44 open) and the two
  off-plan ADRs (0.0.71, 0.0.72) need a disposition in it.
- The handoff system jank itself is open: the programmatic CREATE/RESUME API is
  NOT IMPLEMENTED (this handoff was authored by hand), and the `obpi_id` regex
  rejects slug-form ids (C3, GHI #612).

## Verification Checklist

- [ ] `git branch --show-current` reads `main`
- [ ] `git log --oneline -2` shows `e8ee2624` and `e0de3e67`
- [ ] `uv run gz validate --insights-shape` passes (course-correction insight is valid)
- [ ] GHI #612 exists and is open; #575 and #581 carry this session's evidence comments

## Evidence / Artifacts

- `docs/design/adr/foundation/ADR-0.0.72-meta-governance-coherence/ADR-0.0.72-meta-governance-coherence.md` — tombstoned ADR with DO-NOT-IMPLEMENT banner
- `tests/governance/test_agents_md_map_doctrine.py` — renamed from the OBPI-numbered module
- `tests/governance/test_agents_md_map_doctrine_application.py` — renamed from the OBPI-numbered module
- `.gzkit/insights/agent-insights.jsonl` — course-correction insight (Behavior Rule 11)
- `.gzkit/handoffs/20260613T143354Z-OBPI-0.0.72-01-writer-model-roundtrip-validator-abandoned.md` — the lock-release abandon register entry
- `docs/governance/build-to-1.0-campaign-2026-06-10.md` — the canonical campaign plan

## Environment State

Windows 11, Python 3.13, `main` branch, working tree clean at handoff time.
