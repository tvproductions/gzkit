---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-19T11:27:21Z"
agent: claude-code
obpi_id: OBPI-0.0.73-09-waiver-ratchet-honesty-contract
session_id:
continues_from:
last_lock_event_timestamp: "2026-06-19T10:50:00.000000+00:00"
last_commit_sha: 14c951d0
---

## Current State Summary

OBPI-0.0.73-09-waiver-ratchet-honesty-contract is COMPLETE and operator-attested
("attest completed", g0). It lands the waiver-ratchet honesty contract
(ADR-0.0.73 Boundary Invariant #8): `gz validate --waiver-ratchet` fails closed
(exit 3) on any registered waiver/grandfather/baseline surface lacking one of
{closed-set lock, dated cutover, monotonic shrink-ratchet}, and on any
unregistered `data/*_waivers.json` (silent bypass). 10 surfaces are ratcheted —
`behave_coverage_waivers` (522) under a shrink-ratchet baseline,
`historical_self_close_waivers` under its proven closed-set lock. The verb
self-registers as a `bound` QC step with a genuine negative control. This
handoff is the register entry that pairs with the Stage-5 lock release.

**This completes ADR-0.0.73's recovery**: `gz adr fidelity ADR-0.0.73` now passes
8/8 Fidelity Assertion rows — the ADR passes its own check (Boundary Invariant
#5). OBPI-02 repaired, OBPI-07 (evaluator substance), OBPI-08 (fidelity-presence),
OBPI-09 (waiver-ratchet) all landed.

## Important Context

- A nice proof the law works: the behave REQ-coverage gate wanted scenario tags,
  and the honest alternative (a waiver in `behave_coverage_waivers.json`) would
  have GROWN that list past its 522 baseline — which the new validator forbids.
  So coverage is real behave scenarios (`features/waiver_ratchet.feature`), not a
  waiver. The law prevented its own bypass.
- Completed with `--accept-security-floor` (additive/defensive validate-scope
  overlap, same posture as OBPI-08) and `--accept-stale-reconciliation` (GHI #193
  benign ledger-churn).

## Decisions Made

- Built OBPI-09 whole (not split) despite the manual scorecard's "too broad":
  the registry-driven design makes the retrofit uniform DATA (declare a mechanism
  per surface), not bespoke per-surface code, so the law is one coherent unit.
- `behave_coverage_waivers` → shrink-ratchet (a 522-entry list; per-entry
  `added_under` backfill rejected in favor of the baseline mechanism).
- `distribution_baseline_manifest.json` explicitly `excluded` (a distribution
  manifest, not a debt-waiver).

## Immediate Next Steps

1. Git-sync #2 commits the reconcile + ADR-status refresh.
2. ADR-0.0.73 is now self-passing (8/8 fidelity). Remaining OBPIs under the ADR,
   if any, follow their own state; the ADR closeout ceremony can be considered
   once all OBPIs are complete and the operator directs it.
3. Per-surface waiver DEBT reduction (shrinking the committed baselines as the
   underlying gates are fixed) is named follow-up, not this OBPI.

## Verification Checklist

- [x] `gz validate --waiver-ratchet` exit 0 (10 surfaces ratcheted)
- [x] `gz validate --qc-binding` green (Waiver ratchet bound + genuine NC)
- [x] `gz validate --cli-alignment` + `gz cli audit` green (108/108)
- [x] `gz validate --documents` + `mkdocs build --strict` exit 0
- [x] 6309 unittests green (arb-step-unittest-abac0863); 4 behave scenarios pass
- [x] ruff (arb-ruff-20bc6ab2) + typecheck (arb-step-typecheck-e457c667) clean
- [x] `gz covers OBPI-0.0.73-09` behavior_uncovered_reqs: 0
- [x] `gz adr fidelity ADR-0.0.73` 8 pass, 0 fail (BI #5)

## Evidence / Artifacts

- Completion receipt: `attested_completed`, attestor g0, `operator-verbatim-conversational`
- New validator: `src/gzkit/governance/trust_audits/waiver_ratchet.py`
- Registry: `data/waiver_ratchet_registry.json` (10 surfaces + 1 excluded)
- Tests: `tests/governance/test_waiver_ratchet_scope.py` (12), `features/waiver_ratchet.feature` (4)
- ARB receipts: `arb-step-unittest-abac08632fcc4586876dacb4d59b46f6`,
  `arb-ruff-20bc6ab2121f4436ac106a82986eb485`,
  `arb-step-typecheck-e457c667c8fa4d08834b60c06a336a2a`

## Pending Work / Open Loops

- Per-surface waiver-debt reduction (ratchet the baselines DOWN as gates are
  fixed) — named follow-up, surface by surface.
- ADR-0.0.73 closeout ceremony when the operator directs (the ADR now passes its
  own fidelity check, the precondition the recovery freeze gated on).
