---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-17T08:52:07Z"
agent: claude-code
obpi_id: OBPI-0.0.73-02-qc-binding-validate-scope
session_id: e5955c2b-ddc0-4214-b91d-c1acf5ecc7b1
continues_from:
last_lock_event_timestamp: '2026-06-17T08:53:03.381581+00:00'
last_commit_sha: c80d3284
---

<!-- Handoff document for ADR-0.0.73 — created by claude-code at 2026-06-17T08:52:07Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

OBPI-0.0.73-02-qc-binding-validate-scope is **attested-complete** (operator
attestation "attest completed" relayed via `gz obpi complete`, attestor g0,
completion type operator-verbatim-conversational). This handoff is the
completion register entry required by the ADR-0.0.41 lock-handoff coupling so
the lock can be released; it is NOT an abandonment.

Work delivered: `gz validate --qc-binding` — a fail-closed (exit 3) validator
scope wired into the `gz check` default pipeline as the "QC binding" bound step.
Two detection channels: (1) static theater-signature detection against the six
ADR-0.0.37 facade signatures via `QCStep.theater_flags`; (2) behavioral
negative-control execution via a callable NC registry (a step whose NC returns
exit 0 is hollow/theater). The NC registry ships empty in OBPI-02 — OBPI-06
registers concrete negative controls for every existing step.

All quality gates green: lint, typecheck, unittest (6235 tests + 22 new), mkdocs
--strict, cli-alignment. `gz covers` reports 8/8 REQ coverage (100%). The OBPI
completion required a `--accept-security-floor` override (the brief touches
`src/gzkit/quality.py` which is path-registered in the `subprocess_user_input`
security surface; an independent spec-reviewer confirmed the change is a
hardcoded-constant `run_command` wrapper with no new injection surface —
false-positive-override-justified).

Last action: `gz obpi complete` succeeded (exit 0). Git tree has uncommitted
governance + source edits awaiting git-sync #1.

## Important Context

- **OBPI-01 dependency:** `src/gzkit/qc_binding.py` (the `QCStep` model +
  `build_qc_registry()` deriving from `_build_check_steps()`) was delivered by
  OBPI-0.0.73-01. OBPI-02 consumes that registry; it added the `"QC binding"`
  entry to `_STEP_CLASSIFICATION` because OBPI-01's KeyError sentinel requires
  every `_build_check_steps()` name to be classified.
- **Security floor is coarse/path-based:** any edit to `src/gzkit/quality.py`
  trips the `subprocess_user_input` floor regardless of content. Future
  audit-wrapper additions to that file will re-trip it; the override path is the
  intended discharge (GHI #462).
- **Brief allowlist drift:** the brief was amended in-flight to add 3 coupled
  surfaces (`parser_maintenance.py`, `commands/quality.py`, `qc_binding.py`) and
  later `tests/commands/test_skills.py` + `src/gzkit/traceability.py` (the latter
  added by `gz brief reconcile` because the test imports `@covers`).
- **behave waiver:** all 8 REQs are unit-proven (BEHAVIOR/SUPPORT/STRUCTURAL-FENCE),
  not behave-observable; a waiver entry was added to
  `data/behave_coverage_waivers.json`. The cross-OBPI behave integration belongs
  at ADR-0.0.73 closeout.

## Decisions Made

- **Decision:** Accept the security-scan floor override via `--accept-security-floor`.
  **Rationale:** Independent spec-reviewer determination (false-positive-override-justified)
  + git diff confirmation: the only change to a registered surface is the additive
  `run_qc_binding_audit` wrapper (hardcoded constant command, no caller-supplied
  subprocess argument; `run_command` shlex/shell=False primitive untouched).
  **Alternatives rejected:** (a) self-accept without evaluator — operator
  course-corrected to require the evaluator first; (b) halt and require a real
  security-scan receipt — the canonical security-scan slot is unfilled project-wide
  (awaits the agentic-security-review toolchain ADR), so no receipt is obtainable yet.

- **Decision:** Declare `req_atomic:` for all 8 REQs in brief frontmatter.
  **Rationale:** Each REQ is one indivisible labor unit of the single --qc-binding
  scope; none decomposes into parallel seq=02+ sub-tasks (ADR-0.0.64 task-envelope
  exemption).
  **Alternatives rejected:** subdividing via `gz task start --seq next` — there is
  no genuine labor below the REQ level here.

## Immediate Next Steps

<!-- ADVISORY ONLY — present for operator review before acting. -->

1. Release the OBPI lock: `uv run gz obpi lock release OBPI-0.0.73-02-qc-binding-validate-scope`
   (this handoff is the register entry that unblocks the release).
2. Git-sync #1: `uv run gz git-sync --apply` to commit all governance + source edits.
3. Reconcile: `uv run gz obpi reconcile OBPI-0.0.73-02-qc-binding-validate-scope`.
4. Refresh parent ADR view: `uv run gz adr status ADR-0.0.73-verification-layer-binding-audit --json`.
5. Git-sync #2: `uv run gz git-sync --apply` to commit the reconcile output.

## Pending Work / Open Loops

- **Remaining ADR-0.0.73 OBPIs (0/7 → 2/7 after this):** OBPI-01 (registry —
  landed), OBPI-02 (this — complete). Still open: OBPI-03 (`## Fidelity Assertions`
  + `gz adr fidelity` gate), OBPI-04 (closeout/audit repoint onto fidelity gate),
  OBPI-05 (absorb ADR-pool.obpi-pipeline-dispatch-attestation), OBPI-06 (self-check
  + facade regression corpus — this is where the empty NC registry gets filled),
  OBPI-07 (evaluator truth-binding, GHI #624).
- **OBPI-06 fills the NC registry:** OBPI-02 shipped `register_negative_control()`
  empty by design. OBPI-06 must register an honest negative control per existing
  bound step or the behavioral channel stays dormant.
- **Security-scan canonical slot:** unfilled project-wide; the override will keep
  firing on quality.py edits until the agentic-security-review toolchain ADR lands.

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.0.73-02-qc-binding-validate-scope` shows attested-complete
- [ ] `uv run gz obpi lock list` (lock still held until step 1 releases it)
- [ ] `uv run gz validate --qc-binding` exits 0
- [ ] `uv run -m unittest tests/governance/test_qc_binding_scope.py -v` — 22/22 pass
- [ ] Branch matches: `git branch --show-current` → main
- [ ] `uv run gz validate --cli-alignment` exits 0

## Evidence / Artifacts

- `src/gzkit/governance/trust_audits/qc_binding.py` — the --qc-binding audit (theater-signature + NC execution)
- `tests/governance/test_qc_binding_scope.py` — 22 unit tests, all 8 REQs covered
- `src/gzkit/commands/validate_cmd.py` — `_run_qc_binding_scope` dispatch
- `src/gzkit/cli/parser_maintenance.py` — `--qc-binding` argument
- `src/gzkit/quality.py` — `run_qc_binding_audit` runner
- `src/gzkit/commands/quality.py` — `("QC binding", run_qc_binding_audit)` gz check wiring
- `src/gzkit/qc_binding.py` — `"QC binding"` classification
- `docs/user/manpages/validate.md` — `--qc-binding` manpage section
- `data/behave_coverage_waivers.json` — behave waiver for the 8 unit-proven REQs
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-02-qc-binding-validate-scope.md` — completed brief

## Environment State

- Python 3.13; uv-managed venv. Last commit at handoff: `5b3a5560` (pre-sync).
- Lock claimed at 2026-06-17T08:18:17Z (postdated by this handoff's 08:52:07Z timestamp).
