---
mode: CREATE
adr_id: null
branch: main
timestamp: '2026-07-25T07:01:56Z'
agent: claude-code
session_id: 8b138d99-08e6-48ca-8f39-8c8d4464d5c6
continues_from: 20260725T010546Z-degrading-tier-696-614-closed-580-refuted.md
---

## Current State Summary

Resumed the 2026-07-25T01:05Z handoff under explicit operator authorization (verbatim: 'do 1 to 4') and worked all four advised steps. Step 1 was found stale on arrival: the operator had already ruled on #580's signal 26 minutes after that handoff was written, so the open item was the declaration, not the signal. Drafted and got the survival declaration ratified (must-survive = ranks 1-11, 21582 B, 11186 B of growth headroom against 560 B today). Step 2 destination ruled: split, witness half to #712 and reorder half to pool. Step 3 built and landed as f09bef8e, mechanizing GHI-blocker freshness in ghi-triage; the first live run found five genuine stale blockers and one false positive that was fixed as a class. Step 4's latent tier deliberately NOT pulled because the campaign takes precedence; its #607 half was discharged by unparking the issue. Authoring THIS handoff then surfaced a second defect and it was fixed before the document was accepted: an ADR-less handoff that asserts continues_from carried the chain link but inherited none of its predecessor's rulings, so the first draft of this very file silently dropped four booked operator rulings. Landed as dc0f85ac. Main at dc0f85ac, tree clean, no active OBPI lock, no in-flight pipeline. Open GHI count unchanged at 15.

## Important Context

Three constraints shaped the work and none were obvious from the advised steps. FIRST: a handoff's advised steps can be stale about the operator's OWN rulings, not just about code. Step 1 asked for a ruling that had already been given at 01:31:55Z, recorded in the #580 body but not in the handoff written at 01:05:46Z. The Claim Verification Gate caught it because the gate verifies preconditions, and reading the issue body was the check. SECOND: the survival declaration needed an ordering principle an agent can defend, and 'criticality' is not one. RECOVERABILITY is: control-surfaces plus governance-doctrine-surfaces are 4225 B of pointers whose loss is repairable by reading the named file, while operator-doctrine-verbatim-canon is 4063 B existing nowhere else in the per-turn surface. Near-identical byte cost, opposite consequence on loss. THIRD: the blocker-freshness mechanism nearly shipped with a confident false gate. GHI #691's blocker cites 'skill-surface-sync.md #6' -- a RULE number -- which resolved against the tracker to genuinely-closed GHI #6. Low ordinals almost always hit long-closed issues, so an unguarded bare-hash match manufactures false gates on issues whose preconditions never moved, and one cried wolf gets the whole signal ignored. Fixed as a class with an ordinal-context guard, with a companion test asserting genuine citations survive it. The same bare-hash pattern lives in handoff_api._extract_references; its input is authored next-step prose rather than arbitrary comment bodies, so the exposure is much narrower, but it is the same shape.

## Decisions Made

- [operator-ruled] Work advised steps 1 through 4 (verbatim authorization "do 1 to 4", booked via gz handoff authorize, session 8b138d99).
- [operator-ruled] Reframe #580 from periphery criticality to truncation survival (operator verbatim 2026-07-25: "reframe #580 to truncation survival"). The mechanism is unchanged; the warrant and ranking source are replaced. Arrived AFTER the prior handoff was written and was carried by neither its Decisions nor its Settled section.
- [operator-ruled] #580's survival declaration is ratified with must-survive = ranks 1-11 (operator-doctrine-verbatim-canon first, architectural-boundaries last), cumulative 21582 B, leaving 11186 B of growth headroom. Ranks 12-20 are declared expendable-under-pressure because they are recoverable, not because they are unimportant. Ratified as data only: applying the order to committed AGENTS.md remains a Layer-1 canon change requiring Gate-5 attestation.
- [operator-ruled] #580's destination is SPLIT, not a single home. The witness half (declaration plus the assertion that every must-survive section begins before the vendor cap, plus fail-closed declaration completeness) lands with GHI #712. The reorder half (permuting the surface) parks to pool post-1.0, because only it is expensive and it pays off only once the cap binds. This supersedes the withdrawn whole-issue pool recommendation, which rested on a false claim about Movement C.
- [operator-ruled] GHI #607 is UNPARKED. Attested REQ-0.14.0-04-04 asserts a detection capability and is silent on scope, so an adopter-scope predicate that preserves gzkit's own self-enforcement does not falsify it. No repudiation and no amendment are required to work the issue.
- [operator-ruled] Mechanize GHI-blocker freshness by extending the bundled triage script rather than adding a gz verb, so the signal lands in the report the operator already reads before pulling work.
- [agent-chose] Routed step 3 as a direct fix with NO GHI. Routing facts were computed, not intuited: 289 fix() commits in 60 days against a threshold of 3, one surface, defect surfaced in flight, unit-coverable. Filing a GHI purely to construct a compliant Task trailer is the moratorium violation task-discovery.md names explicitly, so the commit is the work order and the receipt.
- [agent-chose] Fixed the bare-hash false positive as a CLASS (an ordinal-context guard covering rule, item, step, section, and document-name forms) rather than special-casing the observed #6, and paired it with a test asserting genuine GHI citations still survive the guard.
- [agent-chose] Did NOT pull the latent tier. Step 4 is conditional on "if the campaign does not take precedence" and the campaign does, by its own Magna Carta standing. Read the operator's non-authorization of step 5 as scoping the request, NOT as withdrawing the campaign, since step 5 was a restatement of a standing constraint rather than an action.
- [agent-chose] Did NOT edit campaign line 131's stale 1/5 count for ADR-0.34.0, tracking it as pending work instead. The count is factual and the correction is one line, but the campaign is Magna Carta and amendments are operator-ratified.
- [agent-chose] Fixed the ADR-less ruling carry-forward (dc0f85ac) rather than accepting a handoff that under-carried. Authoring this document surfaced it: the first draft asserted continues_from and inherited none of its predecessor's four booked operator rulings, because _carried_settled re-derived lineage through _newest_predecessor, which returns None without an ADR. Shipping the artifact as drafted would have discarded the very rulings this handoff was written to seat.
- [agent-chose] Made _carried_settled take the ALREADY-RESOLVED predecessor instead of re-deriving one, so the chain link and the carried rulings share a single authority. That is what the old docstring already claimed and what the ADR-less path disproved; the alternative, teaching _carried_settled to re-derive the same link a second way, would have left two authorities free to drift apart again.
- [agent-chose] Recorded the three rulings made by selection as selections, and quoted verbatim only the words the operator actually typed. Authoring a verbatim quote for a choice made by menu would manufacture authority, which is the same failure class as fabricating a receipt id.

## Immediate Next Steps

1. Build GHI #712's witness against the ratified declaration. Three parts, all named on the issue: seat the 20-entry ordered survival list as data, assert every must-survive section begins before the consuming vendor's cap, and fail closed when a rendered section is absent from the declaration. Report the budget distance as a warning with byte-distance, never fail-closed on the budget itself, because fail-closing there would re-couple the core to an adapter limit and the 2026-07-06 decoupling ruling forbids it.
2. Decide the campaign line 131 correction. docs/governance/build-to-1.0-campaign-2026-07-18.md records ADR-0.34.0 at 1/5; Layer-2 says 2/5 (OBPI-0.34.0-02 ATTESTED COMPLETED, anchor 1c0f525). The session-orientation banner quotes that line, so the stale count is re-injected at every session boot. Left unedited because campaign amendments are operator-ratified.
3. Refresh the triage cache. .gzkit/cache/triage/rank.json lists closed #696 and #614 and predates #712, and it now under-reports the queue's actual shape. Re-running triage also picks up the new blocker-freshness column, which flags four latent-tier items (#533, #581, #594, #641) whose recorded preconditions have moved.
4. Open the #607 design conversation. The park is lifted but the design is not made; options 1 through 3 in its body remain genuinely distinct, and only option 3 is cleared by the unpark ruling.
5. The campaign RULES sequencing. Movement A is topmost: ADR-0.35.0 at 0/9 and ADR-0.34.0 capstone at 2/5. Steps 1 through 4 above are triage and repair work that the campaign refines rather than substitutes for; none of them is a campaign amendment.

## Pending Work / Open Loops

Campaign line 131 of docs/governance/build-to-1.0-campaign-2026-07-18.md says ADR-0.34.0 is 1/5; Layer-2 says 2/5 because OBPI-0.34.0-02-authoring-time-kind-rejection is ATTESTED COMPLETED at anchor 1c0f525. The session-orientation banner quotes that line, so both are stale by one OBPI. NOT edited unilaterally: the campaign is Magna Carta and amendments are operator-ratified, and correcting a count on it should be the operator's call even though the count is factual. GHI #580 stays open with a ratified declaration and a registered destination per half. GHI #712 now carries the witness half and is unbuilt. GHI #607 is unparked but undesigned; options 1-3 in its body remain genuinely distinct and only option 3 is cleared by the ruling. The triage cache .gzkit/cache/triage/rank.json is stale: it lists closed #696 and #614 and predates #712. Latent tier unworked by design. Four latent items (#533, #581, #594, #641) now show moved preconditions, which changes their pull-worthiness independent of the campaign. Campaign Movement D box 3 retains full scope: typed ruling events, a gz ruling verb, and the campaign body as a rendered Layer-3 view.

## Verification Checklist

gh issue view 580 --json state (OPEN, declaration ratified in comment 5077370364); gh issue view 607 --json state (OPEN, unparked in comment 5077370654); gh issue view 712 --json state (OPEN, absorbs witness half in comment 5077370598); git log --oneline -2 (dc0f85ac, f09bef8e); uv run -m unittest tests.governance.test_handoff_api (41 green, including TestAdrlessChainCarriesRulings); uv run python .gzkit/skills/ghi-triage/scripts/triage.py --limit 20 --format markdown (expect 5 stale-blocker rows, zero on #691); uv run -m unittest tests.skills.test_ghi_triage_blocker_freshness (19 green); uv run -m unittest -q (expect 7393 or more OK); uv run gz check (expect exit 0); uv run python -c to re-measure AGENTS.md byte offsets (expect 32208 B total, 560 B headroom)

## Evidence / Artifacts

Session commits: f09bef8e (ghi-triage blocker freshness), dc0f85ac (ADR-less ruling carry-forward). Changed surfaces: `.gzkit/skills/ghi-triage/scripts/triage.py`, `.gzkit/skills/ghi-triage/SKILL.md` (5.1.1 to 5.2.0), mirrors under `.claude/skills/ghi-triage/`, `.agents/skills/ghi-triage/`, `.github/skills/ghi-triage/`, `src/gzkit/skills/ghi-triage/SKILL.md`, and `src/gzkit/handoff_api.py`. Tests: `tests/skills/test_ghi_triage_blocker_freshness.py` (19 BEHAVIOR tests) and `tests/governance/test_handoff_api.py` (3 added, 41 total). Triage cache: `.gzkit/cache/triage/rank.json`. Campaign: `docs/governance/build-to-1.0-campaign-2026-07-18.md`. Predecessor: `.gzkit/handoffs/20260725T010546Z-degrading-tier-696-614-closed-580-refuted.md`. Receipts: `artifacts/receipts/arb-ruff-3492992facb24e83b45e3a76808b37d2.json`, `artifacts/receipts/arb-step-typecheck-fbda89f65cad4701998605bf36449e1f.json`, `artifacts/receipts/arb-step-unittest-bb1fbe5071da49a5887dcb13157f8944.json`. GHI comments: 580#issuecomment-5077370364, 712#issuecomment-5077370598, 607#issuecomment-5077370654.

## Settled Rulings

- Work the degrading tier starting with #696 (verbatim authorization booked via gz handoff authorize, session 81765765).
- Finish what is on the plate rather than deferring items for later sequencing rulings.
- Do not assert campaign-movement intent without reading it; the claim that Movement C is shrinking the pre-1.0 board was fabricated and is withdrawn. Movement C is Reduce the accretion.
- #696 defect 2 was the buildable cut; defects 3/4 were NOT to be left to an unbuilt ADR.
- Reframe #580 from periphery criticality to truncation survival (operator verbatim 2026-07-25: 'reframe #580 to truncation survival'). The mechanism is unchanged; the warrant and ranking source are replaced. Arrived AFTER the prior handoff was written and was carried by neither its Decisions nor its Settled section.
- #580's survival declaration is ratified with must-survive = ranks 1-11 (operator-doctrine-verbatim-canon first, architectural-boundaries last), cumulative 21582 B, leaving 11186 B of growth headroom. Ranks 12-20 are declared expendable-under-pressure because they are recoverable, not because they are unimportant. Ratified as data only: applying the order to committed AGENTS.md remains a Layer-1 canon change requiring Gate-5 attestation.
- #580's destination is SPLIT, not a single home. The witness half (declaration plus the assertion that every must-survive section begins before the vendor cap, plus fail-closed declaration completeness) lands with GHI #712. The reorder half (permuting the surface) parks to pool post-1.0, because only it is expensive and it pays off only once the cap binds. This supersedes the withdrawn whole-issue pool recommendation, which rested on a false claim about Movement C.
- GHI #607 is UNPARKED. Attested REQ-0.14.0-04-04 asserts a detection capability and is silent on scope, so an adopter-scope predicate that preserves gzkit's own self-enforcement does not falsify it. No repudiation and no amendment are required to work the issue.
