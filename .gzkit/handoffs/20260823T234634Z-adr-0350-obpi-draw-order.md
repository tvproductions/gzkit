---
mode: CREATE
adr_id: ADR-0.35.0
branch: main
timestamp: '2026-08-23T23:46:34Z'
agent: claude-code
session_id: abb68230-58ff-45d2-953f-e379a83fc9b5
continues_from: 20260823T224850Z-v0345-shipped-triage-ruled-adr-0350-is-next.md
---

## Current State Summary

`ADR-0.35.0-canon-entry-corpus-landing` stands at **1/10 OBPIs landed, closeout BLOCKED** (verified 2026-08-23, `uv run gz adr status ADR-0.35.0-canon-entry-corpus-landing`). `OBPI-0.35.0-09-codex-playback-wiring` is the single `attested_completed` brief; its completion events land in the ledger 2026-08-21/22. `OBPI-0.35.0-08-remember-post-append-advisory` is **IN PROGRESS**, Proof State `missing`, Completion `PENDING` (verified, `uv run gz obpi status OBPI-0.35.0-08-remember-post-append-advisory`). The other eight briefs are `pending` with `draft` brief status.

**No OBPI machinery moved this session.** `uv run gz obpi lock list` returns "No active locks". No pipeline marker was launched, no TASK started or completed, no brief edited, no implementer or reviewer dispatched. HEAD is `f55edfb35`, level with `origin/main` (ahead 0, behind 0).

The session's single work product is this document. The operator asked whether a preferred order exists for continuing the 0.35.0 OBPIs. It does, and it is declared in canon rather than a matter of judgment: every one of the ten briefs carries a `**Dependency order (ADR-0.35.0 § Scope Minimization):**` line, and those ten lines compose into one graph. That graph, its authoritative source, and the draw order it admits are transcribed in **Important Context** below so a restarting session draws work in the declared order instead of re-deriving one.

## Important Context

### Where the ordering doctrine actually lives

The briefs all cite `ADR-0.35.0 § Scope Minimization`. **That section is not in the ADR body** — it is `## 7. Scope Minimization` in `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/DESIGN_FORCING_FUNCTIONS.md:247`. Read it before trusting any transcription, this one included; it carries the cut lines and pairing rules verbatim.

### Declared dependency graph

Each bullet is transcribed from the cited brief's own `Dependency order` line. Cite the brief, never this handoff.

- **01 -> 02 -> 03** is the head chain and the smallest version that delivers value. It alone discharges GHI #635 and removes the live double-render (brief 01:57, brief 02:69, brief 03:88; § 7).
- **04** has no prerequisite inside ADR-0.35.0 and may land in parallel with 01-03 (brief 04:63).
- **05** depends on 01 and 04, and brief 03 names itself "a PREREQUISITE for 05, not a parallel workstream". Shipping 05 before 01-03 "ships a REGRESSION BY CONSTRUCTION": the seven byte-identical duplicate groups are invisible today only because `src/gzkit/governance/trust_audits/rendition_floor_coherence.py:72` is a substring test, and they become literal double-emissions the instant a generator materializes (brief 05:53; ADR § Alternatives H; § 7).
- **06** depends on 04 and 05. **04 and 06 are cut together, never separately** — cutting 06 alone leaves ownership as a claim with no enforcement, which is pre-mortem #2 and, in § 7's words, the "worst possible combination" (brief 06:49, brief 04:63).
- **07** depends on 05 and is **not cuttable**: without the generator and `land`, OBPIs 01-03 are schema with no consumer (brief 07:66; § 7).
- **10** depends on 04 and MUST land after it. Its own Prerequisites read "If it has not, STOP: this brief cannot land first" (brief 10:56 and 10:144).
- **08** and **09** are declared independent of both chains and may land at any point (brief 08:70, brief 09:125). 09 has landed. See the open loop on 08 below — its declared independence does not survive its own REQ set.

### Draw order the graph admits

`01 -> 02 -> 03`, then `04`, then `10`, then `05`, then `06` and `07`, then finish `08`.

`04` may be pulled forward and worked alongside 01-03 if two threads are wanted; the graph permits it and § 7 pairs 04 only with 06.

### IRON LAW applies to every arm of this

Only the operator initiates OBPI work — claiming or releasing a lock, launching or clearing a pipeline marker, starting or completing a TASK, dispatching an implementer or reviewer, editing a brief. This handoff **records an order; it starts nothing and licenses nothing**. A resuming agent presents it and waits. When the operator does initiate, it runs through the `gz-obpi-pipeline` skill, and Stage 2 is dispatched, never run inline.

## Decisions Made

- [operator-ruled] Cement the ADR-0.35.0 draw order in a handoff for a session restart (verbatim: "write a handoff that cements that flow, I'll use it in a session restart").
- [agent-chose] Read the order out of the ten briefs' own `Dependency order` lines and `DESIGN_FORCING_FUNCTIONS.md` § 7 rather than proposing a sequence. The ADR already declares one; proposing a second would have manufactured a re-ruling opportunity where canon had already ruled.
- [agent-chose] Placed OBPI-10 immediately after 04 and before 06 in the draw order. This is a **reading, not canon**: brief 10 pins only "MUST land after 04", and the before-06 placement comes from ADR Checklist item #10's phrase "the 36 `Ambiguous` capture-defaults reconciled before ownership binds", read as binding at 06's fail-closed gate. If the operator reads "binds" as 04, then 10 is free to float anywhere after 04.
- [agent-chose] Surfaced three brief-text findings without filing them. Per PRIME DIRECTIVE #6 every defect must be trackable, and these are parked in Pending Work below rather than routed, because the operator asked a question about ordering and filing was not requested.
- [agent-chose] Did not book a `gz handoff decide` ruling against the predecessor handoff resumed at session start. The operator issued no decision on its five advised steps, and authoring `--operator-text` for words the operator did not say is fabrication.

## Immediate Next Steps

1. **Present the draw order below and wait.** The IRON LAW forbids an agent starting any arm of OBPI work. When the operator rules, book their verbatim words with `uv run gz handoff decide --handoff <this file> --session-id <id> --decision proceed --operator-text "<exact words>"`, adding `--set-aside` for any advised step declined.
2. **If the operator initiates OBPI work, the head of the chain is `OBPI-0.35.0-01-corpus-tombstone-schema-and-fold`**, run through the `gz-obpi-pipeline` skill with Stage 2 dispatched rather than run inline. `OBPI-0.35.0-04-section-ownership-and-ratchet` is the only other brief whose prerequisites are already satisfied; everything else in the graph is blocked behind 01 or 04.
3. **Rule the OBPI-08 conflict.** Brief 08 declares itself independent and free to land at any point, but its REQ-0.35.0-08-04 requires the advisory to carry "a runnable `gz content land` invocation naming the surface" — a verb OBPI-07 introduces. 08 therefore cannot complete before 07. Decide whether 08 stays IN PROGRESS until 07 lands, or its REQ set is amended.
4. **Rule the two REQ residuals brief 08 already routes to the operator**, recorded at brief 08:88 as "an operator call": REQ-0.35.0-08-06 is unprovable through this harness because `tests/commands/common.py:69` merges stdout and stderr into one buffer, and REQ-0.35.0-08-05's first disjunct is structurally unreachable. The choice named in the brief is to re-word each REQ to match what is provable, or change the runner.
5. **Decide whether the three brief-text findings in Pending Work get filed** through the `ghi-author` skill, repaired in place under the owning ADR, or left recorded here.

## Pending Work / Open Loops

### Findings raised this session, unrouted

- **OBPI-08's declared independence contradicts its own REQ set.** Brief 08:70 says it "may land at any point"; brief 08:260 REQ-0.35.0-08-04 requires a runnable `gz content land` invocation, and brief 08:87 marks that REQ **OPEN** with the note that it is blocked on the unlanded verb. The dependency line and the REQ disagree about the same brief.
- **Brief 01:57 asserts "Nothing else in ADR-0.35.0 may land first" and that is already false.** OBPI-09 completed 2026-08-21/22 while 01 sits `pending`, and brief 09:125 asserts 09 is independent and may land at any point. The sentence's justification is only about the generator (ADR § Alternatives H), so it reads as stale over-broad brief text rather than a live constraint — but it is a contradiction inside a live brief.
- **All ten briefs cite `ADR-0.35.0 § Scope Minimization` at a location the ADR body does not contain.** The section is `## 7. Scope Minimization` in the package's `DESIGN_FORCING_FUNCTIONS.md:247`. The pointer resolves if read as naming the ADR package rather than the ADR document, but an agent grepping the ADR body for it finds nothing and may conclude the ordering doctrine is unwritten.

### Open operator calls inside OBPI-08

- REQ-0.35.0-08-04 — OPEN, blocked on OBPI-07.
- REQ-0.35.0-08-05 — bound on one disjunct of two; the other cannot co-occur with a `remember` that reaches the advisory, because `src/gzkit/commands/content/remember.py` appends before it warns and `src/gzkit/content/rendition_store.py:56-64` digests every entry, so a successful append always moves the fingerprint.
- REQ-0.35.0-08-06 — unprovable through this harness; stream separation is inexpressible while `tests/commands/common.py:69` merges both streams.

### Carried forward from the predecessor handoff

- Two dossier GHIs remain unfiled: the CREATE destination collision in `src/gzkit/handoff_api.py`, where `path.write_text` is preceded only by `mkdir` with no existence guard, and the empty `branch` and `agent` identity on CREATE.
- The bearing-projection design question is unruled; it is the one predecessor item that is not repair-shaped.
- Whether the CHANGELOG should accumulate during the cycle rather than being authored at release time is undecided; nothing enforces the Unreleased block and it was empty at v0.34.5.

## Verification Checklist

Every claim in this document was VERIFIED at authoring time on 2026-08-23 against Layer 2. Each can go stale; re-verify before relaying any of them.

```bash
uv run gz adr status ADR-0.35.0-canon-entry-corpus-landing
uv run gz obpi status OBPI-0.35.0-08-remember-post-append-advisory
uv run gz obpi lock list
uv run gz status
git rev-list --count origin/main..HEAD
git rev-list --count HEAD..origin/main
```

Expected at authoring time: ADR at 1/10 with closeout BLOCKED; OBPI-08 IN PROGRESS with Completion PENDING; no active locks; ahead 0 and behind 0 against `origin/main`.

Then re-read the ordering source rather than trusting this transcription:

```bash
sed -n 247,275p docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/DESIGN_FORCING_FUNCTIONS.md
grep -n "Dependency order" docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/*.md
```

If `gz adr status` reports a landed count other than 1/10, the graph is unchanged but its starting point has moved: re-read the dependency lines and take the lowest-numbered brief whose prerequisites are satisfied.

## Evidence / Artifacts

Read the ADR package first, the briefs second.

- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/ADR-0.35.0-canon-entry-corpus-landing.md` — parent ADR: § Checklist (10 items), § Decomposition Scorecard, § Alternatives H (the rejected generator-first ordering).
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/DESIGN_FORCING_FUNCTIONS.md` — § 7 Scope Minimization at line 247 is the authoritative cut-line and pairing-rule source every brief cites.
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-01-corpus-tombstone-schema-and-fold.md` — head of the chain.
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-02-content-withdraw-verb.md`
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-03-retire-duplicate-invariant-entries.md`
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-04-section-ownership-and-ratchet.md` — the second unblocked brief.
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-05-corpus-candidate-generator.md`
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-06-validate-rendition-lineage.md`
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-07-content-land-orchestrator.md`
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-08-remember-post-append-advisory.md` — currently IN PROGRESS.
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-09-codex-playback-wiring.md` — the one landed brief.
- `docs/design/adr/pre-release/ADR-0.35.0-canon-entry-corpus-landing/obpis/OBPI-0.35.0-10-classification-reader-and-ownership.md`
- `src/gzkit/governance/trust_audits/rendition_floor_coherence.py` — line 72's substring test is why 05 must not precede 01 through 03.
- `src/gzkit/commands/content/remember.py` and `src/gzkit/content/rendition_store.py` — the append-then-advise ordering behind OBPI-08's REQ-05 residual.
- `tests/commands/common.py` — line 69 merges stdout and stderr, which is what makes REQ-0.35.0-08-06 unprovable here.
- `.gzkit/handoffs/20260823T224850Z-v0345-shipped-triage-ruled-adr-0350-is-next.md` — predecessor handoff.

## Settled Rulings

506 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
