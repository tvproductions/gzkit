---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-17T10:30:46Z"
agent: claude-code
obpi_id: OBPI-0.0.73-03-fidelity-assertions-and-gate
session_id:
continues_from: .gzkit/handoffs/20260617T085207Z-obpi-0-0-73-02-completion.md
---

<!-- Handoff document for ADR-0.0.73 — created by claude-code at 2026-06-17T10:30:46Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

OBPI-0.0.73-03-fidelity-assertions-and-gate is **attested-complete** (Gate 5,
Heavy lane; attestor `g0`, operator-verbatim "attest completed" 2026-06-17).
This is Part 3 of ADR-0.0.73 — the `FidelityAssertion` frozen Pydantic model,
the `## Fidelity Assertions` block parser, and the `gz adr fidelity <ADR>` gate
that RUNS each ADR-declared command and compares observed-vs-expected exit.

Delivered via the gz-obpi-pipeline (Stages 1→5). All quality gates green:
lint, typecheck, 16/16 new unit tests + full suite, mkdocs --strict,
`gz validate --documents`, `gz validate --cli-alignment`. `gz covers` reports
`behavior_uncovered_reqs=0`. `gz obpi complete` returned `attested_completed`.

Lock release, marker cleanup, and the two git-sync cycles are the remaining
Stage-5 mechanics (in progress as of this handoff's authoring).

## Important Context

- `gz adr fidelity` is registered but deliberately has NO wielding skill yet —
  it is entered in `_NO_SKILL_VERBS` (`src/gzkit/governance/trust_audits/cli.py`).
  **OBPI-0.0.73-04** (Closeout/audit repoint) is what wires the gate into the
  `gz-adr-closeout-ceremony` and `gz-adr-audit` skills; remove the waiver then.
- The behave-coverage gate was cleared via an **operator-approved waiver** in
  `data/behave_coverage_waivers.json` (key
  `OBPI-0.0.73-03-fidelity-assertions-and-gate`). Rationale mirrors the
  OBPI-0.0.73-02 precedent: BEHAVIOR REQs are engine-unit-proven; the cross-OBPI
  behave walkthrough of `gz adr fidelity` + `--qc-binding` belongs at ADR-0.0.73
  closeout. Author `features/adr_fidelity.feature` at ADR closeout.
- The completion used `--accept-stale-reconciliation` for an
  **operator-approved false positive**: the reconcile allowlist heuristic
  (`_compute_missing_in_brief`, `src/gzkit/governance/brief_reconcile.py:287`)
  flags `src/gzkit/traceability.py` only because `src/gzkit/fidelity.py` sits at
  the `src/gzkit/` package root, sharing that neighborhood. The OBPI does not
  modify `traceability.py` (it imports the existing `@covers` decorator). The
  function docstring itself names `gzkit.traceability` as the intended exclusion
  — the neighborhood filter just misses package-root files. **Candidate
  follow-up:** tighten the neighborhood filter to exclude package-root infra
  imports, or file a GHI if it recurs across OBPIs.
- The `FidelityAssertion` model uses `observed: int | None` and
  `result: str | None` (both default None) so parsed-but-unrun assertions are
  valid instances; the gate fills them via `model_copy(update=...)` (frozen).

## Decisions Made

- **Decision:** Cleared behave_req_coverage via registered waiver, not a new
  `features/adr_fidelity.feature`.
  **Rationale:** Matches OBPI-0.0.73-02 sibling convention in the same ADR;
  that OBPI's waiver explicitly defers the `gz adr fidelity` behave integration
  to ADR-0.0.73 closeout. BEHAVIOR REQs are unit-proven (`behavior_uncovered_reqs=0`).
  **Alternatives rejected:** Authoring behave now — diverges from sibling
  convention and front-loads cross-OBPI integration the ADR defers to closeout.
- **Decision:** Used `--accept-stale-reconciliation` for the `traceability.py`
  allowlist drift rather than adding it to the brief allowlist.
  **Rationale:** It is a heuristic false positive; the OBPI never modifies
  `traceability.py`. Adding it would be a false scope declaration.
  **Alternatives rejected:** Allowlist amendment — mis-declares an untouched file.
- **Decision:** Declared `req_atomic:` for all six REQs in brief frontmatter.
  **Rationale:** Each REQ is one indivisible labor unit (model / parser / runner
  result / runner failure-exit / docs / fence); none subdivides into seq=02+.

## Immediate Next Steps

<!-- ADVISORY ONLY — present for operator review; do not execute without authorization. -->

1. Finish the remaining Stage-5 mechanics for THIS OBPI if not already done:
   release the lock (`uv run gz obpi lock release OBPI-0.0.73-03-fidelity-assertions-and-gate`),
   git-sync #1, `gz obpi reconcile`, `gz adr status ADR-0.0.73 --json`, git-sync #2.
2. Pull the next ADR-0.0.73 OBPI per the campaign: **OBPI-0.0.73-04**
   (Closeout/audit repoint onto the fidelity gate) — wires `gz adr fidelity`
   into both ceremonies and removes the `_NO_SKILL_VERBS` waiver.
3. Continue ADR-0.0.73 through OBPI-05 (absorb pool ADR), OBPI-06 (self-check +
   facade regression corpus), OBPI-07 (evaluator truth-binding, GHI #624).

## Pending Work / Open Loops

- **OBPI-0.0.73-04 through -07** remain unimplemented (4 of 7 OBPIs left).
- **ADR-0.0.73 closeout** owes: `features/adr_fidelity.feature` BDD walkthrough,
  the `gz adr fidelity ADR-0.0.73` self-check (OBPI-06), and removal of both the
  behave waiver and the `_NO_SKILL_VERBS` entry once OBPI-04 wires the skills.
- **Candidate defect:** `_compute_missing_in_brief` neighborhood filter false
  positive for package-root src files (see Important Context). Not yet filed.

## Verification Checklist

- [ ] `uv run gz obpi status OBPI-0.0.73-03-fidelity-assertions-and-gate` → attested-complete
- [ ] `uv run gz adr fidelity ADR-0.0.73-verification-layer-binding-audit --check` exits 0
- [ ] `uv run -m unittest tests.governance.test_adr_fidelity -v` → 16/16 pass
- [ ] `git branch --show-current` → main
- [ ] `uv run gz obpi lock list` reflects intended lock state

## Evidence / Artifacts

- `src/gzkit/fidelity.py` — FidelityAssertion model + parse_fidelity_assertions() + run_fidelity_gate()
- `src/gzkit/commands/adr_fidelity.py` — gz adr fidelity command
- `tests/governance/test_adr_fidelity.py` — 16 unit tests (model/parser/gate-runner)
- `docs/user/manpages/adr-fidelity.md` — manpage
- `src/gzkit/cli/parser_artifacts.py` — adr fidelity verb registration
- `data/behave_coverage_waivers.json` — operator-approved BDD waiver entry
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-03-fidelity-assertions-and-gate.md` — completed brief

## Environment State

Python 3.13 / uv. Branch main. No feature branches (operator directive).
