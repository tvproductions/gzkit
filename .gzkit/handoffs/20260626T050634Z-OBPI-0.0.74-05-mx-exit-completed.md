---
mode: CREATE
adr_id: ADR-0.0.74
obpi_id: OBPI-0.0.74-05-mx-exit-hard-gate
branch: main
timestamp: '2026-06-26T05:06:34Z'
agent: claude-code-87b9a98b
last_lock_event_timestamp: '2026-06-25T23:34:42.673880+00:00'
last_commit_sha: 7da85fefa3f204866b13c2589959f7facc2e6548
---

<!-- Completion handoff for OBPI-0.0.74-05-mx-exit-hard-gate — register entry for lock release -->

## Current State Summary

OBPI-0.0.74-05-mx-exit-hard-gate is `attested_completed` (attestor g0, lane heavy).
The `gz mx exit` hard exit gate for MX mode is implemented, tested (10/10), and
operator-attested. This handoff is the completion register entry that pairs with
the lock release (token-block discipline § Sub-Invariant 5).

## Important Context

`gz mx exit --attestor <name>` is the MX hangar's hard exit gate (ADR-0.0.74
Decision #5). It temporarily removes the marker so `checkpoint.resolve()` sees no
active session — every guard re-emits at full `GZ_<LEVEL>` strength (no advisory
demotion). Red → exit 3 + marker restored; all-green → `mx_session_closed` written
+ marker stays removed. No `--force`; exit is the sole marker-clearing path
(Boundary Invariant #4).

## Decisions Made

- Implemented `mx_exit_cmd()` via the marker-removal mechanism (temporarily unlink
  the marker before running guards) so the full-strength re-run genuinely bypasses
  the in-hangar advisory demotion — chosen over a checkpoint flag (out of allowed
  paths) or inline guard re-implementation.
- `_run_guards` is injectable for testability; the default runs `uv run gz check`.
- Fixed an adversary-caught caveat inline: bare `except Exception:` → `except OSError:`
  per pythonic.md (no bare broad-except outside CLI boundaries).
- Coupled surfaces touched beyond the brief allowlist (parser_governance.py,
  trust_audits/cli.py, doc-coverage.json, manpage index, both runbooks) follow the
  OBPI-04 `gz mx enter` precedent — required for CLI-audit/skill-alignment parity.

## Immediate Next Steps

1. Lock release (this register entry pairs it).
2. Git-sync the governance edits.
3. Next OBPI in the Movement I MX-kernel sequence: OBPI-0.0.74-06 (auto-assembled MX log).

## Pending Work / Open Loops

- Remaining ADR-0.0.74 MX-kernel OBPIs (campaign Movement I, release 0.29.0):
  OBPI-06 (auto-assembled MX log), OBPI-07 (awareness hook), OBPI-08 (skill + AGENTS rule),
  OBPI-13 (proxy-reality detector), OBPI-14 (hardening: TTL/max-open, no-release-while-open,
  debt-aging, dangling-state detector). The `gz-mx` skill (which will wield enter + exit,
  retiring the `_NO_SKILL_VERBS` waivers) is a later ADR-0.0.74 deliverable.

## Verification Checklist

- [ ] `git rev-parse HEAD` matches a commit at or after the completion git-sync.
- [ ] Branch matches `main`.
- [ ] OBPI-0.0.74-05-mx-exit-hard-gate shows `attested_completed` in `gz adr status ADR-0.0.74`.

## Evidence / Artifacts

- `src/gzkit/commands/mx_cmd.py` — `mx_exit_cmd`, `_mx_session_closed_event`, `_default_run_guards`
- `tests/commands/test_mx_exit.py` — 10 unit tests (@covers REQ-05-01/02/03/06)
- `docs/user/manpages/mx-exit.md` — dedicated exit manpage
- Receipts: `arb-step-unittest-f22414929e8b4382965978e567c654e7`,
  `arb-ruff-68d60b41863e4ae9b8dc4961820b39c6`,
  `arb-step-typecheck-96813abac56145da9f98f9feb23111e1`,
  `arb-step-mkdocs-8083bd55829b47c2a7fcfdd59fad789a`
