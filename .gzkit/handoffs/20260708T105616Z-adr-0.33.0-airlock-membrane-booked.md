---
mode: CREATE
adr_id: ADR-0.33.0
branch: main
timestamp: "2026-07-08T10:56:16Z"
agent: claude-code
obpi_id:
last_lock_event_timestamp:
last_commit_sha: fd070ff4
session_id:
continues_from: .gzkit/handoffs/20260707T102940Z-adr-0.32.0-hull-validated-source-fidelity-fix.md
---

<!-- Handoff for ADR-0.33.0-airlock-membrane — created by claude-code at 2026-07-08T10:56:16Z. -->

## This handoff ADVISES next moves — it is NOT authorization to execute them

On resume, at every freshness level: (1) present the advised next steps and current
state to the operator; (2) obtain explicit operator authorization before executing
any of them — no file mutation, no `gz` ceremony, no implementation until the operator
says go; (3) the agent advises, the operator rules, the agent notes variance and stops.

## Current State Summary

**Movement III Phase 3 (HATCH) is DESIGNED, AUTHORED, and BOOKED — not yet
implemented.** This session ran the whole design-through-booking arc for the entry
airlock, in order:

1. **Airlock-in HATCH pass — GO attested (2026-07-07).** Re-sense GREEN
   (`gz ontology resense` clean, `gz ontology seams` = 0), two-layer seam-map,
   volume (footprint + reach), three pre-registered falsifiers, GO recorded via the
   insights channel + the record's Status (Phase-0 precedent; no `gz` go/no-go verb).
   Record: `docs/governance/airlock-in-hatch-2026-07-07.md`.
2. **Operator go-to-work (2026-07-08).** The second value of "go" — authorized
   authoring the HATCH feature ADR.
3. **ADR-0.33.0-airlock-membrane authored & booked** (feature / heavy) via the
   `gz-adr-create` interview path, with 6 OBPIs co-created 1:1 with the Feature
   Checklist. Brief authoring was delegated to six parallel subagents (disjoint
   scopes), then a human-run reconcile pass resolved three cross-brief seams.
4. **All governance validators green.** `gz obpi validate --authored` 6/6;
   `gz validate --req-kind-discipline --cli-alignment --documents` (3 scopes);
   `gz adr evaluate` STRUCTURALLY COMPLETE 3.40/4.0; `mkdocs build --strict` clean.

At handoff time the ADR package, the campaign annotation, and this handoff are
uncommitted; they sync together in the same guarded commit that carries this handoff.
No source code was implemented — the tracer and breadth are pending OBPIs. The ADR
lifecycle is Pending; OBPIs are 0/6 done (authored, not implemented).

## Important Context

- **The airlock is prosthetic memory.** Root intent (operator verbatim): "the
  model's inability to retain a model of the project without having to reconstruct
  it on each sortie." The seam-map is the externalized working set; map-maintenance
  becomes a byproduct of any work (closes Architectural Boundary #4).
- **Tracer-first / gated-breadth.** FC-01 (data model + events), FC-02 (airlock-IN +
  pipeline Stage 1 + the section-5 live NC — the LANDING KEYSTONE), FC-03 (airlock-OUT
  + Stage 5, co-equal) are the pipeline-door tracer. FC-04 (mx door), FC-05
  (permitted-entry door), FC-06 (doctrine made lawful) are gated-breadth and do NOT
  begin until OBPI-02's section-5 NC bites live.
- **The section-5 NC routes through the EXISTING meta-validator.** `gz validate
  --qc-binding` (ADR-0.0.73) discovers and runs the `airlock-in-unaccounted-seam`
  `@enforces` claim — there is NO `--airlock-nc` scope (one enforcement surface, not
  two; parent ADR Boundary Invariant #6). OBPI-02 solely owns the `enforcement.py`
  claim registration + floor-wiring; OBPI-06 only doctrinally binds the already-
  registered floor member.
- **Two hard lines seated as Boundary Invariants.** The airlock NEVER writes L1 canon
  (it logs L2, proposes attested amendments; BI-1). Its acknowledge-and-decide gate
  (proceed / pause / hold / revert) is NEVER dressed as Gate-5 completion attestation
  (BI-3) — that distinction preserves the force of the sacrosanct word.
- **Implementation is a SEPARATE authorization.** Per the operator's standing "go
  does not mean start work" doctrine, implementing the tracer needs an explicit
  go-to-work. Being contract-bearing (new CLI verbs, ledger events, `@enforces`
  claim), it routes through `gz obpi pipeline`, not freeform edits.
- **Naming defers to Phase I.** `airlock` / `seam` / `ping` are working names;
  `permitted entry` is the operator's chosen name for the ad-hoc/spurious door.

## Decisions Made

The pipeline is the canonical door and the tracer (operator: the airlock "was
written with the pipeline in mind") — the primitive is extracted from its proven
Stage-1 / Stage-5 geometry; rejected a generic `gz airlock` verb and a
permitted-entry-first tracer. The gate is acknowledge-and-decide, a distinct sort
from completion attestation; rejected calling it "attestation" (would cheapen the
canon). The airlock never writes L1; rejected direct canon-writing. One ADR with 6
OBPIs, not a multi-ADR constellation; rejected splitting permitted-entry onto its own
track (would fork the shared primitive). Brief authoring delegated to six parallel
subagents then reconciled; rejected serial self-authoring (scale). graspologic stays
ruled out (3.13-incompatible and a category error in a gating path); undeclared-body
detection, if ever, is `networkx.algorithms.community`, L3-advisory. Reconcile pass
resolved: phantom `--airlock-nc` to `--qc-binding`, slug unified to
`airlock-in-unaccounted-seam`, `enforcement.py` registration owned solely by OBPI-02.

## Immediate Next Steps

Advisory only — present for operator review; do not execute without an explicit
go-to-work.

1. **Operator reviews the committed ADR + briefs** — start with
   `ADR-0.33.0-airlock-membrane.md` (Intent, Decision, the 6 Boundary Invariants),
   then OBPI-0.33.0-02 (the landing keystone).
2. **On explicit go-to-work, implement the tracer OBPI-01 then 02 then 03 via
   `gz obpi pipeline`** (contract-bearing; the pipeline owns stage sequencing). Land
   FC-01 (models + events) first so 02/03 consume it.
3. **Prove the section-5 live NC before any breadth.** OBPI-02's NC — an un-accounted
   seam makes GO structurally unreachable, un-forced production, verified via
   `gz validate --qc-binding` with the airlock claim among the passing set — is the
   gate on FC-04 / FC-05 / FC-06. Do not begin the gated-breadth OBPIs until it bites.
4. **Sequence FC-06 (doctrine made lawful) LAST** — it is the one-way door
   (promoting the North Star docs to binding + the section-2 seam body+boundary
   widening); it does not fire until the tracer NC is proven.
5. **Defer the terminology rename** (airlock / seam / ping to final names) to Phase I
   per the campaign.

## Pending Work / Open Loops

- **Tracer implementation not started** — 0/6 OBPIs done; ADR lifecycle Pending.
- **Phase 3 campaign box stays unchecked** until the membrane is built + Validated;
  the campaign records the authored-and-booked progress only.
- **Phase 4 RECALL** — deferred / severable (post-first-airlock).
- **No blockers. No OBPI locks held.** No in-progress pipeline.

## Verification Checklist

- [ ] `git status` clean and synced (`git rev-list --left-right --count origin/main...main` is `0	0`)
- [ ] `git branch --show-current` is `main`
- [ ] `uv run gz adr report ADR-0.33.0-airlock-membrane` shows 6 OBPIs, lifecycle Pending, 0/6 done
- [ ] `uv run gz obpi validate --adr ADR-0.33.0-airlock-membrane --authored` is 6/6 PASS
- [ ] `uv run gz validate --req-kind-discipline --cli-alignment --documents` passes (3 scopes)
- [ ] `uv run gz ontology resense` is clean (hull stable; re-sense before authoring the tracer if time has passed)
- [ ] No `src/gzkit/airlock/**` implementation exists yet (tracer not started)

## Evidence / Artifacts

- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/ADR-0.33.0-airlock-membrane.md` — the booked ADR (Intent + Decision + 6 Boundary Invariants + Fidelity Assertions + scorecard + 6-item Checklist)
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/ADR-0.33.0-interview.json` — the interview record
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/EVALUATION_SCORECARD.md` — the QC evaluation (3.40/4.0)
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/obpis/OBPI-0.33.0-01-airlock-data-model-and-events.md` — FC-01 SUPPORT (tracer)
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/obpis/OBPI-0.33.0-02-airlock-in-pipeline-tracer.md` — FC-02 BEHAVIOR (landing keystone)
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/obpis/OBPI-0.33.0-03-airlock-out-pipeline-tracer.md` — FC-03 BEHAVIOR (co-equal)
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/obpis/OBPI-0.33.0-04-airlock-mx-door.md` — FC-04 BEHAVIOR (gated-breadth)
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/obpis/OBPI-0.33.0-05-airlock-permitted-entry-door.md` — FC-05 BEHAVIOR (gated-breadth)
- `docs/design/adr/pre-release/ADR-0.33.0-airlock-membrane/obpis/OBPI-0.33.0-06-airlock-doctrine-lawful.md` — FC-06 STRUCTURAL-FENCE (one-way, last)
- `docs/governance/airlock-in-hatch-2026-07-07.md` — the GO-attested airlock-in pass (seam-map, volume, falsifiers)
- `docs/governance/build-to-1.0-campaign-2026-06-30.md` — Magna Carta; Phase 3 annotated (box unchecked, work-start authorized)
- `.gzkit/insights/agent-insights.jsonl` — the airlock-in GO record + the design-dialogue course-correction records
