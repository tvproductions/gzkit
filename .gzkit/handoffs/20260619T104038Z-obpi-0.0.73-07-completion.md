---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-19T10:40:38Z"
agent: claude-code
obpi_id: OBPI-0.0.73-07-evaluate-truth-binding
session_id:
continues_from:
last_lock_event_timestamp: "2026-06-19T10:38:00.000000+00:00"
last_commit_sha: a2e641a0
---

## Current State Summary

OBPI-0.0.73-07-evaluate-truth-binding is COMPLETE and operator-attested
("attest completed", g0). It comprehensively eradicates the shape-as-substance
defect in `gz adr evaluate` (GHI #624) — the facade that let a distrusted
"3.55/GO" stand as an attested quality verdict. The prior cosmetic completion was
repudiated 2026-06-19 (`verification-invalid`, g0) after a multi-agent
verification-layer audit found the GHI #624 "fix" relabelled rather than repaired
(substance-claiming docstrings over keyword-grading bodies). This handoff is the
register entry that pairs with the Stage-5 lock release.

The fix landed in three composing parts:
- **A — demote-and-decouple.** The deterministic evaluator grades STRUCTURAL
  COMPLETENESS only (section presence/depth) and is labelled as such; the
  scorecard and CLI carry a do-not-composite disclaimer. The same ADR that read
  "3.55/GO" now reads "STRUCTURALLY COMPLETE / Substance UNGRADED".
- **B — substance channel.** `src/gzkit/adr_eval_substance.py` (CREATE) reads
  decision substance ONLY from a recorded, disciplined judge verdict
  (record-and-validate, no live LLM call); `UNGRADED` absent one, never derived
  from shape. The two channels are decoupled and never composited.
- **C — self-binding.** The seventh `shape-graded-not-substance` theater signature
  + a regression guard pin the eradication; the evaluator self-registers advisory
  and passes `gz validate --qc-binding`.

Governance: ADR-0.0.73 § Boundary Invariant #6 reworded to the honest contract
(never present shape as authoritative substance); OBPI-07 brief + manpage
repointed. Verified green across the full repo: 6297 unittests
(arb-step-unittest-e87b1fd7), ruff (arb-ruff-b64753ca), typecheck
(arb-step-typecheck-773838b5), `--documents`, `--cli-alignment`, `--qc-binding`,
`mkdocs --strict` exit 0.

## Important Context

- `gz obpi complete` ran with `--accept-stale-reconciliation` (GHI #193 benign
  ledger-churn: the reconcile's own event re-stales the receipt; the reconcile
  reported clean with all deltas 0).
- The brief frontmatter status was reset Completed→Accepted before re-completion
  (the repudiation invalidated the Layer-1 Completed status; the genuine
  completion flipped it back).

## Decisions Made

- Repudiate (not withdraw) the prior OBPI-07 completion: the work INTENT is valid,
  only the cosmetic execution was invalid — `repudiated` clears on genuine
  re-attestation (ADR-0.0.71).
- Path A+B over a live-LLM judge: gzkit's judge subsystem is record-and-validate
  by design; B reuses the proven `advisor_qc` seam. Building all of ADR-0.0.40 was
  rejected as out-of-scope new capability.
- Keep the deterministic dimension names (avoid a 70-derived-scorecard thrash);
  achieve decoupling via honest labels + disclaimer + the distinct substance channel.

## Immediate Next Steps

1. Git-sync #2 commits the reconcile + ADR-status refresh (completes the OBPI-07
   Stage-5 two-sync pattern).
2. Do NOT proceed to OBPI-09 (waiver-ratchet) — it remains NO GO / too-broad
   pending rework and a fresh GO (parent ADR recovery freeze).
3. The substance channel is honestly UNGRADED until ADR-0.0.40 judge governance
   lands — that is the named forced-downstream, not a defect.

## Verification Checklist

- [x] `gz validate --qc-binding` green (evaluator binding-honest, no theater)
- [x] `gz validate --cli-alignment` green
- [x] `gz validate --documents` green
- [x] `mkdocs build --strict` exit 0
- [x] 6297 unittests green (arb-step-unittest-e87b1fd7)
- [x] ruff (arb-ruff-b64753ca) + typecheck (arb-step-typecheck-773838b5) clean
- [x] `gz covers OBPI-0.0.73-07` behavior_uncovered_reqs: 0
- [x] live `gz adr evaluate ADR-0.0.73` renders STRUCTURALLY COMPLETE / Substance UNGRADED

## Evidence / Artifacts

- Completion receipt: `attested_completed`, attestor g0, `operator-verbatim-conversational`
- Repudiation event: `obpi_completion_repudiated` (verification-invalid, g0, 2026-06-19)
- ARB receipts: `arb-step-unittest-e87b1fd709874362ba24ddc4699e52b9`,
  `arb-ruff-b64753ca47064e68b91323e8183389b9`,
  `arb-step-typecheck-773838b5d89c487a8742ddce06288222`
- New module: `src/gzkit/adr_eval_substance.py`; tests: `tests/test_adr_eval_substance.py`
- Plan + receipt: `.claude/plans/OBPI-0.0.73-07-evaluate-truth-binding.md` (+ PASS receipt)

## Pending Work / Open Loops

- **Forced downstream (ADR-0.0.40):** full judge governance (leakage /
  output-discipline / meta-eval validators, JudgeInvocation model) to POPULATE the
  substance channel. Until it lands, substance is honestly UNGRADED.
- **OBPI-02 territory:** behavioral auto-detection of an arbitrary non-self-labeling
  shape-grading step (the seventh signature fires on flagged steps; the guard pins
  this evaluator).
- **Parent ADR-0.0.73 remains under recovery freeze** for OBPI-08/09 per § Decision:
  OBPI-08 is complete; OBPI-09 (waiver-ratchet) was scored NO GO / too-broad by the
  manual scorecard and is NOT to proceed without rework + a fresh GO. The evaluator
  that issued the prior false GO is now honest, so a fresh evaluation will no longer
  launder a shape score into a quality GO.
- **The eval/scorer.py second surface** got a docstring-scope honest fix only; if the
  eval harness later needs the same channel split, that is follow-up.
