---
mode: CREATE
adr_id: ADR-0.0.70
branch: main
timestamp: "2026-06-12T11:21:16Z"
agent: claude-code-8f8d8e8d
obpi_id: OBPI-0.0.70-02-session-correction-mining
session_id: 8f8d8e8d-3e8a-45f2-b65f-0a71d10edfbc
continues_from: .gzkit/handoffs/20260612T104201Z-OBPI-0.0.70-01-completed.md
last_lock_event_timestamp: "2026-06-12T11:10:20.620296+00:00"
last_commit_sha: 5cb68a1e6208824e04bedd6a228d0a6cd44893e7
---

<!-- Register entry coupling lock release for OBPI-0.0.70-02 (token-block § Sub-Invariant 5) -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

On resume, present advised next steps to the operator and obtain explicit
authorization before executing any of them. You advise; the operator rules.

## Current State Summary

**OBPI-0.0.70-02-session-correction-mining is COMPLETE and ATTESTED** (Gate 5
operator-verbatim "attest completed", attestor g0, 2026-06-12). This was
the SECOND of three ceremony-remediation passes: the rogue fable agent had
implemented and committed 02/03/04 in commit `863250d6` WITHOUT the governed
pipeline ceremony; the operator ruled the work salvageable but the skipped
ceremony intolerable, and directed regularization in place, one OBPI at a time.
OBPI-02 ran the full pipeline (plan-audit PASS → lock → Stage 2 defect fixes →
Stage 3 verify → Stage 4 evidence → Gate 5 → this register entry). Three
audit-found defects were fixed TDD RED→GREEN before attestation.

**Branch state:** `main`, synced (ahead=0 behind=0 at claim; completion edits
pending git-sync).

## Important Context

- The two MAJOR defects fixed this pass were real: (1) `_cluster_key` leaked the
  operator email local-part into the git-tracked `cluster_key` field + proposal
  hash — now scrubbed before tokenizing; (2) a non-UTF-8 transcript raised
  `UnicodeDecodeError` (escaping the fail-soft fence) — now caught. Plus the
  missing chore `README.md` was authored.
- `src/gzkit/insights/correction_mining.py` is stdlib-only by ADR Boundary
  Invariant 3 (plain dicts, not Pydantic — named tension, documented in the
  module docstring).

## Decisions Made

- **Decision:** Scope-expanded the PII fix beyond REQ-02-04's literal "quote"
  wording to also scrub the `cluster_key` field.
  **Rationale:** The operator-PII rule binds EVERY emitted record (ADR Boundary
  Invariant 2), and `cluster_key` is git-tracked; coupled-correctness per DO IT
  RIGHT 1a, not gold-plating.
  **Alternatives rejected:** Scrubbing only `quote` (leaves the leak on the
  hash-input field).
- **Decision:** Declared `req_atomic` for all 8 REQs.
  **Rationale:** One cohesive miner + chore package; each REQ maps to one test
  class / proof channel, no separable labor below it.

## Immediate Next Steps

1. Continue ceremony remediation with **OBPI-0.0.70-03** (guardrail-feedback-prose
   rule). Audit found: garbled Buetow quote, and `data/` edits made outside the
   brief's Allowed Paths (never reconciled). Fix both, then run the pipeline.
2. Then **OBPI-0.0.70-04** (fourth-source triangulation). Audit found: appraisal
   line 141 cherry-picks "18 corrections" while omitting "0 clusters at threshold
   3" — restore the honest qualifier, then pipeline.
3. After all four OBPIs land, ADR-0.0.70 is closeout-ready (campaign item B.0).

## Pending Work / Open Loops

- OBPI-0.0.70-03 and -04 still need ceremony remediation (2/4 of ADR-0.0.70 done).
- Parent ADR-0.0.70 closeout pending all four OBPIs + ADR-level Gate 5.

## Verification Checklist

- [ ] `uv run -m unittest tests.chores.test_session_correction_mining -q` → 14 pass
- [ ] `uv run gz validate --chores-layout` exit 0
- [ ] `uv run gz covers OBPI-0.0.70-02` → behavior_uncovered_reqs=0
- [ ] Branch: `git branch --show-current` → `main`

## Evidence / Artifacts

- `src/gzkit/insights/correction_mining.py` — miner (PII + fail-soft fixes)
- `tests/chores/test_session_correction_mining.py` — 14 tests (+3 this pass)
- `.gzkit/chores/session-correction-mining/README.md` — authored this pass
- `docs/design/adr/foundation/ADR-0.0.70-turn-end-feedback-and-correction-mining/obpis/OBPI-0.0.70-02-session-correction-mining.md` — completed brief
- `.claude/plans/OBPI-0.0.70-02-session-correction-mining.md` — remediation plan

## Environment State

Python 3.13; macOS (darwin). Miner is read-only outside its proofs/ dir.
