---
mode: CREATE
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-27T09:02:21Z"
agent: claude-code
obpi_id: OBPI-0.0.74-08-mx-skill-and-agents-rule
last_lock_event_timestamp: "2026-06-27T08:34:36.291916+00:00"
last_commit_sha: 91692b79e5e6c4bebbbaad2eab2c59407fd483f8
session_id: main-2026-06-27
continues_from:
---

<!-- Completion handoff for OBPI-0.0.74-08 — created by claude-code at 2026-06-27T09:02:21Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

This is a completion register entry paired with the lock release for a
*completed* OBPI (not an abandonment). The pending follow-up below ADVISES the
next session; it is not authorization to execute.

## Current State Summary

OBPI-0.0.74-08-mx-skill-and-agents-rule is **completed and attested** (Gate 5
operator attestation `g0`, lane heavy). The two human/agent-facing companions to
the MX marker mechanism landed:

- `.gzkit/skills/gz-mx/SKILL.md` — canonical `gz-mx` skill (`gz_command: mx`),
  the operator's governed interface to the hangar (tool-skill Invariant 1)
- `.gzkit/rules/mx-mode.md` — `paths:`-scoped binding rule: honor-the-marker +
  PRIME-DIRECTIVE-binds-the-whole-hangar-session
- `tests/commands/test_skills.py` — `@covers("REQ-0.0.74-08-03")` behavior test
- Coupled surfaces (AGENTS.md 1a): `docs/user/skills/gz-mx.md` manpage,
  skills index entry, gz-manage router row (→ 0.4.0), advisory scorecard rows 62-63

All quality gates passed: ruff, typecheck, 6548 unittests, mkdocs --strict,
`gz validate --skill-alignment/--cli-alignment/--unscoped-rules/--documents`.
ARB receipts emitted for each.

## Important Context

- `mx-mode.md` is a `paths:`-scoped rule file (ADR-0.0.20), NOT AGENTS.md body
  prose — the brief's denied paths exclude AGENTS.md to avoid the
  invariant-coherence re-render surface.
- The mx-mode rule landing triggered two coupled-surface governance gates
  (AGENTS.md 1a): bullet-retention (scorecard row 62's verbatim bullet must
  appear in `.claude/rules/mx-mode.md`) and surface-weight (yellow band, needs
  a single covering waiver ≥ accumulated delta). Both resolved in the same
  completion sweep.
- The `gz-mx` skill's `gz_command: mx` resolves to the `gz mx enter/exit` verb
  group delivered by sibling OBPI-0.0.74-04/05 — this OBPI WIELDS the verb, it
  does not implement it.

## Decisions Made

Completed OBPI-0.0.74-08 through the full gz-obpi-pipeline (plan-audit PASS →
RGR Stage 2 → Stage 3 quality gates → Stage 4 evidence + adversarial validation
→ Gate 5 operator attestation `g0` "attest completed" → Stage 5 atomic
completion). The mx-mode binding rule landed as a scoped `.gzkit/rules/` file
(NOT AGENTS.md body prose) per the brief's denied paths — avoiding the
invariant-coherence re-render surface. Adversarial validation initially flagged
a stale brief-reconcile receipt; refreshed via `gz brief reconcile` (clean, 0
deltas) before completion. The frontmatter reconcile (status Draft→Active)
re-staled the receipt once; resolved by re-running brief reconcile immediately
before `gz obpi complete`.

## Immediate Next Steps

1. **File a GHI for stale cli.py waivers (out-of-scope cleanup):** The
   `_NO_SKILL_VERBS` waivers for `mx enter` / `mx exit` in
   `src/gzkit/governance/trust_audits/cli.py` (lines ~61-70) still read "the
   gz-mx skill is a later deliverable; waiver holds until the skill lands." The
   skill has now landed. `gz validate --skill-alignment` still passes (the
   waivers cascade harmlessly), but the rationale text is stale. Out of this
   brief's allowed paths — route as a GHI direct-fix.
2. **Continue ADR-0.0.74 MX-kernel OBPIs** per the build-to-1.0 campaign
   Movement I: the staging-flag retirement (OBPI-0.0.74-09) and the
   enforcement-claim meta-validator items (OBPI-0.0.74-15/16/17/18/19).
3. Present these to the operator for authorization before executing.

## Pending Work / Open Loops

- Stale `_NO_SKILL_VERBS` waivers in `cli.py` (see Immediate Next Steps #1).
- Remaining ADR-0.0.74 MX-kernel checklist items (staging-flag retirement,
  gates-as-sensors completion item 20, meta-validator items).

## Verification Checklist

- [ ] `uv run -m unittest tests.commands.test_skills -v` — all pass
- [ ] Branch matches: `git branch --show-current` → main
- [ ] `uv run gz obpi reconcile OBPI-0.0.74-08-mx-skill-and-agents-rule` — receipt and brief agree

## Evidence / Artifacts

- `.gzkit/skills/gz-mx/SKILL.md` — canonical gz-mx skill
- `.gzkit/rules/mx-mode.md` — binding mx-mode rule
- `tests/commands/test_skills.py` — behavior test (REQ-0.0.74-08-03)
- `docs/user/skills/gz-mx.md` — operator manpage
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/obpis/OBPI-0.0.74-08-mx-skill-and-agents-rule.md` — completed brief with evidence
