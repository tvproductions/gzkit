---
mode: CREATE
adr_id: ADR-0.31.0
obpi_id: OBPI-0.31.0-02-withdraw-supersede-transitions
branch: main
timestamp: "2026-07-03T21:15:00Z"
agent: claude-code
last_lock_event_timestamp: "2026-07-03T12:28:37.332695+00:00"
last_commit_sha: 3ad9f62c
---

# Handoff: OBPI-0.31.0-02-withdraw-supersede-transitions — Completion

## Current State Summary

OBPI-0.31.0-02 (withdraw-supersede-transitions) is **attested-complete** as of
2026-07-03. `gz obpi complete` recorded operator-verbatim attestation (`g0`,
"attest completed"), emitted the completion receipt, and set the brief to
`attested_completed`. The work elevated `gz obpi withdraw` to a witnessed,
model-validated transition and built the new `gz obpi supersede` verb — both
consult OBPI-01's `CANONICAL_TRANSITIONS` before emitting and mark the artifact
graph. Ran the full OBPI pipeline (Stage 1–5) including an independent
Stage-4b adversarial validation that REFUTED an initial empty-Implementation-
Summary (REQ-06) and a weak model-consultation test; both were fixed and
re-verified before attestation. The lock is still held pending this handoff;
Stage 5 git-sync cycles follow.

## Important Context

- The completion carried an `--accept-security-floor` override (GHI #462):
  `src/gzkit/commands/obpi_cmd.py` is a registered `auth_boundaries` security
  surface, but the change is strictly additive/defensive (adds a
  `human_attested` `--attestor` witness where none existed). The security-scan
  canonical slot is structurally unavailable (toolchain ADR
  `pool.agentic-security-review` unpromoted), not skipped.
- Two in-flight tool defects were fixed directly during the run:
  `fix(specify)` dry-run lane-source attribution (committed `5842efe0`), and a
  glob-pattern false-positive in `src/gzkit/governance/brief_reconcile.py`'s
  allowlist checker (uncommitted, TDD'd — sibling of GHI #626).
- The scope expanded (Prime Directive #4) to four coupled consumer surfaces the
  initial brief under-declared, all discovered at Stage-3 full-suite verify:
  typed event models (`src/gzkit/events.py`), the `_EVENT_MODELS` registry
  (`tests/test_schemas.py`), skill-alignment (`gz-obpi-reconcile` SKILL.md), and
  `config/doc-coverage.json`. The brief allowlist was amended to declare them.

## Decisions Made

- **`obpi_withdrawn_event.attestor` is a defaulted keyword** (`attestor: str = ""`)
  not bare-required, to preserve 7 positional callers across 6 out-of-scope test
  files; witness enforcement lives at the CLI boundary in `obpi_withdraw_cmd`
  (REQ-03), so the factory default does not weaken the gate.
- **`req_atomic:` exemption declared** for all 6 REQs — each was one indivisible
  RGR cycle; no labor below the REQ warranted a seq=02+ TASK subdivision.
- **The withdraw/supersede transition gate is a binary terminal check** deriving
  from `CANONICAL_TRANSITIONS` — the full 8-state OBPIState resolution is
  deferred-in-keel per the parent ADR. Rejected: resolving exact non-terminal
  state now (out of tracer scope).

## Immediate Next Steps

1. Complete Stage 5: release the lock, clean pipeline markers, run git-sync #1,
   `gz obpi reconcile`, `gz adr status`, git-sync #2.
2. Continue the ADR-0.31.0 checklist: OBPI-0.31.0-03 (runtime-invariant-monitor)
   is the remaining unstarted OBPI — its brief is authored and passes
   `gz obpi validate --authored`.
3. Consider filing/closing follow-ups: GHI #665 (unauthored-scaffold visibility),
   GHI #666 (plan-audit receipt id-normalization) remain open with blockers.

## Pending Work / Open Loops

- **OBPI-0.31.0-03 (runtime-invariant-monitor)** — the ADR's landing falsifier;
  not started. Its authored brief is ready.
- **GHI #665, #666** — open with blocker comments (coordinator/tooling defects).
- The `brief_reconcile.py` glob-fix and the coupled-surface edits are uncommitted
  until Stage 5 git-sync.

## Verification Checklist

- `uv run gz obpi status OBPI-0.31.0-02-withdraw-supersede-transitions` → attested_completed
- `uv run gz obpi lock list` → confirm release after Stage 5
- `uv run gz obpi reconcile OBPI-0.31.0-02-withdraw-supersede-transitions`
- `uv run gz adr status ADR-0.31.0` → 2/3 OBPI complete
- `uv run -m unittest tests.commands.test_obpi_supersede_cmd tests.commands.test_obpi_withdraw_cmd`

## Evidence / Artifacts

- `docs/design/adr/pre-release/ADR-0.31.0-obpi-state-machine/obpis/OBPI-0.31.0-02-withdraw-supersede-transitions.md`
- `src/gzkit/commands/obpi_cmd.py`
- `src/gzkit/ledger_events.py`
- `src/gzkit/events.py`
- `src/gzkit/ledger.py`
- `src/gzkit/schemas/ledger.json`
- `src/gzkit/cli/parser_artifacts.py`
- `src/gzkit/cli/parser_handler_manifest.py`
- `docs/user/manpages/obpi-supersede.md`
- `tests/commands/test_obpi_supersede_cmd.py`
- `config/doc-coverage.json`
