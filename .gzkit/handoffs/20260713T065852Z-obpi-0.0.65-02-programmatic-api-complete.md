---
mode: CREATE
adr_id: ADR-0.0.65
branch: main
timestamp: '2026-07-13T06:58:52Z'
agent: claude-code
obpi_id: OBPI-0.0.65-02-programmatic-api-implementation
---

## Current State Summary

OBPI-0.0.65-02-programmatic-api-implementation is ATTESTED COMPLETED (Gate 5, attestor g0, operator-verbatim conversational attestation). The real src/gzkit/handoff_api.py module ships (create_handoff/scaffold_handoff/list_handoffs/load_handoff_chain/resume_handoff) wrapping the fail-closed validate_handoff_document gate, with tests/governance/test_handoff_api.py (8 @covers BEHAVIOR tests). This completion handoff was itself authored by the new create_handoff API (dogfood). Remaining Stage 5: lock release, marker cleanup, two git-syncs, reconcile. Those new source files are untracked until git-sync #1 commits them, so Evidence cites the committed brief only (the validator checks git-tracked reality).

## Important Context

Heavy/foundation lane; brief-level Gate 5 was universal. The Step 4b Codex adversary REFUTED the first evidence pass and found three real defects (REQ-03 lexicographic sort, REQ-02 weak assertion, REQ-06 mis-cited SUPPORT path) — all fixed pre-attestation and re-validated. Security floor discharged via --accept-security-floor (incidental deserialization overlap on the unmodified handoff_validation.py; scanner slot unfilled).

## Decisions Made

1. REQ-07 needed no regex edit (already widened by OBPI-0.0.72-02) — proven by a covering test instead. 2. No behave waiver added — unit-only OBPI passes the behave-req gate without one (GHI #636); adding one would violate the shrink-only waiver-ratchet. 3. scaffold_handoff keeps observed state injected (hexagonal core purity), so REQ-02 byte-determinism is testable without IO mocks.

## Immediate Next Steps

1. Confirm ADR-0.0.65 remaining OBPIs (03 gz-handoff CLI verb, 04 orientation single-location scan) are the next pulls.
2. File the tracked follow-up: stage4_evidence._covers_counts should read behavior_uncovered_reqs, not uncovered_reqs.
3. Await operator selection of the next campaign pull.

## Pending Work / Open Loops

- OBPI-0.0.65-03 (gz handoff CLI verb) and OBPI-0.0.65-04 (orientation single-location scan) remain under parent ADR-0.0.65.
- Advisory defect: gz obpi present-evidence over-reports NOT-ATTESTABLE for SUPPORT REQs (non-blocking; complete uses BEHAVIOR coverage). Route as fix(governance) or GHI.

## Verification Checklist

- [ ] uv run gz obpi status OBPI-0.0.65-02-programmatic-api-implementation (Completion=attested_completed)
- [ ] uv run gz covers OBPI-0.0.65-02-programmatic-api-implementation --json (all 9 proof_status pass)
- [ ] uv run -m unittest tests.governance.test_handoff_api -v (8/8)
- [ ] uv run gz obpi lock list (no active lock on this OBPI after release)

## Evidence / Artifacts

- `docs/design/adr/foundation/ADR-0.0.65-handoff-system-consolidation/obpis/OBPI-0.0.65-02-programmatic-api-implementation.md`
- HEAD at handoff: 868b332f (src/gzkit/handoff_api.py + tests/governance/test_handoff_api.py land in git-sync #1)
