---
mode: CREATE
adr_id: ADR-0.0.37
branch: main
timestamp: "2026-06-15T08:12:00Z"
agent: claude-code
obpi_id: OBPI-0.0.37-24-advisor-panel-info-retention-qc-loop
session_id: c8f42adc
last_lock_event_timestamp: "2026-06-15T02:17:55.610422+00:00"
last_commit_sha: 0261ea4f78550eab1ba5e4941467271d6fcc7356
continues_from: .gzkit/handoffs/20260614T234540Z-pure-a-reconcile-restore-green.md
---

<!-- Completion register entry for OBPI-0.0.37-24 — couples to the lock release per ADR-0.0.41. -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

A handoff records a *proposed* plan and its context. On resume — at every
freshness level — present the advised next steps and obtain explicit operator
authorization before executing any of them. You advise; the operator rules.

## Current State Summary

**OBPI-0.0.37-24-advisor-panel-info-retention-qc-loop is COMPLETE and attested.**
This is the completion register entry coupled to the lock release (ADR-0.0.41).
The advisor-QC stage of the ADR-0.0.37 corpus pipeline landed: `gz content
advise-rendition` records an advisory information-retained-per-byte verdict as a
doctrine-aligned ARB receipt (`arb-step-judge-<32hex>`, explanation-before-verdict,
`exit_status` zero), advisory-never-gating; the `gz-advisor-qc` LLM-as-judge skill
wields it; a `rendition_advisor_verdict` ledger event witnesses each record. A
coupled pre-existing green-floor red (GHI #621) was fixed in the same session as a
separate direct-fix commit so the sync could land.

Lock claimed at the matching `obpi_lock_claimed` event (2026-06-15T02:17:55Z);
this handoff postdates it. Branch: `main`, ahead of origin by the OBPI-24 sync
commit plus the GHI #621 fix commit at the time of writing.

## Important Context

- Last commit SHA at handoff creation: `9829235b` (the GHI #621 guard fix); the
  OBPI-24 governance-edit sync commit follows it on `main`.
- The tool is deterministic — NO in-code LLM/network call. The judgment is the
  wielding skill's (the agent's); the engine only validates receipt shape and writes.
- Advisory-never-gating is load-bearing: any score exits zero; the ONLY fail-closed
  path is an empty `--explanation` (malformed verdict shape), never the value.
- Cross-ADR: ADR-0.0.39 (llm-as-judge) is still Proposed/Pending. This OBPI binds
  to its stable doctrine plus the existing ARB receipt infra, NOT the un-landed
  `judge_invocation.json`. If ADR-0.0.39 lands, conform the receipt to its schema.
- GHI #621 root cause: the forbid-pytest guard's pytest-dot pattern matched a
  sentence-ending "pytest." in a prose doctrine string; the fix narrows it to
  attribute-access form (identifier required after the dot).

## Decisions Made

- **Receipt schema (Open Implementation Decision):** doctrine-aligned ARB receipt
  (`arb-step-judge-<32hex>`), NOT bound to the un-landed `judge_invocation.json` —
  operator-confirmed at Gate 5.
- **GHI #621 resolution:** operator chose the class fix (narrow the guard regex), not
  the instance fix (rephrase the fixture string); landed as a separate
  `fix(hooks)` commit `9829235b`.
- **Two completion overrides applied, both grounded:** `--accept-stale-reconciliation`
  (two transitive import-neighborhood false positives — `src/gzkit/traceability.py`
  covers-decorator plus `src/gzkit/content/__init__.py` package init; git-confirmed
  unmodified) and `--accept-security-floor` (additive advisory surface; `gz validate
  --sensitivity` passes; parent-ADR security-scan slot unfilled pending the
  security-toolchain ADR, GHI #462).

## Immediate Next Steps

1. Confirm the OBPI-24 sync pushed clean and the parent ADR-0.0.37 view reflects
   the reconciled OBPI state.
2. Per the Magna Carta campaign, pull the topmost unchecked item whose gate is met.
   OBPI-0.0.37-25 (ADR-0.0.33 bullet-retention tier-scoped validator) is the next
   pipeline-sequence item — it witnesses the advisor-QC receipt this OBPI produces
   on the compressible tier.

## Pending Work / Open Loops

- **Parent ADR-0.0.37 remaining OBPIs:** OBPI-0.0.37-25 next per the campaign.
- **Cross-ADR:** if ADR-0.0.39 lands, conform the advisor-QC receipt to its
  `judge_invocation.json` schema at brief-reconcile.
- **Security floor:** the ADR-0.0.37 security-scan canonical slot stays unfilled
  until the security-toolchain ADR (pool.agentic-security-review) promotes; future
  `sensitivity:security` briefs under this ADR will need it filled or overridden.

## Verification Checklist

- Full unittest 6166 of 6166 — receipt `arb-step-unittest-9d00c5c9935f4977baa094a6f97a0242`
- Lint clean — receipt `arb-ruff-c717aeef0bb946e39919698f43f36625`
- Typecheck clean — receipt `arb-step-typecheck-6ef6688b8c584b518345ac4c1f15ec08`
- mkdocs strict clean — receipt `arb-step-mkdocs-5ce3d43c997b426db70e0b230aeabb85`
- behave 4 of 4 @REQ-0.0.37-24-01/02/03 — receipt `arb-step-behave-f9c4552861b34513854a4a3b2b2a4c8f`
- `gz covers` behavior_uncovered_reqs zero; validate documents/surfaces/ledger/cli-alignment/sensitivity pass
- forbid-pytest guard green over the whole repo after the GHI #621 fix

## Evidence / Artifacts

- Engine: `src/gzkit/content/advisor_qc.py`; command: `src/gzkit/commands/content/advise_rendition.py`
- Event: `RenditionAdvisorVerdictEvent` (`src/gzkit/events.py`), factory in `src/gzkit/ledger_events.py`,
  schema block in `src/gzkit/schemas/ledger.json`, no-graph waiver in `src/gzkit/governance/trust_audits/events.py`
- Skill: `.gzkit/skills/gz-advisor-qc/SKILL.md` plus four byte-equal mirrors; manpage `docs/user/skills/gz-advisor-qc.md`
- Tests: `tests/content/test_advisor_qc.py`, `tests/commands/test_content_advise_rendition.py`,
  `features/content_advise_rendition.feature` plus its steps file
- GHI #621 fix: commit `9829235b` (`src/gzkit/hooks/guards.py`, `tests/test_hooks_guards.py`)
- Brief: `docs/design/adr/foundation/ADR-0.0.37-constitutional-invariant-composition/obpis/OBPI-0.0.37-24-advisor-panel-info-retention-qc-loop.md`
