---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-19T20:13:19Z"
agent: claude-code
obpi_id:
session_id:
continues_from: .gzkit/handoffs/20260619T190909Z-sanity-reduction-617-631-boundary.md
last_commit_sha: d399745d
---

<!-- Handoff document for ADR-0.0.73 / Build-to-1.0 Magna Carta — created by claude-code at 2026-06-19T20:13:19Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

This session ran under the Magna Carta campaign
(`docs/governance/build-to-1.0-campaign-2026-06-10.md`, which GOVERNS). Floor is
green — every commit this session passed the full pre-commit gate (ruff + ty +
unittest + xenon) — and `main` is synced to `origin/main` at commit `d399745d`
(ahead=0, behind=0). Three commits landed:

- `ae83d8ac` — Sanity-Reduction **cut (d) CLOSED** as a substantiated
  NO-COLLAPSE verdict. All 13 `data/*` waiver/grandfather/baseline surfaces were
  inventoried (registry read first-hand + per-file consumer grep across
  `src/gzkit/`); each holds non-interchangeable data feeding a distinct gate via a
  distinct consumer. The "stacking" the cut targeted was already collapsed by
  ADR-0.0.73 Boundary Invariant #8 into the single `data/waiver_ratchet_registry.json`
  meta-validator. A baseline-slack observation was logged to `agent-insights.jsonl`.
- `d4e9c30f` — ADR-0.0.73 status reconciled to ledger truth (see Important
  Context) plus a course-correction insight.
- `d399745d` — six session `gate_checked` ledger events.

The Sanity-Reduction named-cut pull-order (a/b/c/d) is now exhausted.

## Important Context

- **ADR-0.0.73 (QC-binding meta-audit) is COMPLETE and validated — ledger-proven.**
  The ledger records `attested` (status=completed, by=g0, 2026-06-19T12:36:04Z),
  `lifecycle_transition` adr → Completed (12:37:49Z), and the closeout ceremony ran
  operator-driven 11:34–12:38 with attestation "Completed"
  (`.gzkit/ceremonies/ADR-0.0.73-verification-layer-binding-audit.ceremony.json`,
  Step 11 COMPLETE). All 9 OBPIs are attested_completed, including OBPI-0.0.73-02
  (repudiated 2026-06-18, repaired + re-attested 2026-06-19). `gz validate
  --qc-binding` exits 0; `gz adr fidelity` is 102/102.
- **Root-cause of the error chain this handoff corrects:** the prior two
  session-handoffs (15:33 and the 19:09 predecessor) were both authored AFTER the
  12:38 closeout yet carried forward a stale "ADR-0.0.73 (0/7), awaiting
  implementation" claim — they never registered the completion. The SessionStart
  digest reads the campaign "Topmost (sequenced)" line, which inherited the same
  stale number; this session's first cut(d) commit propagated it once more before
  it was caught and reconciled.
- **Lesson logged (ledger-first):** the agent initially misread the Layer-3
  derived view `gz adr status` ("Validated" / "Closeout READY") as a PENDING
  signal and asserted a non-existent "dangling closeout," before the Layer-2
  ledger proved completion. Corrective per AGENTS.md § Never #7: query the ledger
  for `attested`/`lifecycle_transition` BEFORE drawing any completion conclusion;
  treat `gz adr status` vocabulary as display, never as a pending signal. Recorded
  as an `improvement` insight in `.gzkit/insights/agent-insights.jsonl`.
- **The predecessor handoff (19:09) is SUPERSEDED by this one.** Its "Immediate
  Next Steps" item 4 ("campaign spine = ADR-0.0.73 (0/7)") is void. Its "Pending
  Work" claim that "ADR-0.0.73 item 7 (evaluator truth-binding) not yet built" is
  SUSPECT — OBPI-0.0.73-07-evaluate-truth-binding completed 2026-06-19 and is in
  the closeout's 9/9 — and must be verified before relying on the retained justify
  band-aid.
- **`gz adr evaluate` gotcha (carried):** it rewrites `docs/design/adr/AGENTS.md`
  with CRLF-only churn (no content change); discard with `git checkout` after
  re-scoring.

## Decisions Made

- **Decision:** Cut (d) closed as a substantiated NO-COLLAPSE verdict; no
  data-surface mutations.
  **Rationale:** all 13 waiver/grandfather/baseline surfaces hold
  non-interchangeable data feeding distinct gates via distinct consumers (verified
  first-hand: registry read + `src/gzkit/` consumer grep); the honesty-mechanism
  "stacking" was already unified by ADR-0.0.73 BI#8's `waiver_ratchet_registry.json`.
  Forcing a merge would obscure validator contracts.
  **Alternatives rejected:** merging the `tautological_*` or `surface_weight_*`
  pairs (separate load fns for separate roles); removing the baseline-0
  grandfathers (intentional permanent locks — removal loosens, not reduces).
- **Decision:** ADR-0.0.73 was NOT re-attested; only the stale campaign prose was
  reconciled.
  **Rationale:** the ledger already carries a complete, g0-attested closeout;
  re-running the ceremony would be double-attestation and verification theater. The
  defect was three hand-authored prose markers, not the ADR's state.
  **Alternatives rejected:** re-running `gz closeout --ceremony` (would have made
  the operator re-attest already-attested work — nearly proposed in error, then
  corrected from the ledger).
- **Decision:** This fresh handoff supersedes the 19:09 predecessor rather than
  editing it.
  **Rationale:** the predecessor is materially stale on ADR-0.0.73; left as the
  resume record it would re-inject the "0/7" error into the next session.
  Superseding via a new chained document preserves the audit trail.
  **Alternatives rejected:** editing the predecessor in place (handoffs are
  append-only register entries).

## Immediate Next Steps

ADVISORY ONLY — present these and await operator authorization before acting.

1. **Pull the campaign-spine topmost: B.1 — ADR-0.0.37 real corpus rebuild.** Per
   the ratified "Topmost (sequenced)" marker, with ADR-0.0.73 done the spine
   advances to the real ADR-0.0.37 CMS-pipeline corpus rebuild (it reopens behind
   the completed meta-audit; OBPIs 02/03/21/22 were repudiated
   `model-induced-fabrication`). This is the governing next pull.
2. **Verify the SUSPECT "item 7" claim** before trusting the predecessor's
   "evaluator truth-binding not yet built / band-aid retained" note: read
   `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-07-evaluate-truth-binding.md`
   and check whether the `_score_feature_checklist` false-RED is resolved; if so,
   retire the retained ADR-0.0.73 justify band-aid.
3. **Triage the phantom-ADR orphaned evals** (ADR-0.47.0 / 0.49.0 / 0.50.0): stale
   low-score `adr-evaluation` ledger events plus justify band-aids, no resolvable
   on-disk package. Decide repudiate-evals vs resolve-lifecycle; file a GHI via
   `/ghi-author` if it warrants tracking.
4. **Investigate the pre-existing control-surface drift** before any `gz agent
   sync`: `gz validate --surfaces` and `gz agent sync --dry-run` disagree on which
   surfaces drifted; determine whether a mirror was edited directly (version-marker
   check per `.claude/rules/skill-surface-sync.md`).

## Pending Work / Open Loops

- **GHI #618 OPEN** — `VALIDATOR_REGISTRY` landed the internal enumerations;
  residual is the 78-param `validate()` signature, the forwarding lambda, and the
  ~80 parser flags + `_build_check_steps` (not registry-derived). Its own ADR/OBPI
  per the GHI scope hint.
- **GHI #632 OPEN** — tautological-test-audit brittleness (E.5 drainage),
  unchanged this session.
- **Phantom-ADR orphaned evals** (0.47.0 / 0.49.0 / 0.50.0) — untracked; needs
  triage (Next Step 3).
- **Control-surface drift** — untracked; investigate cause before any `gz agent
  sync` (Next Step 4).
- **SUSPECT predecessor claim** — "ADR-0.0.73 item 7 / evaluator truth-binding not
  yet built" contradicts the OBPI-0.0.73-07 completion; resolve via Next Step 2.
- **Baseline-slack (cut d census input)** — shrink-ratchet baselines carry slack
  (current entry counts below committed baselines); a baseline-tightening pass is a
  Phase-I action, logged in `agent-insights.jsonl`.

## Verification Checklist

- [ ] `git branch --show-current` is `main`; HEAD at or ahead of `d399745d`,
  synced (`git rev-list --left-right --count origin/main...HEAD` returns `0 0`).
- [ ] ADR-0.0.73 completion confirmed from Layer-2 (NOT from `gz adr status`
  vocabulary): `grep -E '"event":"(attested|lifecycle_transition)"' .gzkit/ledger.jsonl`
  filtered to `0.0.73` shows attested + Completed.
- [ ] `uv run gz validate --adr-status-fresh` exits 0.
- [ ] `uv run gz validate --qc-binding` exits 0 (ADR-0.0.73 self-check green).
- [ ] Campaign "Topmost (sequenced)" line reads ADR-0.0.73 DONE then B.1 next (no
  residual "0/7").
- [ ] Confirm GHI #618 and #632 still OPEN.

## Evidence / Artifacts

- `docs/governance/build-to-1.0-campaign-2026-06-10.md` — Magna Carta; Progress note + Topmost marker reconciled to ADR-0.0.73 completion this session
- `.gzkit/ledger.jsonl` — Layer-2 truth: ADR-0.0.73 `attested` (12:36) + `lifecycle_transition` to Completed (12:37); 9 OBPI attested_completed receipts
- `.gzkit/ceremonies/ADR-0.0.73-verification-layer-binding-audit.ceremony.json` — closeout ceremony state, Step 11 COMPLETE, attestation "Completed"
- `.gzkit/insights/agent-insights.jsonl` — cut(d) baseline-slack `discovery` plus the derived-view-misread `improvement` course-correction
- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/ADR-0.0.73-verification-layer-binding-audit.md` — ADR package (status Validated)
- `data/waiver_ratchet_registry.json` — the BI#8 meta-validator that already unified the waiver/grandfather/baseline honesty mechanism (cut d keystone)
- `.gzkit/handoffs/20260619T190909Z-sanity-reduction-617-631-boundary.md` — superseded predecessor (stale on ADR-0.0.73)

## Environment State

- Python 3.13 with uv; Windows primary. `uv run -m unittest -q` runs ~265s (6329
  tests); the pre-commit hook runs the full gate on every commit. `gz adr evaluate`
  touches `docs/design/adr/AGENTS.md` with CRLF-only churn — discard after
  re-scoring.
