---
mode: CREATE
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-27T11:45:00Z"
agent: claude-code
obpi_id: OBPI-0.0.74-13-mx-proxy-reality-detector
last_lock_event_timestamp: "2026-06-27T10:58:00.309808+00:00"
last_commit_sha: c53ade5a
session_id:
continues_from:
---

<!-- Handoff document for ADR-0.0.74 — created by claude-code at 2026-06-27T11:45:00Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

OBPI-0.0.74-13-mx-proxy-reality-detector is **completed and operator-attested**
(`g0`, attestation_type operator-verbatim-conversational). The proxy-reality
distance detector landed at `src/gzkit/mx/proxy_reality.py`: `scan()` reads the
ledger for `obpi_completion_repudiated` events with `cause=model-induced-fabrication`,
returns a count + per-instance records (count=5 against the real ledger). The
passing-on-violation live NC (`_build_proxy_reality_violation` + `_ep_proxy_reality`)
runs the real `scan()` against a planted violation. All 11 tests in
`tests/mx/test_proxy_reality.py` pass; full suite 6560 tests green.

Mid-pipeline, adversarial Stage 4b (Codex) caught that the brief had deferred
REQ-13-03 to OBPI-0.0.74-19 — which is already **Completed**, so the deferral was
an orphan. The fix wired `_ensure_grader_gaming_registered()` AND the
identically-orphaned `_ensure_gate5_claims_registered()` (OBPI-17) into the single
production-discovery seam `_ensure_production_claims_registered()` in
`src/gzkit/enforcement.py`. REQ-13-03 now resolves `pass` (proven structural fence).
Enforcement floor audit: 41 claims verified.

`gz obpi complete` succeeded. Lock release + git-sync are the remaining Stage 5
steps (this handoff is authored before lock release per ADR-0.0.41 coupling).

## Important Context

- The single production-discovery seam is `_ensure_production_claims_registered()`
  (`src/gzkit/enforcement.py`). Any `@enforces` claim source NOT reachable from it is
  an orphan / floor facade. It now wires three sources: qc_binding, mx.invariants
  (gate5), mx.proxy_reality (grader-gaming). Lazy imports avoid the mx↔enforcement cycle.
- `secrets` and `operator-pii` are deliberately NOT registered (honest-negative,
  named-not-enforced per ADR-0.0.74 § Consequences/Negative #7). Do not bind a
  narrower proxy to "complete" them.
- REQ-13-03 is `[structural-fence]`; its proof channel is the live `@enforces`
  registration (resolved by `resolve_fence_proof` finding the backticked claim id),
  not a `@covers` test.

## Decisions Made

- **Decision:** Wire gate5 + grader-gaming sources into the production seam within
  OBPI-13, editing `src/gzkit/enforcement.py` (outside the original allowlist).
  **Rationale:** Deferring to already-Completed OBPI-19 is an orphan; the operator
  ruled "orphaning is not acceptable." Allowlist amended (operator-ratified). The
  same seam edit cures OBPI-17's gate5 orphan — fix the class, not the instance.
  **Alternatives rejected:** (a) wire grader-gaming only, leave gate5 orphaned —
  knowingly ships a facade; (b) build the BI#9 enrollment enumeration here — operator
  assigned the systemic cure to the AIRLOCK system.

- **Decision:** File GHI #648 for the remaining BI#9 enrollment-completeness gap.
  **Rationale:** The floor audit verifies only registered claims; it never enumerates
  `GATE5_INVARIANTS` membership, so a future missing member is silently absent.
  Routed to AIRLOCK. **Alternatives rejected:** fixing enumeration in-OBPI (operator
  scoped it out).

## Immediate Next Steps

1. Release the OBPI lock: `uv run gz obpi lock release OBPI-0.0.74-13-mx-proxy-reality-detector`
   (this handoff is the required register entry per ADR-0.0.41 — do NOT `--abandon`; this is a completion).
2. Git-sync #1: `uv run gz git-sync --apply` (commits completion governance edits + new source/test files + lock release).
3. `uv run gz obpi reconcile OBPI-0.0.74-13-mx-proxy-reality-detector` to confirm receipt and brief agree.
4. `uv run gz adr status ADR-0.0.74 --json` to refresh the parent ADR view.
5. Git-sync #2: `uv run gz git-sync --apply` (commits reconcile + ADR status refresh).

## Pending Work / Open Loops

- **ADR-0.0.74 remaining OBPIs:** verify whole-ADR closeout state via `gz adr status ADR-0.0.74`.
  Per session orientation, the Build-to-1.0 campaign's MX lean-kernel + hardening items
  and the `0.29.0` release are downstream. OBPI-13 is one checklist item of 19 active.
- **GHI #648 (open):** BI#9 enrollment-completeness enumeration — route to AIRLOCK system.
- **GHI #623 (open, sibling):** cross-linked facade-class finding (ADR-0.0.37 derivation spine).

## Verification Checklist

- [ ] `uv run -m unittest tests.mx.test_proxy_reality -v` — 11 tests pass
- [ ] `uv run gz covers OBPI-0.0.74-13-mx-proxy-reality-detector --json` — REQ-13-03 proof_status `pass`
- [ ] `uv run python3 -c "from gzkit.quality import run_enforcement_floor_audit; from pathlib import Path; print(run_enforcement_floor_audit(Path('.')).stdout)"` — 41 claims verified
- [ ] Branch matches: `git branch --show-current` → main
- [ ] `uv run gz obpi status OBPI-0.0.74-13-mx-proxy-reality-detector` → attested-completed

## Evidence / Artifacts

- `src/gzkit/mx/proxy_reality.py` — the detector + live NC + production-discovery registration
- `tests/mx/test_proxy_reality.py` — 11 tests (5 scan, 3 live NC, 3 wiring regression)
- `src/gzkit/enforcement.py` — production-discovery seam now wires gate5 + grader-gaming sources
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/obpis/OBPI-0.0.74-13-mx-proxy-reality-detector.md` — brief with Implementation Summary + Key Proof
- `.claude/plans/proxy-reality-detector-OBPI-0.0.74-13.md` — the audited plan

## Environment State

Python 3.13 / uv. Receipts under `artifacts/receipts/`:
`arb-step-unittest-16894dfc4acb4cc18b8fb1cde33c5d24` (6560 tests OK),
`arb-ruff-f091d77627d04421a3641c5a576ed01c`,
`arb-step-typecheck-987b339f62234e11bdf18fd46fa242c3`,
`arb-step-mkdocs-42e28f1fb54c45889d58ddfdcd9f9e55`.
