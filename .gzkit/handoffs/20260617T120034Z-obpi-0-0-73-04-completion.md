---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-17T12:00:34Z"
agent: claude-code
obpi_id: OBPI-0.0.73-04-closeout-audit-fidelity-repoint
session_id:
continues_from: .gzkit/handoffs/20260617T103046Z-obpi-0-0-73-03-completion.md
last_lock_event_timestamp: "2026-06-17T10:49:03.645887+00:00"
last_commit_sha: d66a6168cbf727520a7d67baa477f3c0b3cde5dc
---

<!-- Handoff document for ADR-0.0.73 — created by claude-code at 2026-06-17T12:00:34Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. On resume — at every freshness level, Fresh included — present the
advised steps and obtain explicit operator authorization before executing any of
them. You advise; the operator rules.

## Current State Summary

OBPI-0.0.73-04-closeout-audit-fidelity-repoint is **attested-complete** (Gate 5,
Heavy lane; attestor `g0`, operator-verbatim "attest completed" 2026-06-17).
Part 4 of ADR-0.0.73: both the closeout ceremony and the audit ceremony now
invoke the SAME bound fidelity gate (`assert_fidelity_for_ceremony` — one gate,
two consumers), replacing the prose 'Demonstrate Value' step. Delivered through
the gz-obpi-pipeline (Stages 1→5).

## Important Context

The prose 'Demonstrate Value' step was theater (agent-written, graded by
nothing) — the exact facade ADR-0.0.73 exists to kill. It is now a bound,
runnable gate that RUNS the ADR's `## Fidelity Assertions` against the running
system. The `gz-adr-audit` skill's `trust_model` literally documented "does NOT
re-verify evidence" — the facade-enabler the ADR named — and is now corrected.

This OBPI was the first to traverse the pipeline's pre-implementation gates
cleanly without a fabricated receipt, because the first-implementation deadlock
was fixed in flight: **GHI #626** (closed, commit `d66a6168`) — `gz plan audit`
ignored the brief's `**CREATE**` markers, and `gz brief reconcile`
existence-checked `**` globs as literals. Both fixed RED-first.

## Decisions Made

- **Graceful absence policy (operator-ratified):** a missing `## Fidelity
  Assertions` block warns (stderr) but does not hard-block in-flight; presence is
  enforced at ADR closeout (Boundary Invariant #4). This honored the ADR's
  scope boundary (back-fill of existing ADRs is a separate forced sweep) and
  kept blast radius to one fixture migration rather than breaking every audit.
- **Allowlist corrected to reality:** `src/gzkit/commands/adr_audit.py` (a
  registered security surface) was declared but never touched — the prose step
  lived in the skill + closeout, not that module. Removed it (clearing the
  security-floor false positive); added `src/gzkit/fidelity.py` (the gate's home).

## Immediate Next Steps

1. Confirm git-sync landed both commits (`d66a6168` gate fix, `a9dced83`
   implementation) and that `gz reconcile` + ADR status refresh are committed.
2. Pull the next ADR-0.0.73 OBPI (05 — absorb the dispatch-attestation pool ADR)
   per the Build-to-1.0 campaign, or take operator direction.

## Pending Work / Open Loops

- **ADR-0.0.73 remaining OBPIs:** 05 (absorb ADR-pool.obpi-pipeline-dispatch-
  attestation), 06 (self-check + facade regression corpus — flips this ADR's own
  Fidelity Assertions green), 07 (evaluator truth-binding, GHI #624).
- **Forced follow-up (ADR scope boundary):** back-fill `## Fidelity Assertions`
  onto already-VALIDATED foundation ADRs (separate chore/ADR). Until then those
  ADRs hit the graceful-absence warning at closeout/audit.
- **ADR-0.0.73 closeout** runs the full closeout+audit fidelity walkthrough
  end-to-end (the behave coverage waived for OBPI-04 lands its E2E there).

## Verification Checklist

- [x] Full unittest suite green — `arb-step-unittest-4d41eba9c7a24700b71a4f1e506a81d3`
- [x] Lint clean — `arb-ruff-b9548735f27b45ccaef3a7ee3306b13a`
- [x] Typecheck clean — `arb-step-typecheck-526c474a66074b38a45512980f177d11`
- [x] Docs build strict — `arb-step-mkdocs-0b78a421012d4d948176f19196fc64b2`
- [x] `gz validate --documents` (REQ-04) and `--surfaces` (REQ-05) green
- [x] `gz covers` — `behavior_uncovered_reqs=0`; all 5 REQs `proof_status: pass`
- [x] Gate 5 attestation recorded (`attested_completed`, attestor `g0`)

## Evidence / Artifacts

- Commits: `d66a6168` (fix GHI #626 gate deadlock), `a9dced83` (OBPI-04 implementation + governance).
- Implementation: `src/gzkit/fidelity.py` (`assert_fidelity_for_ceremony`),
  `src/gzkit/commands/closeout_ceremony.py` (EXECUTE→ATTESTATION edge),
  `src/gzkit/commands/audit_cmd.py` (pre-receipt).
- Tests: `tests/governance/test_closeout_audit_fidelity.py` (7 tests, @covers REQ-01/02/03);
  `tests/test_audit_pipeline.py` fixture migrated with a `## Fidelity Assertions` block.
- Docs/skills: `docs/user/runbook.md`, `docs/governance/governance_runbook.md`,
  `.gzkit/skills/gz-adr-audit/SKILL.md`, `.gzkit/skills/gz-adr-closeout-ceremony/SKILL.md`.
- Brief: `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-04-closeout-audit-fidelity-repoint.md`.
