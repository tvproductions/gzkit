---
mode: CREATE
adr_id: null
branch: main
timestamp: '2026-08-28T01:59:45Z'
agent: claude-code
session_id: d1eaa4d2-4497-4373-9375-93d3aee1529f
continues_from: .gzkit/handoffs/20260828T011210Z-session-exit-bookmark.md
---

## Current State Summary

GHI #900 closed (disposition fixed) by commit 22ad1659, which closes the CLASS that 709f3b7b had only repaired instance-wise: nothing read wheel-shipped instruction text for path literals that cannot resolve on an adopter's machine, so gz validate --distribution read green the whole time the defect stood. New audit gzkit.governance.trust_audits.wheel_path_literals fails closed on home-rooted, drive-rooted and machine-provisioning path literals in wheel-shipped Markdown; scope is read from pyproject's own wheel include block via the shared wheel_build_config helper, so witness scope cannot drift from delivery scope. Wired as default-tier scope AND named gz check step [3/58] Wheel path literals. uv run gz check returned REAL EXIT 0 on a fully staged tree. GHI #787 was then REOPENED with regression evidence, and GHI #866 cross-linked. One uncommitted file remains: .gzkit/insights/agent-insights.jsonl. HEAD is one commit ahead of origin/main; a git sync was requested and had not run when this handoff was written.

## Important Context

The expensive part of this session was NOT the fix. Adding one gz validate scope required roughly a dozen coordinated registrations, each discovered by tripping it, one per multi-minute gz check cycle (8 full runs at 9,037 tests each). Three of those gates fire only at pre-commit, i.e. after gz check has already returned 0 — the validator-reachability ratchet is the important one, because default-tier membership in VALIDATOR_REGISTRY is NOT sufficient: the ratchet reads the curated pipeline in src/gzkit/commands/quality.py, so a scope must ALSO be a named check step or it reads as ungated. run_corpus_retirement_witness_audit's docstring already records that trap; the coupling checklist does not. A second non-obvious constraint: promoting a scope to default tier changes its input contract, because the QC negative controls exercise default scopes against synthetic project roots that ship no wheel — --distribution's SystemExit(2) on a missing pyproject.toml is wrong there, which is why wheel_build_config grew a missing_ok arm (absence means no delivered instruction text; an UNPARSEABLE pyproject stays fatal). Finally, editing any .gzkit/rules/*.md file leaves its advisory-scorecard grandfather pin behind and obliges scoring its clauses for real; that grandfather is shrink-only, so newly written rows cannot re-enter it.

## Decisions Made

- [operator-ruled] Drive GHI #900 to terminal state this session (operator invoked /ghi-close 900). The prior comment had left the witness scope awaiting a ruling; the invocation was read as the instruction to execute, not to re-ask.
- [agent-chose] Executed witness option (1) of the three the routing comment offered — absolute-path literals in wheel-shipped instruction text. Options (2) and (3) carried named concrete downsides already recorded in the issue thread (repo-wide enforcement collides with ~1,900 record-shaped occurrences; widening --distribution conflates two claims), so this was a dominant choice under DO IT RIGHT #3 rather than a balanced one.
- [agent-chose] Kept option (2)'s DOCTRINE half while declining its witnessing half. The missing clause now exists at .gzkit/rules/cross-platform.md § Delivered path literals (rule 0.6.0) with paths: widened to src/gzkit/**/*.md, so the rule loads where the defect is authored.
- [agent-chose] Corrected the issue body's own framing: ~/ and $HOME/ expand per reader and are the REMEDY shape, not the defect. The body had named ~/Dropbox as an instance. Measured 9 legitimate tilde uses and 23 legitimate /tmp uses in wheel Markdown; flagging them would push authors back to the literal they just left.
- [agent-chose] Scoped the audit to .md only, which excludes the C:/Users/RUNNER~1 docstring in hooks/scripts/validation.py on principle rather than waiving it by line number.
- [agent-chose] Scored advisory-scorecard rows 45b and 45c Promotable, NOT Mechanical. Both are enforced by unit tests, and this scorecard reserves Mechanical for a row citing a registered negative control (the 62c/62d precedent).
- [agent-chose] Declared EXEMPTS_NONE for the new claim against the stated bar: the non-flagged roots are a fixed code property defining what a violation IS, and the .md walk is a scope predicate — neither is a project-controllable admit path.
- [agent-chose] Documented the flag's failing exit code as the MEASURED 1 rather than the 3 transcribed from neighbouring manpage sections.
- [operator-ruled] The 45-to-60-minute cost of a GHI close is a defect, not the expected price (operator verbatim: 'another 45 to 60 minute ghi run - broken'). Recorded as an improvement insight before the corrected work completed, per Behavior Rule 11.
- [operator-ruled] File the coupling-cost finding (operator verbatim: 'yes, file it').
- [agent-chose] Did NOT file a fresh GHI for it. ghi-author Step 0 found the same root cause already existed as GHI #787, closed 19 days earlier; the pre-flight table mandates re-open with regression evidence for a close inside 30 days, never a fresh number. Surfaced the deviation to the operator explicitly so it could be overruled.

## Immediate Next Steps

1. Run the requested git sync — HEAD carries commit 22ad1659 unpushed and .gzkit/insights/agent-insights.jsonl is modified and uncommitted. Use the git-sync skill, not raw git.
2. Decide disposition on GHI #787 (reopened). The remedy is small: add a default-tier branch to the family-B coupling checklist in src/gzkit/commands/quality.py naming the four unnamed obligations (check_step_concurrency.json, _POST_SNAPSHOT_DEFAULT_ADDITIONS, QC_CLAIM_EXEMPTS, the named-step requirement for reachability).
3. Rule on the durable half of GHI #787: whether the validator-reachability ratchet belongs in gz check rather than pre-commit only. Its current placement is what breaks the checklist's own stated 'the witnesses are loud' property.
4. Re-derive GHI #866's blocking premise directly. A comment was posted with measured evidence that the module-size gate now reports 0 grandfathered and 0 over band, but WHY the two zero-headroom ceilings went away is not established, and OBPI-0.35.0-06-validate-rendition-lineage should be re-checked against the current tree before that issue is disposed of.
5. GHI #899 remains open and untouched by this session — the operator-name-in-wheel sibling of #900 [settled].

## Pending Work / Open Loops

GHI #787 — reopened this session; no fix authored, only evidence. GHI #866 — open; commented, not disposed of; its premise has measurably changed but the class-of-failure question (a shrink-only ratchet on a sole registration point) is not established as resolved. GHI #899 — open, untouched. Residual gaps deliberately left by the #900 [settled] fix and stated in its close comment: a machine-specific path under /tmp, /usr, /var or /private is NOT caught; shipped .json and .py are out of scope; a relative path that does not exist for the reader is uncovered; and docs/proposals/REPORT-TEMPLATE-airlineops-parity.md was repaired but is NOT witnessed, because it is instruction-shaped yet not wheel-shipped — a future non-shipped template can reintroduce the shape with nothing to catch it. Advisory drift reported by gz check throughout: 696 unlinked specs, 11 unjustified code changes; unchanged in kind by this session.

## Verification Checklist

uv run gz check  (expect REAL EXIT 0 on a fully staged tree; git add -A first)
uv run gz validate --wheel-path-literals  (expect exit 0; exit 1 with path:line attribution on a violation)
uv run python -m unittest tests.governance.test_wheel_path_literals  (expect 17 tests OK)
uv run python src/gzkit/chores/control-surface-validator-reachability/check_reachability.py  (expect 'ratchet holds')
git rev-list --left-right --count origin/main...HEAD  (confirm the sync landed)
gh issue view 787 --json number,state  (expect OPEN until the coupling checklist is repaired)
Mutation check if the audit is edited: stub audit_wheel_path_literals to return [] and confirm 7 of 17 tests fail.

## Evidence / Artifacts

`src/gzkit/governance/trust_audits/wheel_path_literals.py` — the new audit
`tests/governance/test_wheel_path_literals.py` — 17 tests; 7 killed by a stubbed audit
`.gzkit/rules/cross-platform.md` — rule 0.6.0, § Delivered path literals (doctrine home)
`src/gzkit/commands/quality.py` — the coupling checklist GHI #787 is reopened against, and the new named check step
`docs/user/manpages/validate.md` — per-flag section, exit code documented as measured
`docs/governance/advisory-rules-audit.md` — rows 45b/45c/45d, Coverage Ledger entry, Summary recount to 131 scored rows
`data/check_step_concurrency.json` — read_only declared from measurement, not defaulted
`docs/proposals/REPORT-TEMPLATE-airlineops-parity.md` — three-rung ladder cut to two
`.gzkit/insights/agent-insights.jsonl` — improvement insight, scope 'gz validate scope registration'
Commit 22ad1659; ARB receipts arb-ruff-0859fb839e88491eb5b7c3dd940e4839, arb-step-typecheck-550040c1d1b4478d8ed43c11bce051ea, arb-step-unittest-7455a97424b342debc972580537bde83

## Settled Rulings

559 rulings booked and carried forward. The corpus lives in `.gzkit/handoffs/rulings.jsonl` — read it with `gz handoff rulings`.

Do NOT re-open these. A ruling booked once keeps arriving; it is carried by reference from the append-only store, not by copying the whole corpus into every successor document (GHI #838).
