---
mode: CREATE
adr_id: ADR-0.0.74
branch: main
timestamp: "2026-06-21T23:58:42Z"
agent: claude-code
obpi_id:
session_id:
continues_from: .gzkit/handoffs/20260621T235511Z-obpi-0-0-74-11-levels-vocabulary-completed.md
---

<!-- Handoff document for ADR-0.0.74 — created by claude-code at 2026-06-21T23:58:42Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony, no migration until the operator says go.
3. Treat the human-as-final-witness doctrine as binding from the first step: you
   advise; the operator rules; you note variance and stop.

Barreling into execution from this document is the exact failure this handoff
exists to prevent. The plan is the destination; operator authorization is the
ignition.

## Current State Summary

**ADR-0.0.74 is 3/13; OBPI-0.0.74-11 (the `GZ_<LEVEL>` vocabulary) shipped and
synced this session.** The full `uv run gz obpi pipeline` ran end to end for
OBPI-11: plan-audit PASS, lock claimed, brief reconciled, TDD RED→GREEN, Stage 3
all-green, Stage 4 operator attestation ("attest completed", attestor g0), Stage 5
atomic completion + two git-syncs. Lock released, pipeline markers cleaned.

Delivered: `src/gzkit/mx/levels.py` (STDLIB-FIRST ladder reusing Python `logging`
constants + `NOTICE = 25`, `GROUNDING_THRESHOLD = ERROR`, `grounds()`) and
`tests/mx/test_levels.py` (5 tests, 5/5 pass).

**HEAD `cf7350b3` on `origin/main`; ahead=0 behind=0; working tree clean.** No
active OBPI locks. This is a context-continuity session handoff, not a
lock-release register entry (no lock is held).

## Important Context

- **Dependency order for the remaining substrate** (from the briefs' Prerequisites):
  OBPI-11 (vocabulary, DONE) is the foundation; **OBPI-12 (disposition handler)**
  and **OBPI-03 (gate5 floor + grader-gaming)** consume it; OBPI-13 (proxy-reality)
  makes OBPI-03's grader-gaming membership live; OBPI-14 (hardening) consumes the
  leveled checkpoint; OBPI-05 (exit) consumes OBPI-04 (enter) plus 11/12. OBPI-01/02/11
  are the landed honest base.
- **Contract-bearing heavy OBPIs use the `uv run gz obpi pipeline` runtime** — it
  owns stage sequencing (plan-audit → lock → reconcile → implement → verify →
  Stage-4 human gate → complete → two-sync). Each completion is brief-level
  Gate-5 attested.
- **Tracked defect — Movement II item 3 (encountered live this session):**
  `gz obpi precomplete`'s `behave_req_coverage` check in
  `src/gzkit/commands/obpi_precomplete.py` (~lines 324-396) is NOT REQ-kind-aware,
  unlike `obpi_complete.py` (fixed under GHI #636 / tests.md v0.9.0). It
  false-flagged all 3 of OBPI-11's REQs (two BEHAVIOR-with-@covers, one
  STRUCTURAL-FENCE). The completion chokepoint (`obpi_complete.py`) IS kind-aware
  and accepted them. Clean GHI-direct fix per AGENTS.md § Defect-fix routing.
- **Reconcile-engine observation (lower priority):** `gz brief reconcile --apply`
  added `src/gzkit/mx/__init__.py` to OBPI-11's allowlist via a new-submodule
  coupled-surface heuristic, though `__init__.py` was not actually modified.
  Harmless permissive over-declaration; flagged in the insights log.
- **Both items are logged** in `.gzkit/insights/agent-insights.jsonl`
  (improvement record, ts 2026-06-21T23:52:54Z).

## Decisions Made

- **Decision:** Pull OBPI-11 (vocabulary) first among the unbuilt substrate OBPIs.
  **Rationale:** OBPI-12/03/14 all consume the `GZ_<LEVEL>` vocabulary; it is the
  dependency root.
  **Alternatives rejected:** starting with the precomplete direct-fix (Movement II
  item 3) — deferred as a momentum option, not the campaign-topmost build.

- **Decision:** Route OBPI-11 completion through the kind-aware `gz obpi complete`
  chokepoint despite the stale precomplete `behave_req_coverage` failure, without
  editing `obpi_precomplete.py`.
  **Rationale:** `obpi_precomplete.py` is outside OBPI-11's allowlist (cross-brief
  scope); the skill names precomplete the bypassable pre-flight and complete the
  chokepoint.
  **Alternatives rejected:** editing the precomplete check inside the OBPI-11
  pipeline (scope violation).

## Immediate Next Steps

<!-- ADVISORY ONLY — present to operator, await authorization before acting. -->

1. **Implement OBPI-0.0.74-12 (`mx-gates-as-sensors` / disposition handler)** — the
   next dependency-order pull. Consumes the `GZ_<LEVEL>` vocabulary and wires the
   level → AOG/advisory disposition (ADR § Boundary Invariants #2 — "the one
   disposition handler"). Start with `/gz-plan-audit OBPI-0.0.74-12`, then
   `uv run gz obpi pipeline OBPI-0.0.74-12`.
2. **Or take the quick win first:** discharge Movement II item 3 — make
   `src/gzkit/commands/obpi_precomplete.py`'s behave-coverage check REQ-kind-aware
   (mirror `obpi_complete.py` / GHI #636) so the pre-flight stops false-flagging
   BEHAVIOR-with-@covers and STRUCTURAL-FENCE REQs. Direct `fix(<scope>): … (GHI #N)`.
3. **Then continue the substrate** in dependency order: OBPI-03 (gate5 floor +
   grader-gaming), OBPI-13 (proxy-reality live NC, after 03), OBPI-04 (enter) +
   OBPI-05 (exit), OBPI-06/07/08 (log/hook/skill), OBPI-09 (retire staging flags),
   OBPI-14 (hardening) → release `0.29.0`.

## Pending Work / Open Loops

- **ADR-0.0.74 is 3/13 complete.** Remaining: OBPI-03/04/05/06/07/08/09/12/13/14
  (item 10 withdrawn-hidden). The substrate build is the bulk of remaining work.
- **Movement II item 3 defect** (`obpi_precomplete.py` not REQ-kind-aware) —
  awaiting the direct fix; logged in the insights file.
- **Reconcile-engine `__init__.py` over-reach** — lower-priority observation in
  the same insight.
- **`ADR-0.29.0-precise-auth`** still in the feature list, not yet dropped to pool
  — release bookkeeping for the eventual `0.29.0` MX release.
- **The enforcement-claim meta-validator** (Movement I item 3 — the general §5
  mechanism) remains its own work, not in this ADR.

## Verification Checklist

- [ ] `git rev-parse --short HEAD` is `cf7350b3` and `origin/main` equals it (ahead=0 behind=0)
- [ ] `git branch --show-current` is `main`; working tree clean
- [ ] `uv run gz obpi status OBPI-0.0.74-11-mx-gz-level-vocabulary` shows attested_completed
- [ ] `uv run gz adr status ADR-0.0.74` shows 3/13
- [ ] `uv run gz obpi lock list` shows no active locks
- [ ] `uv run -m unittest tests.mx.test_levels -v` passes 5/5

## Evidence / Artifacts

- `src/gzkit/mx/levels.py` — the `GZ_<LEVEL>` vocabulary (ladder + NOTICE=25, GROUNDING_THRESHOLD=ERROR, grounds())
- `tests/mx/test_levels.py` — 5 unit tests (stdlib-equality ladder, NOTICE drift rung, grounding boundary)
- `docs/design/adr/foundation/ADR-0.0.74-mx-mode-maintenance-hangar/obpis/OBPI-0.0.74-11-mx-gz-level-vocabulary.md` — the completed brief
- `.claude/plans/OBPI-0.0.74-11-mx-gz-level-vocabulary.md` — the approved plan
- `.claude/plans/.plan-audit-receipt-OBPI-0.0.74-11-mx-gz-level-vocabulary.json` — plan-audit PASS receipt
- `.gzkit/handoffs/20260621T235511Z-obpi-0-0-74-11-levels-vocabulary-completed.md` — the OBPI-11 completion handoff (predecessor; this session's continues_from)
- `.gzkit/insights/agent-insights.jsonl` — improvement insight for the two Stage-5 gate-friction items
- `src/gzkit/commands/obpi_precomplete.py` — site of the tracked Movement II item 3 stale behave-check defect (~lines 324-396)

## Environment State

Platform win32; Python 3.13; `uv run` throughout. Branch `main` (operator
directive: no feature branches). HEAD `cf7350b3`, ahead=0 behind=0, working tree
clean. No active OBPI locks. Last lock event this session: `obpi_lock_released`
for OBPI-0.0.74-11-mx-gz-level-vocabulary (claimed 2026-06-21T23:07:13Z, released
after the OBPI-11 completion handoff per token-block coupling).
