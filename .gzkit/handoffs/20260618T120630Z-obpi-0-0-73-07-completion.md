---
mode: CREATE
adr_id: ADR-0.0.73
branch: main
timestamp: "2026-06-18T12:06:30Z"
agent: claude-code
obpi_id: OBPI-0.0.73-07-evaluate-truth-binding
session_id: obpi-0-0-73-07-pipeline
continues_from: .gzkit/handoffs/20260618T104253Z-obpi-0-0-73-06-completion.md
last_lock_event_timestamp: "2026-06-18T11:41:08.896646+00:00"
last_commit_sha: 908b7cc922318162f5cbd836a2c7e5d3d4ba2e42
---

<!-- Completion handoff for ADR-0.0.73 / OBPI-0.0.73-07 — created by claude-code at 2026-06-18T12:06:30Z. This is the register entry required by ADR-0.0.41 to release the completed OBPI's lock (handoff-before-release coupling). -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

A handoff records a *proposed* plan and its context. On resume, present advised
steps to the operator and obtain explicit authorization before executing any of
them. You advise; the operator rules.

## Current State Summary

OBPI-0.0.73-07-evaluate-truth-binding is **completed and attested**. `gz obpi
complete` ran successfully with operator-verbatim-conversational attestation by
`g0` ("attest completed", 2026-06-18). The brief frontmatter is
`status: Completed`; the completion receipt and ADR-level audit ledger entry were
written atomically. The lock was claimed at 2026-06-18T11:41:08Z by agent
`claude-code-9c2ecefd`; this handoff is the register entry that ADR-0.0.41
handoff-before-release coupling requires to release the (completed, not
abandoned) lock.

## What landed (GHI #624 — evaluator shape-vs-substance)

`gz adr evaluate` dim-1 (Problem Clarity) and dim-2 (Decision Justification) now
grade decision SUBSTANCE, not prose SHAPE or KEYWORD presence:

- **dim-1**: the two `_has_keywords(before/after)` checks → concrete-grounding
  (`_references_concrete_artifacts`) + multi-sentence problem-outcome contrast
  (`_substantive_sentence_count`); depth raised to 120 words.
- **dim-2**: the `^\d+\.` numbered-list regex + `_has_keywords(because)` →
  weighed-and-rejected-alternatives + honest-negative-consequences; depth 100 words.
- `gz adr evaluate` self-registers as an `advisory` QC step via a new
  `register_advisory_qc_step()` channel in `qc_binding.py`; `build_qc_registry()`
  extends the derived bound registry with the advisory channel.
- Seventh theater signature `shape-graded-not-substance` added to
  `THEATER_SIGNATURES` + corpus fixture `shape_graded_not_substance.json`.

## Important Context

- The real ADR-0.0.73 scores 4/4 on both rewritten dimensions (GO verdict, 3.55)
  — the Fidelity Assertion `uv run gz adr evaluate ADR-0.0.73` stays green.
- **Brief amendments (plan-audit + reconcile):** allowlist gained
  `src/gzkit/governance/trust_audits/qc_binding.py` (THEATER_SIGNATURES home),
  `tests/governance/test_facade_regression_corpus.py` (count assertion), and
  `src/gzkit/traceability.py` (READ-ONLY `@covers` import, declared because the
  reconciler neighborhood heuristic reports it when top-level src/gzkit modules
  are allowlisted). The fixture extension typo (`.py` → `.json`) was fixed.
- **Coupled-surface coherence (DO IT RIGHT 1a):** 4 OBPI-01/02 registry/signature
  tests were refined for the new advisory channel (bound-scope equality preserved,
  anti-hand-maintenance guard intact); `validate.md` + `adr-evaluate.md` manpages
  updated for the 7th signature and substance grading.
- `req_atomic:` declared for all 7 REQs (each is one indivisible edit — the dim
  rewrite, the self-registration channel, the signature addition; SUPPORT/FENCE
  REQs have no labor below them). Behave waiver added for all 7 REQs (no
  Gherkin-observable surface; behavior_uncovered_reqs=0).
- Do NOT use `--abandon` for the lock release — the OBPI is completed, not
  surrendered. A plain `gz obpi lock release` is correct.

## Decisions Made

- **Decision:** Declare `req_atomic:` for all seven REQs rather than minting
  `seq=02+` subdivision TASKs.
  **Rationale:** Each REQ is one cohesive deliverable; there is no labor below the
  REQ. Mirrors the OBPI-0.0.73-06 precedent.
- **Decision:** Apply the reconciler's `src/gzkit/traceability.py` allowlist
  addition (annotated READ-ONLY) rather than suppress the heuristic.
  **Rationale:** The signal is factually grounded (the test imports `covers`); the
  Gate-Friction doctrine says the brief adjusts to reality. Annotated so a future
  reader understands it is not a modified surface.

## Tracked Defect (out of OBPI-07 scope)

- Sibling **OBPI-0.0.73-05** brief fails `uv run gz validate --sensitivity`
  (exit 3) — confirmed pre-existing on clean HEAD (its allowlist overlaps a
  registered security surface without a `sensitivity:` declaration). Out of
  OBPI-07's brief boundary; recorded in `.gzkit/insights/agent-insights.jsonl`.
  A future pass should either declare `sensitivity:` on OBPI-05 or discharge via
  `--accept-security-floor`.

## Immediate Next Steps

1. **Release the lock:** `uv run gz obpi lock release OBPI-0.0.73-07-evaluate-truth-binding`
   (this handoff is the register entry; the OBPI is completed, not abandoned).
2. **Git-sync #1:** `uv run gz git-sync --apply --lint --test` to commit the
   completion governance edits, lock release, and marker cleanup.
3. **Reconcile:** `uv run gz obpi reconcile OBPI-0.0.73-07-evaluate-truth-binding`
   to confirm receipt and brief agree.
4. **ADR status + Git-sync #2:** `uv run gz adr status ADR-0.0.73 --json`, then
   `uv run gz git-sync --apply`.
5. **Advance the campaign:** ADR-0.0.73 checklist is now 7/8 — only #8
   (Fidelity-presence enforcement, `gz validate --fidelity-presence`) remains
   before the ADR can close out. Consult
   `docs/governance/build-to-1.0-campaign-2026-06-10.md` for the gate check.

## Pending Work / Open Loops

- ADR-0.0.73 OBPI-08 (fidelity-presence enforcement) — the last checklist item.
- ADR-0.0.73 closeout ceremony once OBPI-08 lands (runs `--qc-binding` +
  `gz adr fidelity` over the real ADR; the cross-OBPI behave integration deferred
  by the OBPI-02/03/04/07 waivers belongs here).
- Sibling OBPI-05 sensitivity-floor defect (see Tracked Defect above).

## Verification Checklist

- [ ] `uv run gz obpi lock list` — confirm lock released (done at Step 3)
- [ ] `uv run gz obpi reconcile OBPI-0.0.73-07-evaluate-truth-binding` — confirm ATTESTED COMPLETED
- [ ] `git status --short` — confirm tree clean after git-sync #2
- [ ] `git branch --show-current` → `main`
- [ ] `uv run gz adr evaluate ADR-0.0.73-verification-layer-binding-audit` → Problem Clarity=4, Decision Justification=4, GO
- [ ] `uv run gz validate --qc-binding` → exit 0 (evaluator self-registered advisory)
- [ ] `uv run gz adr fidelity ADR-0.0.73-verification-layer-binding-audit` → all assertions pass

## Evidence / Artifacts

- `docs/design/adr/foundation/ADR-0.0.73-verification-layer-binding-audit/obpis/OBPI-0.0.73-07-evaluate-truth-binding.md` — completed brief with attestation
- `src/gzkit/adr_eval_scoring.py` — dim-1/dim-2 substance-grading rewrite + evaluator self-registration call
- `src/gzkit/qc_binding.py` — `register_advisory_qc_step()` + `_SELF_REGISTERED_ADVISORY_STEPS` + `build_qc_registry()` extension
- `src/gzkit/governance/trust_audits/qc_binding.py` — seventh `shape-graded-not-substance` theater signature + description
- `tests/governance/test_adr_eval_truth_binding.py` — 9 truth-binding tests (substance scoring, registration, signature detection)
- `tests/governance/fixtures/facade_corpus/shape_graded_not_substance.json` — seventh theater fixture
- `tests/governance/test_facade_regression_corpus.py` — seventh-signature corpus test
- `tests/governance/test_qc_binding.py`, `tests/governance/test_qc_binding_scope.py` — coupled-surface test refinements
- `docs/user/manpages/adr-evaluate.md`, `docs/user/manpages/validate.md` — substance-grading + seventh-signature docs
- `data/behave_coverage_waivers.json` — OBPI-07 no-behave-surface waiver
- ARB receipts: `arb-step-unittest-c58e18da75d940bea65ce3d478ae1d2f` (6275 pass), `arb-ruff-e18e7eeff1a74d35b8beb546c43c0146`, `arb-step-typecheck-5c4aca73c85b4de189091490f7d07b4a`, `arb-step-mkdocs-0936d789b32b4e258888f702bff263cc`

## Environment State

- Branch `main`; OBPI-0.0.73-07 completed and attested (g0, operator-verbatim-conversational).
- Git-sync #1 committed the completion edits at `df059967`; push pending the handoff-section fix.
- Lock released; pipeline markers cleaned.
