# Session Handoff: OBPI-0.0.16-04 paused pending OBPI-0.0.16-06

**Date:** 2026-04-18
**Pipeline state:** OBPI-0.0.16-04 paused at Stage 2 Step 4 (REQ-04 fail-closed)
**Resume after:** OBPI-0.0.16-06-validator-pool-skip-parity attested-completed

## What was completed

- **REQ-01 prerequisites:** verified all 4 sibling OBPIs (01, 02, 03, 05) attested-completed via `gz adr status ADR-0.0.16`
- **REQ-02 dry-run #1:** receipt `artifacts/receipts/frontmatter-coherence/20260418T095852Z.json` (257 files, operator signed off)
- **REQ-03 live run:** receipt `artifacts/receipts/frontmatter-coherence/20260418T100437Z.json` (257 files rewritten, 69 pool ADRs skipped, 0 hand-edits)
- **REQ-05 idempotence:** receipt `artifacts/receipts/frontmatter-coherence/20260418T100647Z.json` (0 files rewritten on second dry-run; chore stable)
- **OBPI brief Evidence section:** populated with all three receipt paths, dry-run summary table, live-run metadata, REQ-04 blocker analysis, and Tracked Defects entries

## What blocks completion

**REQ-04 fails:** `uv run gz validate --frontmatter` exits 3 with 56 errors, **all in `docs/design/adr/pool/**`**. Active surface is 100% clean. Root cause: `src/gzkit/commands/validate_frontmatter.py` lacks the pool-skip filter that `src/gzkit/governance/frontmatter_coherence.py` applies (chore library lines 123, 214, 277, 282, 292; comment at line 300 explicitly states *"validator should not emit errors for pool ADRs"*).

REQ-07 forbids new source code in OBPI-04, so the validator fix cannot land here.

## Defects filed during this session

- **GHI #192** — `validate_frontmatter omits pool-ADR skip filter that frontmatter_coherence library applies` (the blocker)
- **GHI #191** — `plan-audit-gate ↔ plan-mode deadlock: ExitPlanMode requires a write that plan mode forbids` (surfaced during pipeline entry; not blocking OBPI-04)
- **Brief command defect (untracked, noted in brief Tracked Defects):** REQ-02/03/05 specify `gz chore run frontmatter-ledger-coherence [--dry-run]`, but actual surface is `gz frontmatter reconcile [--dry-run]`. Address during OBPI-06 ADR amendment.

## Resume protocol

After OBPI-0.0.16-06 lands:

1. Re-claim OBPI-04 lock: `uv run gz obpi lock claim OBPI-0.0.16-04-backfill-and-ghi-closure`
2. Re-run plan audit (this session's plan still aligns; receipt may be stale): `uv run gz plan audit OBPI-0.0.16-04`
3. **Skip Stage 2 Steps 1-5** — already complete and recorded in brief Evidence
4. **Re-run REQ-04** — `uv run gz validate --frontmatter` MUST exit 0 once OBPI-06 has shipped the validator pool-skip fix
5. **Re-run REQ-05** — `uv run gz frontmatter reconcile --dry-run` MUST still produce empty receipt (idempotence holds across days unless ledger cursor changes; if not empty, identify what shifted)
6. **Step 6 (regression test):** create `tests/chores/test_frontmatter_coherence_backfill.py` + `tests/chores/__init__.py` with `@covers(REQ-0.0.16-04-04)` and `@covers(REQ-0.0.16-04-05)` decorators, asserting `reconcile_frontmatter(project_root, dry_run=True).files_rewritten == []`
7. **Step 7 (close 5 GHIs):** `gh issue close 162, 167, 168, 169, 170` with structured comments citing ADR-0.0.16 + the live receipt path
8. **Step 8 (Evidence updates):** parent ADR Evidence section, ADR-CLOSEOUT-FORM.md OBPI-04 status → Completed, OBPI-04 + OBPI-06 cross-references
9. **Stage 3:** lint, typecheck, test --obpi (and --bdd Heavy), docs build, gz covers parity
10. **Stage 4:** ceremony with human attestation
11. **Stage 5:** `gz obpi complete OBPI-0.0.16-04-backfill-and-ghi-closure --attestor … --attestation-text …` + two-sync git cycle + reconcile

## Files in inconsistent state on disk (uncommitted)

- 257 ADR/OBPI files with rewritten frontmatter (live chore run — these are real reconciliations, should be committed)
- `docs/design/adr/foundation/ADR-0.0.16-frontmatter-ledger-coherence-guard/obpis/OBPI-0.0.16-04-backfill-and-ghi-closure.md` — Evidence + Tracked Defects sections updated
- `artifacts/receipts/frontmatter-coherence/` — three receipt files (likely gitignored)
- This handoff document

Recommend committing the chore-rewritten files and brief Evidence updates as a `wip(ADR-0.0.16): OBPI-04 paused pending OBPI-06` commit so the backfill work isn't lost. Receipts directory is gitignored by convention.

## Plan reference

`/Users/jeff/.claude/plans/sorted-soaring-cerf.md` — full Stage 2-5 plan; Steps 1-5 recorded as complete in this handoff, Steps 6-11 pending.
