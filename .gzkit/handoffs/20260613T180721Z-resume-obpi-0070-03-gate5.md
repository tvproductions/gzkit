---
mode: CREATE
adr_id: ADR-0.0.70
branch: main
timestamp: "2026-06-13T18:07:21Z"
agent: claude-code
obpi_id: OBPI-0.0.70-03
session_id:
continues_from: .gzkit/handoffs/20260613T180323Z-OBPI-0.0.70-03-parked.md
---

<!-- Handoff document for ADR-0.0.70 — created by claude-code at 2026-06-13T18:07:21Z -->

## ⚠️ This handoff ADVISES next moves — it is NOT authorization to execute them

**Read this before anything else.** A handoff records a *proposed* plan and its
context. It is **NOT** a clearance to unilaterally execute that plan. On resume —
at **every** freshness level, Fresh included — you MUST:

1. Present the advised next steps and current state to the operator.
2. **Obtain explicit operator authorization before executing any of them** — no
   file mutation, no `gz` ceremony until the operator says go.
3. Treat the human-as-final-witness doctrine as binding: you advise; the operator
   rules; you note variance and stop.

**Trust-warning specific to this chain:** the session that produced this handoff
made FIVE mis-diagnoses (operator flagged the work as unacceptable). Do NOT trust
any state claim in this document on the prior agent's word. Every claim below is
paired with the command that re-verifies it — run the command, believe the
output, not the prose.

## Current State Summary

**Top priority: finish OBPI-0.0.70-03-guardrail-feedback-prose-rule — it is one
step (Stage-4 Gate-5) from done.** The implementation was rogue-committed earlier
in commit `863250d6` and is unchanged on disk; this session regularized the brief
and ran the pipeline to the human-attestation gate, then parked by operator
direction.

No commits were made this session — `git log --oneline -1` is `2a88c01c`. The
OBPI-03 lock was released (register entry:
`.gzkit/handoffs/20260613T180323Z-OBPI-0.0.70-03-parked.md`); `uv run gz obpi
lock list` reports `No active locks`. The brief edits and session artifacts are
uncommitted and reusable. ADR-0.0.70 is campaign item **B.0** (build-to-1.0); it
reaches terminal only after both OBPI-0.0.70-03 (this) and OBPI-0.0.70-04 (still
`status: Draft`) are attested.

## Important Context

- **Lite lane.** OBPI-03 ships a binding rule + scorecard entry + sync; no
  CLI/schema/runtime contract change. Gate 5 (human attestation) is still
  universal (ADR-0.0.36).
- **Brief edits this session (uncommitted):**
  - Acceptance Criteria expanded 3 → 6 REQs (added REQ-03-04/05/06, all
    `[support]`) so `REQUIREMENT:` line count (6) equals acceptance-criteria count
    (6) — the reconcile `req_count` dimension enforces that 1:1 convention
    (`ReqCountDelta.declared_reqs` is documented as "Count of REQUIREMENT: lines").
  - Allowed Paths gained `src/gzkit/traceability.py` (attested reconcile
    `--apply`, attestor g0 — mirrors how sibling OBPI-01 declared the
    same `@covers` decorator source), plus `data/distribution_baseline_manifest.json`
    and `data/surface_weight_waivers.json` (coupled surfaces the impl touched).
- **The 5 SUPPORT REQs prove via LEDGER_PLUS_VALIDATOR, not `@covers`** (ADR-0.0.59):
  `uv run gz covers OBPI-0.0.70-03-guardrail-feedback-prose-rule --json` shows
  `uncovered_reqs: 5` but `behavior_uncovered_reqs: 0` — the latter is the gate;
  `uncovered_reqs: 5` is expected and NOT a blocker.
- **A retrospective plan file exists** at
  `.claude/plans/OBPI-0.0.70-03-guardrail-feedback-prose-rule.md` (the rogue work
  had none); `gz plan audit` PASSes against it.

## Decisions Made

- **Decision:** Regularize OBPI-03 via `gz obpi pipeline --from=verify` (not
  re-implement). **Rationale:** the deliverable exists and verified green; mirrors
  the OBPI-02 regularization. **Alternatives rejected:** re-implement; withdraw.
- **Decision:** Expand acceptance criteria to 6 (operator choice) rather than
  consolidate requirements to 3. **Rationale:** preserves all declared
  constraints as proven obligations. **Alternatives rejected:** consolidate-to-3;
  editing canon to silence a validator (rejected as the V.I.B.E.S. anti-pattern).
- **Decision:** Park OBPI-03 at Gate 5 and release the lock (operator direction).
  **Rationale:** operator removed this session's unreliable diagnosis from the
  loop. **Alternatives rejected:** push to completion; discard the edits.

## Immediate Next Steps

*(ADVISORY — present to operator, await authorization.)*

1. **Re-verify the green state — do not trust this handoff.** Run
   `uv run gz brief reconcile OBPI-0.0.70-03-guardrail-feedback-prose-rule --json`
   (expect `has_drift: false`) and
   `uv run gz obpi precomplete OBPI-0.0.70-03-guardrail-feedback-prose-rule`
   (expect READY 8/8). Re-claim the lock first:
   `uv run gz obpi lock claim OBPI-0.0.70-03-guardrail-feedback-prose-rule`.
2. **Resume the pipeline:** `uv run gz obpi pipeline
   OBPI-0.0.70-03-guardrail-feedback-prose-rule --from verify` — it runs verify
   then presents the Stage-4 evidence packet.
3. **Stage 4:** present the evidence packet; obtain the operator's genuine Gate-5
   attestation. Do NOT fabricate or assume it.
4. **Stage 5:** `uv run gz obpi complete` with the operator's attestation text,
   then the two guarded git-syncs + reconcile + ADR status refresh.

## Pending Work / Open Loops

- **OBPI-0.0.70-04-fourth-source-triangulation** is still `status: Draft` —
  ADR-0.0.70 (campaign B.0) cannot reach terminal until it is built + attested.
- Untracked pipeline markers under `.claude/plans/` (`.pipeline-active*.json`)
  remain from this session; `uv run gz obpi pipeline --clear-stale` clears them,
  or a fresh `--from verify` run supersedes them.
- This session's 2 course-correction insights are unreviewed in
  `.gzkit/insights/agent-insights.jsonl`.

## Verification Checklist

- [ ] `git log --oneline -1` reads `2a88c01c` (no commits this session)
- [ ] `git branch --show-current` is `main`
- [ ] `uv run gz obpi lock list` reports `No active locks`
- [ ] `uv run gz brief reconcile OBPI-0.0.70-03-guardrail-feedback-prose-rule --json` → `has_drift: false`
- [ ] `uv run gz obpi precomplete OBPI-0.0.70-03-guardrail-feedback-prose-rule` → READY (8/8)
- [ ] `uv run gz covers OBPI-0.0.70-03-guardrail-feedback-prose-rule --json` → `behavior_uncovered_reqs: 0`
- [ ] GHI #613 is CLOSED (it was a mis-diagnosis: a local Windows CRLF artifact, not committed drift on main)

## Evidence / Artifacts

- `docs/design/adr/foundation/ADR-0.0.70-turn-end-feedback-and-correction-mining/obpis/OBPI-0.0.70-03-guardrail-feedback-prose-rule.md` — the brief, edited this session (6 acceptance criteria, expanded allowlist)
- `.gzkit/handoffs/20260613T180323Z-OBPI-0.0.70-03-parked.md` — lock-release register entry (predecessor)
- `.claude/plans/OBPI-0.0.70-03-guardrail-feedback-prose-rule.md` — retrospective plan (PASS plan-audit)
- `.gzkit/rules/guardrail-feedback-prose.md` — the canonical rule the OBPI delivers (verified conformant)
- `.gzkit/insights/agent-insights.jsonl` — 2 course-correction records appended this session

## Environment State

Windows 11 / PowerShell primary; Python 3.13 via uv. Branch `main`, 3 commits
ahead of `origin/main`, 0 behind. HEAD `2a88c01cda3332b8b02690f32cb3425138776478`.
Caveat: `gz validate --surfaces` may report RED on a Windows CRLF working copy —
that is a local line-ending artifact (`git add` of the flagged surfaces stages
nothing), NOT committed drift; re-running `uv run gz agent sync control-surfaces`
clears it locally with zero committable change.
