---
mode: CREATE
adr_id: null
branch: main
timestamp: '2026-07-21T11:48:24Z'
agent: claude-code
---

## Current State Summary

Triaged all 44 open GHIs; ranked deliverable committed to docs/governance/ghi-triage-2026-07-21.md. Filed GHI #709 (handoff adr_id mandatory by parity inheritance, not design) and repaired it in the same session.

## Important Context

Operator ruling 2026-07-21, verbatim: 'we can handoff any sort of work, the --adr flag is useful but too narrow. handoff are simply to create work continuity.' This handoff is itself the proof of the repair — it carries no parent ADR and could not have been authored before it.

## Decisions Made

Handoffs are work-continuity artifacts, not ADR children. adr_id relaxed to optional; mode is now the is-this-a-handoff discriminator, since adr_id was doing double duty as discriminator and scope-filter. The ADR-0.0.0 sentinel in write_completion_handoff was removed rather than kept. The triage list was persisted as committed markdown, not a handoff, because it was produced before this fix landed.

## Immediate Next Steps

1. Work the 8 blocking GHIs from the triage list, starting with the corpus-spine pair #654 and #635 (same wound; fixing one alone leaves the path unrunnable).
2. Verify-and-close the 5 stale GHIs whose failures no longer reproduce: #480, #561, #563, #564, #538.
3. Close #709 citing the fix commit SHA.
4. Consider whether mode:DESIGN should be restored to the Literal — one committed handoff still carries it and fails validation.

## Pending Work / Open Loops

GHI #709 open until the fix commit lands. GHI #696 cross-linked as the downstream symptom of the same root cause. The 2026-04-26 harness-engineering handoff still fails validation on mode:DESIGN, which this fix did not address.

## Verification Checklist

uv run -m unittest -q reports 7258 OK. Baseline on clean main was 0 failures, so all 45 intermediate failures were attributable to this change and were fixed, not excused.

## Evidence / Artifacts

docs/governance/ghi-triage-2026-07-21.md, src/gzkit/handoff_validation.py, src/gzkit/handoff_api.py, src/gzkit/handoff_resume_gate.py, scripts/session_orientation.py
