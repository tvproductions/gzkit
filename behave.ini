[behave]
# Skip work-in-progress scenarios by default. Run them explicitly with
# `behave --tags=wip`. WIP scenarios are documented at the feature header
# with a tracking reference (GHI / OBPI) and exist to surface unauthored
# step definitions without blocking pre-merge gates.
default_tags = ~@wip
